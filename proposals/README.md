# Proposals Index

This directory holds two kinds of document: **active proposals**, each
authorizing nothing on its own until a run is directed at it, and
**delivered records**, kept in place (not archived — see each file's own
note) because live documents still cite them as evidence. This file is the
index of both, and the priority list `scripts/opencode-pursue` checks
before falling back to its own center-out judgment.

## How this is used

Before selecting a milestone, an autonomous run should check the **Active
priority** table below. If anything is marked **High**, pursue the
highest-leverage High item first. If nothing is above **Medium**, fall
back to the center-out SGT policy (`docs/1_STRATEGY.md`,
`docs/6_SGT_BACKLOG.md`) as usual. **Low** items are not a queue to work
down — they are proposals whose next step needs a human decision (counsel
sign-off, an operator choice between routes) that an autonomous Lean-work
run cannot make, so leverage-ranking them against Medium/High items would
misrepresent them as simply less urgent when they are actually blocked on
something else entirely.

Keep this table current by hand at proposal boundaries: when a proposal's
blocker resolves, when a new proposal is added, or when one is delivered
— move it to the table below rather than letting two sources of truth
drift apart.

## Active priority

| Priority | Proposal | Why |
| --- | --- | --- |
| Low — technical and governance decision required | [A Grönwall-Type Bound for the Non-Autonomous Propagator](nonautonomous-propagator-gronwall-bound.md) | Added 2026-09-07, restated from the same external request family as the three entries above, its fourth item: a Grönwall-type deviation bound for the adjoint propagator of a time-varying (non-autonomous) generator. Unlike the three items before it, the request's own "current state" claim is accurate — `DiscreteAffine.lean` covers only static discrete updates, and no continuous-time non-autonomous generator theory exists anywhere in this project. Two reasons this is Low rather than Medium: (1) `docs/6_SGT_BACKLOG.md` item 5 explicitly keeps "general graph semigroups" gated (only the constant-generator heat semigroup was opened, 2026-08-19) — a time-varying generator is that still-gated case, not an extension of what was already opened; (2) the request's own formula only holds under an added uniform-contraction hypothesis on the propagator (`‖Φ(t)‖ ≤ 1`) that its text does not state — a judgment call this project's precedent treats as needing sign-off. The technical case is real but tractable: framed as a conditional theorem (`Φ` a hypothesis satisfying the ODE, no Picard–Lindelöf existence machinery needed, mirroring how Mathlib's own Grönwall lemma is packaged), the entire bound follows from already-proved Scaffold operator-norm lemmas (`l2OpNorm_mulVec_le`, `l2OpNorm_transpose`) plus one Mathlib calculus lemma never used in this project before (`image_norm_le_of_norm_deriv_right_le_deriv_boundary`, a variable-boundary mean-value inequality). Not self-authorizing on either count. |
| Low — technical decision required (Steps 3–5 only; Steps 0(m=0)/1/2 delivered under the named-consumer scope 2026-09-07, see the Delivered row) | [Global (Window-Free) Semigroup Contraction Bounds](global-semigroup-contraction.md) | Added 2026-09-06, restated from an external request relayed the operator. **Update 2026-09-07:** the operator-added Medium row (the Boundary Outflow Lemma, Part 2) is the "different named consumer" this document's own gate names as an alternative to explicit adoption, so its Steps 0(m=0)/1/2 — exactly the consumer's named build-order dependency — were delivered 2026-09-07 (hard crust, zero axioms, the interpretation recorded in both proposals' status headers for operator review). **What remains gated is the order-`m` program**: Steps 3–5 (the `(laplacian A)^k` eigenbasis expansion, the order-`m` vector bound and its Euclidean twin) and the full Step-0 Taylor-remainder induction — genuinely new proof content with no named consumer; an operator adoption decision (or a consumer stating an order-`m` need) is still required before those begin. |
| Low — human decision required | [The Higher-Order Cheeger Hard Direction (Lee–Gharan–Trevisan)](multiway-cheeger-hard-direction.md) | Added 2026-08-28. `multiway-expansion.md`'s own delivery record already priced this as "genuinely multi-run, gated on a named consumer"; an external SGT-specialist review (no code access) independently flagged the same gap the same day, which is why it's tracked as its own proposal now rather than staying a note inside another file. No internal Scaffold consumer exists — its only case for adoption is field-standard expectation, the same basis Alon–Boppana was adopted on. Needs an explicit operator decision before even the Step 0 survey, mirroring `alon-boppana-bound.md`'s pre-adoption lifecycle exactly: the classical proof route (randomized rounding of a spectral embedding into `ℝ^k`) needs continuous geometric probability, an apparatus not confirmed present in the pinned Mathlib and not used anywhere else in this repository — a larger unknown than any machinery gap this project has adopted through so far. |
| Low — human decision required | [Clean-Room SGT Export](clean-room-sgt-export.md) | Needs patent counsel sign-off before any phase starts. Potentially the highest-leverage item in this directory overall, but not one an autonomous Lean-work run can act on. |
| Low — human decision required | [Clean-Room SGT Lemma-to-Science Map](clean-room-sgt-lemma-science-map.md) | Supporting provisional review inventory for the export above (linked from its "Initial public boundary" section) — a declaration-level map of every theorem, axiom, and private proof helper in the `GraphTheory/Spectral.lean` export candidate to classical mathematics, reviewed 2026-08-18. Approves nothing on its own; the export's own counsel-sign-off gate still controls every action. Found unindexed here 2026-08-22 while auditing this table — same class of gap as `retire-the-mushy-center.md`'s 2026-08-18 fix below. |
| Low — human decision required | [Get Outside Signal](get-outside-signal.md) | Phase 0 is explicitly "outside this document's authority" — gated on the same counsel sign-off as the export. |
| Low — human decision required | [Sell the Methodology](sell-the-methodology.md) | Gated on the export existing and, per its own text, likely a *stricter* clean-room pass than the code export. |
| Low — human decision required | [A Discovery-Layer MCP Server](discovery-mcp-server.md) | Phase 0 is explicitly outside this document's authority; Phase 1 has nothing to point at until the export is tagged. |
| Low — human decision required | [Submit to the Palomar Registry](palomar-submission.md) | Added 2026-08-19, prompted by Terence Tao's 2026-08-18 announcement of the Palomar registry. Narrower gate than the counsel items above: not exporting the methodology, just one already-proved theorem (`foster_theorem` recommended — provably authored over 14 hours after `spectral-proof/`'s deletion, a temporal provenance argument stronger than a name-diff) from a repository that is already public. Still needs an explicit narrow sign-off (Step 0) before any Lean file is drafted, and every action past drafting needs the operator's own GitHub credentials — this assistant has none and should not be given any. Complicates `docs/traction-plan.md`'s scope note ("applies only to the new clean-room repository") in a way that document does not yet resolve; flagged, not resolved, by this proposal. |
| Low — technical decision required | [The Weighted Matrix-Tree (Kirchhoff) Theorem](weighted-matrix-tree-theorem.md) | Added 2026-08-19 as the answer to "if we build one more piece of genuinely new machinery, what should it be" — compared explicitly against `icebox/`'s three candidates (Golden-Thompson, ODE-trajectory calculus, stochastic calculus) and judged the best fit for this project's idiom (finite determinants and graph induction, not a foreign kind of math) and its most active axis (6). Not self-authorizing: `docs/6_SGT_BACKLOG.md` item 7 already names this exact candidate and gates it explicitly — "admit or define nothing here until a named consumer states which identity it needs" — and this proposal does not supply one. Does **not** accelerate or feed Foster's theorem (`spectral-graph-sparsification.md` Phase A already has its own pseudoinverse-free route); the document is explicit about not overstating that connection. |
| Low — technical decision required | [Mutual Information and the Data Processing Inequality](mutual-information-and-data-processing.md) | Added 2026-09-02, requested by the operator asking what an AI-relevance extension of the information-theory layer should be, if any. `docs/6_SGT_BACKLOG.md` item 6 records its named interface set as complete — this document proposes new machinery beyond that, not a reopening. The technical case is unusually strong: the DPI's hardest classical step (channel monotonicity of KL divergence) decomposes into `sum_klTerm_ge_klTerm` (already proved, "the two-block log-sum bound") plus a homogeneity identity already inlined in that same proof, and the motivating graph-level corollary — mutual information between a start vertex and a depth-`t` walk position decaying with the spectral gap, an oversmoothing/information-bottleneck statement for message-passing — composes directly with the already-proved `klDiv_walkDistribution_le`. Not self-authorizing: no proposal or backlog item names mutual information as a blocking dependency, so an operator decision to adopt (or a different named consumer) is needed before Step 1. |
| Low — technical decision required | [The Erdős–Rényi Random Graph as a Named Model](erdos-renyi-random-graph-model.md) | Added 2026-09-05, prompted by an external repo observer's assessment (relayed by the operator) that random-graph spectral theory is a real charter-vs-reality gap, not a considered deferral like the matrix-tree theorem. Verified: "random graph" appears in exactly two places in the repository — axis 7's own one-line charter and a citation title — with zero backlog item, proposal, gate, or cost estimate, unlike every other adjacency-doesn't-justify-admission candidate this table tracks. The technical case is unusually cheap for the same reason the mutual-information proposal above was: `EdgePerturbation.lean`'s `perturbWeight`, built for an unrelated purpose (Fiedler-subspace stability under small perturbation), is generic enough that specializing its base graph to the complete graph and its per-edge probability to a constant makes it *exactly* the centered adjacency matrix of `G(n,p)` — the object the actual random-graph spectral literature studies — with two already-proved generic tail bounds (`edgePerturbation_degree_tail_bernstein`, `edgePerturbation_norm_tail`) applying directly, needing only concrete constant computation, not new inequalities. Not self-authorizing: no proposal or backlog item names a `G(n,p)` consumer as a blocking dependency, so an operator decision to adopt (or a different named consumer) is needed before Step 1. |
| Low — technical decision required | [Global (Window-Free) Semigroup Contraction Bounds](global-semigroup-contraction.md) | Added 2026-09-06, restated from an external request relayed by the operator (not tracked in this repository) asking whether the heat semigroup's existing remainder bound — window-restricted to `|t·λᵢ| ≤ 1` — has a global, window-free counterpart over all `t ≥ 0`. `docs/6_SGT_BACKLOG.md` item 5 and `reversibility-and-heat-semigroup.md` record the heat-semigroup program, including that remainder bound, as complete; this document proposes new machinery beyond it, not a reopening. Verified: the window is not fundamental to this project's actual graphs — it exists only because the delivered bound is proved for arbitrary symmetric (possibly signed-weight) networks, where negative Laplacian eigenvalues make `e^{-tλ}` blow up; every other heat-semigroup consumer already assumes nonnegative weights (`hnonneg`), and under that same PSD hypothesis the window drops out entirely, using the already-unrestricted Mathlib lemma `Real.add_one_le_exp` in place of the window-restricted `Real.abs_exp_sub_one_sub_id_le` the delivered bound cites. The order-`m` generalization needs one genuinely new scalar lemma (a global Taylor-remainder bound for `Real.exp` on `y ≥ 0`, absent from Mathlib), but it is cheap: an induction using only finite-sum differentiation (already this project's idiom) plus one already-proved Mathlib monotonicity lemma (`monotoneOn_of_deriv_nonneg`). Not self-authorizing: no proposal or backlog item names a global contraction bound as a blocking dependency, so an operator decision to adopt (or a different named consumer) is needed before Step 0. |
| Low — process decision required | [Retire the Mushy Center Systematically](retire-the-mushy-center.md) | Found unindexed while auditing this table (2026-08-18) — added by the same run that delivered the Cheeger easy direction, never previously recorded here. Proposes a heavier apparatus for axiom retirement: a formal elimination board, parallel per-candidate worktrees, a four-stage survey/formalize/falsify/integrate workflow. Worth an honest tension check before adoption: its own pilot section names the normalized-Cheeger transfer as its first candidate, and that has *already been delivered* using the lightweight mechanism already in place (this table plus one-step-per-run) — four axiom-related deliveries landed that way in short order. Whether the heavier process is worth adopting over what's already working is an operator call, not a default; an autonomous run should not stand up the board unprompted. |
| Low — human decision required | [A Python Certificate Bridge for Scaffold's Spectral Certificates](../docs/arch/python-certificate-bridge.md) | Added 2026-08-30. Lives in `docs/arch/` rather than this directory (an ecosystem/adoption architecture note, not a Lean build-order item) and was never indexed here, found missing during a 2026-09-04 governance-currency review of `docs/arch/`. Explicitly gated — human decision required — and outside the center-out SGT-proof priority order (`AGENTS.md` § Priority order); no Lean, Python, or documentation change is authorized by the proposal alone. |
| Low — decided in part, see Axiom Audit Tooling above | [Axiom Stress Testing and Quarantine](axiom-stress-testing-and-quarantine.md) | Originally drafted 2026-08-30 as a standalone `governance/` policy document; reviewed by the commit steward and moved here as a proposal rather than kept as a standing policy. Proposes real vocabulary (mushy center/hard crust/quarantine) and a 6-stage axiom lifecycle, but much of its ground is already covered by two lighter mechanisms adopted the same day — `docs/9_ERRATA.md` (the disposition record for axioms repaired in place) and `docs/arch/commit-steward-protocol.md` (the human verification gate substituting for its proposed machine-only gates). **Decision made 2026-08-30:** the operator selected the concrete middle option — build §7's tooling items standalone (now `axiom-audit-tooling.md`, indexed High above) without adopting the vocabulary or 6-stage lifecycle. This row stays Low/undecided only for the remainder — whether to adopt the full lifecycle vocabulary itself — which was explicitly not chosen and is not queued for any run. |
## Delivered

| Proposal | Delivered | Result |
| --- | --- | --- |
| The Quantized Cheeger Inequality ([`quantized-cheeger-bound.md`](quantized-cheeger-bound.md), COMPLETE + delivery record) | 2026-09-07 | **The Active priority table's Medium row, Steps 1–3 plus the QA plan's fences in one run as its own operating instructions price: the repository's first numerical-robustness layer — `(λ̃₂ − 1/2^b)/2 ≤ φ(G) ≤ √(2(λ̃₂ + 1/2^b))` with `λ̃₂` the `b`-bit quantized spectral gap over the proved a priori range `[0, 2]`, the explicit `±ε_Q` envelope the relayed request asked for (the error entering linearly below and inside the square root above — Cheeger's own pair is asymmetric, so is its quantized propagation).** The new public module `Scaffold/Mathlib/GraphTheory/QuantizedCheeger.lean` (functional 1376 → 1381): `Quantization.quantize` (rescale-round-rescale around Mathlib's `round`), `quantize_mul_step` (bucket-midpoint fixed points, stated at every `k : ℤ`), `abs_sub_quantize_le` (`|x − quantize| ≤ R/2^(b+1)`, pure rescaling of `abs_sub_round`), the two range bridges `secondEval_regularNormalizedLaplacian_nonneg`/`_le_two` (the proved `[0, 2]` interval at the Cheeger theorems' operator spelling — the floor's first standalone packaging), and `quantized_cheeger_le` (pure algebra over the two already-proved Cheeger theorems; hypothesis set exactly the Cheeger pair's, no axiom touched). **The Step-0 survey's finding changed the file plan, not the math**: the sandwich needs `Mixing.lean`'s cap but `Mixing → Stationary → RandomWalk → Cheeger` is an import cycle, so the proposal's pre-authorized standalone-file branch applies (the graph-agnostic quantizer in a nested `Quantization` namespace). QA +11 in the new `Scaffold/QA/SpectralGraph/QuantizedCheeger_QA.lean` (QA 6803 → 6814), zero axiom contact (17-declaration audit via `wip/qcheb_axcheck.lean` — every one exactly `propext, Classical.choice, Quot.sound`): tie pins at `b = 0`/`b = 3` (both rounding up, `round 2⁻¹ = 1` made concrete), the error bound's sharpness at a tie (equality attained, raw route beside the theorem's instance), the boundary-count fence (the closed-range `2^b + 1`-distinct-values off-by-one: generic injectivity through the fixed-point identity plus the `b = 2` card pin `5`), and the K₂ sandwich numerically collapsed at `b = 0`/`b = 3` through the independently pinned spectrum and constant with the tightening witness `1/2 < 15/16 ≤ φ`, plus the range-cap bridge consumed at the fixture (K₂ attains `λ₂ ≤ 2` with equality — added when the delivery's first census pass caught the cap unconsumed: the sandwich never proof-depends on its own range justification). Traps recorded in the delivery record, headline: a standalone numeric `have` defaults its literals to ℕ (the `2 / 2^(b+1) = 1/2^b` elaborated as a `Nat` statement — the `(2:ℝ)` ascription is load-bearing) and `λ` is a reserved token in identifiers. Full ladder: build + completeness 138/138 fresh (both new modules in the default target), `lint_axioms` (4 unchanged), refutation-independence (24-tag), reachability (64 modules), citations, links, QA-name uniqueness (`qc*` collision-free), backlog freshness, consumption closure verified by the census tool (the 5 new theorems landing consumed, inert held at zero — `wip/census_20260907_post19.txt`), scoreboard 1381/6814/4/0 with the verification row, map freshness exit 0 after the stats sync + SVG regeneration (49 stations, no status change — no station cites this proposal), index coverage added. Honest scope: `λ₂` alone (the recorded scope decision — no top-`k`); K₂ at two bit depths (path/cycle carry no exact spectral pins); tie behavior at positive inputs only. |
| Closing the Inert Census to Zero ([`zero-inert-census.md`](zero-inert-census.md), same-run proposal + COMPLETE record) | 2026-09-07 | **The census's last four never-touched theorems — the Azuma `MatrixMDS` structure fields (`cond_mean_zero`, `measurable`, `norm_bound`) and `Core.l_infty_norm_nonneg` — first-consumed, taking the compiler-derived consumption census's never-touched set from 4 to ZERO: every one of the shelf's 1,374 theorem/lemma declarations is now value-consumed by some QA proof (the pins method's fifteenth application, and the program milestone it completes).** The Azuma half builds the first GENUINE (nondegenerate) `MatrixMDS` instance in QA — a one-shot Rademacher ±identity sequence on the delivered coin measure at `V = Fin 1` (`X 0` = the coin-flip sign of the identity matrix, tail zero, `R = 1` attained with equality at both outcomes through the pinned `l2OpNorm_one_fin1_QA`), with the mean-zero integral GENUINELY COMPUTED by raw atom enumeration (`PMF.integral_eq_sum`, masses `1/2` each), the trivial past characterized through the comap existential at the subsingleton `Fin 0 → Matrix`, and each field then consumed as a projection at concrete witnesses: the strong measurability promoted to `AEStronglyMeasurable` (the property whose absence made the pre-repair set-integrals junk zeros — Errata §7's exact mechanism), the conditional-mean field at the trivial past's `univ` AND at a nonempty past event (`k = 1`, the true cylinder, past-measurability witnessed at the entry fiber), and the norm bound two-sided at both outcomes. The Core half is the new `Scaffold/QA/Core/Norms_QA.lean` (the Core subtree's second QA module): the `l_infty_norm` value pin at a two-outcome fixture (both `sInf` directions raw), `l_infty_norm_nonneg` consumed with the raw companion, and the documented unbounded junk corner (`sInf ∅ = 0`) pinned. 21 QA theorems + 4 defs across the two files (QA 6782 → 6803), zero axiom contact (all 21 at `propext, Classical.choice, Quot.sound` via `wip/zeroinert_axcheck.lean`). **Consumption closure verified by the tool**: census `post17` shows exactly the 4 leaving (1370 → 1374 value-consumed, 4 → 0, no bonus, no collateral); never-touched defs 3 → 1 (`RV` and `l_infty_norm` now value-consumed; `MRV` type-only by construction). Full ladder: build + completeness 136/136 fresh, `lint_axioms` (4 unchanged), refutation-independence (24-tag), reachability (63), citations, links, QA-name uniqueness, backlog freshness, scoreboard 1376/6803/4/0, map freshness exit 0 after the stats sync. |
| The AlonBoppana Inert Trio's Positive Pins ([`alonboppana-inert-trio-pins.md`](alonboppana-inert-trio-pins.md), same-run proposal + COMPLETE record) | 2026-09-07 | **The prior handoff's named first cluster of the census's inert tail (7 → 4): the level-class machinery's three never-consumed theorems — `cycleAdj_nonneg`, `levClass_pairwise_disjoint`, `radialVec_apply` — first-consumed at the delivered `abC4` fixture; the pins method's fourteenth application.** The cycle-adapter bridge `cycleAdj 4 = abC4` proved entrywise (the `cycleGraph 4` adjacency decided at all sixteen index pairs — load-bearing: a wrong adjacency convention breaks it), through which the generic nonnegativity interface delivers the fixture's nonneg at the wrap edge `(3,0) = 1` beside the raw value; the level geometry completed (the connected iff, the `0/1/1` level values, the explicit classes — level `0` exactly the edge `{0,1}`, level `1` exactly the far side `{2,3}`); **the disjointness law consumed two routes** (through `levClass_pairwise_disjoint` via an explicit-∀ re-packaging — `Pairwise`'s strict-implicit `⦃a b⦄` are not positionally applicable, the run's recorded trap — and by literal Finset arithmetic on the pinned classes), plus the nonempty-vs-empty instance joined to the delivered level-`2` emptiness; and **the radial test vector's entry form consumed at all three `if` branches** (`ρ⁰ = 1` kept, `ρ¹ = ρ` value, `0` at truncation `k = 0` — the dropped branch), each with a def-unfolding raw companion. 16 QA declarations in `AlonBoppana_QA.lean`'s new inert-trio section (QA 6766 → 6782), zero axiom contact (all 16 at `propext, Classical.choice, Quot.sound` via `wip/abpins_axcheck.lean`). **Consumption closure verified by the tool**: census `post16` shows exactly the 3 leaving the inert set (1367 → 1370 value-consumed, 7 → 4, no bonus, no collateral). Full ladder: build + completeness 135/135 fresh, `lint_axioms` (4 unchanged), refutation-independence (24-tag), reachability (63), citations, links, QA-name uniqueness, backlog freshness, scoreboard 1376/6782/4/0, map freshness exit 0 after the stats sync. |
| The Band + Normalized Inert Quartet's Positive Pins ([`band-normalized-pairs-pins.md`](band-normalized-pairs-pins.md), same-run proposal + COMPLETE record) | 2026-09-07 | **The prior handoff's named next batch: four of the census's eleven never-touched theorems — the two 2-clusters whose scope decisions `heat-irreducible-pairs-pins.md` recorded (inert 11 → 7) — the pins method's thirteenth application, with both recorded obstacles dissolved.** Band at the delivered `diag13` fixture: the right-annihilation half `bandProjector_mulVec_eigvecOf_eq_zero_right` first-consumed (the eigenvalue-`3` mode killed by the low band through the theorem, the raw numeric cross-check and not-fixed companion beside it — the mirror of the consumed left-half pin); the vanishing statement `eq_zero_of_bandProjector_mulVec_eq_self` **consumed as the engine of the partition-shares-no-mode nonexistence refutation** (the honest resolution of the "positive instance is `0 = 0`" concern — the theorem turns both fixed-point hypotheses into `w = 0`), with the raw two-route companion (the band images are the coordinate axes) and **the audit's missing `h₁`/`h₂` fences** (empty below/above-spectrum band × covering band, both directions, all closed-form through the extreme-threshold theorems — no opaque values) with kept-genuine isolations; `hab`/`hcd` classified unfenceable (a negated band is minus an honest projector, fixing only `0`). Normalized at the delivered P₃ fixture, resolving the recorded `eigvecOf`-opacity (Mathlib's `eigenvectorBasis`/`eigenvalues` carry an upstream TODO to sort — no index or sign resolvable): a QA-local **eigenvalue-witness bridge** `eigvalOf_of_eigenpair` (hand eigenpair → `eigvalOf` index via `dotProduct_eigvecOf_mulVec` + `eigvecOf_expansion_apply`) locates the eigenvalue-`1` basis index for `walkLaplacian_mulVec_eigvecOf`'s transfer pin (sign-robust entry shape: the mode-`1` eigenspace is one-dimensional along `(1,0,−1)`, scale/sign never resolved because never consumed), and `walk_eigvec_expansion` is consumed through **the aggregate-quadratic join** — the transferred expansion of the center vertex dotted with its conjugate evaluates to `(√2)⁻¹ = 1/√(deg 1)`, the graph's genuine irregularity, through two independent engines (the theorem vs the plain expansion's column-completeness + `√2` diagonal arithmetic). 24 QA declarations across the two QA files (QA 6742 → 6766), zero axiom contact (all 24 at `propext, Classical.choice, Quot.sound` via `wip/bnpins_axcheck.lean`). **Consumption closure verified by the tool**: census `post15` shows exactly the 4 leaving the inert set (1363 → 1367 value-consumed, 11 → 7, no bonus, no collateral). Full ladder: build + completeness 135/135 fresh, `lint_axioms` (4 unchanged), refutation-independence (24-tag), reachability (63), citations, links, QA-name uniqueness (one duplicate `diag13_exists_three` caught at build time and removed), backlog freshness, scoreboard 1376/6766/4/0, map freshness exit 0 after the stats sync. |
| The Probability Trio's Positive Pins ([`probability-trio-pins.md`](probability-trio-pins.md), same-run proposal + COMPLETE record) | 2026-09-07 | **The prior handoff's named next batch: the census's three remaining probability singles (`indepFun_indicator_coord` + the `hoeffding_iid`/`bernstein_iid` twins — the scalar concentration stack's never-instantiated iid forms), first-consumed on ONE shared fair-coin fixture (inert 14 → 11) — the pins method's twelfth application.** The ±1 family `prX` at `prFair = ![1/2, 1/2]` on `Fin 2 → Fin 2`: centered at exactly the fair coin (the `∫ 1_{=0} − ∫ 1_{=1}` decomposition), bounded, mutually independent through `iIndepFun_coord_apply`. 15 QA theorems in `IIDProduct_QA.lean`'s new `SinglesPins` section (QA 6727 → 6742), zero axiom contact (all 15 at `propext, Classical.choice, Quot.sound` via `wip/prpins_axcheck.lean` — the twins are proved theorems). The pins: the pairwise theorem consumed with **a two-route joint-mass join** (the independence factorization vs raw `toMeasure_cyl_inter` arithmetic — both `1/4`); both twins at `t = 2` where the tail event is the same-value pairs **of raw mass `1/2`** (non-vacuity), with `norm_num` display collapses `2 exp(−1)`/`2 exp(−3/4)`; and **the honest extremal-variance contrast** — at `σ² = a²` (Rademacher) the Hoeffding bound is the strictly smaller one by pure monotonicity, the elaborator having rejected the first draft's backwards "Bernstein sharper" `−3/4 < −1` (the sharper direction needs `σ² < a²` at an asymmetric family — priced, not owed). **Consumption closure verified by the tool**: census `post14` shows exactly the 3 leaving the inert set (1360 → 1363 value-consumed, 14 → 11, no bonus, no collateral). Full ladder: build + completeness 135/135 fresh, `lint_axioms` (4 unchanged), refutation-independence (24-tag), reachability (63), citations, links, QA-name uniqueness, backlog freshness, scoreboard 1376/6742/4/0, map freshness exit 0 after the stats sync. Ten fix rounds' traps recorded (headline: the `Decidable`-instance trichotomy on set-membership ifs — cure by instance-agnostic `Finset.sum_ite_eq'` normalization, not fragile rewrites). |
| The Graph-Layer Singles' Positive Pins ([`graph-layer-singles-pins.md`](graph-layer-singles-pins.md), same-run proposal + COMPLETE record) | 2026-09-07 | **The prior handoff's named next batch: seven of the census's ten never-touched singles (inert 21 → 14), all in the deterministic graph layer — the pins method's eleventh application.** Headline: **`isEffectiveResistance_unique` first-consumed** (the strategy doc's own load-bearing example) — the kernel-shifted potential `![2,1] = ![1,0] + onesVec` proved to solve the same unit demand through `laplacian_ones_in_kernel` (the exact mechanism the uniqueness proof identifies), both potentials certifying resistances, and the theorem forcing their voltage differences EQUAL through `eru_edge_uniqueness_twoRoute`, raw arithmetic beside it. Plus `IsMultiwayPartition.covers` at the K₂ singleton partition; `randomWalkLaplacian_symmetric` at off-degree `d = 2` (entries carrying the genuine `1/2`, the `(1,0)` entry read from `(0,1)` through the theorem); `degreeMatrix_diagonal_nonneg`'s positive half at a weight-`2` edge fixture completing the pair with the file's signed kill; and the `edgeWeight` degenerate-cut trio at `C₄` with the raw double-sum companion. 14 QA declarations across five files (QA 6713 → 6727), zero axiom contact (all 14 at `propext, Classical.choice, Quot.sound` via `wip/gspins_axcheck.lean`). Scope decisions recorded: the probability trio (`indepFun_indicator_coord`, `hoeffding_iid`, `bernstein_iid`) deferred to its own shared-fixture run; `l_infty_norm_nonneg` priced for a future Core-targeting QA module. **Consumption closure verified by the tool**: census `post13` shows exactly the 7 leaving the inert set (1353 → 1360 value-consumed, 21 → 14, no bonus, no collateral). Full ladder: build + completeness 135/135 fresh, `lint_axioms` (4 unchanged), refutation-independence (24-tag), reachability (63), citations, links, QA-name uniqueness, backlog freshness, scoreboard 1376/6727/4/0, map freshness exit 0 after the stats sync. |
| The Rank-One Edge Perturbation Norm Bound ([`rank-one-edge-perturbation-norm.md`](rank-one-edge-perturbation-norm.md), COMPLETE + delivery record) | 2026-09-07 | **The Active priority table's Medium row, both steps in one run as its own operating instructions priced: the exact operator norm of one edge-weight mutation `‖laplacian (edgeAdj i j w)‖ = 2 * \|w\|` (at `i ≠ j` — the omitted-hypothesis corner the proposal itself found) plus the subadditive multi-edge bound `‖∑ₖ L(edge eₖ, wₖ)‖ ≤ ∑ₖ 2\|wₖ\|`. 7 public theorems in `EdgePerturbation.lean`'s single-edge-algebra section (functional 1369 → 1376) + 15 QA theorems in `EdgePerturbation_QA.lean`'s new `NormPins` section (QA 6698 → 6713), hard crust, zero axiom contact (22-declaration audit via `wip/epnorm_axcheck.lean` — every one exactly `propext, Classical.choice, Quot.sound`).** The route as designed, entirely through already-proved shelf material: the upper direction is `l2OpNorm_rankOne_le`'s own Cauchy–Schwarz technique with the scalar carried through the public `quadForm_smul`; the missing lower direction consumes two lemmas built for unrelated purposes (`exists_eigvalOf_eq_of_mulVec_eq_smul` — the bottom-eigenvalue witness bridge — plus `abs_eigvalOf_le_l2OpNorm`), now on their third surface; Step 2 is `norm_sum_le` + Step 1 termwise. The weighted rank-one equality `‖w • rankOne v‖ = \|w\| * (v ⬝ᵥ v)` lands as reusable packaging. QA per the proposal's own plan: the raw closed form at symbolic weight; value pins at positive/negative/zero `w` each joined to the old action-bound route (two routes, one value); **the `i = j` fence in the same delivery** (loop Laplacian raw-zero, `2\|3\| = 6 ≠ 0`); the two-update parallel sequence with the subadditive bound **attained with equality** (`12 = 12`); the three-edge `Fin 3` instance with the summed Laplacian computed raw — signed degrees on the diagonal (`1 + (−2) = −1`), the elaborator having rejected the first draft's unsigned-degree literal on the `(1,1)` entry. Full ladder: build + completeness 135/135 fresh, `lint_axioms` (4 unchanged), refutation-independence (24-tag), reachability (63), citations, links, QA-name uniqueness (`epn_*` collision-free), backlog freshness, scoreboard 1376/6713/4/0, map freshness exit 0 after the stats sync. No `laplacian_edge_update` definition, no probabilistic content, the deferred general-smul-eigenvalue theorem untouched — every scope decision of the proposal honored. |
| The Heat + IrreducibleStationary Pairs' Positive Pins ([`heat-irreducible-pairs-pins.md`](heat-irreducible-pairs-pins.md), same-run proposal + COMPLETE record) | 2026-09-07 | **The prior handoff's "cheap batch of pairs" option: two of the census's four remaining 2-clusters (`heatKernel_decayFactor_le_one`, `normalizedHeatKernel_zero`, `isIrreducible_transpose`, `pow_entry_le_one` — the decay family's `ht` fences existed but the positive `0 ≤ t` statement was never instantiated; the transpose and power-entry theorems never consumed): first genuine consumption of all four** — 13 QA declarations across `Heat_QA.lean` + `IrreducibleStationary_QA.lean`'s new `StructuralPins` sections (QA 6685 → 6698), zero axiom contact (all 13 at `propext, Classical.choice, Quot.sound` via `wip/hipins_axcheck.lean`). The pins: the decay factor **attained with equality at the constant mode** (`e^{-t·0} = 1 ≤ 1` — the undamped kernel, the bound sharp) and **strict at the Fiedler mode** (`e^{-2} < 1` by the file's `Real.exp_lt_exp` idiom); the `t = 0` identity acting on the antisymmetric vector through the theorem (no matrix exponential computed); **the transpose-irreducibility two-route join on GENUINELY DIRECTED input** (the `A3` star's walk matrix — asymmetric even at symmetric `A3`): route A through `isIrreducible_transpose`∘`walkTransitionMatrix_isIrreducible`, route B raw `ReflTransGen` paths in the arc-reversal digraph at the pinned `A3P_*` entries; the chain consumed onward (`exists_pow_pos_of_isIrreducible` on the transpose result, `∃ m, 0 < (Pᵀ^m) 0 1`, with the raw witness `(Pᵀ) 0 1 = 1`); and the `[0, 1]` power-entry bound **attained with equality** at the star's two-step return `(P²) 0 0 = 1 ≤ 1`, raw companion included. Scope decisions recorded: the Band pair's vanishing member left for a fence-audit delivery (its positive instance is `0 = 0`); the Normalized pair priced as residue (opaque `eigvecOf` signs). **Consumption closure verified by the tool**: the census re-run (`wip/census_20260907_post11.txt`, diffed) shows exactly the 4 leaving the inert set (1342 → 1346 value-consumed, 25 → 21 never-touched, no bonus, no collateral). The pins method's tenth application. |
| The Mixing Trio's Positive Pins ([`mixing-trio-pins.md`](mixing-trio-pins.md), same-run proposal + COMPLETE record) | 2026-09-07 | **The prior handoff's natural size pick: the census's Mixing (3) cluster — real mixing-program theorems (`chiSquareDistance_eq_zero_iff`'s iff never applied in either direction; `walkDistribution_tvDistance_le_max_rate`'s χ² and lazy TV twins both consumed, the plain TV form never; `klDiv_lazyWalkDistribution_le` the consumed headline's lazy twin): first genuine consumption of all three** — 15 QA declarations in `Mixing_QA.lean`'s new `StructuralPins` section (QA 6670 → 6685), zero axiom contact (all 15 at `propext, Classical.choice, Quot.sound` via `wip/mxpins_axcheck.lean`). The pins: the K₂ top-eigenvalue card-form pin + the certificate `max(1−λ₂, λ_max−1) = 1` exactly (both branches `λ₂ = λ_max = 2` — the periodic chain's honest `r = 1` corner); the plain TV shadow instances with the certificate COMPUTED from the pinned spectrum (K₂ attained with equality `1/2 = (1/2)·√1`; triangle both sides pinned `1/3` vs inner `1/2` — the computed-certificate twin of the hand-supplied `tri_tv_rate_one_QA`); **the lazy KL twin with both sides collapsing to `0`** (KL vanishes at delivered exact lazy stationarity `k2_lazy_law_one_QA`; the lazy rate `1−2/2 = 0` collapses the bound — attained at `0 ≤ 0`); and **the vanishing iff at the delivered looped triangle, both directions consumed** — χ² `= 0` THROUGH the ⇐ from raw one-step plain stationarity (the looped fixture's law is exactly uniform — the plain-walk analogue of `k2_lazy_law_one_QA`), stationarity THROUGH the ⇒ from the raw zero χ² (two routes, one value) — plus the contrapositive companion: on the periodic K₂ the pinned χ² `= 1 ≠ 0` makes the iff a *non-mixing certificate*. **Consumption closure verified by the tool**: the census re-run (`wip/census_20260907_post10.txt`, diffed) shows exactly the 3 leaving the inert set (1339 → 1342 value-consumed, 28 → 25 never-touched, no bonus, no collateral). The pins method's ninth application. |
| The Poincaré Pair's Connectivity-Route Pins ([`poincare-connectivity-pins.md`](poincare-connectivity-pins.md), same-run proposal + COMPLETE record) | 2026-09-07 | **The census's name-weight pick from the remaining inert set (30): the family's two headline connected twins (`poincare_inequality_of_connected`, `poincare_inequality_normalized_of_connected`), never consumed — every existing pin routed through the `hpos` forms with the gap pinned separately, so the connectivity-transfer route inside the twins was unexercised. First genuine consumption of both** — 9 QA theorems in `Poincare_QA.lean`'s new `StructuralPins` section (QA 6661 → 6670), zero axiom contact (all 9 at `propext, Classical.choice, Quot.sound` via `wip/pcpins_axcheck.lean`). The pins: the support graphs of all three connected fixtures (K₂/P₃/K₃) proved connected by the walk idiom — the `hconn` hypotheses' first discharge through the matrix→`SimpleGraph` adapter at these fixtures; the twin instances at the Fiedler vectors with no spectral certificate in hand (the gap's positivity delivered *inside* the theorem from connectivity), each joined to the delivered raw attainment companions (both sides pinned `2`/`2`/`4` — the connectivity route delivers the sharp constants the wrong-constant refutation proves unimprovable); and the not-connected scope witness (walk induction at the disconnected fixture: no positive cross-block entry, walks stay in-block — `hconn` is exactly the boundary the no-constant fence prices). **Consumption closure verified by the tool**: the census re-run (`wip/census_20260907_post9.txt`) shows exactly the 2 leaving the inert set (1337 → 1339 value-consumed, 30 → 28 never-touched, no bonus, no collateral). The pins method's eighth application. |
| The Edge-Perturbation Design's Independence + PSD Positive Pins ([`edgeperturbation-design-pins.md`](edgeperturbation-design-pins.md), same-run proposal + COMPLETE delivery record) | 2026-09-07 | **The census's largest remaining inert cluster (4 theorems — the pairwise `h_indep` clause shapes of both concentration axioms at the design plus its PSD floor) together with the 2026-08-30 repair's never-consumed generic passthrough: first genuine consumption of all five** — 16 QA declarations in `EdgePerturbation_QA.lean`'s new `StructuralPins` section (QA 6645 → 6661), the 15 hard-crust ones at `propext, Classical.choice, Quot.sound` via `wip/eppins_axcheck.lean`, the passthrough instance honestly carrying `matrix_hoeffding`. The pins: the rank-one PSD at a NON-eigenvector input (raw inner product `−1`, energy `1`, nonnegativity THROUGH the theorem — the sign flip a negative-definite reading breaks); the edge-block energy `4` raw with nonnegativity through the theorem; the negative-weight scope witness (the dropped-`h` statement refuted at energy `−4 < 0`); **the two joint-mass factorizations THROUGH the design's pairwise `IndepFun` theorems** (`1/2 · 1/2 = 1/4`, each preimage its coordinate cylinder) with raw `sum_coord2_mul` companions (two routes, one value) — the matrix side at entry-fiber level sets measurable through `measurable_pi_apply` over the shelf's product σ-algebra (`MeasurableSingletonClass` does not fire through the `Matrix` synonym); and **the passthrough instance** at the full `K₂` `Fintype.equivFin` family, bound collapsed to `4 exp(−1/16)` — the generic-route twin of the existing `epK2_quadForm_tail_QA` value. **Consumption closure verified by the tool**: the census re-run (`wip/census_20260907_post8.txt`) shows exactly the 5 leaving the inert set (1332 → 1337 value-consumed, 35 → 30 never-touched, no bonus, no collateral). The pins method's seventh application. |
| The Signed Switching Layer's Positive Pins ([`signed-switching-pins.md`](signed-switching-pins.md), same-run proposal + COMPLETE delivery record) | 2026-09-07 | **The census's largest remaining inert cluster (4 theorems — the balance theory's engine, including the never-consumed BACKWARD eigenpair transfer `laplacian_mulVec_switchVec`, the spectral bridge of Harary balance): first genuine consumption of all four** — 10 QA theorems in `Signed_QA.lean`'s new `StructuralPins` section (QA 6635 → 6645), zero axiom contact (all 10 at `propext, Classical.choice, Quot.sound` via `wip/sgpins_axcheck.lean`). The pins: the two symmetry theorems with ENTRY-level reads through `IsSymm.apply` (the `(1,0)` entries derived from the raw `(0,1)` entries — `−1` for the signed adjacency, `+1` for the sign-flipped Laplacian edge); **the scope witness** (the frustrated triangle — symmetric but NOT balanced — still gets a symmetric signed Laplacian, pinning the hypothesis set as symmetry, not balance); the switching nonvanishing at a value-carrying vector (`diag(g)·(2,3,5) = (2,−3,−5)` raw, nonzeroness through the theorem); and **the round-trip headline** — the unsigned eigenpair `L·(1,0,−1) = 1·(1,0,−1)` computed raw (route A) AND re-derived through the backward transfer + switching involutivity from the raw signed eigenpair `L_σ·(1,0,1) = 1·(1,0,1)` (route B) — two routes, one value. **Consumption closure verified by the tool**: the census re-run (`wip/census_20260907_post7.txt`) shows exactly the 4 leaving the inert set (1328 → 1332 value-consumed, 39 → 35 never-touched, no bonus, no collateral). The pins method's sixth application and its first signed-graph target. |
| The Boundary Outflow Lemma — COMPLETE (both parts) ([`boundary-outflow-lemma.md`](boundary-outflow-lemma.md), Step 1 + Part 2 delivery records) | 2026-09-07 | **The operator-requested cut↔heat bridge end-to-end. Step 1** (run `051321Z`): the algebraic cut characterization, hypothesis-free — 5 public theorems in `Multiway.lean` + 10 QA (including the finding that `quadForm_laplacian_partIndicator`'s `hA` is removable), hard crust. **Part 2 / Step 2** (run `053227Z`): the regional dissipation bound `|1_S ⬝ᵥ (x₀ − e^{−tL}x₀)| ≤ t·‖L·1_S‖·‖x₀‖` — via the companion's Steps 0(m=0)/1/2 delivered under the named-consumer scope (the Medium row itself being the "different named consumer" the companion's gate names): the scalar engine `sq_one_sub_exp_neg_le`, the global window-free contraction `heatKernel_globalContraction_dotProduct_le`/`_le` (`‖x − e^{−tL}x‖ ≤ t·‖Lx‖` at every `t ≥ 0`, no eigenvalue window), and the general-probe engine `abs_dotProduct_heatFlow_le` + the `partIndicator` specialization (functional 1364 → 1369, QA 6625 → 6635, zero axiom contact — 16-declaration audit all standard-three). QA per both proposals' plans: the K₂ `t = 1` payoff pin (the global bound holds THROUGH the theorem exactly where the windowed bound's hypothesis provably fails), and **the `hnonneg` fence in the same delivery** — on the signed negative-eigenvalue fixture the conclusion provably fails (mode growth `3 < e²` via `Real.add_one_lt_exp`). Both parts' records carry the gate-interpretation note for operator review; the companion's order-`m` Steps 3–5 remain gated. |
| The Functional-Calculus Structural Facts' Positive Pins ([`functionalcalculus-structural-pins.md`](functionalcalculus-structural-pins.md), same-run proposal + COMPLETE delivery record) | 2026-09-07 | **The prior handoff's next cluster by size (5 theorems — the calculus-bridge layer spanning the real wrapper and the complex magnetic propagator): first genuine consumption of all five** — 9 QA theorems in `FunctionalCalculus_QA.lean`'s new `StructuralPins` section (54 → 63; QA 6606 → 6615), zero axiom contact (all 9 at `propext, Classical.choice, Quot.sound` via `wip/fcpins_axcheck.lean`). The pins: the identity-function two-route join at the `K₂` Laplacian (theorem route + eigenbasis route composing to the pinned matrix, each failing independently); the coefficient bridge at the squared spectrum with the propagated vector pinned `![2, -2]` (the `λ = 0` mode killed by `f(λ) = 0` on the right, the `λ = 2` mode by `f(L) *ᵥ y` on the left); **the two-technology resolvent join** — the calculus-route twin `spectralCalc_tikhonovShrinkage_eq_smul_inv'` deriving the shifted inverse's concrete value `!![2/3, 1/3; 1/3, 2/3]` from the eigenbasis-pinned filter (no adjugate anywhere), with the raw `inv_def` companion as the independent engine; and the magnetic-propagator structural pins — Hermitian and diagonal-real through the entry form's exact `U`/`conj U` placement (basis-free), the exhibited eigenpair (spectral-mapping content, nonzeroness via `Basis.ne_zero`), and the basis-vector semigroup through the action theorem three times. **Consumption closure verified by the tool**: the census re-run (`wip/census_20260907_post4.txt`) shows exactly the 5 leaving the inert set (1313 → 1318 value-consumed, 44 → 39 never-touched, no bonus, no collateral). The pins method's fifth application. |
| The Magnetic Structural Facts' Positive Pins ([`magnetic-structural-pins.md`](magnetic-structural-pins.md), same-run proposal + COMPLETE delivery record) | 2026-09-07 | **The census's largest remaining inert cluster (6 theorems — the complex Hermitian layer's action-level half, including the module's kernel-characterization and PSD headlines): first genuine consumption of all six** at the delivered fixtures — 12 QA theorems (the pin eleven plus the `magA_nonneg` hypothesis helper) in `Magnetic_QA.lean`'s new `StructuralPins` section (16 → 28; QA 6594 → 6606), zero axiom contact (all 12 at `propext, Classical.choice, Quot.sound` via `wip/magpins_axcheck.lean`). The pins: the pairing two (the numeric `conj (I)·I = 1` derived THROUGH the unit-phase theorem at `π/2`, the route kept in the proof term); the real-valuedness promotion (the full complex value `2` at `![1, I]` ASSEMBLED from the raw real energy plus the theorem's vanishing imaginary part — `.re = 2` alone does not give `= 2`); PSD at genuinely directed input against the pinned energy `5`; the action-kernel three (the row-algebra reverse direction at the π-flux edge; `magK2_antipodal_kernel`'s conclusion re-derived through the ACTION route; the iff's ⇐ at zero phase); **the classical join** — the same zero-phase kernel fact through the independent cone-agreement route (entrywise the complexified classical Laplacian, constant-kernel by raw `1 − 1 = 0`); and the forward-direction three (the frustrated triangle's trivial kernel; the NEGATIVE action-level witness through the iff's ⇒; and the directed-frustration headline — the asymmetric fixture's `π`/`π/2` phase pair forces the trivial kernel on GENUINELY DIRECTED input, the design fixture never before exercised at action level). The pins method's fourth application and its first complex-valued target; the iff's `hA` forward-half fence priced as residue. **Consumption closure verified by the tool**: the census re-run (`wip/census_20260907_post3.txt`) shows exactly the 6 leaving the inert set (1307 → 1313 value-consumed, 50 → 44 never-touched, no bonus, no collateral). |
| The Sparsification Structural Lemmas' Positive Pins ([`sparsification-structural-pins.md`](sparsification-structural-pins.md), same-run proposal + COMPLETE delivery record) | 2026-09-07 | **The census's largest remaining inert cluster (9 theorems — the sampled-Laplacian machinery under `matrix_bernstein`'s one real theorem consumer, never themselves consumed by QA): first genuine consumption of all nine** at the delivered `spK2` fixture, at the natural unsaturated budget `q = 1` (pair probability `1/2`, inverse-probability weights exactly `2`/`0`) — 15 QA theorems in `Sparsification_QA.lean`'s new `StructuralPins` section (QA 6579 → 6594), zero axiom contact (all 15 at `propext, Classical.choice, Quot.sound` via `wip/sppins_axcheck.lean`). The pins: the projector four (symmetry with the entry equality THROUGH the theorem; idempotence with the diagonal squaring read through it; PSD; the norm bound); the sampling-design four (the sampled operator's symmetry at every outcome; the two distinct ordered pairs' summand independence at the design's own measure — the `h_indep` clause's design fact; the weight nonnegativity at both outcomes made nontrivial by the pinned values `2`/`0`); and the sampled-Laplacian three (symmetry; PSD; and the computed VALUE: at the all-true outcome the sampled Laplacian is exactly TWICE the true one — the alternating vector's form `8`, the diagonal pairs contributing zero through the vanishing voltage difference alone — load-bearing on the weights, the `1/2` ordered-pair factor, and `rankOne` together). **Consumption closure verified by the tool**: the census re-run shows exactly the 9 targeted theorems leaving the inert set (1298 → 1307 value-consumed, 59 → 50 never-touched, no bonus, no collateral). The pins method's third application and its first matrix-level target. |
| The Mixing-Time Interface Layer's Positive Pins ([`mixing-time-interface-pins.md`](mixing-time-interface-pins.md), same-run proposal + COMPLETE delivery record) | 2026-09-07 | **The census's next named target after Tikhonov, re-confirmed by a fresh census run at the run's opening (81 → 70 inert — the Tikhonov closure claim reproduced by the tool itself): first genuine consumption of all 10 designed-but-unconsumed `t_mix` interface theorems** — 18 QA theorems (DirectedMixing_QA +6 at the A2 fixture, Mixing_QA +12 at the triangle/path fixtures; QA 6561 → 6579), zero axiom contact (all 18 at `propext, Classical.choice, Quot.sound` via `wip/mtifaces_axcheck.lean`). The prior QA had *evaluated the interfaces' displays beside the objects* (attainment pins re-deriving both sides separately) without ever applying them; now: the per-start and uniform PageRank rate ceilings re-derive the pinned exact object values THROUGH the theorems (`⌈log 4/log 2⌉ = 2`); the ⌈log⌉ escalation displays at BOTH the Google law (`(⌈log 4/log 4⌉+1)·2 = 4`, the pinned exact `t_mix(1/32) = 4`) and the walk law (`(⌈log 2/log 2⌉+1)·1 = 2`, the pinned exact `t_mix(1/6) = 2`); antitonicity delivers lower bounds the attainment route did not (`2 ≤ t_mix(1/16)` at both the Google and lazy laws, from the pinned `t_mix(1/8) = 2` values); the uniform spec yields the all-depths certificate from the pinned object; the intrinsic-rate lazy ceiling's ≤ direction is consumed for the first time (the docstring's aspirational QA claim — the Tikhonov header pattern, second instance — made true, at the honest slack `3` vs the pinned `2`, the display `⌈log(4√3)/log 2⌉ = 3` by the rational route `1 < √3 < 2`); the depth-form lazy TV ceiling and the entrywise max-rate ceiling (bound `(1/2)·√(2/3)` vs the pinned deviation `1/6`, with the fresh top-eigenvalue companion) complete the set. **Consumption closure verified by the tool**: the post-delivery census re-run shows 11 theorems leaving the inert set — the 10 targeted PLUS `lazyWalkDistribution_tvDistance_le_of_connected`, reached transitively through the ceiling theorem's proof chain (an honest bonus); census 1287 → 1298 value-consumed, 70 → 59 never-touched. |
| The Tikhonov Family's Adversarial Fence Audit and First Positive Pins ([`adversarial-fences-tikhonov-family.md`](adversarial-fences-tikhonov-family.md), same-run proposal + COMPLETE delivery record) | 2026-09-07 | **The consumption census's top named target — the exact tool's first application as a targeting instrument: the library's least-consumed theorem family closed on both halves. Positive pins (`tkp_`)**: the first genuine QA consumption of all 11 never-consumed Tikhonov theorems — the pre-audit QA *re-derived* minimality/uniqueness numerically beside the theorems (`minimality_K2`/`y_ne_min_K2` by direct computation; the header's "consumed contrapositively" claim verified aspirational this run), now the shrinkage-arithmetic six pinned at both `K₂` eigenvalues (the antitonicity read numerically `1/3 < 1` THROUGH the theorem; `eq_one_iff` consumed contrapositively), the FORWARD normal equation re-deriving the hand-solved system from the theorem, minimality and uniqueness THROUGH the theorems (uniqueness forward — a genuinely different route than the normal equation), the eigenvector-input `T v = (1/3) • v`, and non-idempotence through the theorem. **Fences (`tkf_`)**: the shrinkage-arithmetic five's `hπ`/`hlam` clauses at junk-free fixtures with the junk corners recorded where the kill IS junk (`1/0 = 0`, `0/0`) and the genuine boundary `λ = 0` kill of strict attenuation; the `π = 0` matrix guards on the general lemma `tik_K2_zero_gen` (the `π = 0` minimizer is zero for EVERY signal) — the sum/characterization/minimality/uniqueness/sub-minimizer `hπ` clauses, several with kept-genuine companions proving the remaining hypotheses hold at the breaker (the clearest: uniqueness's tie hypothesis GENUINE at `x = 2y` where the objective degenerates to fidelity), the 2026-08-20 free-form witness `not_minimality_pi0_K2` reconciled into the discipline, and the heaviest fence killing the strict-convexity decomposition identity at the degeneration (left `−1` vs Parseval `1`); the KERNEL-MODE kill of the non-idempotence theorem's `hμ` through the eigen theorem itself (`T v = v` at `λ = 0`, the dropped inequality `v ≠ v`); and the PSD clause at the fresh signed network (spectrum `{0, −2}` pinned through trace/determinant, symmetry kept genuine). QA-only, 48 declarations (+48, 6513 → 6561), zero axiom contact (all at `propext, Classical.choice, Quot.sound` via `wip/tikfences_axcheck.lean`); the pole-class deferrals priced with both routes. Delivered across two runs (16:06Z landed the QA; `20260907T013052Z-run-1` verified, audited, and recorded). The family leaves the census's least-consumed rank. |
| The Compiler-Derived QA→Shelf Consumption Survey ([`compiler-derived-consumption-survey.md`](compiler-derived-consumption-survey.md), same-run proposal + COMPLETE delivery record) | 2026-09-06 | **The exactness fix three consecutive terminal handoffs named — the string-heuristic survey keys' false inert-declarations replaced by the elaborator's own answer: `scripts/consumption_survey.py` generates Lean metaprogram probes (umbrella + collision-partitioned QA groups with a load-bearing bisection fallback — the name-collision guard's documented failure mode was hit on the first run, four bisects), computes the memoized transitive constant-closure of every QA declaration's VALUE and TYPE separately over the Scaffold-only universe, and emits an exact value-vs-type-edge TSV; report-only by design (11 probe elaborations, ~8 min — a targeting tool, not a ladder check). Step-0 validation in-script (3 known-true dependencies from the last two deliveries reproduced) plus hand spot-checks of every inert-side string hit (all resolve to fence docstrings, substring artifacts, or the one primed-variant near-miss). First census: 8,988 QA declarations with shelf contact, 76,218 value-edges, 25,571 type-edges; 1,357 public shelf theorems (1,276 value-consumed, 81 never touched), 182 defs (3 never — the definition-only `Core` modules, compiler-confirming the hand survey), 4 axioms with exact QA consumer counts (23/8/6/3 — `perron_frobenius`'s deprecation question is now answerable). Headline finding: the hypothesis-form fences are consumption-free BY DESIGN (~17 of the 81 inert theorems are fenced-but-never-instantiated — exactly where every string key counted name mentions as coverage); Tikhonov is the least-consumed family (11 untouched theorems, the named next audit target); the mixing-time interfaces (`*_spec`/`*_anti`/`*_le_of_*`, 10 theorems) are designed-but-unconsumed; the iid wrappers and the 2026-08-30 repair's `matrix_hoeffding_quadForm` passthrough carry zero positive consumers. Counting-metric reconciliation recorded (elaborated 1,357 vs scoreboard 1,359 source-regex: Prop-field projections vs prose artifacts, both verified at name level). Zero Lean source changed; ladder re-run post-delivery.**
| The Sharp-Layer Family's Adversarial Fence Audit ([`adversarial-fences-sharp-layer-family.md`](adversarial-fences-sharp-layer-family.md), COMPLETE + delivery record) | 2026-09-06 | **The standing audit pattern applied to the day's own capstone family — found by the theorem-level QA-mention survey, the new survey key after the module-level keys were exhausted (the family's sixteen public theorems carried pins only, no fence pass): 22 hypothesis-form fences + 3 fixtures in `PageRank_QA.lean`'s new `SharpFences` section (+22, 6445 → 6467), QA-only, zero axiom contact (21 audited declarations via `wip/sharpfences_spike.lean` — every one exactly `propext, Classical.choice, Quot.sound`).** Every unfenced load-bearing clause closed: the mass lemma's / shadow's / ceiling's `hc` (the constant vector's genuine `1`-pair, mass `2`); the shadow's `hα` (the junk `0/0` at the genuine `0`-pair); the ceiling's `hα` at negative `α` — **with the census finding caught by the spike: at the two-cycle `G(−1) = I`, its off-one spectrum EMPTY, the fence living at the primitive fixture instead** — the ceiling's `hnn` (signed adjacency, eigenvalue `−12/5`); the `hμ0` junk corner; the twin's `hmass`; the iff's `hc1` (at `c = α = 1` the left side holds while no mass-zero `1`-pair of the swap exists); the sign-rigidity engine's `hrow` (a non-stochastic `M` with `M² = 1 > 0`, mixed-sign peripheral pair) and `hk` (the swap, consuming `prCyc_not_primitive`); the doubly-stochastic `hcol` (the delivered `prPrim` witness in fence form). Survey honesty recorded in the proposal: only the direct-mention half of the new survey key is exact; the transitive-closure half produced false inert-declarations and drove nothing. || The Right-Eigenvector Forms of the Sharp Layer ([`right-eigenvector-sharp-layer.md`](right-eigenvector-sharp-layer.md), same-run proposal + COMPLETE delivery record) | 2026-09-06 | **The completed sharp-layer proposal's Slice-4 priced follow-up, delivered in its opening run — pure hard crust, zero new axioms: 18 audited declarations via `wip/righteigen_axcheck.lean`, every one exactly `propext, Classical.choice, Quot.sound`.** The centerpiece is the general **left/right spectrum bridge** `hasEigenvalue_mulVecLin_iff_vecMulLinear` — a square matrix's left and right eigenvalue sets coincide, any field — built from `Matrix.exists_mulVec_eq_zero_iff` + `det_transpose` + `mulVec_transpose` (the kernel-level `det Mᵀ = det M`; the priced charpoly route was heavier than needed; reusable beyond the Google family), with its two raw-pair↔determinant factoring lemmas. On it: the right-convention ceiling `|c| ≤ α` and strictness `|c| < α` (eigen-level + raw `G *ᵥ v = c • v` forms) — Haveliwala–Kamvar at the power-method convention, where every `M^t *ᵥ x` consumer lives. QA (+11) carries the two boundary witnesses recording why a new route was needed — the right eigenvector `(5,−9)`'s NONZERO mass `−4` (the left mass lemma cannot transfer) and the no-right-shadow witness (the left shadow lemma is genuinely FALSE at the right: teleportation shifts right-eigenvectors off the walk's shadow) — plus the two-cycle's right `-α` pair attained (`G(4/5)` pinned closed-form) and the bridge pinned at the walk matrix. Functional 1336 → 1343, QA 6420 → 6431. The proposal's doubly-stochastic follow-on was delivered the same day (run `20260906T110500Z-run-1`): on column-stochastic walks (every Eulerian/regular chain) the right mass lemma, right shadow, and right attainment twin all hold — the left theorems transported at `A := Pᵀ` through `googleMatrix Pᵀ α = Gᵀ` (+5 shelf, +10 QA, all at the standard three via `wip/dblstoch_axcheck.lean`), with the two-cycle `-1` shadow round-trip, the end-mix `0`-pair, and the non-symmetric doubly-stochastic `Fin 3` cyclic-chain fixture proving the hypothesis class genuinely larger than the symmetric cone (on `Fin 2` it collapses to it). A third follow-on delivered the **sorted-spectrum forms — the literal `λ₂` on regular graphs** (run `20260906T114513Z-run-1`): where `G` is symmetric (regular input), the top eval is exactly `1` — through the NEW `1`-eigenspace simplicity argument (teleportation strict positivity + the sign-rigidity engine + min-ratio, no Perron–Frobenius) — with `evals ⟨n−2⟩ ≤ α` and `-α ≤ evals ⟨0⟩` (+9 shelf, +7 QA, functional 1350 → 1359, all at the standard three via `wip/sorted_spike.lean`; QA pins the two-cycle's bottom `−α` attained through the trace engine). A fourth follow-on pinned its priced separating fixture (run `20260906T141850Z-run-1`): the 4-cycle — the smallest regular graph whose `G(4/5)` (spectrum ascending `{−4/5, 0, 0, 1}`, pinned as a concrete matrix) separates the second-from-top index from the bottom — with the exact middle pins `evals ⟨1⟩ = evals ⟨2⟩ = 0` through the subspace Rayleigh–Ritz engine `evals_le_of_linearIndependent` at `k = 2` and `k = 3` (its first exercise on the PageRank family) plus the trace engine, the bottom `−α` attained, and the separation itself stated (`|λ₂| = 0 < 4/5` strictly inside while `|evals ⟨0⟩| = α`; `0 ≠ −4/5` at distinct indices) — QA-only (+39, 6474 → 6513, all 39 at the standard three via `wip/c4_axcheck.lean`). || The Sharp `|λ₂| = α` Layer, COMPLETE — all five slices ([`sharp-second-eigenvalue-layer.md`](sharp-second-eigenvalue-layer.md), delivery records in the proposal) | 2026-09-06 | **The directed-rate program's only remaining item, opened as its own multi-run proposal and closed the same day in five slices — all hard crust, zero new axioms (count stays 4): every slice's declarations at exactly `propext, Classical.choice, Quot.sound` (`wip/sharp_axcheck.lean`, `wip/dobrushin_axcheck.lean`, `wip/exactshadow_axcheck.lean`, `wip/eigen_axcheck.lean`, `wip/strict_axcheck.lean`).** The Haveliwala–Kamvar program on the Google matrix `G(α)`: Slice 1 the ceiling `|c| ≤ α` (the mass lemma, the shadow lemma, the ℓ¹ peripheral bound, the attainment twin, at the vecMul level); Slice 2 (a) the rate connection (the Dobrushin shadow: the exact pointwise TV scaling, the shadow ceiling, the pair rate `pageRankTVPair A α t ≤ (α·δ(P))^t ≤ α^t` at the sharper constant); Slice 3 (a) the exact shadow equality `δ(G) = α·δ(P)` and the exact `t = 1` identity (`Finset.sup'_induction`, no order on the index); Slice 4 the eigenvalue-level forms through the definitional `vecMulLinear` bridge into Mathlib's eigen-API; **Slice 5 the strictness layer** — on a primitive (aperiodic) walk every off-one Google eigenvalue is STRICTLY inside the α-disk, `|c| < α`, by the peripheral sign-rigidity argument (equality in the ℓ¹ contraction at a strictly positive power forces a common sign; zero mass forces the zero vector — **no Perron–Frobenius consumed, elementary over ℝ**), with the eigen-level twin, the strictness pins at the fresh primitive two-vertex fixture (`P² > 0` entrywise, the `-α/2` eigenpair), and the necessity fence at the periodic two-cycle (powers alternate identity/swap — primitivity exactly excludes what attainment needs). Final counts: functional 1313 → 1336, QA 6387 → 6420 across the five slices — and the layer's item-3 characterization iff (the "two halves not composed" note) composed as a follow-on the same day (run `20260906T111951Z-run-1`: `googleMatrix_abs_eigen_eq_alpha_iff` — the ceiling attained ⟺ a mass-zero peripheral walk eigenpair — plus its doubly-stochastic right twin and the primitive fixture's negative witness; functional 1348 → 1350, QA 6441 → 6445, all at the standard three via `wip/iff_axcheck.lean`). Honest scope carried in the proposal: all statements at the left-eigenvector (`vecMulLinear`) convention; the `|λ₂| = α` iff characterization has both halves delivered but not composed into one theorem. || The `walkTVPair`–Dobrushin Engine Join ([`walktvpair-dobrushin-join.md`](walktvpair-dobrushin-join.md), COMPLETE + delivery record) | 2026-09-06 | **The standing frontier's most concretely priced item (named by the terminal handoffs since the uniform-mixing delivery priced the join as "a follow-on if a consumer names it"), delivered as a shelf change with unchanged public statements: the identity `walkTVPair A t = tvDobrushinCoeff ((walkTransitionMatrix A) ^ t)` — NO symmetry, NO stochasticity, via the transpose-power bridge (the `sup'`-defining summands of the two sides are the same function of `p`, closed by `Finset.sup'_congr`) — plus the two proof re-routes: `walkTVPair_submul` (the ~30-line bespoke chain → three identity rewrites + the engine's `tvDobrushinCoeff_pow_add_le`) and the sharp walk contraction (the ~50-line bespoke sign-statistic proof → the vecMul↔mulVec transpose bridge + `tvDistance_vecMul_le_tvDobrushinCoeff`), the retired private pairing core deleted with a tombstone.** Functional 1312 → 1313 (the identity is the one new public theorem), QA 6384 → 6387 (`Mixing_QA.lean`'s new `EngineJoin` section: engine-side identity pins at the delivered `triAdj` — `δ(P) = 1/2`, `δ(P²) = 1/4` matching the pinned `walkTVPair` values — and the attained submultiplicativity instance re-derived). `#print axioms` via `wip/walkjoin_axcheck.lean` on the identity, both re-routed theorems, and the pins — every one exactly the standard three; full ladder green (build + completeness 135/135; the downstream closure covered by the full build through the `EmpiricalStationary` capstone). Finding recorded: the join is strictly more general than the undirected family it was priced on — the identity needs no symmetry. |
| The Primitive-Convergence Remainder: the Walk Quartet + the Positive-Clause Tail + the Trio Classifications ([`adversarial-fences-primitive-convergence-family.md`](adversarial-fences-primitive-convergence-family.md), remainder-closure record) | 2026-09-06 | **The audit's remaining priced deferral, closed — the family's priceable clause surface is now CLOSED IN FULL**: ten hypothesis-form fences in `PrimitiveConvergence_QA.lean`'s new `CancellationFences` section (+42, 6342 → 6384; 52 declarations audited via `wip/pcvfences3_axcheck.lean` — every one a subset of the standard three), zero axiom contact. **The signed-cancellation quartet** — positive power-entries through negative-product arithmetic, every kept clause genuine: `reachable_of_pow_pos`'s `hnn` at `!![1,-2;0,-2]]` (`(P²) 0 1 = 2 > 0` pure cancellation, no positive step leaving vertex 0); `isIrreducible_of_isPrimitive`'s `hnn` at `!![0,-1;-1,-1]]` (NO positive single-edges yet `P²` strictly positive — primitivity genuine, irreducibility failing); `pow_entry_pos_of_pos`'s `hnn` at `!![1,1;0,-2]]` (`(M²) 0 1 = -1` against both kept clauses genuine); `pow_entry_pos_bounce`'s `hnn` at the 3×3 `!![0,1,-2;0,0,1;0,1,1]]` (`(M³) 0 1 = -1`). **The positive-clause tail** the original cull never reached: the `h1`/`h2`/`hz1`/`hz2`/`h1` family at split/degenerate fixtures, and the odd-loop supplier's `hreach` at `!![0,1,0;1,1,0;0,1,1]]` with every kept clause genuine (including `hodd` pinned) yet `(0,2)` unreachable by any power. **The classifications:** the trio's `hnn` truth-removable (primitive stochastic ⟹ Perron peripheral spectrum `{1}` ⟹ convergence — the shelf's `hnn` use is a proof artifact) and `hrow` without an admissible fixture (strict-contraction propagation sketch). |
| The Primitive-Convergence Audit's π-Clause Deferral Cull + the Survey Residual Classification ([`adversarial-fences-primitive-convergence-family.md`](adversarial-fences-primitive-convergence-family.md), deferral-closure record) | 2026-09-05 | **The audit's own priced follow-up delivered: five hypothesis-form fences plus the entanglement pin in `PrimitiveConvergence_QA.lean`'s new `PiClauseFences` section (+22, 6320 → 6342), zero axiom contact (`#print axioms` via `wip/pcvfences2_axcheck.lean` — every one a subset of the standard three). The engine: the strictly-positive fixture `pcvPos` with the two-idempotent spectral decomposition evaluated entrywise (`P^t = (1/2 + r_t/2, 1/2 − r_t/2; …)`, `r_t = (1/2)^t`), so the actual limit of `P^t *ᵥ x` is the averaging vector; the fences kill the claimed wrong limits by `tendsto_nhds_unique` — `hπstat` at the non-stationary mass-one `e₀` (powers converge, the claimed limit doesn't match: `1 = 1/2`), `hπsum` at the stationary mass-two `(1,1)` (the claimed limit doubles), the entrywise `hπstat` twin, and both `primitive_vecMul_tendsto` clauses (`hπstat`; `hνsum` at mass-two `ν`, the row action converging to `(1,1)` not `π`). The `hπnn` clause classified ENTANGLED: the stationary space is `span(1,1)` (proved by linear algebra, `pcvPos_stationary_eq`), and in general primitivity forces a strictly positive one-dimensional stationary space (Perron) — no signed mass-one stationary vector exists anywhere. Plus the survey's residual classified definition-only (`Matrix/Basic`, `Core`, `Core/RandomVariable`, `Core/Norms`), CLOSING the direct-import survey program: every shelf module now either directly QA-covered or definition-only. Remaining priced deferrals: the trio's `hnn`/`hrow` clauses (the `hrow` case with a nonexistence sketch) and the signed-cancellation walk quartet. |
| The Primitive-Convergence Family's Adversarial Fence Audit ([`adversarial-fences-primitive-convergence-family.md`](adversarial-fences-primitive-convergence-family.md), COMPLETE + delivery record) | 2026-09-05 | **The direct-import-coverage consumption survey's pick (58 shelf modules surveyed by DIRECT QA import — the spectral-core audit's blind-spot lesson applied to the import key for the first time; of the 7 uncovered, `Matrix/Basic` is a single instance and `ClusterProjector`/`EdgePerturbation` carry delivered audits, leaving `LinearAlgebra.PrimitiveConvergence` as the genuine gap at 4 transitive non-QA consumers — `Mixing`, `Oversmoothing`, `DirectedMixing`, the `EmpiricalStationary` capstone — 802 lines, ~24 theorems, zero prior fence coverage): nineteen hypothesis-form fences in the NEW `Scaffold/QA/LinearAlgebra/PrimitiveConvergence_QA.lean` (54 declarations, +50, 6270 → 6320), zero axiom contact (`#print axioms` via `wip/pcvfences_axcheck.lean` — every one a subset of the standard three; 24-tag independence check unchanged and clean; the module elaborates with zero warnings).** Three `Fin 2` fixtures: the docstring's own directed 2-cycle (nonnegative, row-stochastic, irreducible, NOT primitive), the signed row-stochastic `!![2,-1;-1,2]]` (`hnn` breaker, `hrow` genuine), the non-stochastic nonneg `diag(2,0)` (`hrow` breaker, `hnn` genuine). The fences: the bookkeeping layer; the whole Doeblin entrywise-range engine at both breakers per theorem (the dotProduct pair at signed/mass-wrong weights; the Doeblin contraction's `hrow` at δ=0 doubling the range and `hle` at inflated δ=1 giving a negative factor); **the convergence headline — all three `hprim` clauses (`primitive_power_tendsto`, `primitive_entrywise_tendsto`, `primitive_vecMul_tendsto`) killed by the cycle's oscillation**: the `(0,0)` entry alternates `1,0,1,0…`, the even/odd subsequence engine (`tendsto_nhds_unique` against `tendsto_const_nhds`, vector and vecMul forms composed through `continuous_apply`), with the genuine stationary `(1/2,1/2)` keeping every kept clause honest; and the primitivity suppliers (`isIrreducible_of_isPrimitive`' `hprim` at the diagonal by the ReflTransGen-tail reachability induction; the odd-loop supplier's `hodd` — the cycle is bipartite, `hreach`/`htwo` companions genuine). Deferred with priced mechanisms: the convergence trio's `hnn`/`hrow`/`hπ*` clauses (signed-primitive / substochastic-primitive-with-stationary-π fixtures, the latter possibly nonexistent) and the signed-cancellation walk quartet. |
| The Scalar Family's Trim-Saturation Deferral Closure ([`adversarial-fences-scalar-concentration-family.md`](adversarial-fences-scalar-concentration-family.md), deferral-closure record) | 2026-09-05 | **The prior terminal handoff's own priced option ("if either deferral route is ever taken, the same machinery unlocks genuinely-independent non-measurable fixtures"), delivered: `hoeffding_inequality`'s and `hoeffding_iid`'s `h_meas` — the scalar family's LAST two open deferrals — fenced at a four-cell-partition family on `(Fin 4, ⊥, δ₀)` in `Scalar_QA.lean`'s new `SaturationFences` section (+29, 6241 → 6270), zero axiom contact (`#print axioms` via `wip/scsat_axcheck.lean` — every one a subset of the standard three; the 24-tag independence check unchanged and clean).** The load-bearing piece: the **saturation lemma `scM_dirac_bot_eq_one`** — on a trivial σ-algebra a Dirac measure sends every nonempty set to exactly `1` (via the packaged `measure_eq_iInf`: the value is the infimum over measurable supersets, and the only `⊥`-measurable superset of a nonempty set is `univ`) — which converts the follow-up's recorded obstruction into the route: every mass exact, so the proved saturated-independence lemma (`scM_indepFun_of_saturated`: every pair of nonempty preimages intersects ⟹ both sides of every independence equality are `1` or `0`, the `Fin 2` `iIndepFun` assembled by erase-case analysis) applies at `X₀ = 2·1_{A}`, `X₁ = 2·1_{B}` (`A = {0,1}`, `B = {0,2}`, all four cells nonempty) — **the repository's first genuinely-independent non-measurable family**. The kills: the tail event `A ∩ B = {0}` carries saturated measure `1` against both bounds `2exp(−16/16) = 2e⁻¹ < 1` (on-file pin, no new pins) at `a = 2`, `t = 4`; kept clauses genuine (`h_indep`, `h_bound`) or junk-recorded (`h_mean`), the dropped `h_meas` genuinely failing at the nonconstant coordinates. The scalar family's falsification surface is now COMPLETE with no open item. |
| The Scalar Family's Measurability/Integrability-Guard Follow-up ([`adversarial-fences-scalar-concentration-family.md`](adversarial-fences-scalar-concentration-family.md), follow-up record) | 2026-09-05 | **The prior terminal handoff's named last recorded follow-up — the audit's own pricing: "the MGF-engine measurability clauses (fenceable only at a non-measurable `⊥`-σ-algebra fixture, the matrix audit's two-point space)". Delivered beyond the priced three to close the family's whole measurability/integrability-guard surface: eleven hypothesis-form fences plus fixture, mass, non-measurability, and pin companions in `Scalar_QA.lean`'s new `MeasurabilityFences` section (+36 by the generator metric, 6205 → 6241; 40 declarations), zero axiom contact (`#print axioms` via `wip/scmfences_axcheck.lean` — every one exactly `propext, Classical.choice, Quot.sound`; no `-- @refutes` tags; the 24-tag independence check unchanged and clean).** The fixture: the two-point `⊥`-space at BIASED masses `9/10 : 1/10` (the fair-coin `mcTwoBot` pattern cannot kill — `9/10 > 2e⁻²` needs the heavy atom), with every kill using only point-membership mass LOWER bounds — sidestepping the delivery's own headline obstruction: **on a trivial σ-algebra, a Dirac-combination measure SATURATES** (`OuterMeasure.trim`, the definitional total function of `ofMeasurable`-built measures, takes every nonempty set to full measure — exact non-measurable-set masses are unavailable), which is why `hoeffding_inequality`/`hoeffding_iid`'s `h_meas` (needing two genuinely-independent non-constant coordinates) are deferred with two priced routes (the deep `δ_a s = 1` unfold making all-saturated independence provable at a four-cell partition; a richer σ-algebra separating the breaker's level sets — shown impossible at four points). The fences: the three engine clauses (`measurable_finset_prod'`/`sum'`'s `hf` — the one-element product/sum IS the breaker; `integrable_of_bounded_measurable`'s `h_meas` with `h_bound` genuine); the same-class integrability guards (`integrable_sq_sub_mean`'s `h_meas`; `markov_tail_of_mgf`'s `hint` at `B = 0`, `t = 0` — the Markov engine's junk-integral guard, the exact Errata §7 clause class; `subgaussian_tail_bound`'s `h_int`); and six head-theorem `h_meas` clauses with falsifiable TAIL conclusions (`hoeffding_inequality_interval`, `hoeffding_empirical`, the Bernstein trio — **the variance statistic `∑∫(X − ∫X)²` is ITSELF the junk zero**, the matrix audit's headline mechanism at the scalar sibling, the denominator collapsing to `2at/3` and the bound to `2e^{-3/2} < 9/10`). Kept-clause honesty recorded: independence genuine by `Fin 1` triviality, `h_mean`/`h_var` held through the junk zero. Classified truth-removable-through-junk: the five MGF-conclusion `h_meas` clauses (junk-zero LHS satisfies every positive bound; the identity `0 = ∏ 0`). |
| The Scalar-Concentration Audit's Priced Deferrals D1 + D2 ([`adversarial-fences-scalar-concentration-family.md`](adversarial-fences-scalar-concentration-family.md), deferral-closure record) | 2026-09-05 | **The prior terminal handoff's named smallest priced single-run completions — the scalar-concentration audit's two recorded deferrals, closing the family's falsification surface with no open item. D1: `mgf_sum_le_bernstein`'s `h_indep`** (the deferred "priciest single fence in the family") at the delivered perfectly-correlated fixture `scCorrX` with `a = 1`, `λ = 1/2` — the MGF `cosh 1 = (e + e⁻¹)/2` against the bound `exp(3/10)`, the kill re-priced through `3/2 < cosh 1 > exp(3/10)` (cosh 1 > 3/2 from `e > 27/10` and `e⁻¹ > 3/10` via `e < 14/5`; `exp(3/10) < 3/2` by the cubing route `exp(3/10)³ = exp(9/10) < e < 14/5 < 27/8`) — the last never-refuted clause of the Bernstein MGF engine. **D2: the `h_mean` centering cluster of the four Hoeffding-family theorems** (`mgf_sum_le_of_iIndepFun`, `hoeffding_inequality_interval`, `hoeffding_inequality`, `hoeffding_iid`) at the NEW genuinely-independent biased product (`scBias = ![9/10, 9/10]`, the ±1 coordinate lift `scBiasX`: independence GENUINE via `iIndepFun_coord`'s `.comp`, measurability and bounds genuine, centering broken at `∫ = 4/5`) — **the family's first independent non-centered witness** (every prior fence broke independence, every genuine-independence instance was centered): the `t = 2` tail event `{ω 0 = ω 1}` at measure `41/50` against `2e⁻¹ < 41/50` (from the new `e⁻¹ < 10/27` pin), the MGF at `λ = 1` exceeding `e` through the `tt` atom alone (`(81/100)e² > e`). Five hypothesis-form fences in `Scalar_QA.lean`'s new `DeferralFences` section (+19, 6186 → 6205; 19 public theorems + 2 fixture `def`s + 9 private enumeration helpers), zero axiom contact (`#print axioms` via `wip/scdefl_axcheck.lean` — every one exactly `propext, Classical.choice, Quot.sound`; no `-- @refutes` tags; 24-tag independence check unchanged and clean). Four technique findings recorded (the asymmetric `ofReal` numeral normalization and its unfolded-shape `have` cure; the single-argument `ofReal_mul` signature with pattern-resolved second factor; `toReal_le_toReal`'s `.mpr` orientation; the section-`open` expiry caught by the explicit module build — a truncated `lake env lean | head` check masks errors behind the pipe's exit code). Full ladder: build + completeness 134/134 fresh, all scripts clean, scoreboard 6205/4/0 with the verification row, map-freshness exit 0 after the stats sync. QA-only, zero axiom contact. |
| The i.i.d.-Product Family's Adversarial Fence Audit ([`adversarial-fences-iid-product-family.md`](adversarial-fences-iid-product-family.md), COMPLETE + delivery record) | 2026-09-05 | **The prior terminal handoff's named natural next target (2 transitive non-QA consumers — `GraphTheory.Mixing` and the `Derived/EmpiricalStationary` capstone, the sampling engine of the empirical-stationary program), whose 284-line QA carried exactly one free-form negative witness (`junk_normalization_fence_QA`) never reconciled into the per-clause discipline. Delivered at the full priced scope: twelve hypothesis-form fences plus the hF-free strengthening twin in `IIDProduct_QA.lean`'s new `AdversarialFences` section (+19 by the generator metric, 6167 → 6186; 22 declarations = 19 public theorems + 2 fixture `def`s + 1 private kill engine), zero axiom contact (`#print axioms` via `wip/iidfences_axcheck.lean` on all 21 nameable declarations — every one exactly `propext, Classical.choice, Quot.sound`; no `-- @refutes` tags — theorem instantiations of an all-proved shelf; the 24-tag independence check unchanged and clean). The fences: the `hq0`/`hq1` bounds clauses of the three ∑-theorems at clamp fixtures keeping the other clause genuine (`![-1, 2]` breaks `hq0` only through the `ofReal` zeroing, `![2, 0]` breaks `hq1` only — `2 ≠ 1` at the mass level, `4 ≠ 1` at the joint level, `4 ≠ 2` at the marginal); `sum_coord2_mul`'s pair at the **three-coordinate breaker** (the Bernoulli-product audit's outside-the-consumed-coordinates rule transposed to sample coordinates: on `Fin 2` both quantified coordinates are consumed and the dropped identity holds by Fubini — `(0+2)² = 2·2`; the third coordinate multiplies the left side only, `2³ = 8 ≠ 4`); the three signature-free `hee` clauses at the all-genuine `q23` (Cauchy–Schwarz strictness `2/3 ≠ 4/9` for the double marginal; a coordinate — and its indicator, killed at the measurable support `{1}` through the preimage identification — is not independent of itself); `iIndepFun_coord_apply`'s `he` at the constant reindexing (`2/3 ≠ 4/9` at `T = univ`). The strengthening twin `iIndepFun_coord_apply_strict` proves `hF` decorative at the shelf's own generality (`V` a `Fintype` with `MeasurableSingletonClass` ⟹ `measurable_of_finite` discharges it for any `F`); the reconciliation consumes `junk_normalization_fence_QA` as `sum_iidMass_eq_one`'s `hq1` fence engine, closing the file's free-form witness layer; the nine `iidPMF q hq0 hq1`-consumers classified signature-entangled. Three technique findings recorded for future audits (the transposed breaker rule; the position-sensitive `ofReal` clamp elaboration trap and its explicit-`have` cure; equation-shaped reconciliation witnesses consumed by `rw`, not application). Full ladder: build + completeness 134/134 fresh, all scripts clean, scoreboard 6186/4/0 with the verification row, map-freshness exit 0 after the stats sync. QA-only, zero axiom contact. |
| The Scalar-Concentration Family's Adversarial Fence Audit ([`adversarial-fences-scalar-concentration-family.md`](adversarial-fences-scalar-concentration-family.md), COMPLETE + delivery record) | 2026-09-05 | **The prior terminal handoff's named next surface (5 transitive non-QA consumers — the scalar tail engine behind the derived degree-concentration chain; all theorems since the 2026-08-30 retirements), whose retirement QA carried real fences but none on the independence clauses — THE hypothesis of every concentration theorem in the family, never refuted in dropped form. Delivered at the full priced scope: eleven hypothesis-form fences in `Scalar_QA.lean`'s new `AdversarialFences` section (+28 theorems by the generator metric, 6139 → 6167; 30 declarations), zero axiom contact (`#print axioms` via `wip/scfences_axcheck.lean` on all 28 public declarations — every one exactly `propext, Classical.choice, Quot.sound`; no `-- @refutes` tags — theorem instantiations of an all-proved shelf; the 24-tag independence check unchanged and clean; the landed module's 10 warnings verified pre-existing at HEAD). The fences: nine `h_indep` fences at the new perfectly-correlated fixture `scCorrX` (both coordinates the ±1 lift of coordinate `0` — every kept clause genuine: measurable, `|X| = 1`, mean `0`, variance `1`; the family is distributionally identical to the delivered `scRadX`, only the joint law differs) — the product-integral identity killed algebraically (`cosh 2 ≠ cosh²1`, difference `(e − e⁻¹)²/4 > 0`, no pins), the MGF bound by `cosh 2 > e` from `27/10 < e < 14/5`, and the seven tail forms by event degeneration (the correlated sum is `±2` everywhere, so the `t = 2` events are all of `Ω` and the measure `1` kills `2e⁻¹ < 1` / `2e^{-3/4} < 1`; the empirical form at the `[0,1]`-lift, `t = 1/2`); plus two `ht` backward-time fences at the genuine `scRadX` (`t = -2`: `{|Σ| ≥ -2}` is all of `Ω`, `1 > 2e^{-3/2}`). The `¬iIndepFun` isolation companion at the fixture; `mgf_sum_le_bernstein`'s `h_indep` (λ = 1/2 rational chain) and the Hoeffding `h_mean` centering cluster (biased-coin fixture, `t = 2` tail kill and `λ = -1` MGF kill) priced and deferred; the subgaussian centered theorem's centering classified truth-removable-through-junk (positive drift empties the constraint set, `sInf ∅ = 0` — the retired axiom's original defect mechanism, unfenceable by construction). Full ladder: build + completeness 134/134/0/0; four technique findings recorded. |
| The Bernoulli-Product Family's Adversarial Fence Audit ([`adversarial-fences-bernoulli-product-family.md`](adversarial-fences-bernoulli-product-family.md), COMPLETE + delivery record) | 2026-09-05 | **The prior terminal handoff's first named natural next move, confirmed by a fresh reverse-import walk: `BernoulliProduct` at 8 transitive non-QA consumers (the umbrella plus `IIDProduct`, `EdgePerturbation`, `Sparsification`, and the Derived `EdgePerturbationTail`, `EdgePerturbationDrift`, `SparsificationTail`, `EmpiricalStationary` capstones) vs the scalar concentration family's 5 — the sampling engine every matrix-concentration design consumes and the instantiation layer of 3 of the 4 remaining admitted axioms, whose QA carried two free-form fences never reconciled into the per-clause discipline. Delivered at the full priced scope: fifteen hypothesis-form fences in `BernoulliProduct_QA.lean`'s new `AdversarialFences` section (+25 theorems by the generator metric, 6114 → 6139; 30 declarations = 25 public theorems + 5 fixture `def`s), zero axiom contact (`#print axioms` via `wip/bpfences_axcheck.lean` on all 25 public declarations — every one exactly `propext, Classical.choice, Quot.sound`, the 3 private helpers audited transitively; no `-- @refutes` tags — theorem instantiations of an all-proved shelf; the 24-tag independence check unchanged and clean). The fences: the four ∑-theorems' `hp0`/`hp1` bounds clauses at out-of-bounds fixtures keeping the other bound genuine (`![2]` breaks `hp1` only, `![-1]` breaks `hp0` only — through the `ofReal` clamp, the coordinate mass sums to the doubled false-mass `2 ≠ 1` from either side; the **pricing finding**: a marginal-identity bounds fence needs a breaker coordinate OUTSIDE the statement's consumed coordinates — on `Fin 1`/`Fin 2` both sides are the same sum by Fubini, so `sum_coord_mul` needs `Fin 2` and `sum_coord2_mul` needs `Fin 3`); the `hee` distinctness clauses at the all-genuine `![1/2]` fixture (Cauchy–Schwarz strictness `13/2 ≠ 25/4` for the double marginal — a genuinely different mechanism: nothing degenerates, a single coordinate cannot be independent of itself; `μ(A ∩ A) = 1/2 ≠ 1/4 = μ(A)²` for both independence statements, the matrix-codomain kill at the measurable entry cylinder through the nested `measurable_pi_apply` composition at the hand-rolled product σ-algebra); the `he` injectivity clauses of the mutual-independence transfer trio at the constant reindexing (what makes the sparsification designs' edge-to-coordinate transfers sound — no witness existed anywhere in the repository); and the `hpne` centering clause, with the file's two pre-discipline free-form fences reconciled as the wrappers' proof engines. The eleven `bernPMF`-consumers' bounds clauses classified signature-entangled (the sampling measure does not exist out of bounds — the mass-level fences are exactly that entanglement's visible boundary). Full ladder: build + completeness 134/134/0/0; four technique findings recorded (the `rw`-vs-`simp only` ∀-equation trap under `Finset.sum` binders foremost). |
| The Matrix-Concentration Family's Adversarial Fence Audit ([`adversarial-fences-matrix-concentration-family.md`](adversarial-fences-matrix-concentration-family.md), COMPLETE + delivery record) | 2026-09-05 | **The prior terminal handoff's first named natural next move — a fresh consumption survey — executed mechanically (a reverse-import walk over every public shelf) and delivered at its pick: the `Probability.Concentration.Matrix` subtree at 14 transitive non-QA consumers, the library's most-consumed unaudited surface and home to 3 of the 4 remaining admitted axioms, whose QA carried only the repair-era refutations with no hypothesis-necessity pass over the remaining clauses (the `PerronFrobenius_QA` pre-audit state). Twelve hypothesis-form `-- @refutes`-tagged fences in `Matrix_QA.lean`'s new `AdversarialFences` section close every remaining named hypothesis clause of all three admitted matrix axioms: `matrix_hoeffding`'s `h_herm` (the alternating nilpotent `±!![0,4;0,0]]`, domination genuine through `N² = 0`, `4e⁻⁸ < 1`), `h_indep` (six identical sign riders — perfect correlation, `2e⁻³ < 1` — with the mechanical `¬iIndepFun` companion at the entry cylinder), `h_bound` (the free variance proxy at `A = (1/10)·1`, `2e⁻⁵⁰ < 1`), `ht` (`t = −3`); `matrix_bernstein`'s `h_mean` (uncentered deterministic, `2e^{−3/2} < 1`), `h_herm` (genuinely independent cube riders × the unit nilpotent `!![0,1;0,0]]` — independence and centering PROVED at the design, the nilpotent variance statistic `0`, mass `1/8` against `4e^{−9/2} < 1/8`), `h_bound` (the rare-value family `99/−1` at masses `1/100`/`99/100` — genuinely centered with the exact variance statistic `99` through `integral_sq_delta_sub`, `2e^{−9801/264} < 1/100`), **`h_meas` — the headline: the variance statistic is ITSELF a junk integral** (the two-point trivial-σ space at `diag(1,0)/diag(0,−1)`: Hermitian, norm exactly one, the second moment two-valued with distinct squares → `Σ = ∫ X²` junk-zero → `4e^{−3/2} < 1` — Errata §7's mechanism class reaching the sibling axiom through `Σ`, not the tail event), `ht` (`t = −9/10`, the mixed denominator shrinking to `1/5`); `matrix_azuma_hoeffding`'s `cond_mean_zero` (constant drift, `m = 8`, `2e⁻¹ < 1`), `norm_bound` (`2•1` at `R = 1`, `2e⁻⁴ < 1`), `ht`. Plus the shared infrastructure (exp-anchor helper set, the coin/rare/cube `bernPMF` measures, the two-point ⊥-space with the packaged `mcTwoBot_not_aeSM` engine, the public C*-spine vector-action route, the identity-norm and smul-bridge pins). QA-only (+53, 6061 → 6114; 61 new file-uniquely-named `mc*` declarations), zero axiom disposition change, zero public statement change (`#print axioms` via `wip/mcfence_axcheck.lean`: all 61 exactly `propext, Classical.choice, Quot.sound`; the mechanically-independence-checked `-- @refutes` tag count rises 12 → 24, all clean). **Tooling repair in-delivery:** `check_refutation_independence.py`'s namespace tracker popped on any `end` line — a latent defect first exposed by fences landing inside a named section — repaired to name-aware `end` handling. Full ladder: build + completeness 134/134 fresh; lint/citations/links/qa-names/backlog all pass; map-freshness exit 0 after the stats sync. Spike-first across two runs (`wip/mcfence_spike.lean`). |
| The Heat Audit's Deferral D1: the Walk-Twin Variance `ht` Fence ([`adversarial-fences-heat-family.md`](adversarial-fences-heat-family.md), D1 RESOLVED + delivery record) | 2026-09-05 | **The standing frontiers' concretely priced completion ("the smallest priced completion anywhere", per the recurring handoff phrasing) — the heat family's only recorded deferral, closed at the full priced scope: `walkHeatKernel_variance_decay`'s `ht : 0 ≤ t` fenced at the priced witness `K₂ ⊕ K₂` on `Fin 4` (`hfDis4Adj`), where disconnection holds the rate at `e⁰ = 1` while backward time (`t = -1`) grows the per-block eigenvalue-`2` mode — output π-variance `2e⁴` against the bound on input `2`, killed by `hf_exp_two_gt_one`. The deferral existed precisely because the walk twin's kept clauses (`hnn`, `hd`) bar every prior fixture (the Fin 3 `disAdj` breaks `hd` at its isolated vertex; the Fin 2 signed `hfRegAdj` breaks `hnn`) — two disjoint edges keep every clause genuine (the packaged isolation proves `hA`/`hnn`/`hd`/`hcard` genuine and `(-1) < 0`). The heaviest single pin the family needed — the four-point `secondEval (normalizedLaplacian) = 0` — landed at the priced route (`secondEval_le_rayleigh` at the centered component indicator `![1,1,-1,-1]`, a genuine kernel vector at the unit degrees where `degreeInvSqrt = 1`; lower bound `normalizedLaplacian_evals_zero` + `evals_sorted`), the Fin 3 combinatorial pin's exact shape lifted to the normalized Laplacian. QA-only (+20, 6041 → 6061, `Heat_QA.lean`'s new `HeatFencesD1` section, a pure insertion), zero axiom contact (`#print axioms` via `wip/d1fence_axcheck.lean`: every one exactly `propext, Classical.choice, Quot.sound`; no `-- @refutes` tags; 12-tag independence check unchanged and clean); `check_qa_name_uniqueness` clean (new `hfDis4*` names collision-free). The family's falsification surface is now complete with no open deferral — both variance-decay twins carry their `ht` fence at independent fixtures. Full ladder: build + completeness 134/134 fresh; lint/citations/links/backlog all pass; map-freshness exit 0 after the stats sync. |
| The QA Name-Collision Guard and the Exhaustive_QA Witness-Layer Reconciliation ([`qa-name-collision-guard.md`](qa-name-collision-guard.md)) | 2026-09-05 | **The collision-class closure the `edgeAdj` repair priced as "unsurveyed", delivered mechanically, plus the standing audit remainder that repair's handoff named as the natural next target. Part A:** the survey found **24 residual cross-module duplicate names** across `Scaffold/QA/**` in three mechanism classes — identical-body fixture redeclarations (`asymAdj2` Exhaustive↔Stationary, `edgeAdj2`, the `pathAdj` family across four files), *different-body* duplicates (`k2Adj`/`path3Adj`/`triAdj` — same graph, different spellings, the subclass a future rename pass must re-pin per file), and shared private-helper spellings (`list_two_eq` ×5 files, `two_point_pin`, `constantStream`) — and `scripts/check_qa_name_uniqueness.py` closes the class in the `lint_axioms` allowlist pattern: any NEW cross-module duplicate fails, any drift in a residual's exact file set fails (settled deliberately at that boundary), doc comments stripped so prose never matches, scope exactly the leaf-built layer where the class is latent (a shelf collision fails the umbrella build immediately). **Both failure modes self-tested** (a probe joining an allowlisted pair → `ALLOWLIST DRIFT`; two probes sharing a fresh name → `NEW collision`), then clean. No renames — repair stays on-demand per the landed per-pair recipe. **Part B:** `Exhaustive_QA.lean`'s 2026-08-17 free-form witness `asym_boundary_not_dual_QA` is reconciled into the per-clause fence discipline as the *proof engine* of `exh_boundary_compl_hA_fence_QA` (`boundary_compl`'s `hA`); the sibling `conductance_compl` clause is fenced at the same `asymAdj2` fixture through pinned conductance values (`2 ≠ 1`), completing the cut-duality pair's coverage at the file's own layer (`Spectral_QA`'s Step-1 `dirB` fences are the independent second fixture/route); isolation companions (`exhAsym_not_isSymm`, the walk layer's positive-side `exhPathAdj_deg_pos` against `Normalized_QA`'s C1); the three `*_separates_QA` theorems classified (interface-separation checks, no fence form); the stale "QA modules must not import each other" header repaired. QA 6027 → 6041 (+14), zero axiom contact (`#print axioms` via `wip/exhwitness_axcheck.lean`: every one exactly `propext, Classical.choice, Quot.sound`; no `-- @refutes` tags); every free-form negative witness in the QA tree is now inside the per-clause discipline. Full ladder: build + completeness 134/134 fresh; lint/refutation-independence (12-tag)/reachability/citations/links/backlog all pass; guard clean; map-freshness exit 0 after the stats sync. |
| The Spectral-Core Family's Adversarial Fence Audit — Steps 0–4, COMPLETE ([`adversarial-fences-spectral-core-family.md`](adversarial-fences-spectral-core-family.md)) | 2026-09-05 | **The audit method's twenty-third application and its largest target — found by this run's fresh consumption survey as the enumeration method's own gap: `Spectral.lean`, the library's root shelf, carries 46 transitive non-QA consumers (24 direct), roughly triple the next-largest unaudited surface, yet was invisible to every prior survey because those keyed on shelves with same-name QA files while this shelf's QA was scattered across ~10 per-topic files with zero fence sections.** Step 4 (delivered fourth, same day): the variational / Courant–Fischer cluster — nineteen hypothesis-form fences plus the `rayleigh_padVec` hypothesis-free P4 companion in `Spectral_QA.lean`'s `AdversarialFencesStep4` section, riding a new `Variational_QA` import (QA-only, +41, 5985 → 6026; 41 public declarations audited, all `propext, Classical.choice, Quot.sound`, the private two-point pin transitively; zero axiom contact, no `-- @refutes` tags): `secondEval_variational`'s and `secondEval_le_rayleigh`'s `hpsd`/`hker`/`hx0`/`hxorth` (the negative Laplacian's `λ₂ = 0` against every admissible quotient `-2`, both sides from on-file `lambda2` pins transferred through the `rfl` theorem `lambda2_eq_secondEval`; `sfProjE0`'s `λ₂ = 1` against the alternating quotient `1/2`; the junk-zero quotient at `x = 0`); **the congruence pair's `h` clauses — resolving Step 3's recorded deferral for both members** (`2 ≠ 0` at two derived `secondEval` pins; the identity's uniform spectrum against `sfProjE0`'s bottom entry, from a new both-entries two-point spectrum pin); `evals_le_of_linearIndependent`'s `hk1`/`hgi`/`hbnd` (all three killed by `evals_one` demanding `1 ≤ 0`, with the vacuous/genuine span-bound companions); `lambda2_variational`'s `hnonneg` **reconciled** from the 2026-08-18 pre-discipline refutation; the `_of_ker` twins' seven clauses (the `w = 0`/`x = 0` junk-orthogonality kills; `e₁`'s kernel quotient `0 < 1`; `onesVec` outside the kernel; the negative-Laplacian `w = onesVec` instance `0 = -2`). Classifications: `secondEval_smul_of_pos`'s `hpsd`/`hker` truth-removable-not-fenceable (positive scaling commutes with sorting — the dropped statements hold everywhere, matching the docstring's own machinery note). Two defects recorded: the shelf's `_of_ker` QA note cited a **nonexistent** `icvQ_psd_fence` (the real fence is `icFence_psd_fence` — docstring repaired), and the `Cheeger_QA`/`PotentialSolvability_QA` `edgeAdj` name collision makes those two QA modules non-co-importable (latent until this run's planned `IrregularCheeger_QA` import hit it; the rename repair is priced as a follow-up). The program is COMPLETE — Steps 0–4. Step 3 (delivered third, same day): the spectral-theorem interface layer's priced residue — seventeen hypothesis-form fences in `Spectral_QA.lean`'s `AdversarialFencesStep3` section, QA-only (+55, 5930 → 5985; 60 declarations audited, all `propext, Classical.choice, Quot.sound`, zero axiom contact, no `-- @refutes` tags): the symmetry-free-conclusion minority (the raw-matrix self-adjointness identity at `dirB`, `4 ≠ 1`; `smul_isSymm` (`1 • M = M` stays asymmetric — scaling preserves the cone exactly); all five clauses of the projector-uniqueness lemma at a four-fixture idempotent family sharing the fixed line `{x | x 1 = 0}`, each member failing exactly one structural clause); and the side clauses killed at the identity/zero matrices through the shelf's own pins (`eigvalOf_one`, `eigvecOf_inner`, `dotProduct_eigvecOf`) — the projector-threshold quartet by the *sibling* theorems' satisfiable hypotheses, the kernel-orthogonality quintet by a Parseval exclusion engine load-bearing on the shelf's own eigenbasis resolution. The `hcard` clauses classified entangled (consumed through `⟨index, by omega⟩` Fin arguments); three deferrals recorded (the `hne`/`heq`/congruence-`h` clauses gated on a concrete non-identity `eigvalOf`-at-index pin — the identity's uniform spectrum cannot kill). Step 2 (delivered second, same day): the connectivity/kernel cluster — eighteen hypothesis-form fences in `Spectral_QA.lean`'s `AdversarialFencesStep2` section, QA-only (+77, 5853 → 5930; 79 declarations audited, all `propext, Classical.choice, Quot.sound`, zero axiom contact, no `-- @refutes` tags): the `hconn` clauses as reconciliations of `Connectivity_QA`'s and `PotentialSolvability_QA`'s delivered disconnected witnesses (the fences' proofs consume them); the `hnonneg` clauses at the new signed path `sfSignedPath` with the kept `hconn` genuine — symmetric, support-connected, kernel genuinely two-dimensional (the nonconstant `![1,-1,0]` survives), and the solvability hinge killed through the shelf's own kernel certificate (the zero-sum unit demand pairs `2 ≠ 0` against the kernel vector); **the census's P4 candidate settled by refutation** (`laplacian_mulVec_eq_zero_of_forall_reachable`'s `hnonneg` genuinely load-bearing — the pricing note "the row-sum argument closes without signs" was wrong; at the signed edge the edgeless support makes component-constancy trivially satisfiable while `L *ᵥ e₀ = (-2,2)`); the Step-1 cut residuals closed at the negative-min division-rescue fixture `sfSignedCut` (`conductance = 2/min(-2,2) = -1 < 0`) plus the hypothesis-free P4 companion `conductance_ge_cheegerConstant_hypothesis_free` (set-finiteness supplies the `BddBelow` that `hnonneg` provided); and the `supportGraph`-entanglement classification (every `hA` clause of a `supportGraph`-carrying statement is non-fenceable — the dropped statement cannot be formed at an asymmetric fixture, the adapter's `symm` field deriving from the proof). Step 0: the census (105 hypothesis-bearing theorems, 214 named clauses, classified fenceable / signature-entangled / P4-removable / already-fenced, priced as a four-step program). Step 1: twelve hypothesis-form fences closing the Laplacian algebra / PSD / cut-duality cluster, delivered in a NEW dedicated `Spectral_QA.lean` (the root shelf's own QA home — closing the survey gap structurally), QA-only, zero axiom contact (count stays 4; `#print axioms` via `wip/skfences_axcheck.lean` on all 96 declarations — every one exactly `propext, Classical.choice, Quot.sound`; no `-- @refutes` tags; QA 5760 → 5853, +93).** The fences: the `hA : A.IsSymm` cone on `laplacian_symmetric` (`L dirA 0 1 = -3 ≠ -1`), `laplacian_quadForm` (at `dirA`, `x = e₀`: quadratic form `4` against the symmetrized double-sum's `3` — the `1/2` pairing genuinely consumes symmetry), `laplacian_psd` (`dirB`, `x = (1,2)`: `-2 < 0` with the kept `hnonneg` genuine — the in/out-degree imbalance is a negative mode), `laplacian_dotProduct_mulVec` (`-4 ≠ -1` — self-adjointness is exactly symmetry), `dotProduct_eq_zero_of_laplacian_mulVec_eq_zero` (the new asymmetric star `sfStar`'s two-dimensional harmonic kernel `{f | 2 f 0 = f 1 + f 2}`: `L *ᵥ (1,2,0) = 0` genuine while `w ⬝ᵥ (L *ᵥ e₀) = 2 ≠ 0`), `boundary_compl` and `conductance_compl` (`dirB`, `S = {0}`: `4 ≠ 1` in both weight and conductance); the `hnonneg` clause of `laplacian_psd` at the new signed edge `sfNegEdge` (the alternating mode `-8 < 0` with the kept `hA` genuine); both clauses of `vol_pos_of_pos_deg` (the zero-degree corner at the zero matrix with `hS` genuine; the empty set at `dirA` with the delivered `dirA_deg_pos` genuine); and the signed-input kills of `degreeMatrix_diagonal_nonneg` and `boundary_nonneg` (`-2 < 0`). Full ladder: spike-first (two fix rounds, all in recorded trap classes — the vecHead/vecTail residue and the eta-literal `show`/`exact` coercion); build + completeness 134/134/0/0 (the new module included); `lint_axioms`, `check_refutation_independence` (12-tag clean), `check_public_reachability` (63 public modules; the QA file correctly unreachable), citations, links, backlog freshness, scoreboard 5853/4/0, map-freshness exit 0 after the stats sync + SVG regeneration (49 stations, no status change owed). New file, pure addition (615 lines). Steps 2-4 priced in the proposal's census table for successor runs. |
| The Directed Family's Adversarial Fence Audit ([`adversarial-fences-directed-family.md`](adversarial-fences-directed-family.md)) | 2026-09-05 | **The audit method's twenty-second application — the prior terminal handoff's named last unaudited shelf of the `IrreducibleStationary`/`Directed` pair (3 transitive non-QA consumers: `Magnetic` and `FunctionalCalculus` direct, `Signed` via `Magnetic`; this run's own fresh import walk), its 613-line QA pre-discipline with two free-form negative witnesses, here reconciled into the per-clause discipline: 4 hypothesis-form fences over the shelf's entire clause surface + the strengthening refutation, 34 declarations total, QA-only, zero axiom contact (count stays 4; `#print axioms` via `wip/directfences_axcheck.lean`: every one exactly `propext, Classical.choice, Quot.sound`; no `-- @refutes` tags — theorem instantiations of an all-proved shelf; QA 5728 → 5760, +32).** The fences: the three `hA : A.IsSymm` cone clauses at the delivered `dirA` (`2 ≠ 4` for both degree agreements — the first reconciling the pre-existing free-form witness as its `ne_comm` twin — and the Step-3 agreement wrapper citing the delivered `dirA_Ldir_ne_normalizedLaplacian` directly); and the square-root-free conjugate's `hd : ∀ i, 0 < deg A i` at the new zero-out-degree fixture `dzA = !![0,1;0,0]]` — the junk `√0·(√0)⁻¹ = 0` collapse makes `L_dir = 1` and erases the symmetrized arc from the left side (`0`) while the right side's `D − ½(A+Aᵀ)` keeps it (`−(1/2)`). **Headline finding — the strengthening question settled by refutation: the conjugate's `hd` is genuinely a positivity clause, not nonzeroness in disguise** (unlike the random-walk family's `hdpos`, whose `d⁻¹·d = 1` route holds for every `d ≠ 0` and got `_of_ne_zero` companions): the route here runs through `Real.sqrt`, whose junk value collapses the whole non-positive half-line, so the nonzeroness-*strengthened* statement is itself false — refuted at the new negative-degree fixture `dzNeg = !![-1,0;1,0]]` (out-degrees `(−1,1)`: every strengthened clause genuine, the original `0 < deg` genuinely failing at vertex 0, the same `0 ≠ −(1/2)` separation). No `_of_ne_zero` companion exists; fence + refutation form an exact characterization — the clause is load-bearing on `d ≤ 0`, not on `d = 0`. Non-fenceables recorded with mechanisms (five hypothesis-free theorems — the two `rfl` identities, handshaking, the entry form, the hypothesis-free symmetry — nothing to drop). Full ladder: spike-first (one fix round, all in recorded trap classes); build + completeness 133/133/0/0; `lint_axioms`, `check_refutation_independence` (12-tag clean, no tags touched), `check_public_reachability` (63 modules), citations, links, backlog freshness, scoreboard 5760/4/0, map-freshness exit 0 after the stats sync + SVG regeneration (49 stations, no status change owed — the audit's proposal is not a map station's cited source). Pure insertion (301/0 in numstat, plus the header-note paragraph). |
| The Irreducible-Stationary Family's Adversarial Fence Audit ([`adversarial-fences-irreducible-stationary-family.md`](adversarial-fences-irreducible-stationary-family.md)) | 2026-09-05 | **The audit method's twenty-first application — the directed axis' stationarity root (3 transitive non-QA consumers: `PageRank` → `DirectedMixing` → the `EmpiricalStationary` capstone; tied with `Directed` on the consumer metric, selected for a ~28-clause unfenced surface vs ~4), its 706-line QA carrying only two hypothesis-FREE refutations, here reconciled into the per-clause discipline: 32 hypothesis-form fences + 2 reconciliations + 3 strengthening declarations, 256 declarations total, QA-only, zero axiom contact (count stays 4; `#print axioms` via `wip/isfences_axcheck.lean`: 254 exactly `propext, Classical.choice, Quot.sound`, 2 axiom-free; no `-- @refutes` tags — theorem instantiations of an all-proved shelf; QA 5494 → 5728, +234).** Headline kills, each a first witness of its class: the Krylov–Bogoliubov cluster lemma's `hgnn` at the new Jordan-block fixture `!![0,1;-1,2]]` — the mass-constant dynamics-following orbit `![2-s, s-1]` escapes the simplex exactly where the telescoping defect bound `|g 0 j| ≤ m` needed nonnegativity, and the conclusion dies on the stationary line `ℝ•![1,-1]` (mass 0, no nonneg member of mass 1); the `hnn` clauses of the entire walk-level family at three complementary signed Fin 3 fixtures (the one-dimensional mixed-sign fixed space killing existence of a positive stationary vector, the two-dimensional wedge killing scale-uniqueness and `∃!`-uniqueness at two nonneg stationary non-multiple rays plus a whole segment of mass-one distributions, and the signed-cancellation vanishing-support witness killing full support — nonneg stationary mass *can* vanish on strong support once entries cancel); `exists_pow_pos`'s `hnn` at the rank-one fixture `!![-1,1,10;1,-1,-10;-1,1,10]]` whose `M² = 8•M` (pinned entrywise) makes every power a positive scalar multiple of `M` — power entries of m-constant sign, impossible on `Fin 2` where strong connectivity forces both off-diagonal arcs positive; the junk `0⁻¹ = 0` row-zeroing `hdeg` corner at `!![-1,1;1,1]]` (irreducibility genuine through the positive arc against the canceling negative diagonal); the transient-vertex `hirr` kills at `!![0,1;0,2]]`; and the full σ/τ clause set at the delivered edge through the swap lemma. **The strengthenings**: `deg_pos_of_nonneg_irreducible` (`hnn + hirr + hex ⟹ hdeg`, first-arc route with the subsingleton loop case — the P4 truth-removability made machine-checked), `exists_pos_entry_of_deg_pos` (one positive row sum ⟹ a positive entry — nonnegativity and irreducibility not needed at all), and `exists_walkPerronVector_of_hex` restating the existence theorem at the strictly weaker hypothesis set. Full ladder: spike-first six fix rounds; build + completeness 133/133/0/0 (with the mtime-staleness remediation exercised per the checker's docstring); `lint_axioms`, `check_refutation_independence` (12-tag clean), `check_public_reachability` (63 modules), citations, links, backlog freshness, scoreboard 5728/4/0, map-freshness exit 0 (49 stations, no status change owed). Pure insertion (1575/0 in numstat). |
| The Stationary Family's Adversarial Fence Audit ([`adversarial-fences-stationary-family.md`](adversarial-fences-stationary-family.md)) | 2026-09-05 | **The prior terminal handoff's named top target, confirmed by this run's own fresh reverse-import walk (4 transitive non-QA consumers — `Mixing`, `Oversmoothing`, `DirectedMixing`, the `EmpiricalStationary` capstone; the 259-line QA pre-discipline with one free-form negative witness never reconciled into the fence discipline), the audit method's twentieth application: thirteen hypothesis-form fences plus isolation companions, kill pins, and five proved strengthening companions in `Stationary_QA.lean`'s new `AdversarialFences` section, a pure 631-line insertion. QA-only (+72, 5422 → 5494), zero axiom contact (`#print axioms` via `wip/stfences_axcheck.lean` on all 75 declarations — every one exactly `propext, Classical.choice, Quot.sound`; no `-- @refutes` tags — theorem instantiations of an all-proved shelf, nothing admitted consumed; 12-tag independence check unchanged and clean).** The fences: **the six `hA` clauses** at the delivered asymmetric `asymAdj2` (degrees positive so every kept `hd` is genuine) — adjoint stationarity dies because `Pᵀ *ᵥ deg` computes *column* sums `(1,2)` against the row-sum degree `(2,1)`; the degree-measure balance fence's proof consumes the delivered free-form witness `asym_detailed_balance_refuted_QA`, reconciling it into the discipline; the stationary-measure twin `2/3 ≠ 1/3` through the pinned vol `3`; the symmetrized matrix `2 ≠ 1`; the regular uniform balance `1 ≠ 1/2` at `d = 1`; **the degree-measure `hd` clauses** at the new signed canceling-zero-degree triangle `stNegAdj = !![0,1,−1;1,0,0;−1,0,0]]` (symmetric, degrees `(0,1,−1)`) — the kernel statement through the junk-√ collapse (`degreeInvSqrt = diag(0,1,0)` pinned, congruence zero, `L_sym = 1`, the kernel vector's positive entry surviving: `1 ≠ 0`), adjoint stationarity through the junk-`0⁻¹` column-zeroing (the zero-degree vertex's edge contributes to `deg 1` but is junk-zeroed in the adjoint product: `0 ≠ 1`), balance at `(0,1)` `0 ≠ 1` — **nonnegative zero-degree rows are inert; signs are what make them load-bearing**; **the stationary-measure and symmetrized `hd` twins** at the volume-nonzero variant `stVolAdj` (degrees `(0,3,1)`, vol `4` — the first triangle's vol `0` would junk-collapse both sides of the division): `0 ≠ 1/4` and `0 ≠ 1`; **the mass-conservation trio** — regular `hd` at the delivered `edgeAdj2` at the wrong claimed degree `d = 2` (`1/2 ≠ 0`), regular `hdpos` at the genuinely `0`-regular zero matrix (the `d = 0` junk corner: `L_rw = 1` does not kill constants), irregular `hd` at the zero matrix (the walk Laplacian of an edgeless graph is the identity). **Five proved strengthenings** record the structural findings: the kernel statement's `hA` is P4 truth-removable (the shelf proof never consumes symmetry; the symmetry-free companion makes the claim machine-checked — the pricing call confirmed by compilation), and the positivity clauses are secretly nonzeroness clauses (`deg ≠ 0` for stationarity/balance/irregular conservation, `d ≠ 0` for the regular one — the last transferring the random-walk audit's `d = 0`-corner finding to its consumer). Non-fenceable: `mulVec_one_eq_deg` carries no hypotheses. Full ladder recorded in the proposal (build + completeness 133/133, all hygiene scripts, map sync). |
| The Random-Walk Family's Adversarial Fence Audit ([`adversarial-fences-random-walk-family.md`](adversarial-fences-random-walk-family.md)) | 2026-09-05 | **The prior terminal handoff's named top target, confirmed by this run's own fresh reverse-import walk (5 transitive non-QA consumers — the walk-mixing chain's root interface via `Stationary` → `Mixing`/`Oversmoothing`/`DirectedMixing`/the `EmpiricalStationary` capstone; the 76-line QA fully pre-discipline — one nonnegative fixture, zero negative witnesses), the audit method's nineteenth application: six hypothesis-form fences plus isolation companions, junk-corner pins, and two proved strengthening companions in `RandomWalk_QA.lean`'s new `AdversarialFences` section, a pure 273-line insertion. QA-only (+27, 5395 → 5422), zero axiom contact (`#print axioms` via `wip/rwfences_axcheck.lean` on all 29 declarations — every one exactly `propext, Classical.choice, Quot.sound`; no `-- @refutes` tags — theorem instantiations of an all-proved shelf, nothing admitted consumed; 12-tag independence check unchanged and clean).** The fences: **both `hA` symmetry clauses** at the asymmetric `!![0,2;1,0]]`, killed at `d = 1` where `1⁻¹ • A = A` (`2 ≠ 1` for the transition matrix, `-2 ≠ -1` for the walk Laplacian — the asymmetry survives the subtraction), with the `d = 0` junk-rescue corner pinned (the scalar `0⁻¹ = 0` symmetrizes every matrix, so the dropped statements fail exactly when `d ≠ 0` ∧ asymmetric); **both `hd` regularity clauses** at the delivered `edgeAdj` at the wrong claimed degree `d = 2` — row sums `1/2 ≠ 1`, a `2×` bridge-diagonal separation `1 ≠ 1/2`, no junk anywhere, kept `0 < d` genuine; **both `hdpos` clauses** at the genuinely `0`-regular zero matrix where the junk `0⁻¹ = 0` collapses the transition matrix to zero (row sums `0 ≠ 1` — the transition matrix of a `0`-regular graph is not stochastic — and the bridge's sides separate `1 ≠ 0`), kept `hd` genuine. Structural finding recorded as the two proved strengthening companions (`transitionMatrix_row_sum_of_ne_zero`, `randomWalkLaplacian_eq_smul_laplacian_of_ne_zero`): **`hdpos` is load-bearing only through the `d = 0` junk corner** — at every genuinely `d`-regular fixture with `d ≠ 0`, negative degrees included, both dropped statements are true by the same `d⁻¹ · d = 1` algebra (the shelf proofs' own routes close with `0 < d` weakened to `d ≠ 0`), making fence+strengthening an exact characterization. Non-fenceable: the hypothesis-free `rfl` bridge. Full ladder recorded in the proposal (build + completeness 133/133, all hygiene scripts, map sync). |
| The Heat Family's Adversarial Fence Audit ([`adversarial-fences-heat-family.md`](adversarial-fences-heat-family.md)) | 2026-09-05 | **Selected by this run's own fresh reverse-import consumption survey, re-run and confirmed from the prior terminal handoff's standing numbers (Heat 8 transitive non-QA consumers — `FunctionalCalculus`, `Krylov`, `Mixing`, `Oversmoothing`, `PolyFilter`, `DirectedMixing`, `BandDavisKahan`, and the `EmpiricalStationary` capstone — vs RandomWalk 5, Stationary 4, IrreducibleStationary 3, Directed 3), the library's most-consumed unaudited shelf, its 1385-line QA (delivered 2026-08-23/31) predating the adversarial-review discipline with four scattered negative witnesses and zero fence sections — the audit method's eighteenth application, delivered at the full priced scope: 30 hypothesis-form fences (the exponential engines' `hM` clauses at the identity/zero/nilpotent fixtures, the rank-one collapse's junk-division `hc` corner and `hM` at `e ≠ 2`, the decay factors' `ht`/`hij`/`hnonneg`, the DC limit's `hconn` (constant flow at the isolated vertex ≠ the global mean) and `hnonneg` (mode growth `e^{4t}` diverging from the mean), both variance-decay twins' `hnn` (the negative edge's `2e⁸ > 2` at rate `1`) and `ht` (backward-time growth `2e⁴ > 2` on the delivered disconnected fixture), the normalized/walk conservation-and-conjugation `hd` sextet at the zero-degree signed fixture, both π-isometry `hd` junk-√ kills, the mean-preservation `hA` at the asymmetric rank-one, the eigenvalue-plumbing `hnn`/`hμ` pair at pinned spectra, and the remainder-window `ht` at `e⁴ − 5 > 16`), QA-only (+88, 5307 → 5395, `Heat_QA.lean`'s `HeatFences` section, a pure 1253-line insertion), zero axiom contact (`#print axioms` via `wip/heatfences_axcheck.lean` on all 93 public declarations — every one exactly `propext, Classical.choice, Quot.sound`, the 3 private pin helpers audited transitively; no `-- @refutes` tags — theorem instantiations, nothing admitted consumed; the 12-tag independence check unchanged and clean).** One priced deferral with recorded mechanism (the walk-twin variance `ht` — the `K₂ ⊕ K₂` four-point normalized secondEval pin) and the non-fenceable classes recorded (signature-entangled eigenbasis displays, the P4 truth-removable `HasDerivAt` pair — the generator identity `d/dt e^{-tL}x|₀ = -Lx` holds for every real matrix, so the hypothesis is proof-route-only — and the `hcard` display class). Technique findings recorded: the `9/4 ≤ e` pin (two `add_one_le_exp` at `1/2` through `exp_add`) suffices for every exponential fence constant; `fin_cases` must precede the literal-entry `simp` at 2×2 matrices (the probe-confirmed trap); and the pricing-stage scalar-vs-matrix-`1` correction — the normalized Laplacian's off-diagonal is `1/2`, not `3/2` (the identity matrix's off-diagonal is zero), caught by a genuinely-nontrivial fixture probe before any landing. |
| The Perron–Frobenius Family's Adversarial Fence Audit ([`adversarial-fences-perron-frobenius-family.md`](adversarial-fences-perron-frobenius-family.md)) | 2026-09-05 | **The prior terminal handoff's named top target, confirmed by this run's own independent reverse-import walk (7 transitive non-QA consumers — the directed axis' axiom shelf, feeding `IrreducibleStationary`, `PrimitiveConvergence`, `PageRank`, `DirectedMixing`, `Mixing`, `Oversmoothing`, and the `EmpiricalStationary` capstone; the 676-line QA carrying the 2026-08-28 degenerate-corner findings but zero fence sections — no hypothesis-necessity pass had ever covered the axiom's three clauses), the audit method's seventeenth application: delivered at the full priced scope — three tagged hypothesis-form fences closing the admitted axiom's *entire* three-clause hypothesis surface in `PerronFrobenius_QA.lean`'s new `AdversarialFences` section, a pure insertion. QA-only (+33, 5274 → 5307), zero axiom contact in the new declarations (`#print axioms` via `wip/pffences_axcheck.lean` on all 38 — every one exactly `propext, Classical.choice, Quot.sound`; the fences refute the axiom-minus-one-clause *shape* and never consume it; the three new `-- @refutes: perron_frobenius` tags take the repository's mechanically-independence-checked count 9 → 12).** Each clause now has its first negative witness: **`hnn`** at the negative-diagonal irreducible `!![-1,2;2,-1]]` — a pricing finding in itself: the fixture's eigen conjuncts stay *satisfiable in isolation* (`r = 1`, `x = (1,1)` pinned), so the kill route is genuinely the domination clause (the pinned complex root `−3` of `charpoly = (X−1)(X+3)` forces `3 ≤ 1`); **`hirr`** at the reducible nilpotent edge `!![0,1;0,0]]` — the mildest possible kill, the stranded vertex's zero row through the eigen clause alone; **`hex`** at the one-vertex zero matrix `!![0]]` — the module documentation's own named corner ("the vacuously irreducible 1×1 zero matrix … would otherwise falsify `0 < r`"), now a proved fence rather than a docstring warning. Plus the `IsIrreducible` positive-arcs definitional witness `!![0,-2;-2,0]]` (negativity creates no arcs). Non-fenceables: none — the hypothesis surface is exactly three clauses. |
| The Resolvent Family's Adversarial Fence Audit ([`adversarial-fences-resolvent-family.md`](adversarial-fences-resolvent-family.md)) | 2026-09-05 | **The prior terminal handoff's named top survey target, confirmed by this run's own independent reverse-import walk (16 transitive non-QA consumers — the library's most-consumed unaudited shelf, feeding the Weyl/Duhamel/ProjectionGap chain, Fiedler, Poincaré, PolyFilter, Sparsification, MasterBound, and four Derived capstones; the 1153-line QA from 2026-08-19/20 predating the adversarial-review discipline with five free-form negative witnesses never reconciled into the fence discipline), the audit method's sixteenth application: delivered at the full priced scope — 12 hypothesis-form fences with pinned-value companions in `Resolvent_QA.lean`'s new `AdversarialFences` section, a pure 535-line insertion. QA-only (+34, 5240 → 5274), zero axiom contact (`#print axioms` via `wip/rfences_axcheck.lean` on all 36 declarations — every one exactly `propext, Classical.choice, Quot.sound`; no `-- @refutes` tags — theorem instantiations, nothing admitted consumed).** The headline findings, each closing a clause class with no prior negative witness: **the empty corner is the only `hc` failure corner** (both upper bridges' `0 ≤ c` clauses are load-bearing only through `Fin 0` — on every nonempty type the eigenvalue hypothesis already forces `c ≥ 0`; the fence pins `‖0‖ = 0` on `Fin 0` by an independent transport route and refutes the dropped statement at `c = -1`); **PSD is load-bearing for shifted invertibility, and so is the shift itself** (`M = -1` with form `-‖x‖²` exhibited kills both `hpsd` clauses; `t = 0` on the delivered `lap2` with PSD genuine kills `ht`); **the resolvent identity needs both determinant clauses one-sided** (a singular shift on exactly one side separates the identity's two sides, `-1 ≠ 0` resp. `1 ≠ 0` — a both-singular pair collapses both sides to `0` identically and cannot serve); **the norm bound's `t = 0` fence requires an invertible PSD fixture** (at `t = 0` on a *singular* PSD matrix the dropped statement is true by junk — the junk inverse is the zero matrix against `0⁻¹ = 0`); **the Lipschitz `quadForm` clauses separate by 4×** (`3 ≤ 3/4` at `negThreeQuart` vs `0` through two new diagonal spectrum pins); and **the injectivity trio's jointly dropped clauses** fall to the delivered `-1` vs `-1 + E` guard. Screened: the upper-bridge `h` clauses, the `+1` norm bound's `hpsd`, the core injectivity's jointly-dropped form (all by delivered witnesses). Non-fenceables with mechanisms: the five `hM` signature-entanglements, `hcard`'s proof-term-in-display, and the injectivity trio's *individually* dropped determinant clauses (P4 truth-removable — a common resolvent value that genuinely left-inverts one factor forces the other's invertibility in finite dimensions; only the joint drop is false). Five technique findings recorded (headline: numeric-literal precedence — `= -3/4 * -3/4` parses as `((-3/4) * (-3))/4`, so negative literal factors must be parenthesized in hypothesis statements).** |
| The Variational-Transfer Family's Adversarial Fence Audit ([`adversarial-fences-variational-transfer-family.md`](adversarial-fences-variational-transfer-family.md)) | 2026-09-04 | **The prior terminal handoff's named top survey target, confirmed by this run's own independent reverse-import walk (13 transitive non-QA consumers; the top tier's only fully pre-discipline QA — 170 lines, zero negative witnesses; Resolvent at 16 already carries free-form witnesses), delivered at the full priced scope over the shelf's *unfenced* clause surface (the transfer engine, the connectivity transfer, the degree sandwich's pointwise engines — the Cheeger-statement and mul-form sandwich/window clauses already fenced by `IrregularFences` and `DegreeSandwich_QA`): 27 fences + 12 explicit isolation companions (plus fixture-pin isolation evidence) in `VariationalTransfer_QA.lean`'s `TransferFences` section, a pure 1126-line insertion riding three QA imports. QA-only (+124, 5116 → 5240), zero axiom contact (`#print axioms` via `wip/vtfences_axcheck.lean` on all 127 declarations — every one exactly `propext, Classical.choice, Quot.sound`; no `-- @refutes` tags — theorem instantiations, nothing admitted consumed).** Three headline findings, each a first witness: **the quotient bracket flips on signed input** — the docstring of `rayleigh_normalizedLaplacian_le_div` warns it, the connected negative cut `!![3,1,−3;1,0,1;−3,1,3]]` witnesses it (`quadForm L x = −2`, degree-weighted `−2/3` vs plain `−1`, both sides of the bracket false with every other hypothesis genuine); **connected support does not force a spectral gap once signs enter** — the signed path's two combinatorial kernel vectors stretch into the normalized kernel, a two-dimensional kernel subspace forces `λ₂ ≤ 0` through `exists_ne_mem_rayleigh_ge_of_finrank_eq` with `hconn` genuine; and **the signed normalized Laplacian dips below zero** — the signed `d = 2`-regular fixture's alternating mode pins the bottom eigenvalue `≤ −1` against the `= 0` claim. Non-fenceables with mechanisms: PSD's `hd` (P4 — zero-degree rows vanish at nonnegative symmetric input), the connectivity `hd` (P4 conditional on `hnn`), the display-entangled `hA`/`hcard` classes, the Fiedler `hpos` clauses (choice-entangled; mechanism fenced by the delivered `ifc_disc_hinge_fence_QA`), and the eigenvalue-level engines' `hnn` (screened — routed through the pointwise bracket). Five technique findings recorded (headline: `norm_num` does not fold numeric sums under `Real.sqrt` — the `sqrt_mul_self` pin plus the `1/√2 = √2/2` normalization is the robust route; the eta-literal trap is asymmetric — ∀-quantified degree pins rewrite under any index form, literal entry lemmas need the `nfFinN_cases` literal source). |
| The Normalized-Laplacian Family's Adversarial Fence Audit ([`adversarial-fences-normalized-family.md`](adversarial-fences-normalized-family.md)) | 2026-09-04 | **The prior terminal handoff's directed fresh Step-0 consumption survey, run in full for the first time (a reverse-import walk over every unaudited shelf, not just the handoff's named candidates) and delivered at its pick: the normalized-Laplacian family (`Normalized.lean` — the congruence bridge `√D L_sym √D = L`, the degenerate-degree corners, the walk form, the eigenpair transfer through the similarity, the conjugated-power transfer), at 20 transitive non-QA consumers the library's most-consumed unaudited shelf (the dk survey's "four most-consumed unaudited" claim was an artifact of its candidate list — above Resolvent 16 and VariationalTransfer 13, all three above every handoff-named candidate). Delivered at the full priced scope: 27 fences + packaged isolation companions at four new fixtures plus the reused K₂. QA-only (+71, 5045 → 5116, `Normalized_QA.lean`'s `AdversarialFences` section, a pure 903-line insertion), zero axiom contact (`#print axioms` via `wip/nfences_axcheck.lean` on all 75 declarations — every one exactly `propext, Classical.choice, Quot.sound`; no `-- @refutes` tags — theorem instantiations, nothing admitted consumed).** Headline finding, forced by the spike: the originally priced Fin 3 fixture was accidentally asymmetric, and repairing it exposed a P4-class truth — **at symmetric zero-degree fixtures the dropped-`hd` statements of the congruence bridge, left-multiplied congruence, commutation form, and power transfer are provable (the junk kills both sides identically); the genuine breaker is the negative-degree row**, where `(deg)⁻¹` is genuine while the square-root factors vanish. One Fin 2 fixture `!![-2,1;1,0]]` (degrees (−1,1), `L_sym` = identity, `P = !![2,−1;1,0]]`, all rational) kills fifteen clauses: the bridge identities, the kernel vector, the similarity, the transfer trio (with genuine eigen-hypotheses), the eigenbasis instantiations D1/D2 (orthonormality-witness index, no eigenvalue pin), the expansion D3, the existential D4 (`walkEvals = 0` via `evals_congr`+`evals_one`, dead by row equations), and the power pair E1/E2; the symmetric `K₂ ⊕ isolated` fixture carries the zero-degree corner's genuine separations (the junk `P`-row `0 ≠ 1`, the product entry `0 ≠ 1`, the annihilated conjugate); `diag(−1,−1)` and the asymmetric `!![0,2;1,0]]` (the family's only signature-free symmetry clause, `−√2 ≠ −1/√2`) and the reused K₂ close the rest. Six technique findings recorded (headline: constant matrix rows defeat `simp`'s entry reduction — `vecHead (vecTail (fun i => 0))` — the robust route is the explicit cons/tail/head chain). |
| The Davis–Kahan Core Perturbation Family's Adversarial Fence Audit ([`adversarial-fences-davis-kahan-core-family.md`](adversarial-fences-davis-kahan-core-family.md)) | 2026-09-04 | **The prior terminal handoff's named fresh Step-0 consumption survey over the remaining pre-discipline QA families, delivered at its pick: the Davis–Kahan retirement chain's load-bearing surface (`DavisKahan.lean`'s sin-Θ theorem, `Weyl.lean`'s four theorems, `Duhamel.lean`'s four Davis–Kahan-proof theorems, `ProjectionGap.lean`'s two equal-rank headline identities) — the survey's decisive numbers: the four shelves carry 4/6/6/7 transitive non-QA consumers respectively (the four most-consumed unaudited surfaces in the library, feeding Fiedler, Poincaré, and both derived-layer projector-drift capstones) vs 0 for every other candidate, with all three QA files (2026-08-19/21) predating the adversarial-review discipline and carrying zero fence sections. Delivered at 20 of 22 priced fences (two deferrals with recorded mechanisms): QA-only (+71, 4974 → 5045, fence sections in `DavisKahan_QA.lean`/`Weyl_QA.lean`/`ProjectionGap_QA.lean`), zero axiom contact (`#print axioms` via `wip/dkfences_axcheck.lean` on all 77 public declarations — every one exactly `propext, Classical.choice, Quot.sound`, the two private helpers audited transitively; no `-- @refutes` tags — theorem instantiations, nothing admitted consumed).** The fences: the sin-Θ bound's `hδ` (negative-`δ` corner: right side `−3/2` against a nonneg norm, separation genuine) and `hsep` (inflated `δ = 100`: pinned exact distance `√(1/10) > 3/400`); the gap-stability `hnorm` (the gap-shrinking `diag(0,−1)`: `2 → 1` at norm `1`, dropped statement at `ε = 1/4` demanding `1 ≥ 3/2`) and `hγ` (inflated `γ = 100`); the Duhamel bound's `hab` (negative-denominator corner, `hcl` genuinely trivial) and `hcl` (inflated window `b = 100` through a new threshold-`1` projector pin — the window `(−∞,1]` captures exactly the `−1/4` eigenspace so `Q` is the delivered pin's own matrix, witness square `‖(1−Q)P‖² ≥ 1/10 > (3/400)²`); the rank pin's no-tie clause (zero-matrix tie: `spectralProjector_eq_one` + `Matrix.rank_one`, `2 ≠ 1`); the trivial endpoint's four projector-structure clauses (asymmetric idempotent `‖P‖² ≥ 5`; symmetric non-idempotent `‖P‖² ≥ 4`); the sorted-step clause (pinned bottom eigenvalue, `2 ≤ 0`); and eight of the ten equal-rank-identity clauses at trivial fixtures through witness-vector norm bounds and one exact projector-norm sandwich. Non-fenceables recorded: the signature-entangled `hA`/`hAE`/`hM`/`hk` class, and a **distinct proof-term-in-display mechanism** first isolated here — the Weyl additive pair's `hcard` is consumed by the conclusion's own `by omega` Fin-index proof, so the dropped statement does not elaborate. The two deferrals (the core identity's `hP`/`hQ`) carry the zero-residual analysis: an idempotent of equal rank with vanishing `(1−Q)P` has `range P = range Q`, forcing `(1−P)Q = 0` too — both computed rank-1 oblique cases came out equal, suggesting truth-removability (the P4 class). |
| The Band-Projector Family's Adversarial Fence Audit ([`adversarial-fences-band-projector-family.md`](adversarial-fences-band-projector-family.md)) | 2026-09-04 | **The prior terminal handoff's directed fresh Step-0 consumption survey over the remaining pre-discipline QA families, delivered at its pick: the band-projector neighborhood (`Band.lean` + `ClusterProjector.lean` — 3 transitive non-QA consumers feeding `PolyFilter` and the set-form Davis–Kahan perturbation bridge, vs 0 for `SpectralCertificates` and `Poincare`), both QA files predating the adversarial-review discipline — delivered at the full priced scope: 22 band + 7 cluster hypothesis-form fences plus isolation companions and two new band pins. QA-only (+43, 4931 → 4974, `Band_QA.lean`'s `BandFences` + `ClusterProjector_QA.lean`'s `ClusterFences`), zero axiom contact (`#print axioms` via `wip/bpfences_axcheck.lean` on all 43 public declarations — every one exactly `propext, Classical.choice, Quot.sound`, the private transport helper audited transitively; no `-- @refutes` tags — theorem instantiations, nothing admitted consumed).** The headline mechanism is one object: **the negated junk band `B(4,0] = P_0 − P_4 = −1`** (delivered pin) kills eleven clauses at once — idempotence (`(−1)² = 1 ≠ −1`), both annihilation statements (`−v ≠ 0` at genuinely-satisfiable eigenvalue clauses), the disjointness trio's `hab`/`hcd` and the flipped twin's `hbc` (negated bands composed with genuinely-overlapping partners at `±diag(0,1) ≠ 0`), both inner-orthogonality clauses (`−1 ≠ 0` dots), the residual-orthogonality engine (`2x ⬝ᵥ (−x) = −2 ≠ 0`), the Hilbert identification (the transported `v ↦ −v` is surjective, so the range is everything and the projection is `x ≠ −x` = the claimed right side), and the closest-point bound (`hy` forces `y = 0` at a negated band, collapsing the claim to `‖2x‖ ≤ ‖x‖`, false at `![1,0]`). The rest: the mode-selection `h₂` (the band `(−1,2]` annihilates the `λ = 3` mode with `h₁` genuine), the covering `h`/`ha`/`hb` trio (a band at an interior eigenvalue misses a mode), the monotone-family `ht` (the delivered nonmono family's `B(4,0]·B(0,4] = −1 ≠ 0` at `k = 0 < 1 = m`) and `hkm` (self-composition `B(0,2]² = diag(1,0) ≠ 0` on the genuine monotone family), the vector-completeness `hb`/`hc` (the endpoint guards' vector forms — distinct statements, sum `= ![0,0] ≠ x`), and the cluster layer's seven (mode-selection both directions at `{0,11}` on `diag(0,5,11)`, the zero/one corners through the delivered projector pins, identical-cluster disjointness `P² = P ≠ 0`, capture-iff against a covering `Ioc` selection `diag(1,0,1) ≠ 1`, and the band agreement's own docstring corner: `Ioc 6 (−1) = ∅` gives the zero cluster projector against the negated band `−diag(1,1,0) ≠ 0`). Screened: 8 clauses with delivered witnesses (the overlap guard, both endpoint guards, the fixed-space guard, the not-fix/fixes mode witnesses, the `h₁` clause). Non-fenceables with mechanisms: the signature-entangled `hM` clauses; and the shared-mode theorem's `hab`/`hcd` — **a negated band's fixed space is provably trivial** (spectrum in `{−1, 0}`), so the dropped-guard statement is *provable* — the P4 truth-removable class, joining the electrical-flow audit's recorded instances. Five technique findings in the proposal (headline: per-branch `norm_num [h]` threshold discharges — every `rw [h]; norm_num` variant fails one way or another, on parse, on branch count, or on rw-closed goals; also simp's sign-asymmetric numeric-contradiction boundary, the `←`-direction self-substitution twin, function-literal application halting simp at composition goals, and the `congrArg`-WithLp.equiv injection route). |
| The Sparsification Core Family's Adversarial Fence Audit ([`adversarial-fences-sparsification-core-family.md`](adversarial-fences-sparsification-core-family.md)) | 2026-09-04 | **The prior terminal handoff's named next target — the electrical cluster's last QA family (the `ResistanceMetric` consumer set the cluster feeds; the program the whole cluster exists to serve) — delivered at the full priced scope: 15 fences + isolation companions at two delivered signed fixtures, the delivered disconnected `K₂ ⊕ K₂` and `K₂`, plus two new local fixtures (an asymmetric `Fin 2` `!![0,2;1,0]]` and a nonpositive-spectrum `Fin 3` re-deriving the tail QA's `sg` shape locally). QA-only (+55, 4876 → 4931, `Sparsification_QA.lean`'s `CoreFences` section, a pure 958-line insertion), zero axiom contact (`#print axioms` via `wip/spfences_axcheck.lean` on all 57 public declarations — every one exactly `propext, Classical.choice, Quot.sound`; no `-- @refutes` tags — theorem instantiations, nothing admitted consumed).** The fences: the eigenvalue-nonnegativity engine's `hnn` in whole-family form (the delivered signed fixture's pinned `quadForm = −2` + the spectral resolution forces a negative eigenvalue without index resolution); the leverage-share identity's `hnn` at the signed fixture's negative pair (edge vector junk-zero through `√(−1/2)`, right side `(−1)(−2)/2 = 1`); the ordered-pair budget identity's `hnn` at the delivered rank-1 signed 4-cycle through the new eigenspace-dimension-1 engine (`L = (1,−1,−1,1)(1,−1,−1,1)ᵀ` pinned entrywise, eigenvalue `4` genuine, per-pair `1/8`, total `1 ≠ 3 = card − 1`); the projector trio and exact-deviation identity's `hnn` at the nonpositive fixture (every eigenvalue `≤ 0` ⟹ every edge vector zero: zero rank-one sum against the nonzero projector, vanishing form against a strictly positive one, trace `1 ≠ 2`, deviation `0 − Π = 0`); the trace identity's `hconn` at the disconnected fixture through the delivered Foster kernel-count engine; the bilinear Dirichlet identity's `hA` (`3 ≠ 4` at the asymmetric `Fin 2`); the sampling layer's seven `hq` clauses and the Bernoulli second moment's `hpne` at `K₂`; and the strict dot-positivity `hv`. Headline non-fenceable: **the junk-√ zero edge vector is an automatic sign guard** — a negative-weight pair's zero edge vector forces probability `0` and weight `δ/0 = 0`, so the sampled Laplacian's PSD theorem survives without nonnegativity; two `hq` bound clauses die algebraically (`coeff = 1/q − ‖v‖²`); two transport clauses were already fenced by the tail QA's `sg` fixture |
| The Kernel-Bridge Family's Adversarial Fence Audit ([`adversarial-fences-kernel-bridge-family.md`](adversarial-fences-kernel-bridge-family.md)) | 2026-09-04 | **The prior terminal handoff's named next target — the audit method's tenth application and the electrical cluster's fourth, over the kernel-equality bridge (`SimpleGraphAdapter.lean`'s `ker_laplacian_eq_ker_supportGraph_lapMatrix`, the component-count transfer, the indicator basis) plus the weighted center characterization chain it consumes (`laplacian_mulVec_eq_zero_iff_forall_reachable` and both halves, the pos-weight engine, the exists-const/span forms) — the most-consumed kernel API on the shelf (`Electrical`'s solvability hinge, `Fiedler` ×3, `Heat` ×2, `Foster` ×3, `Mixing`), whose QA instantiated only nonnegative fixtures so no `hnonneg` clause of the family had a negative witness anywhere in the repository — delivered at the full priced scope: 12 fences + isolation companions at three new fixtures. QA-only (+59, 4817 → 4876, `KernelBridge_QA.lean`'s `BridgeFences` section), zero axiom contact (`#print axioms` via `wip/kbfences_axcheck.lean` on all 65 new declarations — every one exactly `propext, Classical.choice, Quot.sound`; no `-- @refutes` tags — theorem instantiations, nothing admitted consumed).** The fences, in three groups: **α** — the Fin 2 negative edge `!![0,−1;−1,0]]` with edgeless support (the center iff's ← direction's RHS junk-trivially true at `![1,0]` — reachability is equality on an edgeless graph — while `L *ᵥ ![1,0] = ![−1,1] ≠ 0`; the ← half at the same witness; the bridge's orientation 1 through `![1,0]` in the edgeless `lapMatrix` kernel but not the weighted kernel; and the dimension statement at `finrank 1 ≠ 2` — the weighted kernel characterized exactly as `ℝ ∙ ![1,1]` through the row equations + `finrank_span_singleton`, against the edgeless classification's two components); **β** — the Fin 3 signed path `!![0,2,−1;2,0,2;−1,2,0]]` whose support is *connected* (the negative edge `(0,2)` is not a support edge — the co-hypothesis discipline applied, since `hconn` is a co-hypothesis of the span/exists-const forms being fenced, which `Signed_QA`'s existing signed fixture with its disconnected support cannot serve): the pos-weight engine `eq_of_laplacian_mulVec_eq_zero_of_pos_weight`'s `hnonneg` at the mechanism level (`0 < A 0 1 = 2` genuine, `L *ᵥ ![1,2,3] = 0` genuine, `1 ≠ 2` — the termwise-vanishing step of the Dirichlet identity is exactly where nonnegativity enters), the walk-level `eq_of_supportGraph_walk` at the same witness, the iff's → direction (a genuine kernel vector nonconstant on connected support — the direction the α witness cannot reach, so the α fence is not an artifact of the edgeless corner), the span form and exists-const iff with `hconn` genuine, the bridge's orientation 2 (`![1,2,3]` in the *weighted* kernel but not the support `lapMatrix` kernel), and the dimension statement's second witness (`2 ≤ finrank ker ≠ 1` by the linearly independent pair `1`/`![1,2,3]` through `LinearIndependent.pair_iff'` + `finrank_span_eq_card` + `finrank_mono`); **γ** — the Fin 3 asymmetric *nonnegative* sink star `!![0,1,1;0,0,0;0,0,0]]` where only `hA` fails: the pos-weight engine's `hA`, the family's only `supportGraph`-free statement (`hA` is proof-internal, consumed by `laplacian_quadForm`, so the dropped statement is well-formed), refuted with every other hypothesis genuine — entrywise nonnegativity, `L *ᵥ ![1,2,0] = 0` through the vacuous sink rows, `0 < A 0 1` — at `f 0 = 1 ≠ 2 = f 1`: symmetry load-bearing at the quadratic-form step, not merely at the `supportGraph` display. Recorded non-fenceables: every other `hA` clause (statement-entangled through `supportGraph A hA`), the basis def's signature-consumed hypotheses, the `hconn` clauses (already fenced by `Connectivity_QA`'s delivered disconnected witnesses), degenerate corners screened. Five technique findings recorded (headline: `fin_cases` leaves eta-wrapped `Fin` literals that defeat `rw` of `rfl`-entry lemmas — the robust routes are `show` at concrete indices or entry-disjunction lemmas; also `Walk` is data not propositions; `Walk.cons` binds the final endpoint at `w` with the mismatch surfacing as a misleading invalid-constructor error on the adjacency; `refine linearIndependent_pair ?_ ?_` strands the module type at the statement, and the pinned Mathlib's pair lemma is `Set`-shaped anyway — `pair_iff'` is the `![x,y]` route; projection notation must not break across the receiver's closing parenthesis). Full ladder: build ✔ + completeness 133/133 fresh, `lint_axioms` (4 unchanged), `check_refutation_independence` (9-tag), `check_public_reachability` (63), `check_citations`, `check_markdown_links`, `check_backlog_freshness`, scoreboard 4876/4/0 with the verification row, map-freshness exit 0 after the stats sync + SVG regen. |
| The Foster Family's Adversarial Fence Audit ([`adversarial-fences-foster-family.md`](adversarial-fences-foster-family.md)) | 2026-09-04 | **The prior terminal handoff's named next target — the audit method's ninth application and the electrical cluster's third, over the spanning-tree-counting program (`Foster.lean`'s zero-eigenvalue count, spectral kernel, `foster_theorem` itself, the leverage corollary, and the `leverageScore` division guard), whose four-fixture QA predates the adversarial-review discipline — delivered at the full priced scope: 9 fences + isolation companions at the delivered signed rank-1 and disconnected fixtures plus a new one-vertex fixture. QA-only (+37, 4780 → 4817, `Foster_QA.lean`'s `FosterFences` section), zero axiom contact (`#print axioms` via `wip/fosterfences_axcheck.lean` on all 39 new declarations — every one exactly `propext, Classical.choice, Quot.sound`; no `-- @refutes` tags — theorem instantiations, nothing admitted consumed).** The fences: the zero-eigenvalue count's `hnn` (the signed fixture's kernel holds two non-parallel vectors `1` and `![1,1,-1,-1]`, so the count is forced `≥ 2 ≠ 1` — the inversion of the theorem's own at-most-one orthogonality argument through the new generic engine proving a filter of size ≤ 1 confines the whole kernel to one eigen-line) and `hconn` (constants plus the component indicator at the disconnected fixture); the spectral-kernel identity's `hnn`/`hconn` (the junk-fallback LHS `0` against a **strictly positive** RHS — the second generic engine: a genuine eigenvector with differing coordinates has its eigen-expansion confined to the nonzero modes with every term nonnegative, so the sum cannot vanish; PSD at the signed fixture through the **fixture-local rank-1 SOS identity** `quadForm L f = (s ⬝ f)²`, which supplies what `laplacian_psd` cannot without the nonnegativity hypothesis the fixture violates; at the disconnected fixture through `laplacian_psd` directly); `foster_theorem`'s `hnn` (**every off-diagonal demand is unsolvable on the signed fixture** — the kernel-generator coordinate map `x ↦ (w₁ x, w₂ x)` is injective on `Fin 4`, a strengthening of the delivered single-pair unsolvability — so the entire ordered sum computes to `0 ≠ 3`) and `hconn` (the ordered sum `4 ≠ 2·3`: within-block resistances genuinely pinned by witnesses, every cross-block weight zero so no junk value even enters); and the leverage corollary's `hnn` (`0/3 = 0 ≠ 2`), `hconn` (`4/3 ≠ 2`), and **`hcard` — the `0/0` division-guard junk corner at a new one-vertex fixture** (the score is literally division by zero, reads `0`, the sum `0 ≠ 2`: the docstring's "must not be read as a probability" warning exercised at the object itself). The four `hA` clauses recorded non-fenceable (each consumed by its display's `laplacian_symmetric A hA` or by `hconn`'s own `supportGraph A hA`). Five technique findings (headline: the self-substitution rewrite trap at its purest — `rw [hg₂]` with `hg₂ : g₂ = (v ⬝ g₂) • v` rewrites the `g₂` inside its own coefficient; the function-extensional `congrFun` + `field_simp` idiom is the robust route). |
| The Electrical-Flow Family's Adversarial Fence Audit ([`adversarial-fences-electrical-flow-family.md`](adversarial-fences-electrical-flow-family.md)) | 2026-09-04 | **The prior terminal handoff's top-named next target — the audit method's eighth application and the electrical cluster's second, over the routing family (`ElectricalFlow.lean`'s flow predicates, Kirchhoff bridge, flow energy, Thomson's principle and superposition lemma, Rayleigh monotonicity, capacity reinforcement) and the cluster's largest QA family (1072 lines), consuming the prior day's effective-resistance substrate — delivered at the full priced scope: 22 fences + 19 isolation companions. QA-only (+85, 4695 → 4780, `ElectricalFlow_QA.lean`'s `AdversarialFences` section), zero axiom contact (`#print axioms` via `wip/efflowfences_axcheck.lean` on all 89 new declarations — every one exactly `propext, Classical.choice, Quot.sound`; no `-- @refutes` tags — theorem instantiations, nothing admitted consumed).** The headline fences: the energy-agreement and routed-energy-identity `hA` clauses at the new **asymmetric 4-path** `asymPath4Adj` — the unit demand genuinely solvable (`f = ![3/2, 1, 1/2, 0]` solving `L *ᵥ f = e 0 − e 3`) while the ordered-pair energy `1` separates from the quadratic form `3/2` by exactly the row-sum/column-sum correction, and the resistance pins to `3/2` by **fixture-local voltage pinning** (every solution's `g 0 − g 3 = 3/2` by linarith over the row equations — the route that works on asymmetric input, where no symmetry-free uniqueness lemma exists); the superposition lemma's support clause killed by the divergence-free **cyclic phantom** `cycleFlow` riding the cheat network's zero-conductance pairs (`4 ≠ 2 = 1 + 1` — the integration-by-parts cross term dies only because a flow vanishes on zero branches) and its divergence clause by the doubled current (`9 ≠ 5`); **Thomson's `hnneg`** by the unit flow `negRouteFlow` `0 → 3 → 1 → 2` riding *both* negative edges of the delivered signed 4-cycle — symmetric, connected support, genuine unit flow, energy `−1` against the delivered junk resistance `0` (`0 ≤ −1`; the 2026-08-20 witness could not serve, its support graph being edgeless so connectivity fails there); **Rayleigh's `hnnegA`** at the signed edge's *genuinely solvable* demand — a real negative resistance `−1` (`f = ![0,1]`, fixture-local uniqueness from the single row equation), so the dropped statement reads `1 ≤ −1` — and its `hconnA` at the cheat network's junk `0` under the dominating path's `2` (`2 ≤ 0`); the flow-space and energy-comparison clauses (the phantom's negative energies: `1 ≤ −1`, `1 ≤ 0`; the orientation guard's numbers at the energy level `1 ≤ 1/2`); the support-graph and connectivity-adapter clauses (the new cheat-network walk-blocks pin; the delivered two-block disconnectedness reused); and the reinforcement family's corners — the conductance-`1/2` edge (`2 ≤ 1`), the reinforced cheat network computing entrywise to exactly the path (`2 ≤ 0` — the one-hypothesis ICP form genuinely needs the original connectivity). Three P4 removable findings recorded (the identity's `hnneg`/`hconn` truth-removable given `hA`+`hf`; Thomson's `hconn` implied by `hθ`; Rayleigh's `hnonnegB` by entrywise domination, joining the review pilot's own `hconnB`); five technique findings (headline: the `degreeMatrix` diffusion-form route third-confirmed on `Fin 4`; matrix literals fail to evaluate at `Fin 4` while `Fin 2`/`Fin 3` evaluate — a scale-specific trap). |
| The Effective-Resistance Core Family's Adversarial Fence Audit ([`adversarial-fences-effective-resistance-family.md`](adversarial-fences-effective-resistance-family.md)) | 2026-09-04 | **The prior terminal handoff's named continuation class — the audit method's third application beyond the mixing cascade, to the electrical cluster's root family (`Electrical.lean`'s potential-equation definition layer, Dirichlet-bound engines, confinement pair, and metric residuals — the substrate every remaining electrical QA file consumes) — delivered at full planned scope: 16 new fences + isolation companions at three new fixtures. QA-only (+47, 4648 → 4695, `AdversarialFences` sections in `EffectiveResistance_QA.lean` and `ResistanceMetric_QA.lean`), zero axiom contact (`#print axioms` via `wip/ecfences_axcheck.lean` on all 50 new declarations — every one exactly `propext, Classical.choice, Quot.sound`; no `-- @refutes` tags — theorem instantiations, nothing admitted consumed).** The headline Step-0 finding: the **rank-1 signed 4-cycle** `sgnK4Adj` — positive support a connected 4-cycle, negative diagonals, every degree `1`, Laplacian kernel dimension 3 — where the demand `e 0 − e 2` is unsolvable (kernel-vector pairing `2 ≠ 0`), killing both existence-flavored statements' dropped-`hnn` forms: **connected support does not imply demand solvability once signs enter**. Second finding: the **signed overshoot fixture** `sgnOverAdj` (support the path `0—1—3—2`, one negative edge, kernel exactly constants) kills the confinement **max** half's `hnn` — its `e 0 − e 1` solution `![1/2,0,1,1/2]` takes vertex `2` above both boundary values, and no three-vertex signed fixture can (the interior value always solves to a weighted average of the boundary pair — why the 2026-08-24 signed fence only ever killed the min half). Third: the asymmetric nonnegative `!![0,2;1,0]]` fences the Cauchy–Schwarz (`16 ≤ −8` false) and polarization (`−4 = 2` false) engines' `hA` — the family's only two `supportGraph`-free statements, so its only freely-fenceable symmetry clauses. Plus the disconnected fixture's **free-constant mechanism** (foreign-component solutions `![1,0,±7,±7]` escaping both confinement halves' `hconn`; the genuine-energy indicator's ratio `1 ≤ R 0 2 = 0` for the Dirichlet `hconn`; `1 ≤ 0 + 0` for the triangle `hconn` at a foreign middle vertex; `0 < 0` and the definiteness iff identifying distinct vertices), the signed fixture's engine `hnn`s (CS cross `1 ≤ 0`; Dirichlet `1/2 ≤ R 0 1 = 0`; explicit `0 ≤ R 2 1 = −2`), and six screened clauses already fenced by delivered witnesses. Recorded non-fenceables with mechanisms: the structural `hA`s; the uniqueness/agreement `hnn` (solvability forces kernel-invariant voltage differences on symmetric input — the dropped-hnn statement is provable); `effectiveResistance_symm`'s `hnn`/`hconn` (same); `effectiveResistance_nonneg`'s `hconn` (within-component genuine + cross-component junk `0`); `sq_le_mul_of_forall_zero_le_sub`'s `hE` (implied by `h`). Four technique findings incl. the `degreeMatrix` dependent-`if` stall and its `laplacian_mulVec_apply` route (delivered as the generic helpers `ecf_quadForm_lap`/`ecf_dot_lap`) |
| The Regular Cheeger Family's Adversarial Fence Audit ([`adversarial-fences-regular-cheeger-family.md`](adversarial-fences-regular-cheeger-family.md)) | 2026-09-04 | **The prior terminal handoff's named continuation class — the audit method's second application beyond the mixing cascade, to the SGT center namesake and the library's most-consumed unaudited cluster (six non-QA import consumers; its QA predating the adversarial-review discipline) — delivered at the companion-audit-revised scope: 17 core + 4 junk-corner fences with isolation companions. QA-only (+113, 4535 → 4648, `Cheeger_QA.lean`'s `RegularFences` section), zero axiom contact (`#print axioms` via `wip/regcheederfences_axcheck.lean` on all 117 public declarations — the 3 private pinning helpers audited transitively — every one exactly `propext, Classical.choice, Quot.sound`; no `-- @refutes` tags — theorem instantiations, nothing admitted consumed).** The fences: the `hnn` clauses of all four inequality statements at the signed genuinely-`d = 2`-regular `!![3,−1;−1,3]]` (`λ₂ = 0 ≤ 2φ = −1`; `1/8 ≤ λ₂ = 0`; `d·φ²/2 = 1/4 ≤ λ₂(L) = 0`; `λ₂(L) = 0 ≤ 2dφ = −2`) and the sweep's `hnn` at the same fixture's orthogonal mode (`R = −1 < φ²/2 = 1/8`); the `hd` clauses at the nonnegative `!![2,1;1,2]]` at claimed wrong degrees (`d' = 4`: mode eigenvalue `3/4 > 2φ = 2/3`; `d' = 1`: the operator collapses to all-`−1`s, `λ₂ = 0 < 1/18`, `R = 0 < 1/18`; `d' = 40` and `d' = 1/8` against the degree-independent combinatorial `λ₂(L) = 2` — `20/9 ≤ 2` and `2 ≤ 1/12` both false); the upper bound's `hdpos` at the zero matrix with genuine `d = 0` (the identity operator, `λ₂ = 1 > 2φ = 0` — the junk-conductance `0/0 = 0` corner); the sweep's `hx0` (definitional junk Rayleigh) and `horth` (the constant vector — in the kernel, not self-orthogonal); the PSD engine's `hA` at the asymmetric row-regular `!![4,1;3,2]]` (the quadratic form sees the symmetric part `[[4,2],[2,2]]`, whose `![2,1]`-Rayleigh `26/5` exceeds the claimed degree: `quadForm = −1/5 < 0`) plus its `hnn`/`hd`; and the four `cutTestVector_ne_zero` junk corners (the cut test vector identically `0` at the zero matrix, the empty cut, the full cut). **The survey's decisive finding (recorded as a technique finding and a pricing revision): the companion audit invalidates signed-fixture pricing for every `hdpos` clause — `hnn` is a co-hypothesis — and the satisfiable nonnegative corner collapses to the zero matrix where four of the five dropped `hdpos` conclusions survive** (sweep `0 ≤ 1`, lower `0 ≤ 1`, both twins `0 ≤ 0`, PSD via `regNL = 1`): recorded non-fenceables with the corner mechanism, alongside the structural `hA`s/`hcard` and the sweep's genuinely-mathematical `hA` (priced follow-on, 2-vertex analysis recorded). The pinning runs through two new private Fin 2 engines — the eigenvalue-witness route for lower bounds and the whole-space `evals_le_of_linearIndependent` instance (no PSD, no kernel) for upper bounds. |
| The Irregular Cheeger Family's Adversarial Fence Audit ([`adversarial-fences-irregular-cheeger-family.md`](adversarial-fences-irregular-cheeger-family.md)) | 2026-09-04 | **The audit method's first application beyond the mixing cascade — the irregular (volume-weighted) Cheeger program, the strategy's ring-1 namesake cluster (the edge-perturbation capstones, the Ramanujan ceiling, and the Poincaré family all consume it), whose QA (2026-08-25/26) predates the adversarial-review discipline. The prior run's 19-clause priced fence list closed in full: QA-only (+101, 4434 → 4535, `IrregularCheeger_QA.lean`'s `IrregularFences` section), zero axiom contact (`#print axioms` via `wip/icfences_axcheck.lean` on all 110 new declarations — 104 public plus the private helpers audited transitively — every one exactly `propext, Classical.choice, Quot.sound`; no `-- @refutes` tags — theorem instantiations, nothing admitted consumed). The fences: the easy direction's `hnn` (`ichSigAdj`: `λ₂ = 0 ≤ 2φ = −2`); the sweep lemma's `horth`/`hf0`/`hnn` (at `f = ![1,2]` `1/2 ≤ R = 1/5`; at `f = 0` the definitional junk Rayleigh `0`; at the signed antisymmetric mode `R = −2`); the cut theorem's `hf0` (no nonempty proper swept set of a constant exists) and `hnn` (the bound clause alone: `1 ≤ 2R = −4`); the kernel iff's `hconn` (the component-indicator kernel vector at the two-edge fixture, not a multiple of the stretched constants); the disconnected-λ₂ theorem's `hnn` at the new two-disjoint-signed-blocks fixture (both block-antisymmetric modes pinned eigenvectors at exactly `−2` through the subspace Rayleigh–Ritz engine, so `λ₂ ≤ −2 ≠ 0`) and `hd` at the new edge⊕isolated fixture (`λ₂ = 1 ≠ 0` by the variational engine with PSD and kernel supplied by hand — the isolated vertex's junk `D⁻¹ᐟ² = 0` row makes its `L_sym` eigenvalue `1`, not `0`); the positivity corollary's `hconn` (`φ ≤ 0` at the zero-boundary cut) and `hnn` at the new connected negative-cut fixture `!![3,1,−3;1,0,1;−3,1,3]` (`φ ≤ −2` at the cut `{0}`); the Fiedler capstone's `hnn` at the same fixture — λ₂ pinned exactly `0` (engine bounds `e₀ ≤ −5`, `e₁ ≤ 0`, `e₂ ≤ 2` plus trace `−3`), the 1-dimensional eigenspace characterized (`span (1,√2,1)`, three witness eigenvectors), so the sweep vector is provably a nonzero constant and the dropped conclusion fails on the shape clauses; the sandwich interface's and the degree-window pair's wrong-constant `dmin`/`dmax` clauses (`5·λ₂(L_sym) = 10 ≤ λ₂(L) = 2`; `2 ≤ 1` twice); the volume extraction's `hy` (at `y = 1` the swept family is constant — shape) and `hM` (at `y = 0` every positive superlevel is empty — the junk-division corner at the extraction's own display); and attainment's `hcard` at the one-vertex fixture (no nonempty proper subset of `Fin 1` exists) — each with an isolation companion proving every other hypothesis genuine. The sweep statements' `hA` and the sandwich `hnn` analyses stay priced follow-ons exactly as the survey recorded them. Full ladder: spike first (`wip/icfences_spike.lean`, zero errors/zero warnings before any shelf edit; landed as a pure insertion, 1560/0 in numstat), build ✔ + completeness 133/133, `lint_axioms` (4 unchanged), 9-tag refutation independence, 63-module reachability, citations, links, backlog freshness, scoreboard 4535/4/0, map-freshness exit 0 after the stats sync. Six technique findings in the delivery record (headline: the fixture-form tradeoff — `Matrix.of`-if fixtures resist if-decision inside sums, `!![…]` literals resist trailing entry evaluation, the working two-pass entry-lemma-then-def-unfold finisher; and the `evals_sum_eq_trace` index-type bridge through `Finset.sum_fin_eq_sum_range` since `Fin.sum_univ_three` cannot match `Fin (Fintype.card (Fin 3))`). |
| The Two-Start Indistinguishability Twin at the Computed Plain Rate ([`spectral-certificate-plain-walk.md`](spectral-certificate-plain-walk.md), second follow-on delivery record) | 2026-09-04 | **The depth-form delivery's recorded two-line composition — "the two-start indistinguishability twin at the computed rate … recorded here rather than landed (no consumer named)" — landed the next run, the consumer gate discharged from the shelf's own evidence (the caller-certificate twin's docstring has named the consumer since 2026-08-31: "the corollary oversmoothing papers state informally," the representations-become-indistinguishable statement; the computed-rate family's one-start ceiling was landed for exactly that consumer class). Zero new axioms (count stays 4), pure hard crust: `#print axioms` via `wip/twostart_axcheck.lean` on all 7 new declarations (1 shelf + 6 QA) exactly `propext, Classical.choice, Quot.sound`.** The shelf: `Oversmoothing.lean`'s `walkDistribution_sub_walkDistribution_le_of_depth_of_odd_walk` — past *both* starts' own computed-rate thresholds any two `t`-step views are within `2ε` at every target vertex, certificate-free, `_of_odd_walk`-honest for the same bipartite reason (on bipartite input both thresholds degenerate to junk simultaneously). QA +6 (4428 → 4434, `Mixing_QA.lean`'s `SpectralCertificate` two-start blocks): the true two-start values pinned at both certificate fixtures (triangle `t = 3`, starts `0`/`1`: `1/8` inside the certified `1`, the one-start deviations `1/12`/`1/24` in opposite directions summing to it; looped triangle `t = 1`, every pair: exactly `0` by one-step mixing) and the **C₄ two-start fence** — a genuinely two-start witness shape (opposite-parity starts `0`/`1` at `(t, y, ε) = (2, 1, 1/8)`: `|0 − 1/2| = 1/2 > 1/4 = 2ε`, both thresholds junk-satisfiable; same-parity starts coincide at even times, so the one-start fence's witness cannot refute the two-start statement) through the new opposite-parity law pin `c4_dist_two_one_QA`. Three technique findings recorded (the auto-bind notation trap re-confirmed in spikes; standalone threshold lemmas vs. theorem displays — inline `refine ?_` goals elaborated from the theorem; `abs_of_nonneg` ordered after `norm_num`'s arithmetic normalization). Same run: README's two prose axiom-count mentions repaired 5 → 4 (stale since the 2026-09-02 retirement, verified against the generated scoreboard). |
| The Depth-Form Oversmoothing Join at the Computed Plain Rate ([`spectral-certificate-plain-walk.md`](spectral-certificate-plain-walk.md), follow-on delivery record) | 2026-09-03 | **The spectral-certificate delivery's closing scope note — "the depth-form oversmoothing join ... recorded here rather than landed as a separate statement" — discharged the same day: `Oversmoothing.lean`'s `walkDistribution_sub_stationaryVec_le_of_depth_of_odd_walk`, the plain family's first certificate-free depth ceiling at the inflated computed rate `(max (max (1 − λ₂) (λ_max − 1)) 0 + 1)/2`. Zero new axioms (count stays 4), pure hard crust: `#print axioms` via `wip/depthjoin_axcheck.lean` on all 18 new declarations (1 shelf + 17 QA) exactly `propext, Classical.choice, Quot.sound`.** The rate facts proved directly from the constituents (connectivity's `0 < λ₂` + the strict signless `λ_max < 2` + the below-gap workhorse) so nothing depends on rcases witness unfolding; the `_of_odd_walk` suffix honest because, unlike the abs twins, the depth display cannot be stated unconditionally — on bipartite input the rate saturates at `1` and the log threshold degenerates to junk. QA +17 (4411 → 4428, `Mixing_QA.lean`'s `SpectralCertificate` depth blocks): the inflated display rate pinned exact at both certificate fixtures (triangle `3/4`, looped triangle `1/2` — the corner the inflation exists for); the ceiling instantiated with genuinely verified thresholds at both (triangle `t = 3`, `ε = 1/2`, denominator `log(4/3)`, true value `1/24`; looped triangle `t = 1`, denominator `log 2` with the threshold inequality literally the corpus's `tri_ceiling_threshold_one_QA` reused — both fixtures uniform `π = 1/3` — and the conclusion `0` by exact one-step mixing); and the **C₄ junk-threshold fence**: the top spectrum entry pinned `= 2`, the display rate pinned to saturate at exactly `1`, the two-step law pinned `(1/2, 0, 1/2, 0)` — so the dropped-odd-walk statement's threshold hypothesis is junk-satisfiable (`x/0 = 0 ≤ t`) while the conclusion fails at `(t, ε) = (2, 1/8)` with `|1/2 − 1/4| = 1/4 > 1/8`, the junk-division hazard class exercised at the new display itself (isolation cited to the delivered `c4_fence_isolation_QA`, not duplicated). Three technique findings in the proposal's follow-on record (identifiers cannot contain `λ`; `rw` will not match a Fin literal against `⟨Fintype.card V − 1, _⟩` at reducible transparency — repair by an explicit `rfl`-lemma, `exact` is forgiving; `((1 : ℕ) : ℝ)` is not defeq to `(1 : ℝ)` — close casts with `exact_mod_cast`). |
| The Plain Walk's Spectral Certificate ([`spectral-certificate-plain-walk.md`](spectral-certificate-plain-walk.md)) | 2026-09-03 | **The standing handoff's named third frontier — the spectral certificate route for the plain family, its recorded consumer gate discharged by naming the plain family's entrywise oversmoothing ceiling (the message-passing depth setting's non-lazy walk, caller-certificate-gated on exactly the complementary class where the lazy intrinsic rate does not apply) — delivered as the strict signless engine plus the computed certificates plus the χ²/TV/entrywise joins. Zero new axioms (count stays 4), pure hard crust: `#print axioms` via `wip/speccert_axcheck.lean` on all 84 audited declarations (9 shelf + 75 QA) exactly `propext, Classical.choice, Quot.sound`.** The engine: connected + an odd closed walk (the primitivity supplier's exact interface) ⟹ every `L_sym` eigenvalue `< 2` — a `μ = 2` eigenvector unstretches (through the delivered congruence + signless SOS) to an edge-flipping potential, which the odd walk zeroes and connectivity propagates to zero everywhere, contradicting the unit eigenvector. The certificate: `∃ r < 1` (and `∃ r ∈ (0,1)`, the display inflation — `r = 0` is an honest corner) at `r := max (1 − λ₂) (λ_max − 1)`. The joins: `chiSquareDistance_le_max_rate`, its TV shadow, and the Oversmoothing entrywise consumer at the computed rate. QA +75 (`Mixing_QA.lean`'s `SpectralCertificate` section): the triangle certificate pin `1/2` exact (both branches attained) with the max-rate bound attained at `χ²(1) = 1/2`; the looped triangle's `r = 0` corner (spectrum `{0,1,1}`, one-step exact mixing `χ²(1) = 0`, the inflation instantiated); the C₄ unsatisfiability fence (alternating-mode `eigvalOf = 2` forbids every `r < 1`; every closed walk even by the flip-propagation lemma); and the strict engine's `hnn`/`hconn` fences (a negative-diagonal triangle with an eigenvalue escaping `> 2`; triangle ⊕ `K₂` on `Fin 5` with the `K₂` `μ = 2` mode surviving the genuine odd walk). Six technique findings recorded (headline: file-local notations do not export and undefined identifiers silently auto-bind as variables in statements). |
| The Lazy Family's Priced Follow-On Fences ([`adversarial-fences-lazy-family.md`](adversarial-fences-lazy-family.md), follow-on delivery record) | 2026-09-03 | **The standing handoff's top-named continuation — the lazy audit's two priced deferrals, the last priced QA items anywhere in the mixing cascade, closed as `Mixing_QA.lean`'s `LazyFollowOnFences` section. QA-only (+39, 4297 → 4336), zero axiom contact: `#print axioms` via `wip/lazyfollowon_axcheck.lean` on all 39 new theorems — 38 exactly `propext, Classical.choice, Quot.sound`, `lfFin4` stricter at `propext, Quot.sound`; no `-- @refutes` tags (theorem instantiations, nothing admitted consumed).** The two halves: (1) the conjugated-norm contraction twin `dotProduct_self_degreeSqrt_mulVec_pow_lazyWalkTransitionMatrix_contraction`'s `hrate`/`hmode` certificate fences — the √D-weighted mirrors of the delivered ℓ²(π) pair at the same two fixtures (`hrate` on the triangle at the `3/2`-mode direction with the below-mode `r = 1/8`: `LHS = (1/4)²·4 = 1/4 > 1/16 = (1/8)²·4 = RHS`, through the new `conj_norm_smul` helper and the `tri_conj_norm_mode_QA` norm pin `‖√D *ᵥ g‖² = 4`; `hmode` on the edge at the constant zero mode with the genuine `r = 0`: `P_L *ᵥ 1 = 1` and `√D = 1`, so `2 > 0`), with the isolation *packaged* rather than duplicated — `conj_twin_isolation_QA` closes by exact the four delivered ℓ²(π) companions because the two engines share `hmode`/`hrate` statement-identically, so one fence pair now guards both contraction engines' certificate interface; and (2) the lazy `t_mix` object's `_spec` witness-clause (`hne`) junk corner at the disconnected bipartite fixture `dK2Adj = K₂ ⊕ K₂` on `Fin 4`: the full lazy-law closed form (`dK2_lazy_step` — one lazy adjoint step of any `{0,1}`-supported law averages the block — giving `ν₀ = δ₀`, `ν_{t+1} = (1/2,1/2,0,0)`: the law converges to the *component*-stationary, not `π`), the exact TV pins `3/4`/`1/2` (never below `1/8`), the no-mixing lemma, `t_mix = sInf ∅ = 0` (the plain twin's `k2_mix_junk_corner_QA` mechanism at the lazy object — completing the pair), and the dropped-`hne` refutation at `s = 0`; `dK2_fence_isolation_QA` pins every structural hypothesis genuine with connectivity exactly the failure — laziness repairs periodicity, not disconnection. Four technique findings (the `2⁻¹ • (Pᵀ + 1)` adjoint-step route dodging `if j = k` splits; post-normalization `|3/4|` needing explicit `abs_of_nonneg`; `Fin 4` vector-literal indices 2/3 needing rfl-pins; the `rw [lemma, lemma, lemma]` exhaustion trap).** |
| The Capstone Consumer Fences of the Primitivity-Supplier Audit ([`adversarial-fences-primitivity-supplier-family.md`](adversarial-fences-primitivity-supplier-family.md), follow-on delivery record) | 2026-09-03 | **The standing handoff's top-named continuation — the same proposal's two priced consumer follow-ons, the `.hp` and `.hconn` clauses of `empiricalWalkDistribution_tail_selfcontained_of_depth` (the theorem the self-contained plain-walk sampling program stands on), the only two clauses the 2026-09-03 audit deferred — closed with hypothesis-form fences plus isolation companions. QA-only (+6, 4291 → 4297, the capstone subsection of `EmpiricalStationary_QA.lean`'s `PrimitivityFences` section), zero axiom contact: `#print axioms` via `wip/capstonefences_axcheck.lean` on all six new declarations exactly `propext, Classical.choice, Quot.sound`; no `-- @refutes` tags (theorem instantiations, nothing admitted consumed). The fences: `hp` at `K₂` (every other hypothesis genuine *including connectivity*; every closed walk even, so past any threshold the even-time law from `0` is `δ₀`, the sampling measure concentrates on the all-zero trajectory, the deviation `|1 − 1/2| = 1/2` carries full mass `1 > 2 exp(−4)` — the odd-walk clause excludes exactly the bipartite class) and `hconn` at `triIso4` started at the absorbing vertex `x = 3` (a genuine odd closed walk and every walk-level clause genuine, but the law is `δ₃` at *every* time — convergent to the wrong vector, deviation `|1 − 1/7| = 6/7` at full mass at every threshold time — a failure mode distinct from alternation). Both through the new point-mass cylinder helper `toMeasure_cyl_singleton_one` (`toMeasure_cyl_inter` at a factor with `q v = 1`, collapsing to `1 ^ n` — the measure-of-singleton machinery the pricing named, one application cleaner than the priced sketch) with `two_mul_exp_neg_four_lt_one` the shared numeric. One honest parameter adjustment recorded in the proposal: the priced `(ε, n) = (1/4, 32)` needs a strict `2 < exp 1` the non-strict engine cannot deliver; the delivered `(1/2, 32)` closes at `2 exp(−4) ≤ 2/5 < 1` by the `tri_naive_stationary_refuted_QA` route at the same honest deviations. After this delivery the self-contained sampling theorem's falsification surface is complete: every hypothesis fenced or recorded non-fenceable.** |
| The Adversarial Fence Audit of the Primitivity-Supplier Family ([`adversarial-fences-primitivity-supplier-family.md`](adversarial-fences-primitivity-supplier-family.md)) | 2026-09-03 | **The standing handoff's top-named continuation — the audit-shaped adversarial pass over mixing-cascade QA, fifth and LAST family: the primitivity-supplier family (`PrimitiveConvergence.lean`'s concatenation/bounce/supplier trio, `Mixing.lean`'s walk-to-power bridge / walk-level supplier / plain-walk Doeblin rate / convergence corollary), whose QA was delivered 2026-09-02 by its own run and never independently re-read — finding **21 load-bearing clauses with no negative witness anywhere in the repository** and closing every one with a hypothesis-form fence plus isolation companion. QA-only (+197, 4094 → 4291, `EmpiricalStationary_QA.lean`'s `PrimitivityFences` section), zero axiom contact: `#print axioms` via `wip/primsupfences_axcheck.lean` on all 212 new declarations (209 exactly `propext/choice/Quot.sound`, three Fin-case helpers stricter); no `-- @refutes` tags (theorem instantiations, nothing admitted consumed).** The headline Step-0 finding: the support graph's adjacency demands **positive** weights — negative edges are not graph edges — so both `hnn` fixtures hide the negative weight on a diagonal (row sums kept positive): `negWalkAdj` makes the genuine walk `0→1→2`'s own power `(P²) 0 2 = −5`, and `negDiagTri`'s walk matrix carries the eigenvalue `−11/4` with exact closed forms (`P^k (0,1,1)` and `(P^k) 1 0`) — no power entrywise positive, the law divergent. The rest: the supplier's `hreach` at the identity, `htwo` at the directed 3-cycle permutation (`p3³ = 1`, no reciprocal pair anywhere — period 3), `hodd` at the swap, `hnn` at the exact-cube rotation `!![1,−3;1,1]` (`M³ = −8·1` signs every power); the supplier/corollary `hconn` at triangle⊕isolated-vertex (cross-block power entries identically zero; the absorbed law `δ₃` converges to the wrong vector `π 3 = 1/7`) and `hp` at `K₂` (alternation); the rate's `hle` (negative base at `δ = 1`) and `hA` (the asymmetric loop with `P = (1/2)·J`: certificate genuine at equality, `π` not stationary — symmetry load-bearing through stationarity). Non-fenceables recorded with mechanisms; the capstone's two graph clauses priced as follow-ons with worked mechanisms. Nine technique findings (the positive-weight support graph; `rw`-variable-capture in exponent normalization; the `-11/4`-parses-as-`(-11)/4` trap; `pow_succ`-term-unification; `decide`-over-ℝ; `open scoped Classical` poisoning `decide`; `sum_eq_single_of_mem`'s argument order; `Fin.sum_univ_four`; the `Metric.tendsto_atTop` + entry-lower-bound ¬Tendsto idiom). The adversarial re-read of the entire 2026-09-01/02 mixing cascade is now complete. |
| Adversarial Fence Audit of the Poisson-Bridge Family ([`adversarial-fences-poisson-bridge-family.md`](adversarial-fences-poisson-bridge-family.md)) | 2026-09-03 | **The standing handoff's top-named continuation — the audit-shaped adversarial pass over mixing-cascade QA, fourth family (the Poisson bridge: the Poissonization identity, the TV convexity toolkit, the comparability/transfer pair — the continuous↔discrete hinge), whose QA was delivered 2026-09-01 and never independently re-read — finding **28 load-bearing clauses with no negative witness anywhere in the repository** and closing every one with a hypothesis-form fence plus isolation companion. QA-only (+82, 4012 → 4094, `Mixing_QA.lean`'s `PoissonFences` section), zero axiom contact: `#print axioms` via `wip/poissonfences_axcheck.lean` on all 83 new declarations exactly `propext, Classical.choice, Quot.sound`; no `-- @refutes` tags (these refute *theorem* instantiations — nothing admitted is consumed).** The fences: the simplex-diameter lemma's four mass clauses; the head–tail split's summability clause at the junk `0 = 1`; the convexity bound's `hc`/`hc1` (the alternating geometric weights `(3/2)(−1/2)ᵏ` against a geometric family — the signed mixture's TV `1/4` against the weighted average `3/20`) and **`hν`/`hν1` by the corpus's first divergent-tsum junk route** (summable weights against growing laws junk both sides to `0` against the honest `1/2`); the Poisson weight's negative time; the `hnn` clauses of the TV ≤ 1 bound, the adjoint-walk contraction and its power twin, and discrete TV monotonicity at the negative off-diagonal fixture (`3/2 → 9/2`); the `hA` clauses of the whole Poissonization identity triple (reversibility: at the asymmetric swap the constant-density mixture sums against the heat kernel's genuine drift), of the stationary power, and of monotonicity at a **new fixture** `asymFlowAdj` (`1/10 → 7/20`, the existing asymmetric fixtures provably cannot serve); the rate theorems' `hA` at the swap's `t = 2` (`1 − e⁻² > 2/3` from `e² > 4`); the `ht` clauses at `t = −1` on the edge (signed weights, continuous TV `e²/2`); and the transfer corollary's `htail` on the triangle (certificate `ε₁ = 1/6` genuine, tail budget `1/20` understating the honest head, refuted from **a new series bound `e < 3`** — range-5 partial sum plus the factorial-vs-geometric tail). `hmix` already fenced (`k2_no_discrete_mixing_QA`); sixteen non-fenceables recorded with mechanisms (ten `hd` clauses — the junk-collapsed substochastic chain survives on truth; `hπ`/`hπ1` — triangle inequality needs no target structure; the rate's `hnn`/`hd` — the factorial beats spectral growth; the transfer's `hnn` — spectral escape `> 1` screens every negative-entry fixture). Six technique findings (headline: **the matrix-literal index-type trap** — `![a,b]` elaborates at `Fin (Nat.succ 1)` and silently kills the `omega`-rcases idiom under `funext`; fix: explicit `∀ i : Fin 2` binders). Entry records repair: the entropy-family audit's delivered row was found sitting misfiled at the top of the **Active** table — moved to Delivered in the same pass. |
| The Adversarial Fence Audit of the Entropy Family ([`adversarial-fences-entropy-family.md`](adversarial-fences-entropy-family.md)) | 2026-09-03 | **The standing handoff's top-named continuation — the audit-shaped adversarial pass over mixing-cascade QA, third family: the entropy family (`Entropy.lean`'s generic KL/entropy layer, `Mixing.lean`'s entropy leg, `Oversmoothing.lean`'s entropy floor — the mixing program's third distance), the only cascade family whose QA (2026-09-01) predates the adversarial-review discipline — finding **28 load-bearing clauses with no negative witness anywhere in the repository** and closing every one with a hypothesis-form fence plus isolation companion. QA-only (+130, 3882 → 4012, `Entropy_QA.lean`'s Section F + `Mixing_QA.lean`'s `EntropyFences` section), zero axiom contact: `#print axioms` via `wip/entropyfences_axcheck.lean` on all 143 new declarations exactly `propext, Classical.choice, Quot.sound`; no `-- @refutes` tags (these refute *theorem* instantiations — nothing admitted is consumed).** The fences: every mass clause of the generic layer (incl. the transcendental `H(1/3,…) = (4/3)·log 3 > log 4` at mass `4/3` via `3⁴ > 4³`); both strict-positivity readings of `klDiv_eq_zero_iff` (the junk-zero divergence of two different point masses); both mass clauses of Pinsker (a junk-negative divergence collapses `√(D/2)` to `0`); the `hnn`/`ht` clauses of the continuous nonnegativity plumbing through closed-form heat kernels (`exp_eq_one_add_of_mul_self_eq_smul`) — the negative off-diagonal fixture at `t = 1` and the `K₂` *backward* semigroup at `t = −1`, both `h(1) = 1 − e² < 0`; the `hA` of continuous mass conservation at a new asymmetric-swap fixture `!![2,0;4,0]` whose walk Laplacian is idempotent (mass `3 − 2e^{−1} ≠ 1`) and its `hd` at the `zdAdj` junk-zero law against the `0⁻¹ − 1 = −1` bound (no eigenvalue pin needed, by `exp_pos`); the decay form's `hrate` at the triangle's failing `r = 1/8`; and all four analytic clauses of the entropy floor (`hμ` at the kernel eigenpair, `hv` at a fake `μ = 3` mode, `hc` at an undershooting sup, `hA` at the swap's genuine `(1, ![0,1])` eigenpair of its asymmetric `L_sym`). Recorded non-fenceable with reasons (junk log-of-negative contamination, vacuous empty-type corners, proof-shaped `hconn`) and three removable-hypothesis findings — notably the continuous nonneg `hA` clauses: the heat kernel `e^{−t}·e^{tP}` is entrywise nonnegative for *any* nonnegative `P`, symmetric or not (the delivered proofs use `hA` via Poissonization; the truth does not). Eight technique findings in the proposal (headline: entry pins fire under `rw` but not as `simp only` rules; the Fin-literal shape mismatch third-confirmed, now for matrix entries). |
| Adversarial Fence Audit of the Lazy Family ([`adversarial-fences-lazy-family.md`](adversarial-fences-lazy-family.md)) | 2026-09-02 | **The standing handoff's top-named continuation — the audit-shaped adversarial pass over mixing-cascade QA, second family: the lazy family (`Mixing.lean`'s lazy sections plus `Oversmoothing.lean`'s `LazyMixingTime` — the periodicity fix every bipartite input mixes through, the empirical lazy capstone's substrate), whose QA was delivered 2026-09-01 by its own run and never independently re-read — finding **ten load-bearing hypotheses with no negative witness anywhere in the repository** and closing every one with a hypothesis-form fence plus isolation companion. QA-only (+65, 3817 → 3882, `Mixing_QA.lean`'s `LazyFences` section), zero axiom contact: `#print axioms` via `wip/lazyfences_axcheck.lean` on all 68 new declarations exactly `propext, Classical.choice, Quot.sound`; no `-- @refutes` tags (these refute *theorem* instantiations — nothing admitted is consumed).** The fences: the **`hd` clauses of lazy row-stochasticity, mass conservation, and the `t = 0` χ² normalization** at the zero-degree fixture `zdAdj` (the `D⁻¹A` row junk-zero: `P_L *ᵥ 1` reads `1/2 ≠ 1`, the one-step law's mass `1/2 ≠ 1`, the normalization's right side the junk `0⁻¹ − 1 = −1` against the true `1`); the **`hnn` clauses of operator- and law-nonnegativity** at the negative off-diagonal fixture `negOffAdj = !![2,−1;−1,2]]` (`P_L 0 1 = ν₁ 0 1 = −1/2 < 0` — the existing negative-*diagonal* `negDiagAdj` cannot kill these: its off-diagonal `2` keeps every `P_L` entry nonnegative, which is precisely why a new fixture was needed); the **`hA` clauses of detailed balance (`3/16 ≠ 1/16`), stationarity (`5/8 ≠ 3/4`), and attainment persistence** at the asymmetric loop fixture `asymLoopAdj = !![3,3;1,1]]` — the persistence fence's hypothesis *genuine* (the lazy law from `0` hits `π = (3/4, 1/4)` exactly at `t = 1`, `asym_lazy_law_one_QA`) while the conclusion fails at `t = 2`: the law passes through `π` and leaves it; and the **two certificate clauses of the public lazy ℓ²(π) contraction engine** (`sum_stationaryVec_smul_sq_pow_lazyWalkTransitionMatrix_le`): `hrate` refuted on the triangle at the genuine `3/2`-mode direction `g = (1,−1,0)` with `r = 1/8` below the mode's lazy factor `1/4` (`P_L *ᵥ g = (1/4) • g` pinned, `1/24 > 1/96`), `hmode` refuted on the edge at the constant mode with the genuine `r = 0` certificate (`P_L *ᵥ 1 = 1`, `1 > 0`) — the lazy twins of the plain family's C₄ mode-coverage fence, with companions proving the *other* clause genuine at each fixture (the triangle's via `tri_kernel_const`; the edge's via the new `k2_eigvalOf_cases_QA`, every edge eigenvalue `0` or `2` pinned by the summed eigen equation and the unit-eigenvector quadratic form with no basis control). Recorded non-fenceable, with reasons: the headline's `hconn` (on disconnected input `λ₂ = 0` collapses the rate to `1` and every lazy factor still lies in `[0,1]` by PSD + the signless certificate alone — the dropped statement stays *true*: proof-shaped, not truth-shaped, the same class as the prior audit's `0 < δ` verdict); the depth ceiling's `hslt` (`λ₂ < 2` is junk-prevention on the threshold's log denominator — on the connected `λ₂ = 2` class the lazy law is exactly stationary after one step, so the conclusion holds anyway); and `hd` in the two nonnegativity lemmas (**a removable-hypothesis finding**: `D⁻¹A` stays entrywise nonnegative at zero degrees since `(deg i)⁻¹ ≥ 0` including the junk `0⁻¹ = 0`). Verification: spiked (`wip/lazyfences_spike.lean`) to zero errors/zero warnings before any shelf edit; explicit `lake build` target ✔; the 68-declaration axiom audit; full `lake build` + `check_build_completeness.py` (133/133/0/0, exit 0); `lint_axioms` (4 axioms unchanged), `check_refutation_independence` (9-tag clean), `check_public_reachability`, `check_citations`, `check_markdown_links`, `check_backlog_freshness` all clean; scoreboard regenerated (3882/4/0); map-freshness exit 0 after the stats sync. Six technique findings recorded in the proposal (headline: the `fin_cases` id-motive wrapper trap strikes again — the `omega`-rcases variable-rewrite idiom is load-bearing; a leading-dot method split across lines re-parses onto the outer term; `eigvalOf` vs raw `.eigenvalues` never match by `rw` though definitionally equal). |
| Adversarial Fence Completion for the TV/Dobrushin Engines ([`adversarial-fences-tv-dobrushin-engines.md`](adversarial-fences-tv-dobrushin-engines.md)) | 2026-09-02 | **The standing handoff's top unstarted frontier — the audit-shaped adversarial pass over mixing-cascade QA — executed as a bounded audit of the 2026-09-02 directed TV/Dobrushin engine family (`Mixing.lean`'s Doeblin TV-contraction and Dobrushin-coefficient sections, the mixing program's newest reusable engines and the priced undirected join's future substrate), finding **seven load-bearing hypotheses with no negative witness anywhere in the repository** and closing every one with a hypothesis-form fence plus isolation companion. QA-only (+49, 3768 → 3817, `DirectedMixing_QA.lean`'s Section I, the module 131 → 180), zero axiom contact: `#print axioms` via `wip/fences_axcheck.lean` on all 62 audited declarations exactly `propext, Classical.choice, Quot.sound`; no `-- @refutes` tags (these refute *theorem* instantiations — nothing admitted is consumed).** The fences: the **equal-mass clauses of both TV contractions** (`tvDistance_vecMul_le_of_pos_entries`, `tvDistance_vecMul_le_tvDobrushinCoeff`) refuted at the all-half matrix `Qh` — row-stochastic, every entry `≥ 1/2`, identical rows so `δ(Qh) = 0` *exactly* — with the mass-`2` start `twoE0` against `e0`: the mass-dropped contraction reads `1/2 ≤ 0`; the **row-sum and nonnegativity clauses of TV non-expansiveness** (`tvDistance_vecMul_le`) refuted at the doubled identity (nonnegative, row sums `2`: the basis pair's TV doubles, `2 ≤ 1` false) and the signed stochastic fixture `!![2,−1;−1,2]` (row sums `1`, negative entry: the TV triples, `3 ≤ 1` false); the **row-sum clause of Dobrushin submultiplicativity** (`tvDobrushinCoeff_pow_add_le`) refuted at `Qx = !![1,1;1,−1]` with `Qx² = 2I`: `δ(Qx²) = 2 > δ(Qx)² = TV((1,1),(1,−1))² = 1` at `s = t = 1`; and the **zero-mass clauses of both pairing cores** (`abs_dotProduct_le_half_entryRange_mul_sum_abs`, `abs_sum_mul_le_of_pairwise`) refuted at `e0` against itself (`1 ≤ 1/2`, every oscillation clause verified at `D = 1`). Each fence is the negation of the conclusion at a specific instantiation with both sides pinned to numerals; each isolation companion proves the refuted clause is exactly what fails (all other hypotheses verified genuine at the fixture). The new fixtures exist because the existing ones cannot kill these clauses: `Qd`/`Gd` are strictly positive with distinct rows (their contractions are the attainment pins), and `P2` kills the floor clause but is a genuine permutation, so the mass-dropped conclusions still hold there. Recorded non-fenceable: `0 < δ` in the Doeblin contraction is a strength hypothesis (at `δ ≤ 0` the statement stays true by non-expansiveness); the `[Nonempty V]` arguments are structural. Five technique findings recorded in the proposal (headline: `norm_num` **does** evaluate abs of numerals presented as subtraction-of-numerals in `tv_lit`-shaped goals, refining the 2026-09-02 record's note — `rw [tv_lit]; norm_num` is the cheap idiom; plus the `Finset.sup'_le` call shape, the `rintro`-projection trap with its `rcases (show a = 0 ∨ a = 1 by omega)` idiom, the `Eq.le` orientation trap in `sup'` lower bounds, and the trailing-`rfl` gap on numeral subtraction). |
| The Primitivity Supplier and the Self-Contained Plain-Walk Sampling Capstone ([`primitivity-supplier-plain-walk.md`](primitivity-supplier-plain-walk.md)) | 2026-09-02 | **The standing handoff's named first-magnitude frontier — the undirected plain-walk sampling-capstone twin, with its named blocker (connected + odd closed walk ⟹ a strictly positive walk power) delivered as the same package's engine. Zero new axioms (count stays 4), pure hard crust: `#print axioms` via `wip/primsup_axcheck.lean` on all 39 audited declarations (9 new shelf + 20 QA + 10 consumed-layer spot-checks incl. `primitive_power_tendsto` itself) exactly `propext, Classical.choice, Quot.sound`.** The engine (`PrimitiveConvergence.lean`): **`isPrimitive_of_pow_pos_of_odd_loop`** — a nonnegative matrix with strongly connected support, every index on a positive 2-cycle and an odd closed walk, is primitive, by the two-parity covering (reach; bounce `+2` along the 2-cycle for one parity; run the odd closed walk first for the other; witness `m := 1 + ∑ (d + L₀)`); support-level hypotheses deliberately not `IsSymm` — `walkTransitionMatrix = D⁻¹A` is not symmetric on irregular graphs, the first draft's shape that would have been inapplicable at the point of use. The walk level (`Mixing.lean`): the `Walk`-induction bridge `pow_walkTransitionMatrix_pos_of_walk`; **`walkTransitionMatrix_isPrimitive_of_connected_of_odd_walk`** — connected support graph + a single odd closed walk, the walk transported to every vertex by *reversal* at the `Walk` level (`Odd p.length` the honest interface, the pinned Mathlib having no `SimpleGraph.Bipartite`); **`walkDistribution_tvDistance_le_of_pos_power`** — the plain walk's first mixing rate with no spectral certificate and no caller-supplied `r` (`TV ≤ (1 − |V|δ)^{t/m}`, the intrinsic-rate family's non-bipartite member, complementary to the lazy family's bipartite class); and **`walkDistribution_tendsto_stationaryVec`** — the retired `primitive_power_tendsto`'s first undirected consumer. The named consumer (`EmpiricalStationary.lean`): **`empiricalWalkDistribution_tail_selfcontained_of_depth`** — `∃ t₀` past which `n` simulated walk trajectories estimate `π i` to `ε` at `2 exp(−nε²/2)` for every start simultaneously, no caller rate certificate anywhere (the PageRank twin's `(α, ε)` display honestly existential — the produced `(m, δ)` are witnesses, not functions of the visible parameters), plus the bias-term form at `ρ^{t₀/m}·TV(δ_x,π)` with the start-dependent TV kept explicit. QA +20 (3748 → 3768): the primitive square pinned on the triangle (`P² = (1/4)(J−I)` with the `m = 1` zero-diagonal fence), the supplier instantiated through the explicit odd walk `0→1→2→0`, **the Doeblin rate attained exactly at the even time `t = 2`** (truth `1/6` = engine `(1/4)^{2/2}·(2/3)`) with the odd-time factor-`2` slack pinned, the `K₂` periodicity fence (`(P^t) 0 0 = 0` at every odd `t` — `hodd` exactly isolating the bipartite class the lazy program patches), the capstone instance at `ε = 1/4` with the produced threshold's arithmetic (`⌈log 8/log 4⌉ = 2`) and honest slack witnessed (`TV(ν₂) = 1/6 > 1/8`), and the convergence instance. Full verification ladder; six technique findings recorded in the proposal. |
| Parametric (Arbitrary-Scale) QA for the Cycle Family ([`parametric-cycle-qa.md`](parametric-cycle-qa.md)) | 2026-09-02 | **The QA axis's named remainder — "parametric property QA," cited by every score-hold since 2026-08-22 — closed on the cycle family: the repository's first QA section whose lemmas quantify over the scale. Zero axiom contact (QA-only, no shelf changes; count stays 4), pure hard crust: `#print axioms` via `wip/paramqa_axcheck.lean` on all 17 audited declarations (10 QA pins + the 4 private calculators audited transitively + the 7 consumed shelf theorems re-checked) exactly `propext, Classical.choice, Quot.sound`.** `AlonBoppana_QA.lean`'s new `Step8` section (+10, 3738 → 3748), every lemma `∀`-quantified over `n`/`k`/`m` and consuming the shelf theorems at symbolic scale — where a scale-dependent error in a parametric statement (an antipodal-tie off-by-one, a boundary mishandling at `2(j+1) = n`) fails at *every* scale at once, while a fixed fixture (largest ever: `C₁₂`) can only fail below its own size: the short-branch distance pin with the antipodal tie *in* the hypothesis set (`dist a (a+⟨m⟩) = m` at `2m ≤ n`), the translate-invariant edge pin, the family's own antipode at the exact tie, the exact far-apart value `distEdge = 2k+3` (strictness slack exactly `1` at every scale), and the centerpiece — **the tree-ball truth boundary bracketed at every scale**: the even boundary class computed raw from `cycle_levE_eq` exactly where `cycle_levClass_eq`'s strict hypothesis refuses (the adjacent antipodal pair, card `2` — correcting the cycle-family delivery record's boundary note, which had described it as a singleton), `IsTreeBall` **holding** at radius `n/2` one past the shelf theorem's certified radius (the strictness is proof-forced, not truth-forced), failing one past the truth with the empty class, and the odd-boundary singleton (`n = 2j+1`, the two offset branches merge, card `1` — the genuine strictness bite); plus the asymptotic corollary's witness pinned explicitly at symbolic `ε` (`k = ⌈1/ε⌉` through `alonBoppana_cycle_laplacian`). Radar QA axis re-scored **4.0 → 4.5** per the "named absent category gains its first occupant" protocol; the randomized half of the named gap recorded as still absent. Six technique findings recorded in the proposal (headline: the `rcases` second-branch identifier trap — the shelf's `if_neg (by omega …)` idiom is load-bearing style, and "simplifying" it to `if_neg h0` is a silent wrong-branch bug). |
| The Self-Contained Empirical PageRank Capstone ([`selfcontained-empirical-pagerank.md`](selfcontained-empirical-pagerank.md)) | 2026-09-02 | **The standing handoff's first-named frontier — a load-bearing consumer of the now-unconditional stationary layer, delivered as the empirical PageRank program's target distribution *produced by the theorem itself*. Zero new axioms (count stays 4), pure hard crust: `#print axioms` via `wip/selfpr_axcheck.lean` on all 9 audited declarations (1 shelf + 4 QA + the 4 composed suppliers `exists_pageRankVec`/`existsUnique_pageRankVec`/`pageRankMixingTime_le_of_rate'`/`empiricalPageRank_uniform_tail_of_depth`) exactly `propext, Classical.choice, Quot.sound`.** The shelf theorem (`Derived/EmpiricalStationary.lean`'s `PageRankLimit` section): **`empiricalPageRank_tail_selfcontained_of_depth`** — `∃ π` (strictly positive, mass one, stationary at the Google walk, supplied by the re-proved `exists_pageRankVec`) such that past the single `(α, ε)`-computable display threshold `⌈log(2/ε)/log(1/α)⌉` (the uniform α-ceiling's display form at `ε/2`), `n` i.i.d. simulated random-surfer trajectories estimate `π i` to `ε` at `2 exp(−nε²/2)` **for every start simultaneously**. The previously-conditional composition made honest: the sibling capstones take `π` as caller hypotheses precisely because their `∃!` supplier was PF-conditional until the 2026-09-02 Cesàro re-proof — now the whole loop is hard crust and the agent that can only simulate the walk needs to know nothing about `π` in advance (no caller-supplied stationarity, no caller-supplied threshold), closing the directed family's asymmetry against the plain and lazy families whose targets are definitional via `stationaryVec`. QA +4 (3734 → 3738, `EmpiricalStationary_QA.lean`'s `PageRankSelfContained` section): **the identification pin** (any vector carrying the theorem's three certificate clauses equals the hand-verified `u2`, through the proved `∃!`'s uniqueness clause — the composition's falsification surface, load-bearing on the re-proved stationary layer's exact statement), the display-threshold pin (`⌈log 8/log 2⌉ = 3` at `ε = 1/4`), the display-slack witness (the exact uniform object `2 < 3` = display threshold — the start-free packaging's price made visible), and the fully-self-contained instance (the theorem instantiated at the fixture, the produced vector identified as `u2`, the guarantee discharged at `t₀ = 3` for every start, bound `2 exp(−1/32)`). Four technique findings recorded in the proposal (headline: the `1/(ε/2)` vs `2/ε` display-spelling join is a `field_simp` identity rewritten *into the goal*, keeping the composed ceiling's statement untouched for `exact`; and supplier-produced opaque vectors must stay out of the sampling-space arguments or the `rw`-identification pattern dies). Full verification ladder per the delivery record; scoreboard and map stamps at 3738/4/0. |
| The Cesàro Stationary-Existence Engine — discharging `perron_frobenius`'s entire theorem-consumer surface ([`cesaro-stationary-existence.md`](cesaro-stationary-existence.md)) | 2026-09-02 | **`perron_frobenius`'s entire non-QA consumer surface re-proved at unchanged statements without the axiom — the stationary-distribution layer (`IrreducibleStationary`'s engine + four theorems, `PageRank`'s three, every `DirectedMixing`/empirical instantiation producing `π` through them) is hard crust, and the axiom drops to zero non-QA consumers while staying admitted at its own full statement (rootMultiplicity + complex-domination untouched; the deprecation decision flagged as the operator's in `index/load_bearing_axioms.md`). Zero new axioms (count stays 4), pure hard crust: `#print axioms` via `wip/cesaro_axcheck.lean` on all 37 audited declarations (10 engine + 5 stationary + 3 PageRank + 3 directed-mixing + 16 QA) exactly `propext, Classical.choice, Quot.sound`.** The route the admission's own "Replacement path" note had left unpriced, all elementary: **power positivity from strong connectivity** (`exists_pow_pos_of_isIrreducible` — the `ReflTransGen` induction along the arcs), **existence by Krylov–Bogoliubov Cesàro averaging** (`exists_cluster_stationary_of_orbit`, the finite-form cluster lemma — Tychonoff + `IsCompact.tendsto_subseq` in the product topology, the telescoping action-defect `t⁻¹•(g₀ − g_t)` vanishing entrywise — and `exists_nonneg_stationary_of_row_stochastic`, the orbit-of-`1` engine with *no* irreducibility: existence holds on reducible chains too, exactly where uniqueness and positivity provably fail), **strict positivity** (a zero coordinate forces every mass coordinate to zero through power positivity — contradiction), and **min-ratio uniqueness-up-to-scale** (the difference at the minimizing ratio is nonneg stationary with a zero entry, hence zero). QA +22 (3712 → 3734, `IrreducibleStationary_QA.lean`'s Section E): the orbit pins `g₁ = (2, 1/2, 1/2)`, `g₂ = 1` with the **oscillation fence** beside them (the engine's input never converges — only its averages do), **the one-period Cesàro mean exactly stationary** with value pin `μ₁ = 3•π₃` (the averaging route's whole point on the periodic fixture), power positivity at the pinned return arc `(P²) 1 1 = 1/2`, **the engine's output identified** against the file's hand value, **the min-ratio scalar pinned exactly** (`c = 1/4`), and **reducible-input existence instantiated**. Seven technique findings recorded (headline: `rw` silently fails on HO-shaped lemmas — `Matrix.mul_apply`, `Finset.sum_apply`, `Pi.sub_apply` — that `simp only` matches freely). Full ladder: build ✔ + completeness 133/133 fresh, `lint_axioms` 4 unchanged, all nine checks clean, scoreboard 3734/4/0, map-freshness exit 0. |
| The Directed Uniform (Worst-Start) `t_mix` Object ([`directed-uniform-mixing-time.md`](directed-uniform-mixing-time.md)) | 2026-09-02 | **The `t_mix` object family's last missing member, with the consumer gate discharged the same two-halves way the undirected uniform object's was (the uniform object's consumer is the submultiplicativity/ε-escalation class — worst-start statements unstatable per-start — and the escalation's consumer plus the named capstone is the worst-start sampling guarantee: one start-independent threshold certifies `n` simulated random-surfer trajectories for *every* start). Zero new axioms (count stays 4), pure hard crust: `#print axioms` via `wip/unifdtv_axcheck.lean` on all 56 audited declarations (25 shelf + 31 QA) exactly `propext, Classical.choice, Quot.sound`.** The genuinely new reusable piece: **the matrix-level Dobrushin coefficient** in `Mixing.lean` — `tvDobrushinCoeff Q := max_{x,y} TV(Q_x, Q_y)` with the sharp contraction `TV(μ ᵥ* Q, ν ᵥ* Q) ≤ TV(μ,ν)·δ(Q)` at equal masses only (sign statistic → row-statistic pairing → the promoted pairing core `abs_sum_mul_le_of_pairwise`, moved public from Oversmoothing-private per that module's own promotion note) and generic power submultiplicativity at row stochasticity — the mechanism the undirected uniform delivery proved bespoke at the walk matrix, now consumable by every chain on the shelf. The Google layer (`DirectedMixing.lean`): LPW's `d(t)`/`d̄(t)` at the Google law, the law evolution, the engine join, the Google contraction instance, `d`-submultiplicativity, `d̄ ≤ d` **by the contraction at `(δ_x, π)` plus the simplex diameter** (no mixture identity, no TV convexity, no reversibility — hypothesis-lighter than the undirected route), discrete TV monotonicity, the mixed form, the escalation engine, and **the uniform object `pageRankMixingTime`** with its full package (attainment `_spec`, the witness-load-bearing per-start domination, the sup interchange, **the refined α-ceiling `t_mix(ε) ≤ ⌈log(d̄(0)/ε)/log(1/α)⌉`** and its display form, the well-posedness supplier, LPW's ε-escalation corollaries); the named consumer `empiricalPageRank_uniform_tail_of_depth` composes the object's `_spec` + the supplier + the delivered per-start capstone. QA +29 (3683 → 3712, `DirectedMixing_QA.lean`'s Section H + the capstone instance): **the exact closed form `d(t) = (1/2)^t` with submultiplicativity attained with equality at every time**, **the Dobrushin contraction attained exactly** at the basis pair (both sides `1/2`, the engine's sharp constant load-bearing on the directed axis), `d̄ = d/2` pinned with the domination instance beside it, the mixed form attained at every time, **`t_mix^unif(1/8) = 2` pinned in both directions** (both starts independently), **the refined α-ceiling attained exactly** with the display form's slack witnessed (`2 < 3`), **the escalation corollary attained exactly** (`t_mix(1/32) = 4 = (1+1)·2` at the fixture's own pinned certificate constants), and the `ε = 0` junk corner fenced. Seven technique findings recorded (headline: `rw` cannot act inside an un-beta-reduced `sup'` goal — open with `show`; higher-order unification against a bare vector fails with only a misleading "failed to synthesize Nonempty V"). Full ladder: build ✔ + completeness 133/133, `lint_axioms` (4, unchanged), refutation-independence (9-tag), reachability (63 modules), citations, links, backlog freshness, scoreboard **3712/4/0**, map-freshness exit 0. The gated remainder narrows to the *sharp* `|λ₂| = α` layer alone. |
| The Directed (PageRank) `t_mix` Object and Its Empirical Sampling Capstone ([`directed-mixing-time-object.md`](directed-mixing-time-object.md)) | 2026-09-02 | **The standing handoff's named frontier — the `t_mix` object family's missing directed sibling delivered with its consumer gate discharged by naming the empirical PageRank capstone (the repo's idiom). Two of the three consumer-gated directed-rate items landed in one package (the ⌈log⌉-threshold depth form and the object). Zero new axioms (count stays 4), pure hard crust: `#print axioms` via `wip/dtaxcheck.lean` on all 34 audited declarations (15 shelf + 19 QA) exactly `propext, Classical.choice, Quot.sound`.** The shelf: `Mixing.lean`'s **equal-mass entrywise TV extraction** `\|μ i − ν i\| ≤ TV(μ, ν)` (zero-mass triangle route, constant sharp — the directed bias term's engine since the directed program bounds TV, not χ²); `DirectedMixing.lean`'s directed-mixing-time section — the per-start Google-walk law `pageRankDistribution` (probability-certified through the power plumbing), **the depth form `pageRank_tvDistance_le_of_depth`**, and **the object `pageRankMixingTimeFrom`** with its full package (`_spec` well-ordered attainment, `_le_of_rate` the α-ceiling `t_mix(ε) ≤ ⌈log(TV(δ_x,π)/ε)/log(1/α)⌉`, `_anti`); `EmpiricalStationary.lean`'s capstone — **`empiricalPageRank_stationary_tail_of_depth`**: past the object's own threshold at `ε/2`, `n` simulated random-surfer trajectories estimate `π i` to `ε` at `2 exp(−nε²/2)` — on directed input the entire symmetric mixing toolkit is unavailable, the α-rate Doeblin certificate the only route; the object's `_spec` is load-bearing in the hypothesis. QA +15 (3668 → 3683): **the object `t_mix(1/8) = 2` pinned in both directions** on the periodic 2-cycle's Google matrix, **the α-ceiling attained exactly** (`⌈log 4/log 2⌉ = 2` = the object), **the depth form attained with equality at the threshold time**, the entrywise extraction attained (both sides `1/4`), the junk corner fenced at `ε = 0`, and the bias/capstone instances at `2 exp(−1/8)` / `2 exp(−1/32)` past the pinned threshold. Sharp `\|λ₂\| = α` layer stays gated exactly as before. |
| The Doeblin TV Contraction and the PageRank α^t Rate ([`doeblintv-tv-contraction-pagerank-rate.md`](doeblintv-tv-contraction-pagerank-rate.md)) | 2026-09-02 | **The standing handoff's first-named frontier delivered — the Doeblin engine's second load-bearing consumer plus the directed layer's recorded "no rate" scope gap discharged at the coarse TV level. Zero new axioms (count stays 4), pure hard crust: `#print axioms` via `wip/doeblintv_axcheck.lean` on all 38 audited declarations (7 shelf + 31 QA) exactly `propext, Classical.choice, Quot.sound`.** The shelf (`Mixing.lean`'s new Doeblin TV-contraction section, the mixing program's TV toolkit's first *directed-axis* member): mass preservation, **zero-sum interval pinning** (a zero-mass functional pairs with any function to at most half its entrywise range times the `ℓ¹` mass), row-action TV non-expansiveness, **the headline `tvDistance_vecMul_le_of_pos_entries`** — `TV(μ ᵥ* Q, ν ᵥ* Q) ≤ (1 − |V|δ)·TV(μ, ν)` at equal masses, hypothesis-minimal (sign-free on both vectors), proved through the dual/test-function pairing `z ⬝ᵥ s = w ⬝ᵥ (Q *ᵥ s)` (`Matrix.dotProduct_mulVec`) consuming the retirement's engine on a *different mathematical surface* (TV between laws, not coordinate convergence) — so a wrong engine constant now fails a second family of QA attainment pins; the iterated form; and the assembly `tvDistance_vecMul_pow_le_of_pos_power` (sign-free on `ν`, `π` through stationarity and mass alone). The named consumer (`DirectedMixing.lean`): **`pageRank_tvDistance_le`** — `TV(ν ᵥ* (G^t), π) ≤ α^t·TV(ν, π)`, the field-standard PageRank power-method rate, the teleportation floor `(1−α)·|V|⁻¹` read as a Doeblin floor of coefficient exactly `α` — the sharp `|λ₂|` layer stays gated exactly as before. QA +31 (3637 → 3668, `DirectedMixing_QA.lean`'s Section F): the contraction attained exactly at the `Qd` basis pair, the iterated bound attained at every time through the mode closed form, the floor-free refutation fence on the permutation `P₂` (exactly `hle` isolated), and **the Google `α^t` rate attained exactly at every time on the periodic 2-cycle — the periodic chain that provably never mixes (`P2_no_limit`) becomes exactly-`α`-convergent after teleportation**, with the plain walk's never-decay `TV ≡ 1/2` pin beside it. |
| The Retirement of `primitive_power_tendsto` — the Doeblin/Dobrushin Contraction Route ([`retire-primitive-power-convergence.md`](retire-primitive-power-convergence.md)) | 2026-09-02 | **One of the five admitted axioms proved at its unchanged statement and retired (count 5 → 4, `lint_axioms` now reading four: `perron_frobenius` + the matrix trio) — the admission's own "Replacement path" note had left the local route unpriced, and this run priced one: the elementary Doeblin/Dobrushin entrywise-range contraction, no Perron–Frobenius machinery anywhere. Pure hard crust: `#print axioms` via `wip/primpow_axcheck.lean` on all 41 audited declarations (17 engine + the retired theorem + 7 formerly-conditional consumers + 16 QA) exactly `propext, Classical.choice, Quot.sound` — the entire directed mixing layer (the PageRank power iteration, both generic corollaries, the three Google-matrix forms) now has zero admitted-axiom contact.** The shelf (`PrimitiveConvergence.lean`'s new `Doeblin` section): `entrySup`/`entryInf`/`entryRange` with the interval interface, stochastic non-expansiveness, **the contraction `entryRange_mulVec_le_of_pos_entries`** (`range (Q *ᵥ y) ≤ (1 - |V|δ)·range y` for row-stochastic `Q` with entries `≥ δ` — the row-split `δ` + remainder-of-mass `1-|V|δ` route; public and reusable, the same Dobrushin mechanism as the undirected uniform-`t_mix` submultiplicativity family), the power plumbing, and the block iteration `entryRange_pow_mul_le` (`range ≤ ρ^(t/m)·range`); then the ε–N assembly through the invariant π-pairing and convex-combination interval pinning. QA +19 (3618 → 3637, `DirectedMixing_QA.lean`'s Section E) on the strictly positive fixture `Qd` (min entry `1/4`, coefficient `1/2`): **the contraction attained exactly** (`1 = (1−2·(1/4))·2` — load-bearing on the exact constant), the iterated contraction attained at every time via the exact closed form `Qd^t *ᵥ yd = (1/2)^t • yd`, the theorem instance joined to the raw closed form at the zero limit, and the **wrong-δ refutation fence** (pretend `δ = 1/2` claims zero range after one step — refuted at pinned values, the entries-bound hypothesis exactly what fails). Bookkeeping: the retired axiom's `-- @refutes` tag removed (9 tags remain), the `lint_axioms.py` allowlist entry removed, docstrings de-staled across `DirectedMixing` and its QA. Honest scope: no rate clause still; the byproduct `ρ^(t/m)` is the Doeblin bound — explicit, typically loose; sharp `|λ₂|`-type rates stay gated as before. Full ladder: build ✔ + completeness 133/133 fresh, `lint_axioms` (4), `check_refutation_independence` (9 tags), `check_public_reachability` (63), `check_citations`, `check_markdown_links`, `check_backlog_freshness`, scoreboard 3637/4/0, map-freshness after the stats sync. Ten technique findings recorded (headline: `Finset.univ.eq_empty_or_nonempty` dot notation leaves a stuck postponed `Fintype ?m` instance that surfaces as an unrelated error declarations later — ascribe the argument). |
| The Cycle Family and the Asymptotic Alon–Boppana Corollary ([`cycle-family-alon-boppana-asymptotic.md`](cycle-family-alon-boppana-asymptotic.md)) | 2026-09-02 | **Backlog item 3's own named follow-on ("needs a named d-regular family with `diam → ∞`") delivered by naming the canonical family — the cycles `C_n`, carried by Mathlib's own `SimpleGraph.cycleGraph` through the delivered `toWAdj` adapter. Zero new axioms (count stays 5), pure hard crust: `#print axioms` via `wip/cyclefam_axcheck.lean` on all 23 nameable new declarations (17 shelf + 6 QA) exactly `propext, Classical.choice, Quot.sound`.** The new content: the **exact cycle distance formula** `dist a b = min ((b−a).val) (n − (b−a).val)` (walk route up + ℤ-potential route down: every walk realizes a residue representative of `b − start` of absolute value ≤ its length, in `Int.ModEq` form), from which the tree-ball level classes (`levClass a (a+1) j = {a+(j+1), a+(n−j)}` — with the honest *strict* threshold `2(k+1) < n` the elaborator forced: at the boundary `n = 2(k+1)` the level-`k` class is the single antipodal vertex and the ball is not a tree) and the far-apart condition (`2(k+1) < distEdge`, the `4k+8` sizing buying exactly slack `1`) discharge at arbitrary scale; then the headlines — **`alonBoppana_cycle`** (`secondEval (2•1 − C_{4k+8}) ≤ 1/(k+1)`: the Alon–Boppana error term `d − 2√(d−1) = 0` at `d = 2`, every Nilli hypothesis discharged at every scale `k`), the Laplacian spelling via the shelf's `secondEval_congr` bridge, and **`alonBoppana_cycle_asymptotic`** (`∀ ε > 0, ∃ k, λ₂(L(C_{4k+8})) ≤ ε`). The program's first *parametric* (arbitrary-`n`) instantiation — until now `IsTreeBall`/`distEdge` had only fixed literal fixtures. QA +6 (3612 → 3618, `AlonBoppana_QA.lean`'s Step 7) pinned at `C₁₂` (beyond every prior literal-matrix fixture, largest before was `C₈`): the distance formula in four shapes (short/wraparound/antipodal-tie/self), the level class `{4, 9}`, the theorem instance at `k = 1` (`≤ 1/2`), the Laplacian spelling, the asymptotic instance at `ε = 1/2`, and **the far-apart fence** — on `C₁₂` at `k = 2` both radius-3 tree balls are genuine yet the antipodal edges are only `5` apart, `hfar` failing by exactly the family-sizing slack. Full ladder: build ✔ + completeness 133/133 fresh, `lint_axioms` (5), `check_refutation_independence` (10 tags), `check_public_reachability` (63), `check_citations`, `check_markdown_links`, `check_backlog_freshness`, scoreboard 3618/5/0, map-freshness ✔ after stats sync. Nine technique findings recorded in the proposal (headline: `omega` treats ℤ-`|·|` as an opaque atom and does not relate `q*n` with `(q+1)*n` — pre-build `ring` product identities; walk induction with an outer walk in context produces a broken IH — extract to a standalone theorem; `le_csInf` here takes `Nonempty`, and `hconn.preconnected a b` *is* the walk-nonemptiness supplier). |
| The Empirical Lazy Sampling Guarantee ([`empirical-lazy-stationary-sampling.md`](empirical-lazy-stationary-sampling.md)) | 2026-09-02 | **The lazy program's named follow-on delivered — the plain empirical-stationary capstone's three theorems cloned at the lazy law with the bias computed at the intrinsic rate, on the fixture class (paths/trees/grids) where the plain `r < 1` certificate is provably unsatisfiable and the plain program certifies nothing. Zero new axioms (count stays 5), pure hard crust: `#print axioms` via `wip/lazystat_axcheck.lean` on all 15 audited declarations (3 shelf + 12 QA) exactly `propext, Classical.choice, Quot.sound`.** The shelf (`Derived/EmpiricalStationary.lean`'s new `LazyStationaryLimit` section): `empiricalLazyWalkDistribution_tail` (the fixed-time form at the lazy law, its probability-vector certification proved); **`empiricalLazyWalkDistribution_stationary_tail`** (the bias-term form at the **computed** bias `(1 − λ₂/2)^t₀ √(π i ((π x)⁻¹ − 1))` — connectivity the only graph hypothesis, *no caller-supplied rate certificate anywhere*, the plain twin's certificate hypothesis replaced by the computed rate: the lazy program's whole point, delivered at the sampling level); and **`empiricalLazyWalkDistribution_stationary_tail_of_depth`** (the capstone: past the lazy ceiling's own threshold at `ε/2`, `n` simulated lazy trajectories estimate `π i` to `ε` at `2 exp(−n ε²/2)`; the `λ₂ < 2` strictness honest and visible, mirroring the depth-form lazy TV ceiling — K₂'s rate-0 corner excluded, the bias-term form still instantiating there). QA +12 (3600 → 3612, `EmpiricalStationary_QA.lean`'s lazy section) on the bipartite path P₃ at the pinned gap λ₂ = 1: **the plain-certificate unsatisfiability fence** (no `r ∈ (0,1)` exists for the plain walk — `chiSquareDistance_le_of_connected` at a would-be certificate against the pinned `χ²_plain(center) ≡ 1` forces `r² ≥ 1` — the leverage case made negative), the bias-term instance at `(1/2)²·√3/2 = √3/8` with the raw true deviation `1/8` dominated with honest slack beside it, **the capstone instance closing at `2 exp(−1/16)`** past the same `log(4√3)/log 2 ≤ 3` threshold the delivered lazy-ceiling QA pinned, the event-mass non-vacuity witness `25/256`, and **the exact-stationary contrast** (true deviation `0` at every t₀ ≥ 1 from the center start, the quoted bias honestly positive). The elaborator caught one real arithmetic error in the spike (a `−1/8` that should be `−1/16` — the capstone exponent `−n·ε²/2` normalization) before any shelf edit; eight technique findings recorded in the proposal. Full ladder: build ✔ + completeness 133/133 fresh exit 0, `lint_axioms` (5), `check_refutation_independence` (10-tag clean), `check_public_reachability` (63 modules), `check_citations`, `check_markdown_links`, `check_backlog_freshness` pass; scoreboard regenerated (3612/5/0); map-freshness exit 0 after the stats sync + SVG regen (no station — no tier change). |
| The Lazy `t_mix` Object and the Depth-Form Lazy Ceiling ([`lazy-mixing-time-objects.md`](lazy-mixing-time-objects.md)) | 2026-09-01 | **The lazy-walk delivery's two recorded follow-on compositions — the periodicity fix completed at the object level — with the consumer gate discharged by naming the bipartite-input instance (the empirical-stationary capstone's own "agent that can only simulate the walk" setting on paths/trees/grids, where the plain `r < 1` certificate is provably unsatisfiable; the delivered entrywise lazy ceiling is that extension's hypothesis supplier, recorded as the named follow-on). Zero new axioms (count stays 5), pure hard crust: `#print axioms` via `wip/lazymix_axcheck.lean` on all 20 audited declarations (9 shelf + 11 QA) exactly `propext, Classical.choice, Quot.sound`.** The shelf: `Mixing.lean`'s **spectral-gap cap** `secondEval_normalizedLaplacian_le_two` (`λ₂(L_sym) ≤ 2` — sortedness + membership + the pointwise signless bound; the whole normalized spectrum now capped in `[0,2]`, and the lazy rate nonnegative by theorem); `Oversmoothing.lean`'s `LazyMixingTime` section — **the entrywise lazy ceiling at the intrinsic rate** (`|ν_lazy − π y| ≤ (1−λ₂/2)^t·√(π y ((π x)⁻¹−1))`, connectivity the only graph hypothesis, the plain twin's certificate hypothesis *replaced by the computed rate* — the lazy program's whole point, on the class where the plain twin is provably unusable), **the depth-form TV lazy ceiling** (honest visible `λ₂ < 2` — K₂'s rate-0 corner excluded and documented), **`lazyWalkMixingTimeFrom`** with the full package (certificate interface, `csInf_mem` attainment, ε-antitonicity, the intrinsic-rate spectral ceiling in ⌈log⌉ form). QA +11 (3589 → 3600): the exact corner-start TV closed form `TV_lazy(1+t) = (1/2)^{t+2}` at every time (the μ = 2 mode dead after one lazy step, the μ = 1 mode halving), **`t_mix_lazy(corner, 1/8) = 2` pinned in both directions** on the fixture where the plain walk provably never mixes, `t_mix_lazy(center, 1/4) = 1` with **the ceiling attained exactly**, the corner ceiling with honest slack (`3` vs `2`), **the object-level periodicity contrast** (plain `t_mix(K₂, 1/8) = 0` junk — empty witness set — against lazy `= 1` genuine), and the entrywise bound attained exactly on `K₂` (both sides zero at rate `0`). Full ladder clean (build ✔ + completeness 133/133 fresh, `lint_axioms`, 10-tag refutation independence, 63-module reachability, citations, links, backlog freshness, map-freshness after the 3589 → 3600 sync). |
| The Lazy Random Walk — the Discrete Mixing Program's Periodicity Fix ([`lazy-walk-mixing.md`](lazy-walk-mixing.md)) | 2026-09-01 | **LPW ch. 5's lazy chain `P_L = (P + I)/2` with the intrinsic-rate mixing family — the discrete program's periodicity fix, making its certificate families satisfiable on every bipartite graph (paths, trees, even cycles) where they are provably unsatisfiable today (`λ_max = 2` mode, factor `|1 − 2| = 1` — fenced never-decay pins in χ²/TV/entropy). Zero new axioms (count stays 5), pure hard crust: `#print axioms` via `wip/lazywalk_spike.lean` on all 62 new declarations (38 shelf + 24 QA) exactly `propext, Classical.choice, Quot.sound`.** The shelf (`Mixing.lean`'s new `LazyWalk` section): the operator and its law/density/χ² objects (detailed balance and mass conservation cloned at the lazy operator; attainment persists); **the signless-Laplacian SOS engine** — `uᵀ(D+A)u = (1/2)∑ A i j (u i + u j)²`, giving `eigvalOf_normalizedLaplacian_le_two` (`μ ≤ 2`, the bipartite top mode exactly the boundary; `hnn` load-bearing, fenced at a negative-diagonal fixture); the lazy decay engine (commutation `√D P_L = (1 − (1/2)L_sym)√D`, conjugated-power transfer, eigenaction at mode factor `1 − μ/2`, Parseval-exact identity, both contractions); the intrinsic rate `|1 − μ/2| ≤ 1 − λ₂/2` (PSD + signless + the delivered below-gap plumbing, no sign hypothesis anywhere); and **the headline `lazyChiSquareDistance_le_of_connected`** — `χ²_lazy ≤ (1 − λ₂/2)^{2t}·((πx)⁻¹−1)` with connectivity the only graph hypothesis (the continuous family's recorded intrinsic-rate advantage, delivered on the discrete side) — plus TV and entropy corollaries. QA +23 (3566 → 3589): **on `K₂` the bound attained exactly at every time** (`χ²_lazy ≡ 0` at the pinned rate `1 − 2/2 = 0`, `k2_lazy_bound_attained_QA` pinning both sides zero) and the TV contrast pair (plain `1/2` vs lazy `0`); on `P₃` the center start — the pure periodic `λ = 2` mode — **exactly stationary after one lazy step** against the proved plain never-decay pin `χ²_plain(t, center) = 1` at every time (the law's period-two oscillation by induction); the corner values `1/2`/`1/8` with the bound instance evaluated at the pinned gap `λ₂ = 1`; the signless tightness pin at the `K₂` top mode (`0` exactly); and the `hnn` fence (`−4 < 0`, every other hypothesis holding). Full ladder: build ✔ + completeness 133/133 fresh, `lint_axioms` (5), refutation-independence (10 tags), reachability (63), citations, links, scoreboard 3589/5/0, map-freshness exit 0 after the stats sync + SVG regen. |
| The Entropy Leg of the Mixing Program — Pinsker, the D ≤ χ² Bridge, and Entropy Decay ([`entropy-mixing-pinsker.md`](entropy-mixing-pinsker.md)) | 2026-09-01 | **`InformationTheory.Entropy`'s first graph-level consumers and the mixing program's third distance (TV and χ² already have decay + floor families; entropy now has both) — zero new axioms (count stays 5), pure hard crust: `#print axioms` via `wip/entropymix_axcheck.lean` on all 34 nameable new declarations (12 shelf + 22 QA) exactly `propext, Classical.choice, Quot.sound`.** The generic layer (`Entropy.lean`): the termwise `klTerm_le_sub_one_mul` (the `log u ≤ u − 1` route — the elaborator-refuted first draft's termwise `≤ (a−b)²/b` shape was false at `a > b`, the bridge is genuinely sum-level with both mass hypotheses load-bearing), **the entropy–χ² bridge** `klDiv_le_sum_sq_div` in exactly `chiSquareDistance`'s sum shape, **the two-block log-sum** `sum_klTerm_ge_klTerm` (rescaling to block-conditional vectors + Gibbs on the block — the entropy module's first internal Gibbs consumption), and **the binary two-point Pinsker bound** `2(a−b)² ≤ d(a‖b)` by an explicit FTC identity (`d = ∫_b^a (a−t)/(t(1−t)) dt ≥ ∫_b^a 4(a−t) dt` through AM-GM — a genuinely second-order fact the first-order log bounds cannot close, worked through and recorded). `Mixing.lean`: **Pinsker's inequality** `tvDistance_le_sqrt_half_klDiv` (TV-as-positive-part + two-block decomposition + the binary bound; the strict q-positivity load-bearing and refuted in QA at the q-zero junk corner) and **entropy decay** — `klDiv_walkDistribution_le` at the χ² theorem's exact hypothesis set, the continuous twin at `contChiSquareDistance_le`'s — plus the nonnegativity plumbing (incl. `contWalkDensity_nonneg` by Poissonization + `tsum_nonneg`, the identity's first nonnegativity consumer). `Oversmoothing.lean`: **the entropy floor** `klDiv_walkDistribution_ge_of_eigenpair` (Pinsker × the delivered spectral TV floor — the floor family's first non-TV member). QA +22 (3544 → 3566): the triangle's exact entropies `log(3/2)` / `(1/2)log(9/8)` from raw laws, the bridge and decay instances with honest slack, the numeric Pinsker pin, the entropy-floor instance, **the `K₂` never-decay exact pin `D = log 2` at every time**, the one-way `¬(χ² ≤ D)` fence, the continuous `t = 0` join `log 3`, and the q-zero refutation. **Discharges log-Sobolev's recorded consumer gate** (the entropy-decay family is the named consumer an LSI would improve; adoption stays an operator decision). Full ladder: build ✔ + completeness 133/133 fresh, `lint_axioms`, `check_refutation_independence` (10-tag), `check_public_reachability` (63), `check_citations`, `check_markdown_links`, scoreboard 3566/5/0. |
| LPW's Uniform `t_mix` and the Submultiplicativity Class ([`total-variation-mixing-conversion.md`](total-variation-mixing-conversion.md), second follow-on delivery record) | 2026-09-01 | **The deferral's own named remainder — the sup-over-starts uniform `t_mix` object and the submultiplicativity-class statements — delivered as one package whose two halves discharge *each other's* gates** (the uniform object's consumer is the class — `d(s+t) ≤ d(s)d(t)` and the ε-escalation corollary are unstatable per-start; the escalation's consumer is LPW's canonical `t_mix := t_mix(1/4)` bridge, one certified evaluation time yielding every ε-level mixing time). Zero new axioms (count stays 5), pure hard crust: `#print axioms` via `wip/uniformmix_axcheck.lean` on all 46 nameable declarations (19 public shelf + 27 QA, the 3 file-private cores audited transitively) exactly `propext, Classical.choice, Quot.sound`. The shelf (`Oversmoothing.lean`'s new `UniformMixing` section): LPW's two distances `walkTVPair`/`walkTVUniform` (`d`/`d̄` as `Finset.sup'` maxima), **the sharp Dobrushin contraction** `TV(μ(Pᵀ)ᵗ, ν(Pᵀ)ᵗ) ≤ TV(μ,ν)·d(t)` at equal masses (hypothesis-minimal — the recentering-pairing private core against the delivered distinguishing-function bound, avoiding the factor-`2`-losing triangle route), the submultiplicativity class (`d(s+t) ≤ d(s)d(t)` pure Markovity, `d̄ ≤ d`, `d̄(s+t) ≤ d̄(s)d(t)`, the escalation engine), **the uniform object** `walkMixingTime` with certificate/attainment interface, the witness-hypothesized per-start domination (load-bearing, fenced at the junk corner), the finite-sup interchange, **the ε-escalation corollary** in `k`- and ⌈log⌉-forms, and the uniform spectral ceiling. QA +27 (3517 → 3544): the distance pins `d(1) = 1/2`, `d(2) = 1/4`, `d̄(1) = 1/3`, `d̄(2) = 1/6` from raw law literals, **submultiplicativity attained with equality** in all three forms (the sharp constant load-bearing), `t_mix(1/3) = 1`, `t_mix(1/6) = 2` pinned in both directions, the sup-interchange and ceiling instances, and the `K₂` periodicity corner (`d(1) = 1` maximal, no `ρ < 1` certificate, the empty-set infimum pinned and fenced). Full ladder: build ✔ + completeness 133/133 fresh, `lint_axioms`, `check_refutation_independence` (10 tags), `check_public_reachability` (63 modules), `check_citations`, `check_markdown_links`, scoreboard **3544/5/0**, map-freshness exit 0. Spike by run `20260901T124410Z-run-1`; landing and records by `20260901T163200Z-run-1`. |
| The Spectral Mixing Floor — the Program's First Lower-Bound Family ([`message-passing-depth-mixing-bound.md`](message-passing-depth-mixing-bound.md), follow-on delivery record) | 2026-09-01 | **The proposal's deferred over-squashing item delivered on its own named route (the exact eigen-component equality) — the mixing program's first *lower-bound* family and the `t_mix` objects' missing lower half. Consumer gate discharged by naming two: the ceiling's depth certificate completed into a two-sided bracket (enough-layers vs provably-insufficient), and the classical eigenvalue lower bound on mixing time (LPW ch. 12's distinguishing-statistic technique) as the delivered ceiling's textbook companion. Zero new axioms (count stays 5), pure hard crust: `#print axioms` via `wip/spectralfloor_axcheck.lean` on all 39 audited declarations (9 shelf + 30 QA) exactly `propext, Classical.choice, Quot.sound`.** The shelf: the distinguishing-function bound `tvDistance_ge_half_abs_sum` (hypothesis-minimal, `Mixing.lean`'s TV toolkit); `Oversmoothing.lean`'s `SpectralFloor` section — kernel orthogonality of nonzero modes *derived* from `μ ≠ 0` (no connectivity), **the exact law-level test-function evolution** at arbitrary `L_sym` eigenpairs (the deferred item's engine — an equality, not a bound), **the TV floor** `(1/2)|1−μ|^t·|v x|/(√D x·c) ≤ TV` (periodic modes never decay — exactly the chains no `r < 1` ceiling reaches), **the χ² floor** `(1−μ)^{2t}(v x)²/(π x·‖v‖²) ≤ χ²` (Cauchy–Schwarz's slice of the Parseval identity), the **`t_mix` floor gate** (witness-existence load-bearing, fenced at the junk corner), the strict-direction calculus twin `pow_lt_of_lt_log_div`, and **the log-form spectral floor** `⌈log(|v x|/(√D x·2εc))/log(1/|1−μ|)⌉ ≤ t_mix` (with `hv0` honestly dropped as unneeded). QA +30 (3487 → 3517): **both floors attained exactly at every time on `K₂`** (TV `1/2`, χ² `1` — certified non-mixing on the periodic chain, tight forever), **the χ² floor attained exactly at every time on the triangle** at the `triG` eigenpair (`2·(1/4)^m` — load-bearing on the eigenpair constant), **the log-form floor hitting all three pinned `t_mix` closed forms exactly** (`1`, `2`, `3` — floor `2` = truth `2` ≤ ceiling `3` at `ε = 1/6`: the two-sided depth bracket on one fixture), the kernel-mode refutation fences in both metrics (the genuine eigenpair `(0, ![1,1,1])` reads `1 ≤ 1/8` and `1/2 ≤ 1/6` against the pins — `μ ≠ 0` load-bearing), and the `K₂` witness-existence fence (`¬∃T, ∀s ≥ T, TV ≤ 1/4`). Eight technique findings recorded in the proposal (headline: `Pi.single x (1 : ℝ)` under an untyped context leaves the dependent codomain as a metavariable and the smul TC search times out — ascribe the full application; plus `rw`-no-beta under applications, `field_simp`-then-`ring` as a no-goals liability, `rw`'s trailing `rfl` closing mid-chain, `omega`'s inability to abstract `Nat.ceil (r : ℝ)` atoms, `pow_succ`'s right-factor order in this pin, `mul_div_mul_left`'s implicit `c`, and `subst`'s elimination direction). |
| The Discrete `t_mix` Object — `walkMixingTimeFrom` and Its Package ([`total-variation-mixing-conversion.md`](total-variation-mixing-conversion.md), follow-on delivery record) | 2026-09-01 | **The TV-conversion proposal's own deferred `t_mix` item delivered as its follow-on, the standing handoff's first-named plain choice — its consumer gate was discharged by the Poisson-bridge delivery (the transfer corollary's `hmix` clause is exactly the object's witness condition), and this delivery closes the loop in the other direction: the object's attainment specification now *discharges* that `hmix` clause. Zero new axioms (count stays 5), pure hard crust: `#print axioms` via `wip/dtmix_axcheck.lean` on all 16 audited declarations (7 shelf + 9 QA) exactly `propext, Classical.choice, Quot.sound`.** The shelf (`Oversmoothing.lean`'s new discrete-mixing-time section — placed beside its certificate engine `walkDistribution_tvDistance_le_of_depth`, imports already pointing toward `Mixing.lean`'s transfer corollary, zero duplication/import changes): **the object** `walkMixingTimeFrom A x ε := sInf {t : ℕ | ∀ s ≥ t, TV_disc(s) ≤ ε}` (LPW ch. 20's per-start reading, the discrete twin of `contMixingTimeFrom`, junk corner documented), the certificate interface `_le_of_cert`, **the attainment specification `_spec`** — the discrete object's own advantage: `ℕ` is well-ordered so the witness infimum is a *member* (`csInf_mem`, no monotonicity needed) and membership is the uniform bound — the one-liner that composes with the transfer corollary, **the spectral ceiling** `t_mix(ε) ≤ ⌈log(√C/(2ε))/log(1/r)⌉` at the depth-form TV certificate's own `0 < r < 1` hypothesis set (the two-line `Nat.le_ceil` composition; the strictness honest and load-bearing — periodic chains admit no such certificate), ε-antitonicity, and **the bridge composition** `contWalkDistribution_tvDistance_le_of_walkMixingTime` (the named consumer: a Poisson lower-tail bound below `t_mix(ε₁)` gives `TV_cont(t) ≤ ε₁ + ε₂`, the discrete certificate supplied by the object). QA +9 (3478 → 3487, `Mixing_QA.lean`'s `DiscMixingTime` section): **the exact closed forms on the triangle pinned in both directions** (`t_mix(1/3) = 1`, `t_mix(1/6) = 2`, `t_mix(1/12) = 3` — the certificate interface above, the attainment specification below, both load-bearing), the antitone instance at exact values, **the ceiling attained exactly** at `ε = √2/4` (threshold ratio exactly the power the certificate names; object = ceiling's own right side = `1`), the ceiling computed with honest slack at `ε = 1/6` (`3` against the true `2`, via `4 < 3√2 ≤ 8`), **the `K₂` junk corner pinned and fenced** (`= 0` beside the no-certificate fence, the ceiling's undischargeable `r < 1` on `K₂` exactly why no theorem instantiates there), and the bridge instance re-deriving the hand-certified transfer bound `5/24` with no hand-supplied certificate. Eight technique findings recorded (headline: `csInf_mem` through `[WellFoundedLT ℕ]` needs no monotonicity argument because the "∀ s ≥ t" lives in the membership predicate; plus `Nat.sInf_empty`, `Nat.le_ceil`'s one-term real→natural threshold bridge, `pow_le_pow_right₀`'s un-normalized `2^1`, the `lt_div_iff₀`/`div_lt_iff₀` direction trap, the counterproductive `by norm_num` coercion bridge that normalizes goal denominators, the `field_simp` unconditional `ring_nf` note and its `div_eq_iff` avoidance, and `nlinarith`'s tactic-block-only syntax). Full ladder: build ✔ + completeness 133/133 fresh (after the documented single-module mtime remediation), `lint_axioms`, `check_refutation_independence` (10 tags), `check_public_reachability` (63 modules), `check_citations`, `check_markdown_links`, scoreboard 3487/5/0, map-freshness exit 0 after the stats sync. The submultiplicivity-class statements and the sup-over-starts uniform `t_mix` remain undelivered, consumer-gated. |
| The Poisson Bridge — Continuous↔Discrete Mixing-Time Comparability ([`continuous-time-chi-square-mixing.md`](continuous-time-chi-square-mixing.md), second follow-on delivery record) | 2026-09-01 | **The proposal's named follow-on ("the comparability theorem is the named follow-on that would justify the discrete `t_mix` object") delivered — zero new axioms (count stays 5), pure hard crust: `#print axioms` via `wip/poisson_axcheck.lean` (completed to full section coverage by the recording run) on all 42 audited declarations (23 shelf — `Heat.lean`'s two generic helpers `pow_smul_matrix`/`matrix_exp_smul_one` plus `Mixing.lean`'s 21-declaration Poisson-bridge section — and all 19 QA) exactly `propext, Classical.choice, Quot.sound`.** The shelf: **the Poissonization identity** `hasSum_poisson_walkDensity` (`e^{−tL_walk} *ᵥ h₀ = ∑'ₖ e^{−t}tᵏ/k! • h_k` — LPW ch. 20's `H_t` at the law level, the mixing layer's first tsum construction, proved through `Matrix.exp_add_of_commute` at `−t(I−P) = tP − tI` plus the scalar-matrix exp) with density/law forms; **the TV contraction toolkit** (the simplex diameter, the `Pᵀ` ℓ¹-contraction, iterated/stationary/evolution, **discrete TV monotonicity** `TV_{t+s} ≤ TV_t` — field-standard facts new to the shelf); the head–tail split and **TV convexity in countable mixtures** (honest summability throughout); the Poisson-averaged bound; **the comparability** `TV_cont(t) ≤ ∑_{k<m} e^{−t}tᵏ/k! + TV_disc(m)` (tail exact, no Chernoff rounding); and **the discrete-certificate transfer corollary** — `hmix` is exactly the still-deferred discrete `t_mix` object's witness condition, so that object's consumer now exists on the shelf. QA +19 (3459 → 3478): **the periodic-chain fence** on `K₂` (`TV_disc ≡ 1/2` at every time, no discrete certificate exists, and the **reverse comparability dead** — at `t = 2` the continuous TV is `e^{−4}/2 < 1/4` while the discrete is `1/2` at every time), the `t = 0` mixture corner collapsing to `δ_x`, and the triangle closed forms (**`TV_disc(m) = (2/3)·2^{−m}`** by the `triG` eigenroute, the comparability instance `TV_cont(8) ≤ 9e^{−8} + 1/6` with both sides exact and the domination honest, and the transfer instance `TV_cont(8) ≤ 5/24` from `9e^{−8} ≤ 1/24`). Records note: the Lean landed in run `20260901T024900Z-run-1`, which exited before its records; the commit steward independently re-verified before committing, and continuation run `20260901T054637Z-run-1` re-ran the full ladder itself before writing this record and the proposal's delivery record. Full ladder: build ✔ + completeness 133/133 fresh, `lint_axioms` (5), `check_refutation_independence` (10 tags), `check_public_reachability` (63 modules), `check_citations`, `check_markdown_links`, scoreboard (3478/5/0), map-freshness exit 0. |
| Continuous-Time Mixing Time — the `t_mix(ε)` Object and Its TV/Ceiling Package ([`continuous-time-chi-square-mixing.md`](continuous-time-chi-square-mixing.md), Deferred items 1+2) | 2026-08-31 | **The continuous-time χ² delivery's first two deferred items discharged together as one package, both consumer gates discharged by the repo's name-the-consumer idiom — zero new axioms (count stays 5), pure hard crust: `#print axioms` via `wip/ctmixtime_axcheck.lean` on all 37 audited declarations (10 shelf + 27 QA) exactly `propext, Classical.choice, Quot.sound`.** The shelf (`Mixing.lean`'s ContinuousTime extension): `contWalkDistribution` (the continuous walk *law* `π·h_t`) with its `t = 0` join to `walkDistribution` (the wrong-weight detector), the χ² sum-div bridge, **the continuous-time ℓ²→TV conversion** `contWalkDistribution_tvDistance_le` (unconditional — the delivered `tvDistance_le_half_sqrt` composed with the delivered `contChiSquareDistance`), **the decay form** (`TV ≤ (1/2)·e^{−t·λ₂(L_sym)}·√((πx)⁻¹−1)` at exactly `contChiSquareDistance_le`'s hypothesis set — no connectivity, no caller-certified rate), **the mixing-time object** `contMixingTimeFrom` (the per-start `sInf` over witness times at exactly LPW ch. 20's `∀ s ≥ t` reading, junk corner documented), the certificate interface `_le_of_cert`, **the spectral ceiling** `t_mix(ε) ≤ max 0 (ln(√((πx)⁻¹−1)/(2ε))/λ₂(L_sym))` (the honest two-case `max 0` shape, load-bearing on `secondEval_normalizedLaplacian_pos_of_connected`), and ε-antitonicity. QA +26 (3433 → 3459, `Mixing_QA.lean`'s `ContMixingTime` section): on `K₂` the trace-route gap pin `λ₂(L_sym) = 2`, **the exact TV value `e^{−2t}/2` at every time** (the ceiling attained at every time — Cauchy–Schwarz equality), **the exact mixing-time closed form `t_mix(ε) = ln(1/(2ε))/2`** (a `sInf` pinned in both directions: the closed form is a witness time with `TV` exactly `ε` there, and a lower bound of every witness), the ceiling **attained exactly** at `ε = e^{−2}/2` (`t_mix = 1 = bound`), the wrong-gap refutation (pretend gap `3` claims `1 ≤ 2/3`), the big-`ε` corner `t_mix(3/4) = 0`, the antitone instance with closed-form consistency; on the triangle **the exact TV values `(2/3)e^{−3t/2}`** with the Cauchy–Schwarz slack proved strict, and the `t = 0` law join. The *discrete* `t_mix` object stays deferred with its gate unchanged — the continuous↔discrete comparability theorem is the recorded follow-on that would justify it. Full ladder: build ✔ + completeness 133/133 fresh, `lint_axioms` (5), `check_refutation_independence` (10 tags), `check_public_reachability` (63 modules), `check_citations`, `check_markdown_links`, scoreboard (**3459/5/0**), map-freshness exit 0 after the stats sync. Eight technique findings recorded in the follow-on record (headline: `Real.sqrt_mul` carries a first-factor nonnegativity hypothesis that silently becomes a metavariable side goal under bare `rw`). |
| Continuous-Time χ² Mixing — the π-Weighted `L_sym` Heat-Variance Twin ([`continuous-time-chi-square-mixing.md`](continuous-time-chi-square-mixing.md)) | 2026-08-31 | **The heat-variance delivery's own named deferred follow-on, with its consumer gate discharged by naming the field-standard continuous-time mixing statement — zero new axioms (count stays 5), pure hard crust: `#print axioms` on all 46 audited declarations (28 shelf + 18 QA) exactly `propext, Classical.choice, Quot.sound` (`wip/ctmix_spike.lean`'s audit block).** The shelf: `Heat.lean`'s new walk-heat sections — `normalizedHeatKernel := e^{-tL_sym}` with the mode-decay/coordinate-damping/Parseval-exact clones at `L_sym`, the eigenvalue plumbing and the positive-gap kernel lemma at the kernel vector `√D·1`, `walkHeatKernel := e^{-tL_walk}`, the commutation `√D·L_walk = L_sym·√D`, and **the exp conjugation lemma** `√D *ᵥ (e^{-tL_walk} *ᵥ f) = e^{-tL_sym} *ᵥ (√D *ᵥ f)` — the exact continuous analogue of the shelf's conjugated-power transfer, by per-power conjugation of the exponential series through `expSeries_hasSum_exp` + `HasSum.map` — the π-isometry pair, and **`walkHeatKernel_variance_decay`** (the π-weighted twin, hypothesis-minimal: at `λ₂(L_sym) = 0` the true rate-1 statement). `Mixing.lean`'s `ContinuousTime` section: `contWalkDensity`, the centered-density conjugation shift, `contChiSquareDistance` with its `t = 0` join, and **`contChiSquareDistance_le`** — `χ²_cont(t,x) ≤ e^{−2tλ₂(L_sym)}·((πx)⁻¹−1)` at the discrete theorem's hypothesis set *minus connectivity* and with **no caller-certified rate** (the continuous rate is intrinsic, where the discrete theorem needs an `r`). QA +19 (3401 → 3420): **exact attainment at every time on the triangle** (`2e^{−3t}`, λ₂ pinned `3/2` by cases + trace), the wrong-constant refutation (pretend gap `2`: `2e^{−3} ≤ 2e^{−4}`), the disconnected exact form `1/3 + (8/3)e^{−3t}` with the gap pinned exactly zero and the positive-gap fence (pretend `λ₂ = 1` refuted from `2 < e`), the `t = 0` corner, mass preservation. The path exact-decay QA was deferred with its `λ₂(L_sym P₃) = 1` pin and **delivered the same day as the follow-on run `20260831T221623Z-run-1`** (QA 3420 → 3433, `Mixing_QA.lean`'s path section: the gap pin by the trace route — the witnesses `0`/`1`/`2` pulled into the sorted spectrum, the file's SOS certificates bounding it into `[0,2]`, the trace closing the middle entry — the **non-scalar conjugation pin** `√D(−1,1,−1) = (−1,√2,−1)` (the only fixture class where the conjugation shift acts through a non-scalar `√D` with a positive gap), the exact single-mode decay `χ²_cont(t, center) = e^{−4t}` at every time, the strict-slack witness `e^{−4t} < e^{−2t}` for `t > 0`, the `t = 0` corner, mass preservation, and the wrong-constant fence; all 13 new declarations at the standard three, `wip/ctpath_spike.lean`; full ladder re-run, scoreboard 3433/5/0, map-freshness exit 0). Full ladder: build ✔ + completeness 133/133 fresh, `lint_axioms` (5), `check_refutation_independence` (10 tags), `check_public_reachability` (63 modules), `check_citations`, `check_markdown_links`, scoreboard regenerated (**3420/5/0**), map-freshness exit 0 after the stats sync + SVG regeneration. Radar QA axis 4.0 held per protocol. |
| Heat-Variance Decay ([`heat-variance-decay.md`](heat-variance-decay.md)) | 2026-08-31 | **The Poincaré delivery's own named deferred follow-on, the heat family's consumer of λ₂ — delivered as a same-run proposal (`Var(e^{-tL}f) ≤ e^{−2tλ₂}Var(f)`, zero new axioms (count stays 5), pure hard crust: `#print axioms` via `wip/heatvar_axcheck.lean` on all 26 audited declarations (7 shelf + 19 QA) exactly `propext, Classical.choice, Quot.sound`).** The recorded cost blocker ("the semigroup-plus-Poincaré route needs derivative machinery not priced here") dissolved by route choice: the **eigenbasis contraction** — the mixing program's proved ℓ²(π) technique (`dotProduct_eigvecOf` Parseval + per-mode factor domination) transferred from `P^t` to `e^{-tL}` through `heatKernel_mulVec_eigvecOf`; no derivatives anywhere. `Heat.lean`'s new section: the eigenvalue plumbing (`eigvalOf_mem_evals`, `secondEval_le_eigvalOf_of_ne_zero` — below-gap eigenvalues *are* kernel eigenvalues), the positive-gap kernel lemma `eigvecOf_ker_eq_smul_onesVec_of_secondEval_pos` (a centered kernel residual would force λ₂ ≤ 0 through the Rayleigh bound), mean preservation `sum_heatKernel_mulVec`, the coordinate-damping and Parseval-exact heat identities, and **`heatKernel_variance_decay`** — *hypothesis-minimal* (no connectivity, no gap positivity: at λ₂ = 0 the true rate-1 statement), two branches over one Parseval identity (positive gap: zero modes carry no coordinate + the eigenvalue comparison; nonpositive gap: every factor ≤ 1 ≤ e^{−2tλ₂}, no mode hypothesis at all). QA +19 (3382 → 3401, `Heat_QA.lean`'s variance-decay section): **exact attainment at Fiedler vectors at every time** (K₂ `2e^{-4t}`, P₃ `2e^{-2t}` — the same shape at the other gap value on an irregular graph, both instances closing at equality), the wrong-constant refutation (gap `3` reads `2e^{-4} ≤ 2e^{-6}`, refuted via `e^{-6} < e^{-4}`), the **λ₂ = 0 disconnected branch pinned exact** (both sides exactly `2/3` at the `Fin 3` component indicator, the fixture's gap proved *exactly zero* — ≤ 0 by the Rayleigh bound at the centered kernel vector `![1,1,−2]`, ≥ 0 by sortedness + `laplacian_evals_zero`), mean preservation by two independent routes, and the `t = 0` corner. Radar axis 3 held at 4.5 (a consumer composition of the already-counted Poincaré and heat families; log-Sobolev remains the named gap); QA axis held at 4.0 per protocol. Full ladder: build ✔ + completeness 133/133 fresh (after the documented mtime remediation), `lint_axioms` (5), `check_refutation_independence` (10 tags), `check_public_reachability` (63 modules), `check_citations`, `check_markdown_links`, map-freshness exit 0 after the 3382 → 3401 stats sync. Six technique findings recorded in the proposal (the first-argument dotProduct lemma names, `Monotone`'s strict-implicit application + `Fin.le_def`, `List.mem_iff_get`'s two-component destructuring, the `WithLp`-coercion `exact`-not-`rw` rule, the `Real.exp_lt_exp` iff shape, and the stale-olen import-boundary recurrence). |
| The Poincaré Inequality Family ([`poincare-inequality.md`](poincare-inequality.md)) | 2026-08-31 | **Radar axis 3's named absent category "Poincaré" closed — zero new axioms (count stays 5), pure hard crust: `#print axioms` via `wip/poincare_axcheck.lean` on all 55 audited declarations (7 shelf + 48 QA) exactly `propext, Classical.choice, Quot.sound`.** The new `Scaffold/Mathlib/GraphTheory/Poincare.lean`: the centering-invariance helper `quadForm_laplacian_sub_const`; the division-free engine `poincare_variance_mul_le` (`λ₂(L)·∑(f − mean f)² ≤ fᵀLf`, *no* λ₂-positivity hypothesis — vacuous at `λ₂ = 0`, with QA proving that the exact boundary of the true region); `poincare_inequality` (`∑(f − mean f)² ≤ fᵀLf/λ₂`) with its connected twin; `poincare_inequality_normalized` — the degree-weighted π form `∑ deg·(f − E_π f)² ≤ fᵀLf/λ₂(L_sym)` through the general-kernel Rayleigh bound at `√D(f − E_π f·1)` and the congruence `√D L_sym √D = L` (the denominator is `L_sym`'s own eigenvalue — not scalar-related to the combinatorial one on irregular graphs), with its connected twin; and `spectral_gap_edge_expansion` — `λ₂·\|S\|·(\|V\|−\|S\|)/\|V\| ≤ boundary A S` for every vertex set, linear in the gap, consuming `Multiway.lean`'s indicator energy identity (its first consumer outside its own module). QA +44 (3338 → 3382, the new `Poincare_QA.lean`): **exact attainment at Fiedler vectors** on K₂ (`2 = 4/2`), P₃ (`2 = 2/1`), and K₃ for the normalized form (`4 = 6/(3/2)`, the gap pinned through the entrywise `L_sym(K₃) = (1/2)•L(K₃)`); the **wrong-constant refutation** (gap `3` reads `2 ≤ 4/3` at the very fixture that attains the true constant); the **no-constant connectivity fence** (disconnected two-edge fixture: variance `1`, energy `0` — no finite `c` works); both **edge-expansion instances attained** (`1 = 1` on K₂, `2 = 2` on K₃ singletons). Radar axis 3 re-scored 4.0 → 4.5 (log-Sobolev the remaining named gap); QA axis held at 4.0 per protocol. Full ladder: build ✔ + completeness 133/133 fresh, `lint_axioms`, `check_refutation_independence` (10 tags), `check_public_reachability` (63 modules), `check_citations`, `check_markdown_links`, map-freshness exit 0 after the stats sync. |
| The ℓ² → Total-Variation Mixing Conversion ([`total-variation-mixing-conversion.md`](total-variation-mixing-conversion.md)) | 2026-08-31 | **The mixing program's deferred optional Step 4, delivered as its own same-run proposal — closing the random-walk radar axis's single named remaining absent category. Zero new axioms (count stays 5), pure hard crust: `#print axioms` via `wip/tv_axcheck.lean` on all 35 audited declarations (10 shelf + 25 QA) exactly `propext, Classical.choice, Quot.sound`.** Both of Step 4's recorded gates discharged on the record: the consumer named (the field-standard `t_mix(ε)` statement — every textbook mixing bound is TV-based — instantiated as the **TV twin of the delivered oversmoothing ceiling**), and the 2026-08-22 cost blocker ("a probability-measure wrapper Scaffold has never used") dissolved by scoping (vector TV `(1/2) ∑ |ν − π|` on the vertex type — the module's own Step-2 no-MeasureTheory idiom). The shelf: `Mixing.lean`'s `TotalVariation` section (`tvDistance` with nonneg/symmetry/triangle, the generic Cauchy–Schwarz conversion `tvDistance_le_half_sqrt` at the sharp classical constant `TV ≤ (1/2)·√χ²` — sign-free on `ν`, so strictly stronger than the textbook probability-vector form — the unconditional walk-level shadow, and the rate form `walkDistribution_tvDistance_le_of_connected` at exactly the χ² theorem's hypothesis set) plus `Oversmoothing.lean`'s TV-twin section (`walkDistribution_tvDistance_le_of_rate`, the **depth-form TV ceiling** `walkDistribution_tvDistance_le_of_depth` — the entrywise ceiling's own threshold at `2ε`, past which the walk law is within `ε` of stationarity *in total variation* — and the two-start `2ε` twin through the triangle inequality). QA (+25, 3313 → 3338): the **exact-attainment pin** `k2_conversion_attained_QA` (on `K₂` at `t = 1`, `TV = (1/2)·√χ² = 1/2` with both sides independently pinned — the constant sharp, no smaller constant possible), the triangle's exact TV values `1/3, 1/6, 1/12` against both rate-form instances with the domination proved, the depth-2 certificate at `ε = 1/4` with **depth 1 proved to fail the threshold**, the two-start instance at depth 3 with raw value `1/8`, and the **mass-one fence** (at a mass-2 weight the un-guarded conversion reads `1/2 ≤ (1/2)√(1/2)`, refuted, with the surviving hypothesis proved genuine at the fixture). Full ladder: build ✔ + completeness 131/131 fresh, `lint_axioms` (5), `check_refutation_independence` (10 tags), `check_public_reachability` (62 modules), `check_citations`, `check_markdown_links`, map-freshness exit 0 after the stats sync + SVG regeneration. Radar axis 5 re-scored **4.0 → 4.5** (its named absent category closed). |
| Concentration of the Empirical Stationary Distribution — Steps 0+1 and Step 2, program COMPLETE ([`empirical-stationary-distribution-concentration.md`](empirical-stationary-distribution-concentration.md)) | 2026-08-27 (Steps 0+1); 2026-08-31 (Step 2) | **Steps 0+1 delivered 2026-08-27** (the V-valued i.i.d. sampling space `Probability.IIDProduct`, the walk-law nonnegativity layer, and the fixed-time empirical tail `empiricalWalkDistribution_tail` — `hoeffding_empirical`'s first theorem consumer, hard crust since that axiom's 2026-08-30 retirement). **Step 2 — the stationarity-limit form — delivered 2026-08-31** (run `20260831T103057Z-run-1`, zero new axioms, count stays 5, QA 3306 → 3313, the Step-2 QA section of `EmpiricalStationary_QA.lean`), the Medium-High row's own pricing gate ("a named consumer for the bias-term shape") discharged by naming the consumer: **the oversmoothing ceiling's empirical counterpart** — `Derived/EmpiricalStationary.lean`'s new `StationaryLimit` section gives `empiricalWalkDistribution_stationary_tail` (the bias-term form: `P{|p̂_i(n) − π i| ≥ t} ≤ 2 exp(−2n(t − r ^ t₀ √(π i ((π x)⁻¹ − 1)))²)` under exactly the oversmoothing ceiling's hypothesis set, the proved entrywise extraction folded in by the triangle-route event inclusion) and `empiricalWalkDistribution_stationary_tail_of_depth` (the capstone: past the ceiling's own threshold at `ε/2`, `n` simulated trajectories estimate `π i` to `ε` at `2 exp(−n ε²/2)` — the depth certificate becomes a sampling guarantee for an agent that can only simulate the walk). Pure hard crust end to end: `#print axioms` on all 11 audited declarations (both new theorems, the seven QA members, both Step-1 theorems) exactly the standard three (`wip/empstat2_axcheck.lean`); an incidental honesty repair rode along (the module and Step-1 docstrings still claimed conditionality on the retired `hoeffding_empirical` — de-staled with the fact re-verified). QA: the raw-vs-bias domination pin (`1/6 ≤ (1/4)√(2/3)` at `t₀ = 2`), the depth-form instance closing at exactly `2 exp(−1/16)` on the same depth-3 certificate the oversmoothing QA pinned sharp, the event-mass non-vacuity witness (cylinder mass `1/16`), the **bias-free naive-form refutation** at `t₀ = 0` (event measure `1` against `2 exp(−16/9) < 1` — the bias term load-bearing), and the `t₀ = 0` corner of the delivered form (graceful degradation, not exclusion). Full ladder: build ✔ + completeness 131/131 fresh, `lint_axioms`, `check_refutation_independence` (10 tags), `check_public_reachability` (62 modules), `check_citations`, `check_markdown_links`, map-freshness exit 0 after the 3306 → 3313 stats sync. |
| The Per-Pair Resistance Refinement of the Oversmoothing Ceiling ([`message-passing-depth-mixing-bound.md`](message-passing-depth-mixing-bound.md), Deferred item 1) | 2026-08-31 | **The deferred per-node-pair refinement delivered as a same-file follow-on — zero new axioms (count stays 5), QA 3275 → 3306 (+31, `Mixing_QA.lean`'s per-pair section).** The new section of `Oversmoothing.lean`: the entrywise eigenbasis expansion `walkDistribution_eq_sum_eigbasis` (`ν_t x y = ∑_k (1 − λ_k/d)^t u_k x u_k y` over the combinatorial Laplacian's orthonormal basis on any `d`-regular graph, connectivity-free), the **four-point contrast bound** `walkDistribution_pair_contrast_abs_le` (`|(ν x₁ y − ν x₂ y) − (ν x₁ y' − ν x₂ y')| ≤ ρ_t · √R(x₁,x₂) · √R(y,y')` under the hypothesis-shaped mode-rate `λ_k |1 − λ_k/d|^t ≤ ρ_t`, Foster's per-pair spectral resistance formula joined through Cauchy–Schwarz against the eigenvalue weights — the first theorem anywhere connecting the electrical axis to the mixing axis), the `λ ≤ 2d` engine, and the packaged `2 d r^t` corollary at the family's own certificate shape. Statement design pinned before Lean, with two negative findings recorded (no single-target pure-resistance form — the pseudoinverse diagonal is unavoidable and unbounded by incident resistances, verified numerically on C₆; the universal `d/(t+1)` polynomial rate is dead since `λ_max > d` always). QA: exact attainment on K₃ at `t = 1` and `t = 2` (contrast = bound: `1 = 1`, `1/2 = 1/2` — the strongest QA shape a bound theorem can have), the packaged instance with honest slack, the engine pinned both routes, and the C₄ mode-coverage fence (a certificate covering only the decaying mid modes is refuted by contrast `1` against bound `0` — the mode quantifier load-bearing; C₄'s spectrum `{0,2,4}` derived from `A³ = 4A` pinned by raw enumeration). `wip/pairres_axcheck.lean`: all 30 audited declarations exactly the standard three. Full ladder: build ✔ + completeness 131/131 fresh, `lint_axioms`, `check_refutation_independence` (10 tags), `check_public_reachability` (62), `check_citations`, `check_markdown_links`, map-freshness exit 0 after the 3275 → 3306 stats sync. |
| The Rank-`k` Spectral-Encoding Drift Pipeline ([`spectral-encoding-drift-pipeline.md`](spectral-encoding-drift-pipeline.md)) | 2026-08-31 | **The LapPE delivery's priced automatic-`δ` follow-on as the random-resampling composition at general rank — zero new axioms (count stays 5), QA 3252 → 3275 (+23, `EdgePerturbation_QA.lean`'s RankKDrift section).** The new section of `Derived/EdgePerturbationDrift.lean`: the general-`k` Weyl discharge helper `separation_of_norm_lt'` (the proved `weyl_inequality` at index `k+1`, its first rank-parameterized consumer) plus the four theorems `edgePerturbation_spectralEncodingSubspace_drift{'}` / `edgePerturbation_spectralEncoding_drift{'}` — the k=1 Fiedler-drift pipeline's structure at arbitrary encoding rank, with the separation discharged *inline* from the base graph's deterministic rank-`k` gap `γ ≤ evals ⟨k+1⟩ − evals k`: the caller supplies only the base gap, never a perturbed-spectrum `δ`. The ML-facing sentence: under centered Bernoulli edge resampling, the `k`-dimensional positional-encoding subspace (and its kernel-isolated informative component) stays within `s/(γ−s)` of the base graph's with the stated tail probability. Conditional on `matrix_hoeffding` via the tail alone (`wip/lappedrift_axcheck.lean`, 15 audited: the four theorems at the standard three + that axiom, nothing else; the hard-crust pins at the standard three). QA on the star `K₁,₃` at `k = ⟨2⟩` — a rank the k=1 family cannot express, with the **trace-route vacuity pin** proving `evals ⟨2⟩ − evals ⟨1⟩ = 0` (the λ₂ = λ₃ tie makes `k = ⟨1⟩` vacuous on the fixture, the rank parameter load-bearing) — the variance proxy pinned (`4 • L(K₁,₃)`, norm `16`), the gap pinned (`4 − 1 = 3`), and three closed-form instances at `8 exp(−1/32)`. Full ladder: build ✔ + completeness 131/131 fresh, `lint_axioms`, `check_refutation_independence` (10 tags), `check_public_reachability` (62 modules), `check_citations`, `check_markdown_links`, map-freshness exit 0 after the 3252 → 3275 sync; scoreboard regenerated at 3275/5/0. |
| Certified Sparsification Schedules for Scalable GNN Training, Track A ([`spectral-graph-sparsification-gnn-training.md`](spectral-graph-sparsification-gnn-training.md)) | 2026-08-31 | **The GNN-facing packaging of the delivered sparsification guarantee — zero new axioms (count stays 5), QA 3246 → 3252 (+6, `SparsificationTail_QA.lean`'s closed-form budget section).** The shelf section of `SparsificationTail.lean`: `sparsificationBudget n ε δ := max 1 ⌈(8/3)·log(2n/δ)/ε²⌉` — the **minimal natural certified budget**, with `_pos`/`_le`/`_min` proving positivity, the budget inequality, and minimality among naturals (all pure arithmetic: `#print axioms` exactly `propext, Classical.choice, Quot.sound`, per the acceptance criterion) — plus the plug-in `sparsification_graph_budget_closedForm` (pass only the graph and `(ε, δ)`; honestly conditional on `matrix_bernstein`, standard three + that axiom). QA: two exact-`e` design pins (8 and 24 exactly), the `max 1` floor proved load-bearing at `n = 1`, `δ = 10`, minimality pinned through `_min` itself (fails at 7), the closed form joined to the file's own hand budget (`≤ 100`, same `log 8 ≤ 300/32` route), and the `K₂` interface pin (`wip/gnnbudget_axcheck.lean`: the six arithmetic pins at the standard three, the interface pin carrying `matrix_bernstein`). The ML-facing usage note `docs/gnn-sparsification-budget.md` (trust caveat first, worked `n = 10⁶` interpretation, certificate-vs-recommendation distinction), the python-bridge cross-reference naming the budget check as the second candidate certified quantity, README (3252 + docs bullet + sparsification-highlight extension), radar QA axis (4.0 held), `index/map/spectral_graph.md`, both map data tables + regenerated SVG, scoreboard verification row. Full ladder: build ✔ + completeness 131/131 fresh, `lint_axioms`, `check_refutation_independence` (10 tags), `check_public_reachability` (62 modules), `check_citations`, `check_markdown_links`, `check_scaffold_map_freshness` exit 0. Track B stays gated at `matrix-master-bound-first-slice.md` Step 2. |
| Certified Oversmoothing Ceiling from Mixing ([`message-passing-depth-mixing-bound.md`](message-passing-depth-mixing-bound.md)) | 2026-08-31 | **The depth-form consumers of the mixing program's closing bound — zero new axioms (count stays 5), QA 3225 → 3246 (+21, `Mixing_QA.lean`'s oversmoothing section).** The new module `Scaffold/Mathlib/GraphTheory/Oversmoothing.lean` (umbrella-imported, 6 proved declarations): `stationaryVec_le_one`, the **entrywise extraction** `walkDistribution_sub_stationaryVec_abs_le` (`|ν_t x y − π y| ≤ r^t · √(π y · ((π x)⁻¹ − 1))` — one χ² summand against the whole sum, the mixing bound's normalization entering the constant unchanged), the **log-threshold calculus bridge** `pow_mul_le_of_log_threshold` (the corrected sign-of-`log` step, `C = 0` trivial), the **ceiling** `walkDistribution_sub_stationaryVec_le_of_depth`, the **two-start indistinguishability corollary** `walkDistribution_sub_walkDistribution_le_of_depth` (within `2ε` past the depth — the informal oversmoothing statement made formal), and the **rate monotonicity** `oversmoothing_log_threshold_mono`. QA on the triangle at two rate certificates — the sanity contrast as *the same graph, two certificates*, isolating the mechanism: the honest `r = 1/2` certifies `ε = 1/8` at **exactly** depth 3 (`tri_ceiling_threshold_three_sharp_QA` proves depth 2 fails the threshold — the hypothesis load-bearing), the loose `r = 4/5` provably needs 9 (`tri_ceiling_loose_sharp_QA` excludes 8); true values beside every conclusion (deviations `1/6`, `1/12`, `1/768`; the two-start value `1/8` at exactly half its `2ε` bound); the `t = 0` corner soundness instance; the monotonicity pinned at the fixture; walk laws by raw literal iteration (`tri_dist_nine_zero_QA` = `(85/256, 171/512, 171/512)`). `#print axioms` on all 27 new declarations (6 shelf + 21 QA) exactly `propext, Classical.choice, Quot.sound` (`wip/oversmoothing_axcheck.lean`); full ladder: build ✔ + completeness 131/131 fresh, `lint_axioms` (5), `check_refutation_independence` (10-tag clean), `check_public_reachability` (62 modules), citations, links, scoreboard **3246/5/0**, map freshness exit 0 after stats sync. The honest scope (linearized mean-aggregation operator; global rate; bipartite graphs outside the ceiling's reach) lives in the module docstring; the genuine over-squashing floor stays a priced, consumer-gated follow-on per the proposal's correction section. |
| Certified Stability for Laplacian Positional Encodings ([`spectral-positional-encoding-stability.md`](spectral-positional-encoding-stability.md)) | 2026-08-31 | **The general-rank Davis–Kahan pair delivered — zero new axioms (count stays 5), QA 3172 → 3225 (+53, `Fiedler_QA.lean`'s SpectralEncodingStability section).** `spectralEncodingSubspace_stability`/`spectralEncoding_stability` in `Fiedler.lean` (the rank-`k` re-parameterizations of the delivered Fiedler-stability pair — `davis_kahan_sin_theta` and the kernel identification were already rank-general), with `fiedlerSubspace_stability`/`fiedlerLine_stability` re-proved as one-line corollaries at unchanged public statements, machine-checking that nothing in their proofs used `k = 1` specifically. The first machine-checked instance of the LapPE/SAN subspace-stability class (motivation: Dwivedi & Bresson; Kreuzer et al.; informal precedent: von Luxburg, Gama & Ribeiro, Levie et al. — recorded in the module docstring with the honest scope limits: subspace distance not entrywise, both graphs connected for the isolated form, `δ` the caller's obligation). QA at `k = ⟨2⟩` on the star `K₁,₃` → `K₄` fixture (a rank the `k = 1` theorems cannot express): the separation `4 − 1 = 3` pinned independently at that rank (eigenvector-witness bridge + Rayleigh–Ritz engine both sides), the full star spectrum `{0, 1, 1, 4}` pinned, the perturbation norm `‶L(triangle)‶ = 3` pinned two-sided, the rank-3 projector witness at the strict gap `1 < 4`, both theorem instances with the bound exactly `1`, and the **exact-attainment pin** `‶P_{K₄}⟨2⟩ − P_star⟨2⟩‶ = 1` (the `K₄` projector at threshold `4` is the identity; the star projector kills the top unit eigenvector by orthonormality; the norm→form transfer closes the lower side — the bound is tight at the fixture). All 18 audited declarations (both theorems, both corollaries, the load-bearing QA) exactly `propext, Classical.choice, Quot.sound` via `wip/lappe_axcheck.lean`; full ladder green (both modules elaborate with zero warnings — the QA baseline verified zero-against-zero with HEAD; explicit build targets; full build + completeness 130/130 fresh; `lint_axioms` 5; refutation-independence 10-tag clean, no tags added — nothing touches an axiom; citations/links; scoreboard 3225/5/0; map-freshness after the stamp sync). Deferred as proposed: the Python certificate bridge, automatic `δ` certification, entrywise extensions. |
| Axiom Audit Tooling ([`axiom-audit-tooling.md`](axiom-audit-tooling.md)) | 2026-08-30 | **All three audit checks delivered in one run — zero new axioms (count stays 5), QA counts unchanged (comment-only edits), every discipline the repair records verified by hand now mechanical.** (1) `scripts/check_refutation_independence.py`: the `-- @refutes: <axiom>` tag (Step-0 survey: naming can't identify axiom-target refutations among the tree's 60+ `_refuted`/`_fence` declarations, nearly all targeting proved theorem shapes), bound to full names by namespace tracking, checked by real dependency extraction — a generated `lake env lean` file runs `#print axioms` on each; ten existing declarations tagged (the five matrix-axiom pre-repair refutations, the pairwise pair, the PF `hex`/strict-dominance and `hπsum` corner audits the lint allowlist itself cites); tags naming retired names, targeting `private` decls, or vanishing entirely all fail. Broken fixture (tag on the axiom-consuming `matrix_hoeffding_zero_QA`) and anti-rot probe (tag naming retired `hoeffding_lemma`) each confirmed failing, then reverted. (2) The replacement-path lint in `lint_axioms.py` (label family recognized; survey verdict: no axiom carried the note, so all five gained a labeled `Replacement path:` paragraph — the matrix trio pointing at the delivered master-bound retirement route's gated Step 2, the PF pair recording the honest no-priced-local-route answer; empty per-axiom allowlist mirroring the degenerate-corner pattern); the check's arrival caught and fixed a phantom `axiom was` the raw declaration regex had been producing from Azuma docstring prose (axiom extraction is now block-comment-aware). (3) `scripts/check_public_reachability.py`: static import-closure walk from `Scaffold.lean` (61 repo modules; `wip/` confirmed the only non-public Lean directory; fails on `wip.`-prefixed import edges even when unresolvable). Wired at all five ladder locations + `governance/CONTRIBUTING.md`'s admission checklist; full ladder green on the final tree (`lake build` clean, `check_build_completeness` 130/130 fresh, `lint_axioms`/citations/links/map-freshness pass, both new scripts pass). The parent document's remaining vocabulary/lifecycle questions stay deferred exactly as its §Deferred records. |
| The Matrix Master Bound: Retirement Route, First Slice ([`matrix-master-bound-first-slice.md`](matrix-master-bound-first-slice.md)) | 2026-08-30 | **The first proved slice of the matrix concentration retirement route — Tropp's Proposition 3.1, the Laplace-transform master bound every matrix concentration proof consumes, in its two-sided spectral-norm form — plus its deterministic engine, the trace-exponential spectral identity `tr (exp (θ•M)) = ∑ i, exp (θ·λᵢ(M))` (the identity the pinned Mathlib's own `MatrixExponential.lean` lists as an open TODO). Pure hard crust, zero new axioms (count stays 5).** The module (`Matrix/MasterBound.lean`, 8 proved declarations): the identity (conjugation through the spectral theorem's unitary diagonalization — the exp-carrying extension of the shelf's `eigvalOf_sum_eq_trace` route), nonnegativity, the eigenvalue≤trace domination, the continuity/measurability pair for the trace-exponential (at the `l∞`-instance topology), Markov on the lower integral (no integrability hypothesis — honest for unbounded summands), the norm–eigenvalue attainment bridge, and `matrix_master_bound` itself (`μ {t ≤ ‖Y‖} ≤ e^{−θt}(E tr e^{θY} + E tr e^{−θY})` as `ℝ≥0∞` lower integrals, `[Nonempty V]`-guarded). Step-0 degenerate corner checked **before** stating: at `V = ∅`, `t = 0` the un-guarded bound is false (`1 ≤ 0`, empty trace sums) — the axioms' own 2026-08-28 corner class, guarded at birth and fenced in QA (`master_bound_fin0_unguarded_refuted_QA`). QA +5 (3167 → 3172): the diagonal-fixture trace pins at both θ signs against an independent diagonal route, the constant-design instance with the bound proved **≥ 1** (non-vacuous), and the corner fence. `#print axioms` on all 13 new declarations (operator-verified at `0a1cb8c`, `wip/verify_masterbound.lean`): exactly the standard three. The Lieb-class sum-MGF step (Tropp Thm 6.1) is deliberately **not** here — the route's Step 2 is gated on an operator decision between admitting the analytic core and proving it. Lean delivered by run `20260830T202747Z-run-1`; records ladder by run `20260830T221307Z-run-1`. |
| Repair `matrix_hoeffding`'s Missing Centering Hypothesis ([`repair-matrix-hoeffding-centering.md`](repair-matrix-hoeffding-centering.md)) | 2026-08-30 | **Errata §9 — the largest-consumer admitted axiom materially false in hypothesis shape, repaired in place; axiom count stays 5.** The statement hypothesized measurability, `iIndepFun`, Hermitianity, and semidefinite domination `X i ω² ⪯ A i ²` — and **no centering**, its own docstring asserting "the source needs no centering hypothesis" — while the sibling `matrix_bernstein` has carried `h_mean` all along (the 2026-08-30 Bernstein §8 audit checked the sibling safe on this axis and never ran it on this file). The deterministic constant-ones family refutes the uncentered shape in one line: at `V = Fin 1`, `n = 2`, `X i = A i = 1` every pre-repair hypothesis holds *genuinely* (self-domination at equality; each clause separately proved as `ones_family_old_clauses_QA`), and at `t = 2` the tail event is all of `Ω` (norm exactly `2`, pinned two-sided by the new `norm_one_add_one_fin1_QA` through the eigenvalue sandwich) against the bound `2·1·exp(−1) < 1` from the pinned `2 < exp 1` — `old_matrix_hoeffding_refuted_uncentered_QA`, with the exclusion fence `ones_mean_ne_zero_QA` proving the repaired clause rejects exactly the refuting family (integral `1 ≠ 0`). Repair: `h_mean` inserted (the sibling's idiom); the window family's engine gained `integral_perturbSummand_eq_zero` (the matrix-codomain clone of the deg-design's `integral_delta` centering, honest integrability); every downstream consumer — the norm/quadForm/eval/λ₂ tails, both Cheeger windows, the unconditional dissolution trio, both Fiedler-drift theorems — re-proved at **unchanged public statements**, only the generic passthrough `matrix_hoeffding_quadForm` gaining the matching hypothesis; `matrix_hoeffding_zero_QA` re-instantiated. `#print axioms` via `wip/mhrepair_axcheck.lean` (19 declarations): the four new QA declarations and the engine lemma exactly the standard three; every audited consumer conditional on `matrix_hoeffding` alone — the same single-axiom load as before. QA +4 (3163 → 3167). Full ladder passed (build completeness 129/129 fresh; `lint_axioms` 5 — no guard-surface change; scoreboard 3167/5/0; map freshness exit 0). The axiom remains admitted — the repair fixes the hypothesis *shape*; every conditional Derived tail stays honestly conditional on `matrix_hoeffding`. |
| Retire `hoeffding_lemma` by the Pointwise-Collapse Route ([`retire-hoeffding-lemma-pointwise-collapse.md`](retire-hoeffding-lemma-pointwise-collapse.md)) | 2026-08-30 | **The last scalar concentration axiom and the last zero-consumer axiom retired in one slice: axioms 6 → 5, zero new axioms — after it every remaining admitted axiom (the PF pair, the matrix trio) has a theorem consumer and the scalar concentration subtree is axiom-free end to end.** The Step-0 finding: the defining set `{K | 0 < K ∧ ∫ exp (X²/K²) ∂μ ≤ 2}` cannot see the mean — only the bound and the mass — so pointwise `exp (X ω²·log 2/a²) ≤ exp (log 2) = 2` gives the sharper companion `subgaussianNorm_le_of_bounded` (`≤ a/√(log 2)`, **no mean-zero, no measurability, no numeric pins**; `1/√(log 2) ≤ √6` from `Real.one_sub_inv_le_log_of_pos` at `x = 2`), and the admitted statement is proved at its unchanged shape with the mean-zero hypothesis retained for stability and documented as unused — at the `√6` constant the statement never carried the source's λ-form content (that content is the separately proved `hoeffding_lemma_mgf`). Falsification content (QA +3, 3160 → 3163): the **exact two-sided pin** `rademacher_norm_eq_QA` (`subgaussianNorm rademacherX rademacherMeasure = 1/√(log 2)` — the companion's constant *attained*, membership at equality `exp (log 2) = 2`, the member lower bound `1/K² ≤ log 2`, the 2026-08-28 refutation's lower-bound class re-derived two-sided), the **uncentered witness** (`X ≡ 1`, mean `1 ≠ 0` — the centering hypothesis dead weight at this constant), and the **companion's own mass-guard fence** (at the mass-19/10 fixture the norm is exactly `1/√(log (20/19))`, attained, exceeding `1/√(log 2)` since `20/19 < 2` — the `[IsProbabilityMeasure]` instance load-bearing for the new theorem, the refuted target strictly sharper than the axiom's `√6`). `#print axioms` via `wip/hlretire_axcheck.lean` (11 declarations): the retired name, both companions (`subgaussianNorm_eq_zero_of_forall_eq_zero` the `a = 0` corner), the three new QA, and the four pre-existing consumers each exactly `propext, Classical.choice, Quot.sound` — hard crust, blast radius zero. Full ladder: build + completeness 129/129/0/0, `lint_axioms` (5), citations, links, scoreboard 3163/5/0, map-freshness exit 0. |
| Repair-and-Retire the Scalar Bernstein Pair ([`repair-and-retire-bernstein-pair.md`](repair-and-retire-bernstein-pair.md)) | 2026-08-30 | **A repair (Errata §8) and a retirement in one delivery: both scalar Bernstein axioms hypothesized the *uncentered* bound `|X i ω| ≤ a` while centering internally, materially understating the Bennett price (the cited sources bound the mean-zero variables; the centered variable satisfies only `|X − EX| ≤ 2a`) — repaired in place to the source-faithful centered bound and *proved* by a local Bennett MGF engine, axioms 8 → 6, zero new axioms.** The engine (`Bernstein.lean`, hard crust, `#print axioms` on all eight lemmas exactly the standard three): Bennett's ratio with the integral representation `q(u) = ∫₀¹(1−s)e^{us}ds` (antiderivative route), whole-line monotonicity, the series bound `q(v) ≤ 1/(2(1−v/3))` on `(0,3)` (termwise `(j+2)! ≥ 2·3ʲ`), the pointwise bound, the per-variable MGF bound `bennett_mgf`, the sum bound through the Hoeffding retirement's proved `integral_prod_exp_of_iIndepFun`, and the Chernoff assembly at `λ = t/(V + at/3)` closing at exactly `−t²/(2V + 2at/3)` — honest integrability throughout (every hypothesis-side integral discharged through `integrable_of_bounded_measurable`/`integrable_sq_sub_mean`, never a junk zero), the `V = 0` corner a dedicated null-event case, the dead `0 ≤ v` budget clause dropped (strictly stronger statement). Falsification content (QA +15, 3145 → 3160): the MGF-separation witness at the biased coin `q = 1/10` — every pre-repair hypothesis genuinely held at `a = 1` with the *true* variance `9/25`, yet at `λ = 5/9` the claimed per-variable exponent is exactly `3/44` and `E e^{λY} ≥ (9/10)(17/18)² + e/10 > 44/41 > e^{3/44}` (bounds from `add_one_lt_exp`/`one_sub_le_exp_neg`/the pinned `e` alone) — plus the refutation fence, the numerical LD record (`I(1/2) = 0.2269` vs claimed `0.2373`, violated for `n ≳ 500–1000`, recorded honestly as numerical evidence), and the repaired statements' first genuinely random closed-form instances (fair coin, `2 exp(−3/8) > 1`, `iIndepFun.of_subsingleton`). Consumers: `bernstein_iid` re-derived; **the two Derived Bernstein-twin tails and their QA pins now hard crust** (33-declaration audit via `wip/bernretire_axcheck.lean`: window family on `matrix_hoeffding` alone, sparsification on `matrix_bernstein` alone — both unchanged). Full ladder: build + completeness 129/129/0/0, `lint_axioms` (6), citations, links, scoreboard 3160/6/0, map-freshness exit 0. |
| Retire `hoeffding_inequality` and `hoeffding_empirical` by Proving Hoeffding's Lemma Locally ([`prove-hoeffding-inequality-mgf.md`](prove-hoeffding-inequality-mgf.md)) | 2026-08-30 | **The largest single trust-surface reduction of the project's history by axiom count: two of the ten admitted axioms retired to proved theorems (10 → 8), zero new axioms.** `hoeffding_lemma_mgf` (the MGF interval engine `E exp(λX) ≤ exp(λ²(b−a)²/8)` — secant/convexity bound, the sharp two-point `φ(u) ≤ e^{u²/8}` by the perfect-square identity `φ² − 4(φ''φ − φ'²) = (…)²`, monotone-on-half-line integration) plus the Chernoff assembly (`iIndepFun` factorization → Markov → λ-optimization) derive both retired statements at unchanged public shapes, every pin lemma from the pinned Mathlib. Delivered across two runs: `20260830T030715Z-run-1` proved the core and both retirements (committed at e77df27) but exited before its terminal entry; `20260830T053158Z-run-1` added the planned adversarial QA (+27 in `Scalar_QA.lean`): the wrong-constant fence at the MGF level (the exact fixture MGF `(e+e⁻¹)/2` refuting the `K = 12` sharpening), the mass-guard fence (every other hypothesis genuine at the mass-`19/10` measure, conclusion failing), the tail-level denominator fence (the exact two-coin agreement-event measure `1/2` refuting the factor-`2`-less exponent denominator), and closed-form instances — `hoeffding_inequality`'s first genuinely random instantiation. `#print axioms` (`wip/hoeffding_qa_axcheck.lean`, 37 declarations): the new QA all standard-three; `edgePerturbation_degree_tail`, `degree_tail_all`, `hoeffding_empirical_iid`, and `empiricalWalkDistribution_tail` now hard crust; the six window-family members conditional on `matrix_hoeffding` alone. No Errata entry — an honest axiom proved is the retirement lifecycle working. |
| Audit the `MatrixMDS` Martingale Encoding for the Ambient-Measurability Junk-Integral Hazard ([`audit-matrix-azuma-mds-measurability-hazard.md`](audit-matrix-azuma-mds-measurability-hazard.md)) | 2026-08-29 | **Errata §7 — the standing §12 junk-integral residual's own deferred Step 0, found real and repaired in place; zero new axioms (count stays 10).** The `MatrixMDS` encoding of Tropp's adaptedness carried no ambient strong-measurability clause and its `adapted` field was content-free (`mdsFiltration` is the comap σ-algebra the `X j` generate themselves, so `StronglyMeasurable[mdsFiltration X (k+1)] (X k)` holds for every `X` — proved as `measurable_mdsFiltration_QA`), so the `cond_mean_zero` set-integrals were `integral_non_aestronglyMeasurable` junk zeros and the martingale hypothesis was satisfiable by a bounded non-measurable drift — `matrix_azuma_hoeffding` and both Derived consumers (`eventStreamTail`, `eventStreamProjectorDrift`) materially false in hypothesis form. Refuted at a 33-point caterpillar fixture (trivial σ-algebra, mass `1/2` at the top point plus `1/2` uniform; 32-step drift `X k = 1` on `{i | k < i}`): every old field *proved* (`azDrift_adapted_QA`, `azDrift_cond_mean_zero_QA` with the junk mechanism explicitly exhibited, `azDriftSeq_norm_bound_QA` at `R = 1`) while the tail event carries mass `> 1/2` against the bound `≤ 1/2` (`old_matrix_azuma_refuted_nonmeasurable_QA`, standard three axioms only). Repair: the vacuous field replaced by `measurable : ∀ k, StronglyMeasurable (X k)` (junk content swapped for load-bearing content, the vacuity itself proved and recorded), both Derived consumers' `h_adapt` threaded to ambient `h_meas` (public statements otherwise unchanged), QA consumers re-instantiated, and the **exclusion fence** `azDrift_not_stronglyMeasurable_QA` proving the repaired field rejects exactly the refuting drift (trivial-σ-algebra SM = constants, the drift takes both values `0` and `1`) — the repair does real exclusion work. `#print axioms` via `wip/azumajunk_axcheck.lean`/`wip/azumajunk_axcheck2.lean`: the refutation/fence/vacuity/junk-engine/fixture family (11 declarations) at exactly the standard three; `matrix_azuma_zero_QA`, `eventStreamTail_constant_QA`, `eventStreamProjectorDrift_constant_QA`, `eventStreamTail`, and `eventStreamProjectorDrift` each conditional on `matrix_azuma_hoeffding` *alone*. QA +19 (3099 → 3118, `Matrix_QA.lean`'s repair-record section). Delivered across two runs (`20260829T235239Z-run-1` — the spike-verified Step-0 finding, the repair, the consumer threading, the refutation family; the completing run — the two shelf/spike divergence fixes, the exclusion fence, Errata §7, the proposal, and the full ladder: 129/129 build completeness, axiom lint with no guard-surface change, citations, links, scoreboard 3118, map freshness). |
| The Pairwise-Independence Repair of the Six Concentration Axioms ([`pairwise-independence-concentration-repair.md`](pairwise-independence-concentration-repair.md)) | 2026-08-29 | **Errata §6 closed — the six independence-carrying concentration axioms (`hoeffding_inequality`, `hoeffding_empirical`, `bernstein_inequality`, `bernstein_bounded_variance`, `matrix_hoeffding`, `matrix_bernstein`; Azuma's `MatrixMDS` martingale shape unaffected) were all found hypothesizing only *pairwise* `IndepFun`, which does not suffice for Hoeffding/Chernoff bounds, and were all repaired in place to `iIndepFun` (mutual independence) — zero new axioms (count stays 10), every conditional Derived statement's public shape unchanged.** The falsification content: the fifteen nontrivial Walsh characters of the fair `BernoulliProduct` on `Fin 4 → Bool` are pairwise independent (proved through the xor-translation group action forcing all four joint sign cells of any distinct nontrivial pair to mass `1/4`) with zero means (flip-involution) and sum `15` on the all-false atom (mass `1/16`) — six hypothesis-form refutations (`old_*_pairwise_refuted_QA`, `Scaffold/QA/Concentration/PairwiseIndependence_QA.lean`, +89 QA declarations, 3008 → 3099) proving each pre-repair statement false with every other hypothesis clause *proved* at the fixture, all at exactly the standard three axioms. Consumer threading: the `iIndepFun` engines on both sampling spaces (`BernoulliProduct`'s `toMeasure_cyl_inter`/`iIndepFun_coord`/`iIndepFun_of_injective`/`iIndepFun_coord_apply`/`iIndepFun_coord_matrix`; `IIDProduct`'s `iidPMF` analogue plus `iIndepFun_indicator_coord`), the design-level clauses (`iIndepFun_perturbSummand`, `iIndepFun_degPerturbSummand`, `iIndepFun_ssSummand`), and the three Derived tail modules threaded — `#print axioms` verifies all nine engines hard-crust and each of the eight Derived consumers conditional on its own axiom alone. The pairwise lemmas remain on the shelf as the refutation records' interface. Full ladder green on the repaired tree (129/129 build completeness; axiom lint with no new guard surface — the clause got *stronger*, guards unchanged; citations; links; scoreboard idempotent; map freshness). Delivered across three runs (`20260829T173340Z-run-1` — the audit and six signatures; `20260829T195611Z-run-1` — engines and threading; `20260829T222414Z-run-1` — independent re-verification, QA-file linter cleanup, records closure). |
| `hoeffding_inequality`'s First Theorem Consumer: Vertex-Degree Concentration under Edge Resampling ([`hoeffding-inequality-degree-concentration.md`](hoeffding-inequality-degree-concentration.md)) | 2026-08-29 | **Created and delivered in one run — the scalar Hoeffding axiom's first theorem consumer, closing the same zero-consumer gap class as the matrix trio's first consumers: the per-vertex degree-deviation tail `μ {|deg G_ω v − deg A v| ≥ t} ≤ 2 exp(−t²/(2 S_v))` at `S_v = ∑ₑ w_v(e)²` plus the all-vertices union bound, at the same centered Bernoulli edge-resampling design the delivered window family lives on. Zero new axioms (count stays 10; `#print axioms` via `wip/degconc_axcheck.lean` on all 23 audited declarations: the seven engine lemmas and twelve hard-crust QA lemmas exactly `propext, Classical.choice, Quot.sound`; the two Derived tails and two axiom-instantiating QA pins honestly carrying `hoeffding_inequality` alone).** Of the ten admitted axioms, four had zero theorem consumers after the 2026-08-28 audit (`hoeffding_inequality`, `bernstein_inequality`, `bernstein_bounded_variance`, `hoeffding_lemma` — audited safe but never load-borne); this gives the first one a graph-theoretic consumer serving the window family's recorded honesty-note gap (the λ₂/norm tails carry zero degree information, `L(E_ω)·1 = 0` identically — the degree tail is the missing complementary concentration, and the union bound is the first step toward *deriving* `perturbAdmissible`'s degree clauses rather than hypothesizing them). Engine: `deg_add`/`deg_sum` (`Spectral.lean`'s degree-linearity package) + `EdgePerturbation.lean`'s section 5 (`degPerturbWeight`/`degPerturbSummand`, the identity `deg_resampled`, and all four axiom clauses proved at the design — sign-free, no hypothesis on `A`; the centering needs no `p ≠ 0` guard, unlike the sparsification design). The scalar side's degenerate-corner analysis transfers clean: the prefactor is the constant `2`, never collapsing — no `Nonempty` guard needed. QA +14 (2963 → 2977, `EdgePerturbation_QA.lean`'s degreeTail section): the variance statistic pinned exactly (`S₀ = 2`) with the single-counted `1` refuted, the identity at all-true by two independent routes (raw `2 • K₂` vs the design's `1 + ½ + ½`) and at all-false joined to the corner audit, the closed-form instances (`2 exp(−1/4)`, union `4 exp(−1/4)`), and the **exact event measure `1/2`** computed through the design's own independence machinery (`indepFun_coord` + `toMeasure_cyl`), independently of the tail theorem — the bound's fixture-scale slack on the record beside it. Priced follow-ons (not owed): the admissibility dissolution (union with the window bracket + the `p e + p eᵀ ≤ 1` nonnegativity design condition) and the Bernstein twin (the variance-adaptive degree tail, the second zero-consumer scalar axiom's consumer). **The Bernstein twin was DELIVERED the same day** (run `20260829T125827Z-run-1`, the standing handoff's top priced follow-on, unblocked by this delivery's clause machinery): `bernstein_inequality`'s *and* `bernstein_bounded_variance`'s first theorem consumers in one slice — `edgePerturbation_degree_tail_bernstein` (`μ {|deg dev| ≥ t} ≤ 2 exp(−t²/(2 σ²_v + 2Mt/3))` at the true variance statistic `σ²_v = ∑ₑ w_v(e)² p e (1 − p e)`, magnitude budget `M ≥ |w_v(e)|`) and the `_budget` corollary at any `σ²_v ≤ Vbud` — through the new second-moment engine (`BernoulliProduct`'s `integral_sq_delta_sub`: `∫ (δ_e − p_e)² = p_e (1 − p_e)`, beside `integral_delta`; the design instantiation `integral_sq_degPerturbSummand`). Zero new axioms (count stays 10; `#print axioms` via `wip/berntwin_axcheck.lean` on 14 declarations: engine + hard-crust QA at the standard three, each tail on its own axiom alone, zero cross-contact). QA +10 (2977 → 2987, the bernsteinTwin section): the true statistic pinned (`σ²₀ = ½` on K₂ at the fair coin) with the Poisson-trial shape (`∑ w² p = 1`) refuted, the fourfold variance reduction as an equation (`σ²₀ = S₀/4`), the **strict variance-adaptivity improvement proved** (`2 exp(−3/5) < 2 exp(−1/4)`, the cross-axiom coherence check — hard crust), the budget relaxation pinned honest, and the two closed-form conditional instances. Only `hoeffding_lemma` remains zero-consumer (its natural consumer is `bernstein_inequality`'s own proof — upstream work); the admissibility dissolution — then the sole priced follow-on, with both degree tails as candidate engines — was **delivered the same day too** (run `20260829T142812Z-run-1`, QA → 3003: the engine lemma `perturbWeight_entry_nonneg` at the pair design condition `p e + p eᵀ ≤ 1`, the transfer helper, and `edgePerturbation_normalized_connectivity_bracket_unconditional` — the window family's first measured event with *no* admissibility conjunct, bounded by the window tail plus the degree tail; the family's first deliberate two-axiom member, `matrix_hoeffding` + `hoeffding_inequality`, each via its own sub-theorem; the pair condition *refuted when dropped* at `p ≡ 9/10`; the boundary pin at `p ≡ ½`; the positive transfer witness at `p ≡ 1/10`; and the closed-form `4 exp(−1/4096) + 4 exp(−1/16)` instance — see the proposal's dissolution delivery record). The dissolution's own priced follow-on — the floor and swept-cut members' own admissibility conjuncts — was **delivered the same day** (run `20260829T155521Z-run-1`, QA → 3008: `edgePerturbation_normalized_cheeger_floor_unconditional` and `edgePerturbation_fiedler_sweep_cut_tail_unconditional`, the identical decomposition at the two remaining conditional theorems, completing the window family — floor, bracket, swept cut — to unconditional measured events throughout; the **strict-containment witness** proving the dissolution genuinely enlarged the sweep event (the all-false outcome in the unconditional bad event while provably not admissible, hard crust), the good-outcome conjunction, and the two closed-form instances; see the proposal's completion delivery record). |
| The Degree Eigenvalue Sandwich ([`degree-eigenvalue-sandwich.md`](degree-eigenvalue-sandwich.md)) | 2026-08-29 | **The irregular-Cheeger-window follow-on's own named missing engine, delivered complete in one run — the classical two-sided degree comparison `λₖ(L)/dmax ≤ λₖ(L_sym) ≤ λₖ(L)/dmin` at every sorted index, zero new axioms (count stays 10; `#print axioms` on the audited engine/interface/QA declarations: exactly `propext, Classical.choice, Quot.sound`).** Analysis first established that no pointwise test-vector route exists (the `x ⊥ 1` vs `x ⊥ √D·1` constraint sets mismatch under the degree substitution), so the proof rides the shelf's subspace Courant–Fischer machinery — `evals_min_max` with both witness forms, witness subspaces transported through the degree stretch (`degreeSqrtEquiv` + `finrank_map_eq_of_injective`), the pointwise Rayleigh-quotient bracket, and `laplacian_psd` — load-bearing on all of it. Interface pair at `lambda2`/`secondEval` plus division-free mul forms, and the supporting `normalizedLaplacian_evals_zero` pin (the normalized counterpart of `laplacian_evals_zero`). QA +37 (2865 → 2902, the new `DegreeSandwich_QA.lean`): the P₃ exact pin `λ₂(L_sym) = 1` by two independent raw computations, the upper side **attained at equality** (`1 = λ₂/dmin`), the lower-side instance with the bracket's honest slack, the engine at a non-second index (`k = 2`: `3/2 ≤ 2`, trace-route pins), the **wrong-constant pairing fence** (`1 ≤ 1/2` refuted), the K₂ regular squeeze (`2 ≤ 2 ≤ 2`, both ends attained), and the **`dmin = 0` fence** — on `K₂ ⊕` isolated vertex (where the degree-floor hypothesis still holds at `dmin = 0`, only the positivity guard fails) the un-guarded upper side reads `1 ≤ lambda2/0 = 0`: refuted. The irregular window assembly — the sandwich's own priced follow-on — was **delivered the same day** (run `20260829T043741Z-run-1`: the window-Cheeger engine pair beside the sandwich plus `edgePerturbation_normalized_cheeger_floor`/`_connectivity_bracket` at the Derived layer with the admissibility window event-internal; both Step-0 design questions settled as predicted; see the Delivered matrix-Hoeffding row's follow-on clauses and the sandwich proposal's struck-through residual). |
| Spectral Sparsification via Leverage-Score Sampling — **program COMPLETE: Step 0 + Step 1 (three slices) + all three priced follow-ons** ([`spectral-sparsification-via-leverage-scores.md`](spectral-sparsification-via-leverage-scores.md)) | 2026-08-27 (Steps 0+1); 2026-08-28 (all follow-ons) | **Zero new axioms at every slice (count stays 10; `matrix_bernstein`'s first real theorem consumer — the program's strategic aim — with `#print axioms` audits at each delivery reading exactly `propext, Classical.choice, Quot.sound` for all hard crust, the axiom carried only by the Derived tails and their QA interface pins).** Steps 0+1 delivered 2026-08-27 across three slices (the sampling-space module `Probability/BernoulliProduct.lean`; the deterministic SS algebra `GraphTheory/Sparsification.lean` — the eigen-coordinate edge vectors with `‖v_e‖² = w_e R_eff/2`, `∑ v_e v_eᵀ = Π_{im L}` exactly, every `matrix_bernstein` clause lemma proved at the classical constants; the assembly `Derived/SparsificationTail.lean` — `sparsification_norm_tail` `μ{‖S(ω) − Π_{im L}‖ ≥ t} ≤ 2 d exp(−t²/(2/q + 2t/(3q)))` + the additive `sparsification_quadForm_tail`). **The two priced follow-ons DELIVERED 2026-08-28** (run `20260828T183001Z-run-1`): `sparsification_multiplicative_tail` — the field-standard "S is a (1±ε)-sparsifier" shape, the two-sided quadratic-form bound on `im Π`-coordinate vectors at the same exponential tail (the engine lemma `quadForm_imageProjector_eq_of_mulVec_eq` making the conversion legitimate exactly on the cone, where `qF(Π) x = x ⬝ᵥ x`) — and `sparsification_multiplicative_budget`, the `q ≥ (8/3)·log(2d/δ)/ε²` sample-complexity corollary driving the failure measure below `δ` at the Tropp exponent's exact constant; QA 2800 → 2808 with the order-independent im-Π membership pin, the tight ε = 1 instance (upper bound attained), failure-event nonemptiness at hand values, and the **cone fence** (the un-guarded pointwise claim *refuted* at the all-false outcome: `S = 0`, `(1/2)·2 > 0`). **The final priced follow-on DELIVERED 2026-08-28** (run `20260828T200603Z-run-1`): the *graph-vector* form — `sparsification_graph_tail`/`sparsification_graph_budget`, `(1±ε)xᵀLx ≤ xᵀL̃(ω)x ≤ (1+ε)xᵀLx` for **every graph vector with no `im Π` restriction**, through the transport (`ssTransport`, on-cone by construction, `L`-isometric), claim A (`ssTransport_dot_ssEdgeVec`), and the sampled-Laplacian form correspondence (`quadForm_ssLaplacian_eq`); QA 2808 → 2830 with the raw `4`/`8` pins, a two-route correspondence join, the tight `ε = 1` instance, and **signed-fixture fences** refuting both new engines' nonnegativity hypothesis (the transport provably junk-zero at a nonpositive spectrum while `xᵀLx = −36`); zero new axioms — the proposal now has no open follow-ons. |
| Audit `perron_frobenius` and `primitive_power_tendsto` for the Degenerate-Cardinality Hazard ([`audit-perron-frobenius-family-degenerate-corner.md`](audit-perron-frobenius-family-degenerate-corner.md)) | 2026-08-28 | **Both axioms confirmed safe — zero axiom changes (count stays 10). At `Fintype.card V = 0`, `perron_frobenius`'s `hex` and `primitive_power_tendsto`'s `hπsum` are each unsatisfiable (no witness on an empty type; the empty sum is `0`), proved unconditionally in QA as `perron_frobenius_hex_unsat_card_zero_QA` and `mass_one_unsat_card_zero_QA` (`#print axioms`: the standard three only) — the axiom hypothesis sets have *no instantiation* at the degenerate dimension, safe by unsatisfiability rather than by a guard, exactly the outcome the structural read predicted but now kernel-checked rather than docstring-asserted.** The `card V = 1` adjacent corners spot-checked per scope item 2: at `Fin 1`/`!![1]` every hypothesis of each axiom is satisfiable and the conclusions pin to hand data (`perron_frobenius_S1_QA`: root exactly `1`, simplicity exactly the hand `rootMultiplicity 1 = 1` via `S1_charpoly_QA`/`S1_rootMultiplicity_QA`; `P1_singleton_hand_QA` proves the primitive limit shape *without* the axiom as the constant sequence, and `P1_singleton_axiom_QA` instantiates the axiom non-vacuously). The two provisional entries in `lint_axioms.py`'s degenerate-corner allowlist upgraded to Lean-confirmed verdicts citing the theorem names; `lint_axioms` exit 0. The proposal's honesty note stands: this cleared the degenerate-cardinality mechanism only, not a general bill of health for either axiom's mathematical content. |
| Lint Every Axiom Signature for a Missing Degenerate-Corner Guard ([`lint-axiom-degenerate-corner-guards.md`](lint-axiom-degenerate-corner-guards.md)) | 2026-08-28 | **No Lean, no axioms, no QA change (counts stay 2785/10/0) — `scripts/lint_axioms.py`'s degenerate-corner guard check, folded into `main()` so every existing ladder invocation runs it: flags each `Scaffold/Mathlib` axiom whose effective signature carries a `Fintype`-carried index type (`Matrix V …`, `V →`, `Fintype.card V`) with no visible `Nonempty` guard, or a `Measure` argument it uses with no `IsProbabilityMeasure`/`IsFiniteMeasure`/total-mass guard, unless a per-(axiom, kind) allowlist in the script records why the corner is accepted.** The signature model: bracket-matched binder parsing (multi-name and anonymous-instance groups), namespace-scoped `variable` tracking with Lean's name-mention inclusion semantics (six of the ten axioms carry their measure guard in a `variable` line), and Unicode-aware identifier matching — the acceptance bar's own first fixture run caught the scanner blind to Greek identifiers (`μ`, `Ω`), i.e. the false-silence failure mode, before delivery. First-run output on the post-repair tree: exactly the two anticipated findings (`perron_frobenius`, `primitive_power_tendsto`), both carrying provisional allowlist entries pointing at the companion audit for Lean-confirmed verdicts; the matrix trio and `hoeffding_lemma` do not flag (their repaired guards recognized). Acceptance bar fully met: pre-repair `matrix_hoeffding` and `hoeffding_lemma` reconstructed verbatim from `a1e59ac~1` both flag; guard-recognition, namespace-scope (a guard inside a closed namespace cannot leak past its `end`), allowlist-expiry (removal re-flags), and transitive-inclusion (an index type entering only via a variable binder's type) fences all pass; the literal `Fin n` sample-index scoping decision is recorded in the tool's docstring, not silent. Ladder wired at all five `check_build_completeness.py` locations plus the admission-time rules (`docs/2_ARCHITECTURE.md` §5/§10, `governance/CONTRIBUTING.md`'s checklist, `AGENTS.md`, `scripts/README.md`, `opencode-pursue`, the activity-log format block's new axiom-admission rule). |
| Concentration of a Sampled Laplacian's Quadratic Form — `matrix_hoeffding`'s first theorem consumer + the degenerate-dimension repair of all three matrix concentration axioms ([`matrix-hoeffding-spectral-gap-estimation.md`](matrix-hoeffding-spectral-gap-estimation.md)) | 2026-08-28 | **Zero new axioms (count stays 10; `#print axioms` via `wip/mh_axcheck.lean`: the 17 engine declarations and all hard-crust QA exactly `propext, Classical.choice, Quot.sound`; the two Derived tails and the two axiom-instantiating QA pins honestly carrying `matrix_hoeffding` alone). Step 0's defect check found each of `matrix_hoeffding`, `matrix_bernstein`, `matrix_azuma_hoeffding` materially false at `Fintype.card V = 0`, `t = 0` — the tail event is all of `Ω` (norms nonnegative) so a probability measure gives `1` against the prefactor bound `2 · 0 · exp … = 0`, the pre-repair-`cheeger_lower_bound` failure class, i.e. each axiom as stated was inconsistent.** All three repaired in place with the `[Nonempty V]` guard (the `d ≥ 1` the cited Tropp theorems carry implicitly), with hypothesis-form refutation records (`old_matrix_*_refuted_fin0_QA`, standard three axioms only) and the `t = 0` honesty lemma; threaded through `eventStreamTail`/`sparsification_*_tail` (binder) and `eventStreamProjectorDrift` (derived internally from its own `k : Fin (card V)`, no signature change). Step 1: the new `GraphTheory/EdgePerturbation.lean` (single-edge algebra joined to the `rankOne` family by `laplacian_edgeAdj`; the PosSemidef helpers the pin lacks; the centered Bernoulli edge design `(δ_e − p_e) • L_e` with every axiom clause proved — **sign-free: no hypothesis on the weight matrix at all**, the only load-bearing clause hypothesis being `p ∈ [0, 1]`, fenced at `p = 2` and witnessed at a negative weight) and `Derived/EdgePerturbationTail.lean` (`matrix_hoeffding_quadForm` with the load-bearing `x ≠ 0` guard + the assembled tails at the classical constants, `Fin n` transport by `Fintype.equivFin`). QA +16 (2749 → 2765): the variance statistic pinned exactly (`‖∑_e L_e²‖ = 8`, rank-one norm two-sided), the all-true outcome's sum exactly the unit edge Laplacian, the closed-form tail instance `4 exp(−1/16)`, the degenerate zero-weight graph, the interval fence, the sign-free witness. **Follow-on DELIVERED 2026-08-28** (run `20260828T214358Z-run-1`, the standing handoff's named third-concentration-consumer candidate in its SGT-native member): the eigenvalue-level packaging — `edgePerturbation_eval_tail` (`μ{|λᵢ(L(A+E_ω)) − λᵢ(L A)| ≥ t} ≤ 2 d exp(−t²/(2‖∑ₑ L_e²‖))` at every sorted index, the proposal's filename namesake, the norm tail joined to the *proved* Weyl inequality), the one-sided gap-survival and λ₂ forms, and the priced uniform/existential-x quadratic-form residual — QA +13 (2830 → 2843) with the Weyl transfer pinned *tight* at a genuine design outcome (`|4 − 2| = ‖L(E_ω)‖ = 2` on K₂, both sides independent routes) and the `x = 0` guard fence refuting the un-guarded statement in proved arithmetic; zero new axioms, the same honest `matrix_hoeffding`-alone conditional structure. **Cheeger-window follow-on DELIVERED 2026-08-28** (run `20260828T230752Z-run-1`, the standing handoff's named conductance/Cheeger-level consumer of the λ₂ tail): the proved Cheeger pair transported to the combinatorial `lambda2` spelling as public API (`cheeger_lower_bound_laplacian`/`cheeger_upper_bound_laplacian` via `L = d • L_sym` and `secondEval_smul_of_pos`), composed with the λ₂ tail into the high-probability connectivity window — the floor `μ{λ₂(G_ω) ≤ d·φ²/2 − t} ≤ 2 d exp(…)` and the two-sided bracket at the same constant, both Cheeger directions load-bearing on the inclusion; QA +12 (2843 → 2855) with the ceiling attained at equality on K₂ (`λ₂ = 2 = 2·(1·φ)`) and both window sides witnessed at concrete outcomes; zero new axioms. See the proposal's Cheeger-window follow-on delivery record. **Sharpened drift interface DELIVERED 2026-08-29** (run `20260829T012051Z-run-1`, the pipeline's priced `t/δ`-sharpening): `edgePerturbation_fiedlerSubspace_drift'`/`edgePerturbation_fiedlerLine_drift'` — at `0 < s < γ ≤ λ₃ − λ₂`, `μ{‖rotation‖ ≥ s/(γ−s)} ≤ 2 d exp(−s²/(2‖∑ₑ L_e²‖))`, the `s/(γ−s)` shape of `eventStreamProjectorDrift` exactly with the gap consumed inline — the envelope-optimal instance of the `t/δ` family at every threshold; QA +10 (2855 → 2865) with the envelope arithmetic *proved* (domination + same-threshold identity, general and at the naive `(1, ½)` instance), the closed-form path instances at the join point and at threshold `2` (`6 exp(−2/27)`), and the strict-improvement pin `6 exp(−2/27) < 6 exp(−1/24)`; zero new axioms. See the proposal's sharpened-interface follow-on delivery record. **Irregular (normalized) Cheeger window DELIVERED 2026-08-29** (run `20260829T043741Z-run-1`, the same-day degree sandwich's named consumer): the window-Cheeger engine pair `dmin·φ²/2 ≤ λ₂(L) ≤ 2·dmax·φ` (`cheeger_{lower,upper}_bound_laplacian_of_degree_window`, hard crust) plus the normalized-connectivity window at the Derived layer — `edgePerturbation_normalized_cheeger_floor`/`_connectivity_bracket` with the admissibility window event-internal (`perturbAdmissible`: nonnegative resampled adjacency + degrees in the base window), the floor consuming the sandwich's lower side at the perturbed degree ceiling and the ceiling the upper side at the degree floor, no regularity anywhere; QA +16 (2902 → 2918) with the engine ceiling attained at equality on K₂, the genuinely-irregular P₃ instances, the admissibility witnesses on both sides of the window (all-false excluded by the degree floor, P₃ all-true by the degree ceiling), and the two closed-form instances (`4 exp(−1/64)`); zero new axioms — the honest `matrix_hoeffding`-alone conditional structure throughout. See the proposal's irregular-window follow-on delivery record. **Swept-Fiedler-cut consumer DELIVERED 2026-08-29** (run `20260829T060601Z-run-1`, the standing handoff's named "volume-weighted sweep extraction joined to the normalized window's floor" composition): `edgePerturbation_fiedler_sweep_cut_tail` — outside the bracket's tail set, every admissible outcome's resampled graph is *connected* and carries an explicit swept level set of its own Fiedler sweep vector at `conductance² ≤ 2·(2·dmax·φ + t)/dmin`, at the new floor-positivity guard `0 < dmin·φ²/2 − t` (the connectivity transfer's exact input — proof-load-bearing, with no fixture-scale refutation possible since the tail bound exceeds `1` on small fixtures); the window family's algorithm-facing capstone, three delivered families load-bearing on one statement; QA +4 (2918 → 2922) with the all-true K₂ good-outcome witness (connected by a raw walk, swept cut at `conductance² ≤ 2·λ₂ = 4` inside the window bound) and the closed-form K₂/P₃ instances (`4 exp(−1/4096)`, `6 exp(−1/6144)`); zero new axioms. See the proposal's sweep-cut follow-on delivery record. |
| Audit the Four Zero-Consumer Scalar Concentration Axioms for the Subgaussian Junk-Integral Hazard ([`audit-scalar-concentration-integrability-hazard.md`](audit-scalar-concentration-integrability-hazard.md)) | 2026-08-28 | **Step 0 complete on all four axioms; one materially false axiom found and repaired; zero new axioms (count stays 10; `#print axioms` via `wip/asc_axcheck.lean` on all 21 audited declarations — the 17 refutation-family QA lemmas and both safety lemmas exactly `propext, Classical.choice, Quot.sound`; the two axiom consumers honestly carry `hoeffding_lemma` alone).** `hoeffding_lemma` was **materially false in two ways beyond the two recorded junk mechanisms**: the constant `1` refuted at the fair-coin Rademacher probability fixture (`subgaussianNorm ≥ 6/5 > a = 1`, every old hypothesis genuinely satisfied — `Real.log_two_lt_d9` arithmetic, no junk involved), and *every* constant `≤ 4` refuted at the mass-19/10 rescaling of the same variable (mean still genuinely zero; threshold `1/√(log(2/M))` unbounded as `M → 2⁻`; rational arithmetic via `Real.log_le_sub_one_of_pos` + `Real.add_one_lt_exp`). Repaired in place per the subgaussian-retirement template: `[IsProbabilityMeasure μ]` + conclusion `subgaussianNorm X μ ≤ √6 * a`, the `√6` derived in the docstring from the cited λ-form (two-sided tail + layer-cake gives `E exp(X²/(6a²)) ≤ 2` exactly at `K² = 6a²`); junk-safe because both junk mechanisms collapse the norm downward and the conclusion is an upper bound. The three guarded axioms (`hoeffding_inequality`, `bernstein_inequality`, `bernstein_bounded_variance`) audited **safe**: their guard set discharges integrability, now proved on the shelf as `integrable_of_bounded_measurable` (`Hoeffding.lean`) and `integrable_sq_sub_mean` (`Bernstein.lean`) — `Integrable.mono'` at `integrable_const` under the probability hypothesis — with QA consumers at the Rademacher fixture. QA +18 (2731 → 2749 by the generator metric): the refutation family `old_hoeffding_lemma_refuted_{constant,guard}_QA`, the re-instantiation `hoeffding_lemma_rademacher_QA` (`≤ √6·1` at the very fixture that kills the old constant), the norm lower bounds, and the safety-lemma instances; `subgaussian_norm_zero_QA` carries over unchanged at `a = 0` (and its unused-instance warning *disappears* — the repair gives it a use). Records: the proposal (per-axiom verdicts + delivery record + technique findings), `prove-subgaussian-tail-bound.md`'s adjacent-hazard note resolved with the per-file guard correction, the map/source/load-bearing index rows, and the Scalar README. The matrix trio's own `MatrixMDS`-shaped junk surface stays the recorded open residual, per the proposal's Non-goals. |
| Verify Transit-Map Freshness by Reconciliation ([`verify-scaffold-map-freshness.md`](verify-scaffold-map-freshness.md)) | 2026-08-28 | **No Lean, no axioms, no QA change (counts stay 2731/10/0) — `scripts/check_scaffold_map_freshness.py` in the verification ladder: Tier 1 hard-fails any `Repo-wide: …` stats stamp in either map file that disagrees with the scoreboard's generated numbers, and any station/status/`source` divergence between the SVG generator's data table (parsed by `ast`) and the HTML's (a string-literal-aware brace scanner — a station note embeds `{2t}`, which ate a naive regex); Tier 2 reads each station's cited proposal's `**Status:**` paragraph, buckets it (gated-words precedence — exactly what keeps the mixed-phase Spielman–Srivastava/Foster file honest — then `COMPLETE`/`DELIVERED` → {proved, axiom}, then bare `Proposed` → {open, gated}, the sanctioned open/gated narrowing), and fails on a station tier outside the bucket, quoting the proposal's own status line in the finding.** The `source` schema landed in both files (32 of 36 stations linked; the four unlinked — Cauchy Interlacing, Foster's Theorem, Matrix Concentration, Consensus & Sync. — listed on every run as visible coverage gaps, with each unlinking's reason recorded). Wired at all five `check_build_completeness.py` ladder locations (`opencode-pursue`'s `verify_for_commit`, `AGENTS.md` § Verification, `docs/2_ARCHITECTURE.md` §10, `scripts/README.md`, `docs/AGENT_ACTIVITY.md`'s format block) plus report-only in `.git/hooks/pre-commit`. Acceptance bar fully reproduced against reconstructed pre-fix fixture roots: historical stats drift (9/1503 vs 10/2731) nonzero, synthetic single-file station nonzero, the Alon–Boppana gated-vs-COMPLETE regression nonzero, deleted stamp nonzero, post-fix tree exit 0. One anticipated repair dissolved on inspection (`decidable-spectral-certificates.md`'s header records COMPLETE one sentence past where the preview truncated — recorded as the same reading-error class the check exists to catch). Residual: the stamps' hand-maintained commit-SHA/date text stays unchecked by design. |
| Fiedler Subspace Stability via Davis–Kahan — **program COMPLETE, Steps 0+1+2** ([`fiedler-subspace-stability-davis-kahan.md`](fiedler-subspace-stability-davis-kahan.md)) | 2026-08-28 | **Zero new axioms at every step (count stays 10; `#print axioms` via `wip/fsd_axcheck.lean` + `wip/fsd2_axcheck.lean` on all 46 audited declarations reads exactly `propext, Classical.choice, Quot.sound`, every one — both statements consume only *proved* theorems, so the whole chain is unconditional hard crust).** Step 1: `fiedlerSubspace_stability` — `‖initialProjector (laplacian (A+E)) 1 − initialProjector (laplacian A) 1‖ ≤ ‖laplacian E‖/δ` at `3 ≤ card V` and separation `δ ≤ λ₃(L(A+E)) − λ₂(L A)`, `davis_kahan_sin_theta`'s first graph-theoretic consumer (the `DavisKahan.lean` zero-Mathlib-consumer valley closed), with engine `laplacian_zero`/`laplacian_add`/`evals_congr`/`laplacian_evals_zero` and QA +19 (exact combinatorial P₃ pins λ₂ = 1 and λ₃ = 3, the two-route zero-perturbation cross-check, the K₃ tie-witness with the hypothesis set provably empty at the `λ₂ = λ₃ = 3` tie). **Step 2, the payoff: `fiedlerLine_stability` — the Fiedler vector's own rotation** `‖(P₁' − P₀') − (P₁ − P₀)‖ ≤ ‖laplacian E‖/δ` on connected base and perturbed graphs, via the priced common-kernel identification `initialProjector_laplacian_zero_eq_of_connected` (any two connected Laplacians carry the same index-0 projector) and its engine layer in `Spectral.lean`: `eq_of_isSymm_idempotent_of_forall_mulVec_eq` (symmetric idempotents determined by fixed space — `ker P = Fix(P)ᗮ` algebraically; nothing in the pin supplies it), `spectralProjector_mulVec_eq_sum` (the projector action in its own eigenbasis), `eigvecOf_expansion`, the self-adjoint `dotProduct_mulVec_comm_of_isSymm`, and the connectivity-free fixed-space iff `initialProjector_laplacian_zero_fix_iff` — making the kernel-characterization ecosystem load-bearing on a projector-equality statement for the first time. QA +17 (2714 → 2731): the P₃ → K₃ edge-addition instance (separation `δ = 2` load-bearing against the pinned spectra; the perturbation norm pinned exactly `2` from both sides — rank-one bound and quadForm witness through the Sparsification delivery's `l2OpNorm_rankOne_le`/`abs_quadForm_le_of_l2OpNorm_le`, consumed cross-module; the derived bound exactly `≤ 1` with the perturbed side sitting on Davis–Kahan's own tie branch, zero slack), the identification instance at the genuine two-spectrum pair P₃/K₃, the fix-iff vector pins, and the disconnected fence (the empty graph's projector provably ≠ P₃'s: connectivity load-bearing, both iff directions exercised). |
| The Ramanujan Expansion Ceiling — `AlonBoppana.lean`'s first theorem consumer, the Alon–Boppana obstruction composed with the Cheeger hard direction ([`ramanujan-expansion-ceiling.md`](ramanujan-expansion-ceiling.md)) | 2026-08-27 | **Zero new axioms (count stays 10; `#print axioms` via `wip/ram_axcheck.lean` on all 13 audited declarations — 5 module + 8 QA — reads exactly `propext, Classical.choice, Quot.sound`; the ceiling consumes two *proved* theorems, so the whole chain is unconditional hard crust) — `ramanujan_expansion_ceiling`: `cheegerConstant A ≤ √(2 (1 − 2√(d−1)/d + 2√(d−1)/(d (k+1))))` under exactly `alonBoppana_nilli_classical`'s hypothesis stack, the field's textbook ceiling on expansion quality, the first shelf statement connecting the "how good can expansion be" upper-limit direction to the delivered lower-bound machinery.** The engine: `secondEval_smul_of_pos` in `Spectral.lean` (exact positive scaling by the variational route — the constraint set of `c • M` is the positive-scaled image, `sInf` commuting; the hypothesis-free generalization stays priced, `eigenvalues_smul`/`charpoly_smul` absent from the pin) plus `secondEval_congr` (the proof-irrelevance bridge for composing across equal operator spellings — the robust route around the motive-not-type-correct trap of rewriting matrix equalities under proof-carrying applications); the operator identity `smul_one_sub_eq_smul_regularNormalizedLaplacian` (`d • 1 − A = d • L_sym` under regularity, the missing `degreeMatrix = d • 1` half) and the ceiling in `AlonBoppana.lean`'s new `ExpansionCeiling` section (imports `Cheeger`, no cycle). QA +7 (2659 → 2666): the K₂ scaling pins by two independent routes (raw trace/determinant/sortedness `[0, 6]` vs the lemma joined to the on-file `λ₂ = 2` — a wrong scaling constant breaks exactly one) with the negative-`c` fence (sorted spectrum `[-2, 0]`, `0 ≠ -2`: the `0 < c` hypothesis load-bearing), and the C₈ ceiling QA (the instance evaluating numerically to `√2` at `k = 0`; the fixture's actual Cheeger constant pinned `≤ 1/4` by the exhibited half-set cut — the independent cut-level route, the ceiling non-vacuous with strict slack `1/4 < √2`; the two-`k` improvement as arithmetic on the statement's own constants at `d = 2` with the honest C₈-hosts-only-`k = 0` note per the on-file far fence). **Obstruction scope only — no tightness or attainment claim anywhere** (the proposal's hard acceptance-bar item; tightness is the separate Ramanujan-graph construction program). |
| The Alon–Boppana Bound for d-Regular Graphs — **program COMPLETE, Steps 0–5** ([`alon-boppana-bound.md`](alon-boppana-bound.md)) | 2026-08-26/27 | **Zero new axioms at every step (count stays 10; each step's delivery record records its `#print axioms` audit reading exactly `propext, Classical.choice, Quot.sound`) — the field's most conspicuous gap next to the Cheeger/expander toolkit closed as pure hard crust via Nilli's two-edge variational method: `alonBoppana_nilli`, `secondEval (d•1 − A) ≤ d − (1 + 2k√(d−1))/(k+1)` under two far-apart full-BFS-tree-ball edges at radius `k+1`, with the classical error shape `alonBoppana_nilli_classical` (`≤ d − 2√(d−1) + 2√(d−1)/(k+1)`), the far-apart-to-diameter bridge `distEdge_le_diam`, and the honest hypothesis-side diameter bookkeeping `alonBoppana_diam_ge` (`k+1 ≤ ⌊diam/2⌋` — the tree-ball hypothesis never derived from the diameter, per the proposal's own qualification-trap section).** Built across six deliveries in `GraphTheory/AlonBoppana.lean` + `AlonBoppana_QA.lean` (Steps 0+1 the tree-ball spike and d-regularity interface; Step 2 the `levE`/`levClass`/`ballE` BFS machinery and `IsTreeBall` cardinality predicate; Step 3a the radial test vector `ρ^{lev}` with the exact squared-norm identity `2(k+1)`; Step 3b the from-below numerator harvest `2 + 4k(d−1)ρ ≤ xᵀAx`; Step 4 the two-vector orthogonalization and the first eigenvalue-level statement through `secondEval_le_rayleigh` at `d•1 − A`; Step 5 the `√` packaging, the diameter bridge — the Step-2 priced `ediam ≠ ⊤` blocker dissolved via `exists_edist_eq_ediam_of_finite` — and both priced QA residuals: the loop-pair fence isolating `IsTreeBall` at a loop's one-element level 0, and the independent integer-witness engine route reaching `secondEval (2•1 − C₈) ≤ 2/3` — strictly stronger than the theorem routes' `≤ 1` — at `![1,1,0,−1,−1,−1,0,1]` with fully raw arithmetic). QA 2506 → 2585 across the program (C₈ the full-hypothesis positive at every step, pinned by two independent routes wherever a raw route exists; the C₄ wrap-around, K₃,₃-style overlap, `hfar`-threshold, `hedge`/`h01`/`hxy`, `hd1`, loop-pair, and pseudo-edge fences isolating each hypothesis where it enters). The deferred items (the asymptotic family corollary — needs a named graph family; Route B's walk-counting) stay deferred on the proposal's own record. |
| The ρ_k Partition-Minimum Packaging — the classical λ_k ≤ 2ρ_k statement form at the delivered every-family certificate; the multiway delivery's top priced follow-on ([`multiway-expansion.md`](multiway-expansion.md), the follow-on delivery record) | 2026-08-26 | **Zero new axioms (count stays 10; `#print axioms` via `wip/rho_axcheck.lean` on all 34 audited declarations — 9 public + 25 QA — reads exactly `propext, Classical.choice, Quot.sound`) — `cheeger_upper_bound_multiway_rhoK`: on every symmetric nonnegative positive-degree graph, `evals (L_sym) ⟨k−1⟩ ≤ 2 · multiwayExpansion A k` whenever `2 ≤ k ≤ card V` and a k-way partition exists (supplied at `k ≤ card V` by `exists_isMultiwayPartition_of_le_card`).** The packaging's only new content is attainment: the value set is finite (a subset of the range over the Fintype of k-part families), so `Set.Nonempty.csInf_mem` realizes the `sInf` at an actual partition, and the delivered every-family conductance theorem applies at the minimizer — no new engine. QA +22 (2422 → 2444, the file 73 → 95 by the generator metric): the C₄ second-eigenvalue pin **λ₂ (L_sym C₄) = 1** with both sides independent (the counting bridge at two raw eigenvectors for `≤ 1`; the `(x₀+x₂)²` sum-of-squares through `secondEval_variational_of_ker` at `√D·1` for `≥ 1`), the star instance **ρ₂(C₄) = 1/2 exact** (adjacent-pair witness above; the headline joined to the pinned λ₂ below — attainment falsifiable, not decorative), the minimum provably beating the diagonal partition's `1` (`ρ₂ = 1/2 < 1`), `ρ₂(K₂) = ρ₃(P₃) = 1` at the pre-existing independent top-eigenvalue pins, and the **empty-set junk fence** (no 3-partition of two vertices exists — a chosen point per part is an injection `Fin 3 ↪ Fin 2` — so `ρ₃(K₂) = sInf ∅ = 0`: partition-existence load-bearing). |
| Multiway Expansion — the Higher-Order Cheeger Easy Direction, every-family form; radar axis 4's previously-absent category gains its first occupant ([`multiway-expansion.md`](multiway-expansion.md)) | 2026-08-26 | **Zero new axioms (count stays 10; `#print axioms` via `wip/mw_axcheck.lean` on all 33 audited declarations — 15 public + 18 QA — reads exactly `propext, Classical.choice, Quot.sound`) — `cheeger_upper_bound_multiway`: for every disjoint nonempty k-family on every symmetric nonnegative positive-degree graph, `evals (L_sym) ⟨k−1⟩ ≤ 2 · maxᵢ boundary(Sᵢ)/vol(Sᵢ)`, with the conductance corollary `cheeger_upper_bound_multiway_conductance` at `2 ≤ k`.** The Step-0 verdict (recorded before any shelf Lean) dissolved both previously priced obstructions at the **every-family statement form**: no partition-space attainment is needed (any disjoint family certifies; the ρ_k minimum stays a follow-on), and cross-part energy is *absorbed* — not eliminated — pointwise by `(a−b)² ≤ 2a² + 2b²` at the theorem's constant exactly 2 (the prior centering concern was an artifact of reusing the k = 2 kernel-based engine; the subspace engine's dimension count replaces the constraint). Two k-general engine pieces in `Spectral.lean`, both reusable beyond this theorem: the order-statistics↔counting bridge `evals_le_of_card_eigvalOf_le` (the shelf had only the two endpoints) and the general-k subspace Rayleigh–Ritz engine `evals_le_of_linearIndependent` (no PSD, no kernel hypothesis; `LinearMap.ker_ne_bot_of_finrank_lt` at the k−1 orthogonality constraints), plus the QA spin-offs `evals_sum_eq_trace` and `exists_eigvalOf_eq_of_mulVec_eq_smul`. The application layer in the new `Multiway.lean`: the per-part indicator energy identity `quadForm_laplacian_partIndicator`, the weighted-norm identity, and the absorption lemma `laplacian_quadForm_multiwayCombination_le`. QA +73 (2349 → 2422, `MultiwayCheeger_QA` a new file): tight `k = n` equalities on K₂ (`2 = 2·1`, both forms, against the raw anti-aligned pin) and the all-rational C₄ (`λ₄ = 2 = 2·1` by alternating vector + trace-`4`), the P₃ non-covering family (every-family scope exercised), the P₃ `k = 3` partition with `λ₃ = 2` pinned by trace arithmetic + raw eigenvector, the `k = 1` edge, absorption at *equality* on both fixtures, and the C₄ cyclic-pair **overlap fence** (every other hypothesis verified, disjointness refuted, conclusion refuted at `2 > 1`). Delivered across three sessions (opening run recorded the Step-0 verdict + spike; continuation landed the Lean; a third run re-verified from scratch and closed the records — the elaboration, axcheck, full build + 109/109 completeness, linters, and scoreboard idempotence all re-run by the closing run). Priced follow-ons: the ρ_k packaging, the multiway hard direction. |
| The Irregular Fiedler Instantiation — the family's algorithm-facing capstone; the sweep extraction's first eigenvector input ([`irregular-cheeger-variational-transfer.md`](irregular-cheeger-variational-transfer.md), the follow-on delivery record) | 2026-08-26 | **Zero new axioms (count stays 10; `#print axioms` via `wip/ifc_axcheck.lean` on all 23 audited declarations — 14 public + 9 QA — reads exactly `propext, Classical.choice, Quot.sound`) — the irregular Cheeger family is now algorithm-facing end to end: on every connected symmetric nonnegative positive-degree graph, an explicit closed superlevel/sublevel cut of the sweep vector itself satisfies `conductance S² ≤ 2 λ₂ (L_sym)`, exactly the regular family's `fiedler_sweep_cut` constant `2λ₂/d` on the cone.** The interface layer (`VariationalTransfer.lean`'s new "irregular Fiedler instantiation" section; no new imports, no umbrella change): `fiedlerIndexNormalized`/`fiedlerVectorNormalized` (the `Fiedler.lean` pattern at `L_sym`: eigen equation, unit norm, `quadForm`/`rayleigh = λ₂`) and `fiedlerSweepVector := D^{-1/2} u` (the pullback the spectral-partitioning algorithm actually sorts) with the stretch cancellation `√D f = u`, nonvanishing, and the constraint conversion — the sweep family's own hypothesis `∑ deg · f = 0` *obtained* from the eigen-orthogonality hinge `eigvecOf_ortho_of_mulVec_eq_zero` at the true kernel vector `√D·1` (`0 < λ₂` from the delivered connectivity transfer: connectivity's exact entry point). The headline composes `cheeger_sweep_cut_normalized` at its first eigenvector input with `fiedlerVectorNormalized_rayleigh` closing the spectral side at the eigenvector. QA +9 (2340 → 2349): the exact pin `λ₂ (L_sym P₃) = 1` (the new `≥ 1` side a `2 x₁²` sum-of-squares through `secondEval_variational_of_ker`, nonemptiness witnessed by the concrete eigenpair at Rayleigh exactly `1`; the `≤ 1` side predates the family — non-circular, and the family's Cheeger bracket on `P₃` collapses to `1/2 ≤ 1 ≤ 2`), the instances on `P₃` and `K₂` at independent pins, the pullback hinge raw at the concrete eigenpair, and the connectivity mechanism fenced on the disconnected fixture (at the pinned `λ₂ = 0`, a kernel eigenvector provably violates the hinge — kernel-ness does not supply the constraint; the gap does). |
| The Volume-Weighted Sweep-Cut Extraction — the irregular family's own `sweep-cut-extraction` port; **the regular family's attainment route at the volume measure** ([`irregular-cheeger-variational-transfer.md`](irregular-cheeger-variational-transfer.md), the follow-on delivery record) | 2026-08-26 | **Zero new axioms (count stays 10; `#print axioms` via `wip/vsc_axcheck.lean` on all 13 audited declarations — 2 public + 11 QA — reads exactly `propext, Classical.choice, Quot.sound`) — the irregular Cheeger pair now returns *the cut the spectral-partitioning sweep returns*, not just a bound on the conductance infimum, on every symmetric nonnegative positive-degree graph.** `sweep_level_extract_vol` (the new `VolumeSweepExtraction` section of `Cheeger.lean`): for any `y` with volume-minority closed superlevel sets, a positive level `t` with `S = {i : t ≤ y²}` nonempty, proper, and `conductance S² ≤ E'(y) / ∑ deg y²` — the `Finset.exists_min_image` attainment route at the `boundary / vol` ratio (the card denominator replaced by the level set's volume, positivity from `vol_pos_of_pos_deg`), the degree-weighted layer-cake a structural clone of `coarea_core_vol`'s proof, closed by the already-degree-weighted Component A and the minority-volume conversion `min (vol S) (vol Sᶜ) = vol S` (pure `vol_compl` arithmetic — no `vol_eq_of_regular` anywhere), with no `2 ≤ card V` hypothesis (minority at a nonempty level forces the complement nonempty). `cheeger_sweep_cut_normalized` (`VariationalTransfer.lean`, placed with the family): every nonzero degree-weighted zero-sum `f` has a nonempty proper closed superlevel or sublevel cut of `f` itself at `conductance S² ≤ 2 · R_{L_sym}(√D f)` — the volume median feeding the extraction's hypothesis verbatim for both parts, the product test with degenerate single-part cases, the fused contraction consumed verbatim (already degree-weighted), the weighted norm split, and the `2 · R = E'/∑ deg f²` normalization; exactly the hard direction's own constraint shape (no regularity, no connectivity, no cardinality). The whole delivered `VolumeHardDirection` layer consumed a second time — it now carries weight from two theorem families. QA +10 (2330 → 2340, `IrregularCheeger_QA` 166 → 176): the extraction witness *forced* to `{0}` on the irregular `P₃` fixture through its level-membership iff (conductance `1` from the exhaustive-cut pin, the bound `1 ≤ 2/1` both sides raw); the sweep instances on the pair's shared `P₃` cut test vector (family characterized in-instance, `1 ≤ 8/3` at the pinned `R = 4/3` — one test object now consumed by both Cheeger directions *and* the sweep) and on `K₂` (`1 ≤ 4` at the pinned `R = 2`); and the degree-weighted zero-sum fence in proved form (`f = ![1,2]` on `K₂`: `∑ deg · f = 3 ≠ 0`, exactly two nonempty proper swept members each of conductance `1`, demanded bound `1 ≤ 2/5` with `R = 1/5` computed raw — refuted for *every* candidate, `horth` isolated exactly). Remaining priced follow-on on the axis: multiway expansion (radar axis 4's absent category). |
| Resolvent identity + general-symmetric layer — the two priced follow-ons of the consumer stub, **proposal family now fully closed** ([`hermitian-calculus-consumer-tikhonov-heat.md`](hermitian-calculus-consumer-tikhonov-heat.md)) | 2026-08-25 | **Zero new axioms (count stays 10; `#print axioms` via `wip/resid_axcheck.lean` on all 17 audited declarations — 6 public + 11 QA — reads exactly `propext, Classical.choice, Quot.sound`) — the Tikhonov filter IS `π` times the shifted inverse, and the textbook solve `x* = π•((L+πI)⁻¹ y)` is a shelf theorem.** Two independent proof technologies: the **matrix-algebra route** (the delivered normal equation + the Aug-19 resolvent program's spectral-gap-free `isUnit_det_add_smul_one_of_quadForm_nonneg` — form-PSD plus `0 < π` makes the shift nonsingular with no eigenvalue gap; `Analysis/OperatorTheory/Resolvent.lean`'s first FunctionalCalculus consumer, so that module now carries weight from a second module — + `laplacian_psd`, closed by `nonsing_inv_mul_cancel_left`) and the **calculus route** at strictly greater generality (the *general-symmetric* identity `f(M) = π•(M+π•1)⁻¹` for a merely symmetric `M` under *spectrum-avoidance* `x + π ≠ 0`, through Mathlib's `cfc_inv` + `Matrix.nonsing_inv_eq_ring_inverse` at the additive layer — no determinant or cancellation anywhere); the delivered PSD normal equation re-derived from its general parent, statement shape unchanged. QA +11 (2238 → 2249, Section G on the shared `K₂` fixture): the raw inverse `!![2/3,1/3;1/3,2/3]` pinned, **both routes delivering that same matrix** (a wrong `cfc_inv` specialization or junk-inverse misalignment breaks one while the other stands), the minimizer three-way join (resolvent form vs hand-solved Gaussian `tik_K2_eq` vs Section E's calculus instance — three constructions, one number), the supplier witnessed (`det = 3`), and the **two fences at `π = -2`** — the avoidance failure *proved spectral* (`2 = -π` exhibited as an eigenvalue), the singular inverse pinned to junk zero, the identity refuted at `1/2 ≠ 0`: exactly `hπ` isolated for the headline, exactly avoidance for the general theorem. |
| A Genuinely Complex Magnetic-Laplacian Calculus Consumer — **proposal COMPLETE, Steps 0+1 in one run** ([`hermitian-calculus-consumer-magnetic.md`](hermitian-calculus-consumer-magnetic.md)) | 2026-08-25 | **Zero new axioms (count stays 10; `#print axioms` via `wip/magcfc_axcheck.lean` on all 30 audited declarations — 8 public + 22 accessible QA — reads exactly `propext, Classical.choice, Quot.sound`, every one) — the bridge's first complex consumer and the shelf's first complex calculus instance: `magneticHeat A Θ t`, the calculus of the delivered `magneticLaplacian` at `x ↦ e^{-t·x}` through Mathlib's `RCLike`-generic `cfc` at 𝕜 = ℂ directly (no second wrapper, the bridge's Step-0 verdict), hypothesis-free for any directed weights and any phases.** The substantive general lemma: **`cfc_mulVec_eq_smul_of_mulVec_eq_smul`** — the calculus acts at eigenvalues on EVERY eigenvector (`M *ᵥ x = (μ : 𝕜) • x → hM.cfc f *ᵥ x = (f μ : 𝕜) • x` at a Hermitian `M` and real `μ`), proved by pure matrix algebra through the unitary diagonalization `Uᴴ * M * U = diagonal λ` with no eigenspace-completeness machinery — the interface every downstream complex consumer needs and Mathlib's CFC file lacks. The interface layer (a new section of `GraphTheory/FunctionalCalculus.lean`; `Magnetic.lean` untouched per the consumer pattern): the entry form (the falsifiability anchor), the arbitrary-eigenvector action, the basis action (the proposal's literal ask), time zero, and the semigroup through the generic calculus algebra (`cfc_mul`/`cfc_congr` at `Real.exp_add`, the complex instantiation of the delivered `spectralCalc_exp_mul`). QA +22 (2216 → 2238, `MagneticCalculus_QA` a new file): the flux pair `K₂` at antisymmetric phase π/2 (`M = !![1, −I; I, 1]`, conjugate-pair off-diagonals pinned raw, Hermitian on directed-flux input); the kernel vector `![I, 1]` cross-checked against the delivered gauge characterization by two routes; the frustrated mode `![I, −1]` at λ = 2; the closed form `½ !![1+q, I(1−q); −I(1−q), 1+q]` at `q = e^{-2t}` **derived on the calculus route** (public action theorem + the spanning-recovery helper) and **verified by raw matrix arithmetic**; the diffusion fence `magneticHeat … 1 ≠ 1` (`e^{-2} < 1` forces the off-diagonal nonzero — phase frustration provably moves mass); and **the zero-phase join** — at vanishing flux the magnetic propagator IS the complexification of the delivered real `heatKernel`, Mathlib's `cfc` against `Heat.lean`'s exponential series, two independent proof technologies agreeing on one operator. Non-goals held: no magnetic Cheeger/synchronization/eigenvalue-gap content. |
| Recovering Tikhonov as a Functional-Calculus Instance — **proposal COMPLETE, both halves delivered 2026-08-25** ([`hermitian-calculus-consumer-tikhonov-heat.md`](hermitian-calculus-consumer-tikhonov-heat.md)) | 2026-08-25 | **Zero new axioms (count stays 10; `#print axioms` via `wip/tikcfc_axcheck.lean` on all 19 new declarations — 3 public + 16 QA — reads exactly `propext, Classical.choice, Quot.sound`) — the bridge's named falsifiability test at a real consumer, chosen over Heat by the parent proposal's recorded Step-0 pricing (Tikhonov's reconciliation gap smaller).** Two layers in `GraphTheory/FunctionalCalculus.lean`'s "Recovered instances" section (`Tikhonov.lean` untouched per the non-goals): the **hypothesis-free equality theorem** `tikhonovMinimizer A hA π y = spectralCalc (laplacian A) hL (tikhonovShrinkage π) *ᵥ y` (the eigenbasis-defined minimizer IS the calculus at `π ↦ π/(λ+π)` — the bridge's action form was stated in exactly the minimizer's filter-sum shape, so the proof is the definition identity, the reconciliation gap confirmed as priced) plus the public helper `continuousOn_of_finite_real_spectrum` (every function continuous on a matrix's finite real spectrum — the supplier future CFC consumers need, since `cfc_cont_tac`'s `fun_prop` cannot discharge spectrum-restricted continuity); and the substantive second layer, **the normal equation `(L + π•1) * f(L) = π • 1` re-derived through Mathlib's generic CFC algebra** (`cfc_add_const`/`cfc_id'`/`cfc_mul`/`cfc_congr`/`cfc_const` via `Matrix.IsHermitian.cfc_eq`; the pointwise `shrink π λ · (λ+π) = π` promoted from the spectrum by `cfc_congr`, PSD + `0 < π` keeping the division non-junk at spectral points) — an eigenbasis-free route to a statement the shelf had only through the eigenbasis expansion: two routes, one nontrivial statement. QA +16 (2189 → 2205, `FunctionalCalculus_QA.lean` Section E on the `Tikhonov_QA` K₂ fixture): the sign-free structure pins (kernel outer products `1/2`, `λ = 2` outer products `±1/2`), the arbitrary-`f` master entrywise lemma, the calculus instance pinned to `!![2/3, 1/3; 1/3, 2/3]`, the **reconciliation witnessed numerically** (the calculus routes `![1,0]` to the hand-solved `![2/3, 1/3]` of `tik_K2_eq` — two constructions, one number), the normal equation by two independent routes plus a raw matrix check, the kernel-mode action instantiation, and the **fence at `π = -2`**: the junk shrinkage at the spectral point `2 = -π` drives the hypothesis-free normal equation to a proved refutation — `0 < π` load-bearing. |
| A Scaffold–Mathlib Hermitian Functional-Calculus Bridge — **proposal COMPLETE, Steps 0+1 in one run** ([`hermitian-functional-calculus-bridge.md`](hermitian-functional-calculus-bridge.md)) | 2026-08-25 | **Zero new axioms (count stays 10; `#print axioms` via `wip/hfc_axcheck.lean` on all 25 audited declarations — 7 public + 18 QA — reads exactly `propext, Classical.choice, Quot.sound`, every one; zero contact with any of the ten admitted axioms, machine-checked).** The Step-0 verdict (recorded before any shelf Lean): ONE `cfc` definition covers real-symmetric and complex-Hermitian via `[RCLike 𝕜]` — no API split to bridge; `Matrix.IsHermitian.cfc` takes a bare `ℝ → ℝ` function with NO continuity hypothesis (finite spectrum); eigenbasis reconciliation free (`eigvecOf` IS `eigenvectorBasis` coerced). The new `GraphTheory/FunctionalCalculus.lean`: the thin wrapper `spectralCalc M hM f := (isHermitian_of_isSymm hM).cfc f`; the hypothesis-free eigenvector action `f(M) *ᵥ vₖ = f (λₖ) • vₖ`; the three-layer equality with the shelf's filter-sum expansion (`spectralCalc_apply` entry form, `spectralCalc_mulVec_apply` action form, and `dotProduct_eigvecOf_spectralCalc_mulVec` consuming the Tikhonov workhorse **verbatim**); the recovery of the hand-built `spectralProjector` as the calculus at indicator functions; `spectralCalc_id`. QA +16 (2173 → 2189, `FunctionalCalculus_QA` a new file): all three mandated obligations — two API paths to one value on `diag13` (arbitrary-`f` entry-form computation vs the projector route through the recovery theorem, plus the f-dependence fence `3 ≠ 9`), the non-basis eigenvector action (generic `vᵢ+vⱼ` linearity + the `![1,1]` action by two independent routes), the repeated-eigenvalue boundary on `2 • 1` (two genuinely independent routes: entry form + completeness with no basis choice anywhere; raw unitary conjugation with no completeness anywhere) — plus the complex-half elaboration witness `fcM2c_cfc_id` (`cfc` at `𝕜 = ℂ` through the generic `cfc_id'`), discharging the gated magnetic consumer's Step-0 precondition. Found untracked (with its two gated consumer stubs) per the previous run's standing handoff, indexed as the table's only Medium row, pursued once its own gating condition (an Active table empty of higher rows) held. |
| The Hoffman-Type Independence Number Bound — **proposal COMPLETE, Steps 0+1 in one run** ([`expander-independence-number-bound.md`](expander-independence-number-bound.md)) | 2026-08-25 | The Expander Mixing Lemma's **first theorem consumer**: `hoffman_independence_bound`, the classical Hoffman ratio bound `|S| ≤ μ·n/(d+μ)` for every independent set at exactly the lemma's own `μ` hypothesis, with `IsIndependentSet` (diagonal-inclusive — the reading that collapses `edgeWeight A S S = 0`), the collapse lemma, and the deg-univ corner fence, all in `GraphTheory/Expander.lean` §8. Both Step-0 questions answered as *proved* facts: the whole-graph corner fenced on `C₄` (`2 = 0` refutation), `0 < d` shown load-bearing by the `2×2` zero-adjacency isolation (every other hypothesis verified, `μ = 0` exact via eigen-action spectrum pins, the division-form conclusion refuted). QA +21 in `Expander_QA.lean` Step 5: `C₄`'s opposite pair and `K₃`'s singleton both **attain the bound with equality** at the exact Step-4 `μ` pins (the bipartite and clique tight cases); the `∅`, whole-graph, non-independent (mechanism-blocked: internal cut weights `8 ≠ 0` and `2 ≠ 0`), and `d = 0` fences. Pure hard crust, zero new axioms (count stays 10; all 26 new declarations at the standard three); QA 2152 → 2173. Found committed-but-unindexed (`4e60ce6`), triaged per the previous run's standing handoff, indexed as the table's only High row, pursued per priority item 0. |
| The Irregular Cheeger Hard Direction — the volume-weighted coarea/median program; **the proposal COMPLETE, both halves delivered** ([`irregular-cheeger-variational-transfer.md`](irregular-cheeger-variational-transfer.md)) | 2026-08-25/26 | **Zero new axioms (count stays 10; `#print axioms` via `wip/ich_axcheck.lean` on all 40 audited declarations — 16 public + 24 QA — reads exactly `propext, Classical.choice, Quot.sound`) — the deferred hard half delivered, and with it the full Cheeger pair `φ²/2 ≤ λ₂(L_sym) ≤ 2φ` holds in the volume-weighted measure on every symmetric nonnegative positive-degree graph: no `d`-regularity, no connectivity, the family's last regular-only caveat retired.** Opened by run `20260825T221207Z-run-1` (intent, Step-0 survey, full Lean drafted), verified and recorded by continuation `20260826T010711Z-run-1` after the opening session was interrupted pre-verification. Three layers: the **volume machinery** (the new `VolumeHardDirection` section of `Cheeger.lean`: the volume median `exists_median_vol` — the same maximizing-vertex/minimal-member Finset argument with `vol` replacing `card`; minority conductance at volume strength where `min (vol S) (vol Sᶜ) = vol S` is pure `vol_compl` arithmetic, no `vol_eq_of_regular`; the degree-weighted layer cake `coarea_core_vol`; the per-part bound — the decisive Step-0 finding holding: `core_sum_abs_sq_sub_sq` and the fused contraction are *already degree-weighted* in the regular file and are consumed verbatim, no regularity bridge anywhere; the weighted norm split), the **general-kernel sInf engine** `secondEval_variational_of_ker` (`Spectral.lean`: Courant–Fischer at an arbitrary kernel vector `w` — the delivered `secondEval_le_rayleigh_of_ker` is exactly its hard half, reused; the new witness half producing an orthogonal candidate at both `0 < λ₂` and the `λ₂ = 0` double bottom — every future irregular consumer, whose operator is killed by `√D·1` not `1`, shares it), and **the sweep + headline** in `VariationalTransfer.lean`: `cheeger_sweep_normalized` (every nonzero degree-weighted zero-sum `f` has `φ²/2 ≤ R_{L_sym}(√D f)`) and `cheeger_lower_bound_normalized` (the sInf engine at the true kernel vector `√D·1` reduces the spectral claim to the sweep lemma; the sInf set's nonemptiness witnessed by the easy direction's own cut test vector — one test object, both directions of the pair). QA +31 (2249 → 2280, the file 85 → 116): the P₃ volume-median pins, the K₂ coarea equality both raw and as a theorem instance, the sweep instances on K₂ *and* on P₃'s cut test vector (`1/2 ≤ 4/3` — the same object the easy direction consumed), **the headline joined to the easy delivery's independently pinned spectral bracket** (`φ²/2 = 1/2 ≤ λ₂` with the eigenpair-witness `λ₂ ≤ 1` — non-circular), the K₂ regular recovery at `λ₂ = 2` raw, and three proved-form fence families (minority-drop on the coarea and boundary lemmas at the full vertex set; the nonnegativity-drop on the headline at the signed adjacency `!![2,−1;−1,2]]` with `cheegerConstant = −1` and `λ₂` pinned — exactly `hnn` isolated). Priced follow-ons recorded in the proposal: the connectivity-free `λ₂ > 0` transfer for `normalizedLaplacian`, the volume-weighted sweep-cut extraction, multiway expansion. |
| The Irregular Cheeger Inequality via Variational Transfer — **easy direction COMPLETE, Steps 0+1 in one run** ([`irregular-cheeger-variational-transfer.md`](irregular-cheeger-variational-transfer.md)) | 2026-08-25 | **Zero new axioms (count stays 10; `#print axioms` via `wip/icv_axcheck.lean` on all 36 new declarations — 10 public + 26 QA headline — reads exactly `propext, Classical.choice, Quot.sound`) — the standing "regular graphs only" caveat on the Cheeger upper bound is removed: `secondEval (normalizedLaplacian A) ≤ 2 * cheegerConstant A` on arbitrary symmetric nonnegative positive-degree graphs, no `d`-regularity, no connectivity.** `GraphTheory.VariationalTransfer` — built for exactly this consumer ("statement shapes for a future irregular Cheeger family") and until now carrying zero theorem consumers — gets its first three: the congruence engine supplies the energy transfer, `normalizedLaplacian_psd` the PSD hypothesis, and the module hosts the headline. The Step-0 survey's decisive finding: no new definitions were needed (`vol`/`boundary`/`conductance`/`cheegerConstant` already volume-general; `cutTestVector`'s energy identity already regularity-free) and the degree-weighted sum of the *existing* cut indicator vanishes without regularity (`vol S · vol Sᶜ − vol Sᶜ · vol S = 0`), so the irregular test object is the degree-stretched vector `√D *ᵥ cutTestVector A S` — orthogonal to the normalized Laplacian's *actual* kernel vector `√D · onesVec`, which is why the one genuinely new engine lemma was the general-kernel `secondEval_le_rayleigh_of_ker` in `Spectral.lean` (the delivered onesVec form provably cannot consume this test object: QA pins `dotProduct z onesVec = 2 − √2 ≠ 0` on P₃). The Rayleigh quotient is the *same* `boundary · vol V / (vol S · vol Sᶜ)` as the regular family — closing arithmetic and infimum pass verbatim. QA +85 (2067 → 2152, `IrregularCheeger_QA` a new file): the P₃ fixture with the conductance computed by hand, the energy pinned by three independent routes, the spectral side bounded independently through the eigenpair witness `![1,0,−1]` (`λ₂ ≤ 1`, non-circular vs the theorem's `≤ 2`), the K₂ tight regular recovery, and two proved-form fences (PSD-drop on the engine at `diag(−1,0)`; degree-drop on the headline at the all-zero adjacency where `λ₂ = 1 > 0 = 2·cheegerConstant`). The irregular *hard* direction was priced as its own multi-step program per the proposal's own authorization — **delivered 2026-08-25/26, see the row above**. |
| Verify Build Completeness by File Reconciliation, Not Exit Status Alone — **proposal COMPLETE, Steps 0+1 in one run** ([`verify-build-completeness.md`](verify-build-completeness.md)) | 2026-08-25 | **`scripts/check_build_completeness.py` delivered and wired into all five documented verification-ladder locations (AGENTS.md § Verification, `opencode-pursue`'s `verify_for_commit` after `lake build`, `scripts/README.md`, architecture §10, the activity-log format block); no Lean, no axioms, no QA declarations.** Fails nonzero when any `Scaffold/**/*.lean` source (plus the root umbrella) has a MISSING or mtime-STALE `.olean` artifact. Semantics calibrated by two decisive experiments on the pinned toolchain: a content-changed source is rebuilt/rewritten by an explicit target, but a pure `touch` of an up-to-date source is a no-op (Lake's up-to-date check is content-hash based) — so STALE fails closed with an always-terminating remediation documented in the docstring (direct rebuild; else remove the gitignored artifact and rebuild to force re-elaboration). **On its first run against the real tree the check caught 8 live findings: two full QA modules with no artifact at all (`ProjectionGap_QA`, 586 lines; `Expander_QA`, 1,181), an unimported legacy shim (`Mathlib/Core.lean`), a genuine never-rebuilt content edit (`Dynamics_QA`: 08-20 source, 08-18 artifact — the incident class caught live, not reconstructed), two more stale QA modules, and two benign byte-identical roundtrips — all remediated by real elaboration; the tree now reads 103/103 fresh, 0 stale, 0 missing, exit 0, immediately after a full `lake build`.** The acceptance bar's incident reconstruction (a three-error `Magnetic_QA.lean` with no artifact) passes as a synthetic regression: flagged MISSING, exit 1; healed tree exits 0; usage/bad-root exit 2. |
| The Magnetic Laplacian — the Directed-Native Hermitian Operator — **proposal COMPLETE, Steps 0+1 across two runs** ([`magnetic-laplacian.md`](magnetic-laplacian.md)) | 2026-08-25 | **Zero new axioms (count stays 10; `#print axioms` via `wip/mag_axcheck.lean` on all 39 accessible declarations reads only `propext, Classical.choice, Quot.sound`) — the shelf's first complex-valued object and the directed axis' third spectral toolkit: `magneticLaplacian A Θ := D_sym − ½(W + Wᴴ)` with `W := A ∘ e^{iΘ}` entrywise (the Crucoli–Pérez–Bungert–Van Mieghem directed convention), Hermitian by construction hypothesis-free for any real possibly-asymmetric weights and any real phases, the single-`W` classical form *derived* on the `A.IsSymm ∧ Θᵀ = −Θ` cone.** The layer every consumer needs and nothing beyond it: **the magnetic energy identity** `x*Mx = ½ ∑ A_uv \|x_u − e^{iΘ_uv} x_v\|²` (hypothesis-free — the finite-algebra Dirichlet principle; the diagonal splits into out-/in-degree halves, the cross sums matched termwise and swap-conjugated), PSD on nonnegative weights, and **the balanced-potential gauge characterization** `x*Mx = 0 ↔ x_u = e^{iΘ_uv} x_v` on every positive edge — a frustrated cycle admits no nonzero aligned potential, forcing the form positive definite (**spectral localization of flux at form level, no complex spectral theorem anywhere**); plus the zero-phase agreement with the complexified classical Laplacian of the symmetrized weights (on symmetric `A`, of `laplacian A` itself — the join with the real shelf). QA +16 (2051 → 2067, `Magnetic_QA` a new file at 16 by the generator metric, 30 declarations total): the asymmetric-flux fixture pinning `W`'s raw entries (`−2`/`I`) and the operator's conjugate-pair off-diagonals `1 ± I/2` with Hermitian verified on directed input and the energy identity pinned at `5`; the **frustrated-vs-consistent kernel pair** (the same `π` flux forcing the triangle's kernel trivial while leaving `K₂`'s antipodal phase potential in the kernel, verified through the iff and cross-checked by the raw vanishing energy — the characterization exhibited *discriminating*); the classical bridge (constants in the zero-phase kernel, the antipodal vector outside it at energy exactly `4`); and the **nonnegativity fence** (symmetric signed input refutes PSD in proved form at `−1 < 0` — exactly `hA` isolated). Run `20260825T001310Z-run-1` drafted the module+QA and recorded Step 0 but was interrupted before verification; continuation run `20260825T021040Z-run-1` verified both (the module green as drafted; the QA's three first-pass errors fixed in place — two redundant match alternatives and one false numeric pin, the zero-phase energy stated `2` where `norm_num` correctly forced the true `4`, the numeric-QA failure mode doing its job), wired the umbrella import, ran the full battery (`lake build` root ✔ 2264 targets; explicit targets ✔; `lint_axioms` 10 unchanged; citations and links pass; scoreboard regenerated idempotent 2067/10/0), and completed the records. Route provenance (LAA 2023, arXiv:2202.02497 §2) in the module docstring; the statements are proved, so no axiom citation applies. Priced follow-ons: the complex spectral layer (eigenvalues of `M`, magnetic Cheeger — gated on a consumer naming a bound), the module-level gauge transport at `Θ = 0`, and the synchronization functionals (the community-detection consumer itself). |
| Primitive Power Convergence — the Directed Mixing Gate Opened by Admission; the PageRank Power Iteration — **proposal COMPLETE, Steps 0+1 in one run** ([`primitive-power-convergence.md`](primitive-power-convergence.md)) | 2026-08-24 | **One new axiom (count 9 → 10, the first *convergence* axiom — Horn & Johnson §8.5 at the row-stochastic specialization; `#print axioms` via `wip/ppc_axcheck.lean` on all 13 public + 22 QA declarations: the unconditional ones the standard three only, the six convergence theorems `primitive_power_tendsto` + the standard three, and **no `perron_frobenius` anywhere** — the two directed-axis axioms' trust costs machine-verified independent).** The standing handoff's named gate ("directed-axis mixing/rate work, gated on a primitivity-shaped admission — `perron_frobenius` deliberately claims no strict dominance") is cleared by the established proposal+admission mechanism: `LinearAlgebra/PrimitiveConvergence.lean` admits `primitive_power_tendsto` (powers of a primitive row-stochastic matrix converge entrywise to the rank-one stationary projector `(Pᵗ) *ᵥ x → (π ⬝ᵥ x) • 1`, given-π form, **no rate** — that is a separate future admission gated on a consumer) with `Matrix.IsPrimitive` (H&J's positive-power definition) and the unconditional transfer layer (primitivity from positivity; primitivity → irreducibility by the entry-of-power walk decomposition; row-stochastic powers fixing the constant-one vector). The consumer loop closes with the delivered PageRank layer: the teleportation floor makes the Google matrix strictly positive hence **primitive at `k = 1`** (`googleMatrix_isPrimitive` — aperiodicity *derived*, strictly stronger than the delivered irreducibility), so **`pageRank_powerIteration` proves the classical PageRank algorithm as a theorem** (existence, uniqueness, and now *computability* of the PageRank distribution), plus the entrywise column form and the walk-evolution form `ν ᵥ* Gᵗ → π` — all conditional on the new axiom **alone**. QA +30 (2021 → 2051, `DirectedMixing_QA` a new file on the reused `PageRank_QA` fixtures): the reducible-fixture positive witness (limit pinned raw to the uniform `1/4`, second iterate computed completely raw at `3/16 < 1/4`), the **2-cycle periodicity refutation** (nonnegative, row-stochastic, *irreducible*, stationary verified raw; `P2_not_primitive` + the power action provably having *no limit at all* via even/odd subsequences — exactly `hprim` isolated, the convergence-form mirror of `strict_dominance_refuted_QA`), and the `onesVec` coherence join deriving `π ⬝ᵥ 1 = 1` from the axiom against the raw sum. One QA declaration deliberately removed as inert (an equality-of-proofs join — rfl by proof irrelevance; documented in-file). |
| The Sweep-Cut Extraction — the Explicit Fiedler Level-Set Cut — **proposal COMPLETE, Steps 0+1 in one run** ([`sweep-cut-extraction.md`](sweep-cut-extraction.md)) | 2026-08-24 | **Zero new axioms (count stays 9; `#print axioms` via `wip/sweep_axcheck.lean` on all 5 public + 22 QA declarations reads only `propext, Classical.choice, Quot.sound`) — backlog item 4's named strengthening and the Fiedler module header's own recorded follow-on delivered: the certified Cheeger cut is now an *explicit closed superlevel or sublevel set of the Fiedler vector* (the object the spectral-partitioning sweep returns), not the non-constructive conductance minimizer of Phase B's `cheeger_cut_existence`.** Three layers: `sweep_level_extract` (per part, in `GraphTheory.Cheeger`'s new `SweepExtraction` section — for any `y` with minority closed superlevel sets, exactly `coarea_core`'s hypothesis, some level set `S = {i : t ≤ y i²}` at a positive `t` attains `conductance S² ≤ E'(y)/(d·M)`), `cheeger_sweep_cut` (the median assembly — for any `x ⊥ 1 ≠ 0` a closed super-/sublevel set of `x` itself with `conductance S² ≤ 2·R_{L_sym}(x)`: **the same constant as `cheeger_sweep` with the witness explicit**, a genuine strengthening of the sweep lemma), and `fiedler_sweep_cut` (`GraphTheory.Fiedler` Phase C — the algorithm-facing `2λ₂/d` statement on connected `d`-regular graphs, composed through the Phase B Rayleigh bridge). The route replaces the textbook averaging-pigeonhole (which needs a *strict* integral inequality) with **attainment**: the boundary-to-size ratio minimized over the finitely many positive values of `y²` (`Finset.exists_min_image`), every closed superlevel set equal to one at an attained value (`Finset.min'`), and a non-strict layer-cake integration cloned from `coarea_core`'s own proof — the delivery load-bearing on the whole Step-1a/1b/1c chain at once. QA +22 (1999 → 2021, across `Cheeger_QA`/`Fiedler_QA`): the per-part extracted set forced to `{0}` through its level-membership iff (conductance `1` vs bound `4/2`), the assembled family characterized at two orthogonal `C₄` vectors (at `cycX2` the sweep **ties the global optimum `1/2`**, at `cycSweepX` the best swept cut is `1`, honestly inside bound `2` — the fixtures mark both ends of the quality range), the **orthogonality fence refuted in proved form** on the constant vector (empty sweep family — the hypothesis-free conclusion false for *every* candidate), and the `K₂` Fiedler-value family characterization with the optimality tie `conductance S = cheegerConstant = 1`. |
| The Resistance Metric — the Maximum Principle, Definiteness, and the Triangle Inequality — **proposal COMPLETE, Steps 0+1 in one run** ([`resistance-metric.md`](resistance-metric.md)) | 2026-08-24 | **Zero new axioms (count stays 9; `#print axioms` via `wip/rm_axcheck.lean` on all 5 public + 28 QA declarations reads only `propext, Classical.choice, Quot.sound`) — backlog item 7's two named non-gated residuals closed in a new section of `GraphTheory.Electrical`: with the proved nonnegativity/symmetry/self-distance laws, `effectiveResistance` is now a genuine metric on every connected network (the classical resistance distance).** The step's new mathematics is the **maximum principle** for unit-demand potentials (`min (f u) (f v) ≤ f x ≤ max (f u) (f v)`): at a max-point outside the boundary, the diffusion form `∑ j, A x j (f x − f j) = 0` is a sum of nonnegative terms, so the max value propagates across every positive-weight edge and a walk induction floods the connected graph — the kernel-characterization argument run at an inequality. The Step-0 survey records why the eigenbasis route *cannot* replace it: there `R(u,v)` is a squared Euclidean distance and the Euclidean triangle yields only the **root**-triangle `√R(u,w) ≤ √R(u,v) + √R(v,w)`, whose cross term is exactly what the sharp triangle must cancel — the cancellation *is* the maximum principle (the cross term `f v − f w ≤ 0` of the polarization expansion of `f + g`, the two demand potentials summed). Definiteness needs no max principle: the one-sided Dirichlet bound at the indicator `e u` (energy `∑_{j≠u} A u j > 0` by connectivity, voltage difference `1`) gives `0 < R u v` off-diagonal, hence `effectiveResistance_eq_zero_iff`. QA +28 (1971 → 1999, `ResistanceMetric_QA` a new file): the triangle's **equality case** pinned on the 3-path (`2 = 1 + 1`, the new edge witnesses `![1,0,0]`/`![0,0,−1]` — any proof route with a slack constant dies there) with the degenerate `v = u` instantiation exercising the theorem's self-distance branch; the **strict case** on `K₃` via the imported Foster pins (`2/3 < 4/3`); the confinement/definiteness positive witnesses at the actual unit-current potential `![2,1,0]` (voltage difference the pinned `R = 2`, interior value pinned *strictly* between the boundary values, both confinement halves at every vertex, the `iff` consumed off-diagonal and on); and **one signed fixture fencing `hnonneg` across all three theorems** (`![0,1,1;1,0,−1;1,−1,0]`: symmetric, support graph the connected path `1—0—2`, NOT nonnegative — in-file solution-shape analysis pins `R(0,1) = 0` at distinct vertices (definiteness **refuted in proved form**), `R(0,2) = 0`, `R(2,1) = −2`, the triangle **refuted** at `¬(0 ≤ 0 + (−2))`, and confinement **refuted** for *every* solution at the interior vertex) — the nonnegativity hypothesis load-bearing everywhere, exactly that hypothesis isolated. Verification: both modules `lake env lean`-clean (zero errors/warnings), explicit build targets ✔ (2009/2009, 2015/2015), full `lake build` ✔ (2261 targets), `lint_axioms` (9, unchanged), `check_citations`, `check_markdown_links` pass, scoreboard regenerated (1999/9/0, idempotent). |
| Cluster-Projector Symmetric Form — the Two-Sided Rank-Free Constant-2 Set-Form Difference Theorem — **proposal COMPLETE, Steps 0+1 in one run** ([`cluster-projector-symmetric.md`](cluster-projector-symmetric.md)) | 2026-08-24 | **Zero new axioms (count stays 9; `#print axioms` via `wip/cps_axcheck.lean` on the new public theorem + all 18 new QA declarations reads only `propext, Classical.choice, Quot.sound`) — the both-gaps difference shape at arbitrary eigenvalue sets: `‖P_A(S) − P_B(T)‖ ≤ 2‖A − B‖/δ` under two-flank center/radius membership separation (A's in-`S` cluster within `rS` of `cS`, B's out-of-`T` cluster ≥ `rS + δ` from `cS`; B's in-`T` cluster within `rT` of `cT`, A's out-of-`S` cluster ≥ `rT + δ` from `cT`), **no rank hypothesis anywhere** — completing the set-valued family to the window family's full shape (product/difference/symmetric all at sets).** The theorem is a *pure composition* and that is the finding: the set-form product bound is unconditional (no `‖A−B‖ < δ` regime, unlike the window pairwise engine), so the ring identity `P − Q = (I−Q)P − Q(I−P)` plus the complement law `1 − Q_T = Q_{Tᶜ}` at both residuals, `l2OpNorm_transpose` swapping the second's argument order, and the triangle inequality close — the constant 2 is exactly that triangle, no case split anywhere. Statement shape carries **two** center/radius pairs (one per cluster; a single common center would exclude the QA's own unequal-rank witness, where an out-of-`S` A-eigenvalue sits *between* the clusters). QA +18 (1953 → 1971, `ClusterProjector_QA` 28 → 46): the **unequal-rank non-interval witness** (`S = {0,5}` rank 2 on `clusterA` vs `T = {5}` rank 1 on `bm`, the delivered equal-rank family's hypothesis exhibited failing on the covered fixture, theorem bound `≤ 4` against the independent raw lower `1` at `e₀` from entrywise pins, the two-pair shape load-bearing — no single center covers the flanks), the ε = 0 attainment through the new theorem with both flanks genuinely discharged, the **two-sided fence** on the interior-gap configuration with *each* `hfar` flank refuted in proved form at its own interior eigenvalue while both `hnear` sides hold and the hypothesis-free conclusion dies at norm `≥ 1` (the two-sidedness itself exercised), and the ring-identity coherence witness pinned entrywise. |
| Cluster Projector — the Set-Valued Spectral Projector and Its Davis–Kahan Pair — **proposal COMPLETE, Steps 0+1 in one run** ([`cluster-projector.md`](cluster-projector.md)) | 2026-08-24 | **Zero new axioms (count stays 9; `#print axioms` via `wip/cp_axcheck.lean` on all 18 public module + 2 set-form + 27 public QA declarations reads only `propext, Classical.choice, Quot.sound`) — the interval-only projector shelf's recorded blind spot repaired: `clusterProjector M hM S := ∑_{λᵢ ∈ S} vᵢvᵢᵀ` gives the orthogonal projector onto an **arbitrary eigenvalue set** (a consumer wanting `P_{{|λ| ≥ 3}}` — two half-lines — had no object to name; the classical Davis–Kahan/YWS statements are set-based), junk-free (a set has no orientation, so none of the band family's `a ≤ b` guards reappear).** The new `GraphTheory.ClusterProjector` (minimal imports Spectral + Band): the component action (the mirror of PolyFilter's band form), the **intersection product law** `P_S * P_T = P_{S ∩ T}` by the *action* route, idempotence (its diagonal), disjoint-set orthogonality (its empty intersection), guard-free mode selection, commutation, capture at sets, the **complement law** `1 − P_S = P_{Sᶜ}` — the structural fact the window family lacks, where `1 − bandProjector` is not a band projector — the **band agreement** `clusterProjector (Set.Ioc a b) = bandProjector a b` (the whole delivered band family is the interval-set special case), and the trace/rank supplier. The `SetForm` sections of `Perturbation/BandDavisKahan.lean`: the engine re-run at sets (membership filters for window indicators) delivering the **set-form product bound** and, at equal rank, the **set-form difference bound** `‖P_A(S) − P_B(T)‖ ≤ ‖A−B‖/δ` — *no dichotomy, no interior case*: the complement law *is* the complement engine at sets. QA +28 (1925 → 1953): the non-interval pin `P_{{0,11}} = diag(1,0,1)` through a four-lemma composition; the non-interval difference instance at exact-fit `c, r, δ` (theorem `≤ 1` vs raw `≥ 1` at `e₀`); the ε = 0 attainment with separation genuinely discharged; and the `hfar` fence on the interior-gap configuration — the proposal's recorded obstruction itself exercised as a hypothesis fence (ranks equal, `hnear` verified, conclusion refuted at norm ≥ 1). The YWS-literal *pairwise* set shape recorded as honestly blocked (an out-of-`S` A-eigenvalue can sit inside `S`'s range, killing the interior-case trivial regime; whether the shape is true at all is open to this repo). |
| Band Davis–Kahan Symmetric Form — the Two-Sided (Both-Separations) Constant-2 Difference Theorem without Rank Equality — **proposal COMPLETE, Steps 0+1 in one run** ([`band-davis-kahan-symmetric.md`](band-davis-kahan-symmetric.md)) | 2026-08-24 | **Zero new axioms (count stays 9; `#print axioms` via `wip/bdks_axcheck.lean` on both new public + all 17 public QA declarations reads only `propext, Classical.choice, Quot.sound`) — the two-sided form of the band Davis–Kahan difference family: `‖P_A(a₁,b₁] − P_B(a₂,b₂]‖ ≤ 2‖A − B‖/δ` when *both* out-of-window flanks are δ-separated in the literal pairwise YWS shape, with **no rank hypothesis anywhere** — the Yu–Wang–Samworth Theorem 1 both-gaps selling point (the consumer states spectral separation and nothing else; no multiplicity counting through the rank supplier), at constant 2 which is *exactly* the triangle inequality at the ring decomposition `P − Q = (I − Q)P − Q(I−P)` (no hidden loss; on equal-rank inputs the delivered pairwise form gives the strictly better constant 1, and when ranks genuinely differ `δ ≤ ‖A − B‖` is forced inside the proof — the honest degradation to the trivial regime).** The new `SymmetricForm` sections of `Analysis/OperatorTheory/Perturbation/BandDavisKahan.lean` (**no new imports**): the private rank-free pairwise product bound (the cluster theorem's dichotomy/shrink/engine body minus the identity conversion, factored once, instantiated at **both argument orders** — the complement engine's fourth and fifth consumers, now with the empty-cluster corner being `P = 0` rather than a rank transfer) and the headline `l2OpNorm_bandProjector_sub_bandProjector_le_two_of_symm`, plus the public interface lemma `l2OpNorm_transpose` (`‖Mᵀ‖ = ‖M‖`, absent from the pinned Mathlib and the shelf — two pairing-characterization applications; every future two-sided perturbation consumer that bounds a right action while the engine states the left action needs it). One committed-statement correction recorded: `hsepAB`'s inner hypothesis restated as a conjunction (family-convention shape) over the draft's two-arrow form, bridged to the engine by a one-line eta-expansion. QA +17 (1908 → 1925, a new file on the *public* cluster fixtures — no new Fin 3 machinery needed): the **unequal-rank witness** (ranks pinned 2 ≠ 1 through the supplier — the delivered family's hypothesis exhibited failing on the very fixture the new theorem covers; raw norm ≥ 1 by the in-file eigen-equation support pin; bound ≤ 28), the constant-2 cost exhibited numerically on the equal-rank rotated fixture (delivered ≤ 1, new ≤ 2, raw `√(1/10)`), the ε = 0 attainment at genuinely distinct windows (cross-checked by the imported raw zero pin), the **`hsepAB`-isolated fence** (`hsepBA` provably vacuous-holding — `Q = 1` through `bandProjector_eq_one`, the hypothesis-free conclusion refuted at `‖P − 1‖ ≥ 1`; the mirror of the cluster QA's fence, so both separation sides are now isolated across the QA family), and the decomposition-coherence witness (the consumed identity's action-level content on rotated input, both composite parts from imported pins). The module's engine ran green on its first complete pass. |
| Band Davis–Kahan Cluster Form — the Pairwise (YWS-Literal) Difference Theorem — **proposal COMPLETE, Steps 0+1 in one run** ([`band-davis-kahan-cluster.md`](band-davis-kahan-cluster.md)) | 2026-08-24 | **Zero new axioms (count stays 9; `#print axioms` via `wip/bdkc_axcheck.lean` on all 3 public + 37 QA declarations reads only `propext, Classical.choice, Quot.sound`) — the eigenvalue-cluster-separated generalization of the difference form: `‖P_A(a₁,b₁] − P_B(a₂,b₂]‖ ≤ ‖A − B‖ / δ` at constant 1 under *pairwise* separation (every eigenvalue of B outside its window δ-away from every eigenvalue of A inside its window — the literal Yu–Wang–Samworth Theorem 1 δ), strictly weaker than the delivered closure separation and strictly stronger coverage: when A's cluster has an internal gap of width ≥ 2δ, a B-eigenvalue may sit inside A's window yet δ-away from every in-window A-eigenvalue — closure unreachable, pairwise covered.** The new `ClusterForm` sections of `Analysis/OperatorTheory/Perturbation/BandDavisKahan.lean` (**no new imports** — every consumed declaration already on shelf): the headline `l2OpNorm_bandProjector_sub_bandProjector_le_of_pairwise` (interior B-eigenvalues handled inside the proof by a projector-free Parseval expansion at the eigenvector — `Bv = μv` makes `A − μI` and `A − B` agree on `v`, forcing `δ ≤ ‖A−B‖`, the trivial regime closed by `‖P−Q‖ ≤ 1` with **no Weyl/`evals` bridge anywhere**; boundary ones give range separation, where the complement engine re-runs at the *cluster-range* center/radius — exactly, with no slack, which is why the core takes arbitrary `c, r`), the public capture-equality lemma `bandProjector_eq_of_forall_mem_iff` (two windows selecting the same eigenvalues give the same projector — the window-existence argument's transfer step, with window guards recorded as a committed-statement correction: the unguarded iff-form is false at reversed junk windows), and the empty-cluster zero lemma. QA +35 (1873 → 1908, a new file with an in-file Fin 3 spectral layer — distinct-entry diagonals force eigenvector support onto single coordinates and orthonormality forces eigenvalue classes to be singletons): the **interior witness** on `diag(0,5,11)` vs `diag(1,2,4)` where the closure shape is *refuted in proved form* (`7 ≤ 4` false at the 4-mode) while the pairwise hypothesis discharges and both projectors pin to `diag(1,1,0)`; the rotated two-route witness (bound `≤ 1` vs the imported raw `√(1/10)`); the ε = 0 attainment at genuinely distinct windows through the capture lemma; and the separation fence refuted with ranks equal — exactly `hsep` isolated, the mirror of the sibling's rank fence. |
| Band Davis–Kahan Difference Form — the Equal-Rank Identity's First Consumer — **proposal COMPLETE, Steps 0+1 in one run** ([`band-davis-kahan-difference.md`](band-davis-kahan-difference.md)) | 2026-08-24 | **Zero new axioms (count stays 9; `#print axioms` via `wip/bdkd_axcheck.lean` on all 5 public + 21 QA declarations reads only `propext, Classical.choice, Quot.sound`) — the sin-Θ (subspace-distance) form of the band Davis–Kahan theorem: `‖P_A(a₁,b₁] − P_B(a₂,b₂]‖ ≤ ‖A − B‖ / δ` at constant 1 under one-sided eigenvalue separation (B's out-of-window eigenvalues δ-away from A's window closure), the delivered product theorem's own recorded follow-on and the first consumer of the delivered-but-never-consumed equal-rank identity `ProjectionGap.l2OpNorm_sub_eq_of_rank_eq`** (per the strategy's load-bearing-growth principle, the identity now carries weight — a misstatement breaks the derivation loudly). The route re-runs the commutator/shift engine verbatim at the complement projector `Q' = 1 − Q_band(B)` (F1 compression and range invariance reused unchanged; the one new mathematical fact is the *complement expansion* — `Q'`-fixed vectors have vanishing in-band components, so Parseval leaves exactly the separated modes); the bounded A-window is load-bearing as in the product form, the complement's unboundedness harmless (expansion needs only separation, never a containment radius). A new public **rank supplier** `rank_bandProjector_eq_card` (rank = in-band eigenvalue count via trace, through the exact `{0,1}` spectrum of a symmetric idempotent — a new private eigen-equation fact) makes the equal-rank hypothesis *checkable*; the interval-margin corollary `_of_mem` (`a₂ + δ ≤ a₁`, `b₁ + δ ≤ b₂`) discharges the separation for contained windows. QA +21 (1852 → 1873, a new file on the reused `diag13`/`bdkB` fixtures): the rotated positive witness with the rank supplied through the new supplier, raw lower `1/√10` (sign-independent eigenbasis resolution `Q *ᵥ e₀ = ![9/10, −3/10]`) against the theorem's `1`; the **identity-coherence witness** (`(I−Q)·P` and `P − Q` actions pinned equal by two independent raw routes); the ε = 0 attainment (separation genuinely holding at δ = 1/2); the margin instance at δ = 1; and the **rank fence refuted in proved form** — A = B = diag13, windows empty vs occupied, ranks 0 ≠ 1 through the supplier, every other hypothesis verified (exactly `hrank` isolated). Verification: module + QA `lake env lean` zero errors/warnings, explicit build targets ✔, `#print axioms` all 26 the standard three, full `lake build` ✔ (2260 targets, +1), lint/citations/links pass, scoreboard 1873/9/0 idempotent.
| Band Davis–Kahan — Perturbation Stability for Band Projectors — **proposal COMPLETE, Steps 0+1 in one run** ([`band-davis-kahan.md`](band-davis-kahan.md)) | 2026-08-24 | **Zero new axioms (count stays 9; `#print axioms` via `wip/bdk_axcheck.lean` on all 5 public + 20 QA declarations reads only `propext, Classical.choice, Quot.sound`) — backlog item 9 complete: the bounded-window sibling of the proved `davis_kahan_sin_theta`, the first perturbation-stability result for `GraphTheory.Band`'s projectors, and the first Band×Perturbation composition.** The new `Scaffold.Mathlib.Analysis.OperatorTheory.Perturbation.BandDavisKahan`: the headline pair `l2OpNorm_bandProjector_mul_bandProjector_le_of_lt` / `_of_gt` — `‖Q * P‖ ≤ ‖A − B‖ / δ` for the band projectors of two symmetric matrices on δ-separated windows, constant 1, by the survey's recorded algebraic commutator/shift route (no integral, no sorting, no rank counting) — plus the two Step-0-priced shelf gaps landed (`l2OpNorm_mulVec_le`, the vector action bound absent from the pinned Mathlib; `mulVec_bandProjector_comm`, the 2026-08-21 survey's spike-verified commutation). The proof's content is the **r-cancellation**: the naive single-shift assembly strands `+ r` (the recorded catch that kills the technique at the half-line window), rescued by range invariance of `A`'s band under the shifted operator — the compressed term bounded by `r · ‖Q * P‖ · ‖y‖`, the `r` cancelling algebraically. Load-bearing on four delivered layers at once (Band idempotence/action, Spectral Parseval/eigenaction, PolyFilter component action, Duhamel pairing/norm engine). QA +20 (1832 → 1852, `Perturbation/BandDavisKahan_QA.lean` on the `diag13` fixture reused from `Band_QA` plus the rotated `!![1, 3/4; 3/4, 3]`): the positive witness with `‖Q * P‖` pinned from below by `1/√10` completely independently (eigenbasis resolution, eigen-equation direction constraints `v j 1 = 3 · v j 0`, no `eigvecOf` value assumed) against the theorem's `3/4` upper bound (perturbation norm bounded by the pairing engine's swap trick); the ε = 0 commuting case *attained* exactly; the overlapping-window fence **refuted in proved form** (product = the nonzero projector `diag(1,0)`, norm ≥ 1 through the action bound, RHS = 0) with every `hsep`-shaped hypothesis exhibited impossible; the mirror `_of_gt` orientation at δ = 1/2. Recorded follow-on: the *difference* form `‖P_A(S) − P_B(S')‖` (needs the delivered equal-rank identity, a natural `ProjectionGap` consumer). |
| PolyFilter Band Projection — the Chebyshev Layer's Second Consumer — **proposal COMPLETE, Steps 0+1 in one run** ([`polyfilter-band-projection.md`](polyfilter-band-projection.md)) | 2026-08-24 | **Zero new axioms (count stays 9; every new declaration `#print axioms`-clean at the standard three) — the Krylov Step-0 survey's recorded follow-on shape `‖p_d(M) − bandProjector M hM a b‖₂ ≤ ε(d, gaps)` delivered as hard crust, making the Chebyshev scalar layer a two-consumer layer and giving the Band module its first approximation-theory consumer.** The new `Scaffold.Mathlib.GraphTheory.PolyFilter` (minimal imports Band + Krylov + Resolvent; the umbrella importing it): the component-action pair (`eigvecOf_dotProduct_spectralProjector_mulVec` / `..._bandProjector_mulVec`, the `dotProduct_eigvecOf_mulVec` mirrors at the two projectors through symmetry + the delivered eigenbasis action); **the filter-agnostic transfer theorem** `aeval_sub_bandProjector_l2OpNorm_le` (within `ε` of `1` at every in-band eigenvalue and of `0` at every out-of-band one ⇒ `‖p(M) − B_{a,b}‖ ≤ ε`) — the survey's key structural finding cashed out: *no eigenvalues of the difference are computed anywhere*, the Resolvent upper-bridge route (Parseval + `opNorm_le_bound`/`cstar_norm_def`) resolving the difference's action through the eigenbasis; the **power instantiation** `powFilter_l2OpNorm_le` (ε = `(t/Ltop)^d`, the power-method rate); and the **Chebyshev-damped instantiation** `chebyshevFilter_l2OpNorm_le` (ε = `1/T_d(w(Ltop))` at the Krylov `bandMap`) — the genuine second consumer of `abs_T_eval_le_one` / `one_le_eval_T_of_one_le` / `bandMap`, the same scalar layer that carried Kaniel–Paige. Load-bearing by the falsifiability test: the transfer theorem consumes the exact shapes of four delivered layers at once — a misstatement in any breaks it loudly. QA +28 (1804 → 1832, a new file on the `diag13` fixture reused from `Band_QA`): the affine exact filter driving the engine to ε = 0 with both sides pinned raw (two routes), both instantiations' ε constants **attained** at the out-mode (`(1/3)^d`, `1/3`, `1/17` — unimprovable for these designs), the power-vs-Chebyshev comparison `1/17 < 4/9` proved at one gap, and the out-of-band hypothesis **refuted in proved form** (a filter perfect in-band whose conclusion fails, the norm lower-bounded at `1 > 1/2` through the fixed unit vector). Scope honestly recorded: the wide-band minimax designs stay a follow-on gated on scalar approximation theory absent from the pin. |
| PageRank Distributions — the Second Perron–Frobenius Consumer — **proposal COMPLETE, Steps 0+1 in one run** ([`pagerank-distributions.md`](pagerank-distributions.md)) | 2026-08-24 | **Zero new axioms (count stays 9) — the second *theorem* consumer of the admitted `perron_frobenius` axiom, extending the directed axis' stationary theory to reducible input (the entire point of teleportation).** The new `Scaffold.Mathlib.GraphTheory.PageRank` (namespace `SpectralGraphTheory`; minimal imports IrreducibleStationary + Normalized; the umbrella importing it): the nine unconditional structural declarations — `googleMatrix` with its entry form `α·P i j + (1−α)·(card V)⁻¹`, `walkTransitionMatrix_nonneg` (the sign-pattern reading, previously inline in the parent engine), `googleMatrix_nonneg`, **`googleMatrix_pos`** (the teleportation floor: `0 < G i j` at every pair on `[0,1)` with nonempty `V`, regardless of the input walk's support/reducibility/asymmetry), `googleMatrix_row_sum` (α-free row stochasticity — affine, surviving both fence endpoints where uniqueness dies), `googleMatrix_deg_eq_one`, **`googleMatrix_isIrreducible`** (irreducibility *derived*, not assumed: one `ReflTransGen.single` per pair through the floor), and **`walkTransitionMatrix_eq_of_row_sum_one`** (the general row-stochasticity bridge, stated at any matrix for future row-stochastic consumers) — and the three axiom-**conditional** theorems `exists_pageRankVec` / `existsUnique_pageRankVec` / `pageRankVec_pos`: existence, the `∃!`, and full support of the PageRank distribution on **reducible** input, **no irreducibility hypothesis anywhere**, pure compositions of the same-day `IrreducibleStationary` layer at `M := googleMatrix A α` with every hypothesis derived structurally and the conclusion translated through the bridge — zero new axiom contact (the two axiom applications stay inside the delivered engine, `#print axioms`-verified). Statement-shape decisions recorded before stating: `0 ≤ α < 1` (not `0 < α` — pure teleportation is legitimately covered), `[Nonempty V]` (the floor dies as `0⁻¹ = 0` on the empty type), the row-stochastic/transposed-convention difference from the Page–Brin source recorded, and nothing about power-iteration convergence (needs the strict dominance the axiom deliberately does not claim). QA +89 (1715 → 1804, a new file, reusing `IrreducibleStationary_QA`'s fixtures by QA-to-QA import): the reducible two-edge fixture's uniform PageRank `(1/4,1/4,1/4,1/4)` verified **completely raw** against all 16 pinned entries with the `∃!` join identifying every stationary distribution with the hand value — on the very fixture whose raw walk has two stationary distributions (the imported fence); the asymmetric star's PageRank `(4/9, 5/18, 5/18)` verified raw and provably **distinct** from the raw stationary `(1/2,1/4,1/4)` (teleportation shifting mass to the leaves — the construction's observable content); and *both* endpoint fences refuted in proved form with complementary fixtures — `α = 1` (teleportation removed, `G = P` pinned entrywise, the `∃!` refuted through the imported witnesses, exactly `hα2` isolated) and `α = -1` on `K₂` (the mixture pinned to the identity, two distinct stationary point masses, row stochasticity provably surviving, exactly `hα` isolated). Verification: `lake env lean` on module and QA zero errors/warnings each; explicit build targets both ✔; `#print axioms` on all 12 public + 85 QA declarations — the intended split (3 public + 6 QA theorems carry `perron_frobenius` + the standard three; everything else, fences included, the standard three only); full `lake build` ✔ 2258 targets; `lint_axioms` (9, unchanged), `check_citations`, `check_markdown_links` pass; scoreboard regenerated 1804/9/0 idempotent. Backlog item 8's named-consumer program (irreducible stationary distributions, PageRank) now fully delivered. |
| Irreducible Stationary Distributions — the First Perron–Frobenius Consumer — **proposal COMPLETE, Steps 0+1 in one run** ([`irreducible-stationary-distributions.md`](irreducible-stationary-distributions.md)) | 2026-08-24 | **Zero new axioms (count stays 9) — the first *theorem* consumer of the admitted `perron_frobenius` axiom (2026-08-22), exactly the follow-on its module docstring and backlog item 8's update name; the directed axis' first theorem.** The new `Scaffold.Mathlib.GraphTheory.IrreducibleStationary` (namespace `SpectralGraphTheory`; minimal imports PerronFrobenius + Normalized; the umbrella importing it): the unconditional transfer lemmas `isIrreducible_transpose` (strong connectivity is arc-reversal invariant — the private `ReflTransGen` flip/head induction) and `walkTransitionMatrix_isIrreducible` (positive row scaling preserves the support digraph — the private congruence, the pin having `mono` for `ReflGen` only), and the five axiom-**conditional** theorems: `exists_walkPerronVector` (the transposed Perron engine — strictly positive, fixed by `Pᵀ *ᵥ ·`, unique up to positive scale among nonnegative fixed vectors; the walk's Perron root pinned to `1` by the axiom's eigenvalue-identification clause at `onesVec` through the shelf's row-stochasticity, the transposed root by the bilinear pairing `v ⬝ᵥ (P *ᵥ u) = (Pᵀ *ᵥ v) ⬝ᵥ u` — no charpoly, no complex domination), `exists_stationaryVec_of_irreducible` (existence, full support, normalization), `stationaryVec_smul_of_irreducible` (scale-free uniqueness), `existsUnique_stationaryVec_of_irreducible` (the textbook `∃!` among nonnegative distributions), `stationaryVec_pos_of_irreducible` (no stationary vector of an irreducible chain can vanish anywhere). Load-bearing by the strategy's falsifiability test: clauses 5 and 6 are consumed structurally — a misstated either breaks the derivation loudly. QA +94 (1621 → 1715, a new file): the asymmetric directed star `(1/2, 1/4, 1/4)` with every hypothesis hand-verified, all three predicates completely raw, and the `∃!`'s uniqueness *identifying* its witness with the hand value; the symmetric-cone `K₂` agreement pinning the PF-unique distribution *equal* to `Mixing.stationaryVec` through the shelf's detailed-balance chain (two independent API paths to one value); and the reducibility fence (two disjoint edges) refuting the hypothesis-free `∃!` and scale-uniqueness conclusions in proved form with `hnn`/`hdeg` verified intact. `#print axioms` split-verified: the axiom-route theorems list `perron_frobenius` + the standard three, every raw lemma the standard three alone. |
| Approximate Spectral Projection with Error Bounds — **program COMPLETE, Steps 0/1a/1b/1c** ([`approximate-spectral-projection.md`](approximate-spectral-projection.md)) | 2026-08-24 | **Pure hard crust, zero new axioms at every step (count stays 9) — the Lanczos/Kaniel–Paige finite-`k` bound is the program's delivered public statement.** Step 0 (run `20260823T132040Z-run-1`): the scoping survey that CLEARED the Lanczos/Kaniel–Paige shape as the one tractable one (the exact finite-`k` existence-form statement recorded — no sup, no tridiagonal iteration, the Chebyshev value left explicit — every proof dependency verified against pin and shelf, both shallow gaps priced, and a green axiom-clean skeleton spike with the spectral layer entering as five named discharge sites); Nyström deferred with its obstruction named (sampling-without-replacement matrix concentration and `Matrix.pinv` both absent), the Chebyshev-filter shape recorded as the natural second consumer of the same layer. Step 1a (run `20260823T215632Z-run-1`): the interface layer — the new `GraphTheory.Krylov` with the real `krylovSpan` definition, the polynomial-eigenaction transfer, the pin-gap `sum_mulVec`, the Chebyshev band bound `\|T_n(x)\| ≤ 1`, and both priced gaps closed (`natDegree (T ℝ n) = n`, the growth lemma); QA a new file at 19. Step 1b (run `20260823T232953Z-run-1`): the spectral discharge — the general-eigenvector transfer layer, the eigenbasis component form with Parseval/quadratic-form resolution, the affine band map, the unit decomposition, and the composite `kanielPaigeChebyshev` with every spectral site discharged from the eigenbasis-level band hypothesis (the band/parallel dichotomy the mathematical crux); QA 19 → 35, the composite instantiated end-to-end on `diag(3,1,0)` with the bound pinned `16/75` and an independent raw route proving the true Krylov-witness gap `32/241` strictly inside. Step 1c (run `20260824T012049Z-run-1`): the final statement — **`kanielPaige` at the Step-0 recorded shape** (unit `b` with `u ⬝ᵥ b ≠ 0` the only starting-vector hypothesis, the decomposition internal via `exists_unit_decomposition`, the bound in the classical `tan²φ`/`γ` form `(Ltop − Lbot)·tan²φ/T_{k−1}(1+2γ)²` at `γ = (Ltop − Ltwo)/(Ltwo − Lbot)`, composed from the composite by the three identifications `c = u ⬝ᵥ b`, `s² = 1 − c²`, `w(Ltop) = 1 + 2γ`; the Saad §6 locator attached to the statement's docstring with the clean-room verify-against-the-physical-copy caveat) plus the four proposal-mandated QA witnesses: the final-form bound **proved expression-equal** to the composite's `16/75` with the raw gap strictly inside, the `k = 1` degenerate case (`T₀ ≡ 1`, bound = the plain Rayleigh-gap value `16/3` against the raw `R(b) = 43/25`), the `b = u` tightness (bound `= 0`, the delivered witness pinned to Rayleigh value exactly `Ltop` from both sides), and the `λ₁ = λ₂` guard **refuted in proved form** on the top-multiplicity `diag(3,3,0)` fixture (`hpar` itself false by orthonormality; the hypothesis-free conclusion refuted with every other hypothesis verified — the `k = 1` Krylov line's Rayleigh value computed raw to `8/3`, true gap `1/3 > 1/4` = bound). QA 35 → 50 (1606 → 1621 total). |
| Discrete Affine-Control Convergence on Finite Vector Spaces — **proposal COMPLETE, both steps in one run** ([`discrete-affine-convergence.md`](discrete-affine-convergence.md)) | 2026-08-23 | **Pure hard crust, zero new axioms (count stays 9) — the external `sgt-gaps.md` consumer's item-2 interface discharged, and the discrete-affine slice of `docs/6_SGT_BACKLOG.md` item 5 opened** (consensus maps, synchronization, and general graph semigroups remain gated, per the proposal's own scope note; explicitly not the iceboxed continuous-time ODE gap). The new `Scaffold.Mathlib.Dynamics.DiscreteAffine` (namespace `Scaffold.Dynamics`, a new `Dynamics` area — no graph structure in the statements, so not the event-driven `GraphTheory/Dynamics`): `tendsto_pow_smul_atTop_nhds_zero` (Step 1: `r ^ n • x → 0` for `|r| < 1` — the pin's `tendsto_pow_atTop_nhds_zero_of_abs_lt_one` lifted through the pin's `Filter.Tendsto.smul_const`, both located by the survey and re-verified on delivery), `affineIteration_eq` (the closed form `x_n = (1−α)^n • (x_0 − e) + e`, pure module induction), and `affineIteration_tendsto_atTop` (Step 2: convergence to the equilibrium under `0 < α < 2` at the consumer's exact `Tendsto x atTop (𝓝 equilibrium)` shape). **Statement-shape decision recorded:** all three at a general real normed space `E` — the proofs never use finiteness or coordinates, the drafted `V → ℝ`/`Fintype V` shape is the instance (the Tikhonov Section-5 generality precedent). QA +25 (1546 → 1571, the new `Dynamics` QA domain): the positive fixture `α = 1/2` with the recurrence in the theorem's exact hypothesis form, per-step values, closed form, per-coordinate geometric decay, and convergence by **two independent routes** (delivered theorem vs. entrywise/Pi-topology limit from the pin's scalar lemma); the wrapper likewise two-route with numeric decay instances (`(1/2)^5`/`(1/2)^10` pins); and the proposal's two mandated boundary witnesses as **proved refutations**, complementary by construction — `α = 2` oscillation (`xosc n = (−1)^n • ![1,0]`, ε-δ non-convergence, `0 < α` intact so exactly `hα2` isolated) and `α = 0` constancy (constant `![1,0] ≠ 0` never converges, `α < 2` intact so exactly `hα0` isolated). Verification: spike first (`wip/dac_spike.lean` green); `lake env lean` on module + QA zero errors/warnings; explicit `lake build` targets ✔; `#print axioms` on all 28 declarations the standard three; full `lake build` ✔ (2253 targets); lint/citations/links pass; scoreboard regenerated **1571/9/0** idempotent. |
| Tikhonov Regularization, Phase 2 — the hard-filter limit; **proposal COMPLETE** ([`tikhonov-shrinkage-filter.md`](tikhonov-shrinkage-filter.md)) | 2026-08-23 | **Both steps in one run, pure hard crust, zero new axioms (count stays 9) — the external `sgt-gaps.md` consumer's item-1 interface discharged.** `GraphTheory.Tikhonov` Section 5: `tikhonovShrinkage_tendsto_zero` (the consumer's exact statement `Tendsto (fun π => tikhonovShrinkage π lam) (𝓝 0) (𝓝 0)` at fixed `0 < lam` — **two-sided**, strictly stronger than the requested right limit, via `ContinuousAt.div` at the nonzero denominator) and the tail-suppression corollary in two forms — the general symmetric-matrix `tikhonovShrinkage_tail_energy_tendsto_zero` (statement-shape decision: nothing in the proof uses PSD or the Laplacian, so the carrier is any symmetric matrix, the module's own `dotProduct_eigvecOf_filter` precedent) and the consumer-facing Laplacian `tikhonovMinimizer_tail_energy_tendsto_zero` (the filtered signal's coefficient energy on any selected positive-eigenvalue mode set vanishes as `π → 0`) — **suppression-stated per the requester's explicit non-overclaim instruction**, no band-projector convergence claimed anywhere. QA +24 (1522 → 1546): the **two-mode tail** on the diagonal `diag13` fixture *reused from `Band_QA`* with the energy pinned in closed form `tikhonovShrinkage π 1 ^ 2` and evaluated collapsing (`1/121` at `π = 1/10`, `1/10201` at `π = 1/100`), the corollary consumed at a concrete tolerance (`∃ δ > 0` ⇒ tail energy `< 1/100` near `0`); the **scalar `lam = 0` refutation** (the hypothesis-free form false — the factor has no limit at all); the **tail-level boundary refutation on `K₂`** (kernel mode included ⇒ energy provably `≥ 1/2` at every nonzero `π` — the requester's mandated "mode below lam" witness, the low-pass one-sidedness exhibited in proved form); and the **minimizer-form instantiation** with the `π = 1` tail energy pinned to `1/18` through the coefficient identity + Parseval + the kernel line. Radar QA axis count synced 1522/40 → 1546/40, held at 4.0; axis scores unchanged (a quantitative refinement within the counted smoothing-operator layer). |
| Reversibility and the Heat Semigroup, Phase C Step 2 — the first-order remainder bound; **proposal COMPLETE** ([`reversibility-and-heat-semigroup.md`](reversibility-and-heat-semigroup.md)) | 2026-08-23 | **The quantitative half of the dissolution-theorem input, pure hard crust, zero new axioms (count stays 9) — Phase C complete in two runs, the external `spectral-proof` consumer's full requested interface discharged (four Phase B items + the derivative/remainder analytic pair).** `GraphTheory.Heat` gained `heatKernel_firstOrder_remainder_apply_le` (on the window `|t · λᵢ| ≤ 1`: `|(e^{-tL} x) a − x a + t (L x) a| ≤ t² · ∑ᵢ λᵢ² |vᵢ ⬝ᵥ x| |vᵢ a|` at every coordinate — the boundary-observable form; flow coordinate, input coordinate, and generator coordinate each expanded over the proved eigenbasis, combined termwise, bounded per mode by the Step-0-surveyed `Real.abs_exp_sub_one_sub_id_le`; **no nonnegativity hypothesis**, per-mode valid for any symmetric network) and the `[0, T]` interval packaging `heatKernel_firstOrder_remainder_interval` (uniform whenever `T` meets the window, monotonicity hypothesis-transfer). QA `Heat_QA` 50 → 66 (1507 → 1522 total): the K₂ eigenvalue inventory per vertex index (PSD/trace/determinant, no sort machinery), the eigenmode structure `v = c • ![1,-1]` with `2c² = 1` making the spectral constant **exactly `4`** independent of the spectral-theorem choice's orientation sign, the window fact `[0, 1/2]`, the concrete-bound witness at the endpoint (`e⁻¹ ≤ 1`, RHS evaluated to `1`), the raw closed-form value `1 − 2t − e^{-2t}` at every time, the interior interval-route bound `|1/2 − e^{-1/2}| ≤ 1/4`, the **degradation witness** pinning the `t²` scaling at both times (a wrong power of `t` contradicts at least one component), and the **fence** refuting the window hypothesis at `t = 1` — local by construction, not accidentally global. Radar axes 5 and QA **held** at 4.0/4.0 per protocol (a quantitative refinement within the counted calculus layer); counts synced 1507/40 → 1522/40. |
| Reversibility and the Heat Semigroup, Phase B Step 4 — eigenmode decay + the DC limit; **proposal COMPLETE** ([`reversibility-and-heat-semigroup.md`](reversibility-and-heat-semigroup.md)) | 2026-08-23 | **The payoff statement, pure hard crust, zero new axioms (count stays 9 at every Phase B step) — the external consumer's four-item interface fully discharged: identity, semigroup law, mass conservation, and now eigenmode decay with the connected-graph DC limit.** `GraphTheory.Heat` gained the **eigenmode engine** `exp_mulVec_eq_smul_of_mulVec_eq_smul` (`M *ᵥ v = μ • v → exp ℝ M *ᵥ v = Real.exp μ • v` — the load-bearing bridge the plan required: Step 3's `expSeries_hasSum_exp` pushed through the continuous action at an eigenvector, the scalar series summed by the pin's `NormedSpace.exp_series_hasSum_exp'` at ℝ via `Real.exp_eq_exp_ℝ`; Step 3's kernel engine re-derived as its `μ = 0` case at unchanged statement), the **rank-one-idempotent collapse** `exp_eq_one_add_of_mul_self_eq_smul` (`M * M = c • M → exp = 1 + ((e^c − 1)/c) • M` — the square-zero collapse's sibling; its scalar tail shifted by the pin's topological-group `hasSum_nat_add_iff'`, the pin having no plain tail-shift `HasSum` lemma), `heatKernel_mulVec_eigvecOf` (every eigenbasis vector an eigenvector of `heatKernel A t` at every time, eigenvalue `e^{−t·λᵢ}`), the decay monotonicity + PSD `≤ 1` dissipation bound, the **eigenbasis expansion** `heatKernel_mulVec_eq_sum` (the spectral-calculus identity in action form, over the proved orthonormal basis), and the headline **DC limit** `heatKernel_mulVec_tendsto_atTop` — on connected graphs free diffusion leaves only the mean `((∑ x)/|V|) • onesVec`, assembled from PSD, kernel-mode existence via `det L = 0` + `det_eq_prod_eigenvalues`, the kernel characterization (a second kernel mode would put two orthogonal unit vectors in one line), and finite-sum limit passage. QA `Heat_QA` 25 → 46 (1482 → 1503 total): eigenmode decay and the DC limit each by **two independent routes to one statement** (engine vs. the new K₂ closed form `1 + ((e^{−2t} − 1)/2) • L` + hand arithmetic; theorem vs. raw limit computation), the engine⇄collapse cross-validation at a nonzero eigenvalue (`exp m2 *ᵥ 1 = e² • 1` both ways), the K₂ Laplacian spectrum pinned `[0, 2]` from trace/determinant/sortedness with the monotonicity constants read from the pin. Radar axis 5 **re-scored 3.5 → 4.0** (the named trigger — the continuous-time side is now a closed capability family; the axis's only remaining absent category is the ℓ² → TV conversion). |
| Reversibility and the Heat Semigroup, Phase B Step 3 — mass conservation ([`reversibility-and-heat-semigroup.md`](reversibility-and-heat-semigroup.md)) — **Phase B in delivery, Step 4 remains** | 2026-08-23 | **Mass conservation, pure hard crust, zero new axioms (count stays 9) — completing the external consumer's three-item interface (identity + semigroup law + mass conservation), all proved.** `GraphTheory.Heat` gained `heatKernel_mulVec_onesVec` (`heatKernel A t *ᵥ onesVec = onesVec` at every time, **hypothesis-free**: no symmetry, no sign restriction on `t`) through the shelf's `laplacian_ones_in_kernel`, plus the general kernel-vector engine `exp_mulVec_eq_of_mulVec_eq_zero` (`M *ᵥ v = 0 → exp ℝ M *ᵥ v = v`) — conservation for *any* Laplacian-harmonic vector, the interface the per-component QA consumes. **The run's sharpest output is a survey finding turned into infrastructure:** `NormedSpace.exp` at this pin is topological-algebra-level (why the module's statements elaborate with the Pi topology on matrices), the pin's normed `expSeries_summable'` cannot be applied at matrix type without `letI`-ing `linftyOp` (whose topology has no transfer lemma to the Pi one), and the pin has **no Pi-type HasSum/Summable lemmas at all** — so the series summability was built entrywise from scratch: the power bound `abs_pow_apply_le` (`|(M ^ n) i j| ≤ B ^ n` at `B = ∑ |M p q|`), the comparison `summable_exp_term` (against `Real.summable_pow_div_factorial`), the pin-gap assembler `hasSum_pi`, and the assembled convergence `expSeries_hasSum_exp` (the exponential series converges to `exp ℝ M` in the entrywise topology — the foundation Step 4's eigenbasis expansion builds on); the action push is the `HasSum.map`-with-continuous-additive-hom pattern the pin's own `Matrix.transpose_tsum` uses, and the collapse is `tsum_eq_sum` over `{0}` at `pow_add`-factorized power annihilation. QA `Heat_QA` 17 → 25 (1474 → 1482 total): conservation by **two independent routes** (theorem on the asymmetric fixture vs the closed form `!![1-t, t; -t, 1+t]` hand-multiplied onto `onesVec`), the raw kernel-instance check, the symmetric-fixture instantiation, the proposal's named **per-component no-leakage witness** on the new disconnected `Fin 3` fixture (the `{0,1}`-component indicator checked `L`-harmonic raw from the definitions then fixed by the engine at every time; the isolated-vertex indicator fixed too — heat provably does not cross components, so conservation is not accidentally vacuous), and the **kernel-hypothesis guard** (a non-kernel vector provably *not* fixed, `![0,-1] ≠ ![1,0]` — the hypothesis-free strengthening refuted with every other structural fact intact). Verification: `lake env lean` on module and QA clean; explicit `lake build` targets ✔; `#print axioms` on all fourteen new declarations the standard three only; full `lake build` ✔ (2252 targets); `lint_axioms` (9), `check_citations`, `check_markdown_links` pass; scoreboard regenerated (1482/9/0). |
| Reversibility and the Heat Semigroup, Phase B Step 2 — the semigroup law ([`reversibility-and-heat-semigroup.md`](reversibility-and-heat-semigroup.md)) — **Phase B in delivery, Steps 3–4 remain** | 2026-08-23 | **The semigroup property, pure hard crust, zero new axioms (count stays 9) — completing the external consumer's two-item core interface ("identity at time zero + semigroup law"), both halves now proved.** `GraphTheory.Heat` gained `heatKernel_mul_heatKernel` (`heatKernel A s * heatKernel A t = heatKernel A (s + t)`, **hypothesis-free**: no symmetry, no sign restriction on the times) through the pin's `Matrix.exp_add_of_commute` at the commuting negated scalar multiples of `laplacian A` (`(s • L) * (t • L) = (s * t) • (L * L)` both ways), with no ball/radius hypothesis — the Step-0 survey's norm-free finding holding exactly as recorded — and the every-time square-zero collapse `exp_neg_smul_eq_one_add_of_mul_self_eq_zero` (`exp (-(t • M)) = 1 + -(t • M)`, the Step-1 `t = 1` handle generalized to all times; the exponent squares to `(t·t) • (M·M) = 0`). QA `Heat_QA` 10 → 17 (1474 total): the law witnessed by **two independent routes to one closed-form statement** — `heatKernel_asym_semigroup_raw_QA` multiplies the fixture's closed forms `!![1-s, s; -s, 1+s]` and `!![1-t, t; -t, 1+t]` by hand (`Matrix.mul` on literals, every entry `ring`, independent of the theorem) while `heatKernel_asym_semigroup_theorem_QA` composes the delivered law with one closed form — a wrong time-combination constant anywhere in the chain contradicts the hand computation; plus the every-time closed form `!![1-t, t; -t, 1+t]` for symbolic `t`, the numeric instance at `s = 2, t = 3` (`!![-4, 5; -5, 6]`), the **group property** `heatKernel asymAdj 1 * heatKernel asymAdj (-1) = 1` (forward-then-backward flow is the identity — the semigroup law and the time-zero identity working together), and the degenerate `t = 0` identities on K₂. Verification: `lake env lean` on module and QA clean (zero errors, zero warnings); explicit targets ✔; `#print axioms` on all nine new declarations the standard three; full `lake build` ✔ (2252 targets); `lint_axioms` (9), `check_citations`, `check_markdown_links` pass; scoreboard regenerated (1467 → 1474). |
| Reversibility and the Heat Semigroup, Phase B Steps 0–1 ([`reversibility-and-heat-semigroup.md`](reversibility-and-heat-semigroup.md)) — **Phase B in delivery, Steps 2–4 remain** | 2026-08-23 | **The graph heat semigroup opened, pure hard crust, zero new axioms (count stays 9) — the first Scaffold work selected by a real external named consumer** (`sgt-gaps.md`, the independent `spectral-proof` clean-sheet rewrite project, whose operator gate resolution promoted this row to High on 2026-08-23). The new `Scaffold.Mathlib.GraphTheory.Heat` (namespace `SpectralGraphTheory`): `heatKernel A t := NormedSpace.exp ℝ (-(t • laplacian A))` — the consumer's first interface item — with `heatKernel_isSymm` (the shelf's `laplacian_symmetric` through `Matrix.IsSymm.smul`/`.neg` and the pin's `Matrix.IsSymm.exp`; every `t`, under `A.IsSymm`), `heatKernel_zero` (hypothesis-free, via `NormedSpace.exp_zero`), and the general square-zero exponential collapse `exp_eq_one_add_of_mul_self_eq_zero` (`M * M = 0 → exp ℝ M = 1 + M`, by `exp_eq_tsum` + `tsum_eq_sum` over `range 2`). **Step-0 survey recorded before any statement, with a material simplification:** the pin's `MatrixExponential.lean` is purpose-built norm-free (each matrix-level lemma `letI`s the `linftyOp` norm inside its proof), so the sketch's `expSeries_radius_eq_top` worry does not arise and Step 2's semigroup law will carry no ball hypothesis; also corrected — there is no `Matrix.exp` declaration (the function is `NormedSpace.exp` at matrix type) and all required topology instances are global. QA (`Heat_QA.lean`, 10 theorem declarations, a new file): the time-zero identity on both fixtures (all entries computed; hypothesis-free); symmetry through the theorem on K₂ with the hypothesis derived from the literal; the **exact closed form** `heatKernel asymAdj 1 = !![0, 1; -1, 2]` on the asymmetric fixture whose Laplacian `!![1,-1;1,-1]` is square-zero (the spike caught the first draft's wrong `(1,1)` entry — the failure mode numeric QA exists for); the **symmetry hypothesis refuted-on-omission** (the computed kernel provably asymmetric, `1 ≠ -1`, with the fixture's `IsSymm` violated at the same pair); and the **sign witness** `heatKernel asymAdj 1 ≠ exp ℝ (1 • laplacian asymAdj)` (`0 ≠ 2` at `(0,0)`). QA 1457 → 1467. Steps 2–4 (semigroup law; `onesVec` mass conservation; eigenmode decay with the connected-graph DC limit) remain, one per run. |
| The Fiedler Vector and a Certified Spectral Partition — **program COMPLETE** ([`fiedler-partitioning.md`](fiedler-partitioning.md)) | 2026-08-23 | **Phase B delivered as pure hard crust, zero new axioms (count stays 9) — the first consumer of the same day's `cheeger_lower_bound` retirement, whose recorded operator gate ("run against the admitted hard direction, or defer until it is proved") was dissolved by the retirement itself: the deferral option completed, so Phase B composes proved theorems only.** `GraphTheory.Cheeger` gained `cheegerConstant_attained` (the conductance `sInf` realized as a minimum — `Finset.exists_min_image` over the filtered powerset of nonempty proper subsets, no regularity hypothesis; the step that converts Cheeger's *inequality about an infimum* into a statement about an actual cut); `GraphTheory.Fiedler` (now importing `Cheeger`) gained the Rayleigh transfer `fiedlerVector_rayleigh_regularNormalizedLaplacian` (`R_{L_sym}(f) = lambda2 / d` via the quadratic-form transfer, unit norm, and energy identity) and the headline `cheeger_cut_existence`: on every connected `d`-regular graph, `∃ S` nonempty proper with `conductance S ^ 2 ≤ 2 · lambda2 / d` — the classical Cheeger cut-existence corollary. **Statement-shape deviation recorded before stating:** the sketch's sign-partition bound (`conductance (fiedlerPartition …) ≤ bound`) is not certifiable from the Cheeger inequalities (they bound the conductance *minimum*; no λ₂-only bound on the sign half-space holds in general) — the certified object is the conductance minimizer, and certifying an explicitly swept Fiedler level set is the recorded follow-on. QA `Fiedler_QA` 57 → 65 (1457 total), load-bearing on the retirement end-to-end: `cheegerConstant (K₂) = 1` pinned both directions (every nonempty proper cut on `Fin 2` identified as a singleton by the card count); attainment instantiated at the pinned value; the **Rayleigh transfer cross-checked** against the independently pinned `lambda2 (K₂) = 2` (a defective `/d` normalization or quadForm transfer breaks the agreement); the certified cut **identified** with its bound theorem-sourced and displayed numerically (`1 ≤ 4`); and the **regularity refutation** — `hd` dropped at `d = 100` (every other hypothesis holding on `K₂`, connectivity proved) demands `1 ≤ 1/25`, false. Phase A had landed 2026-08-18 (the vector, `lambda2_pos_of_connected`, the sign partition's sanity facts). |
| Discharge Davis–Kahan / Weyl / Cheeger Hard Direction — **program COMPLETE** ([`discharge-perturbation-axioms.md`](discharge-perturbation-axioms.md)) | 2026-08-23 | **The Cheeger hard direction retired — `cheeger_lower_bound` proved at the unchanged statement, explicit axioms 10 → 9 — all three of the proposal's axioms are now proved hard crust (Weyl 2026-08-20, Davis–Kahan 2026-08-21, Cheeger easy 2026-08-18, Cheeger hard 2026-08-23).** The final component (Step 1c, median + assembly, delivered as the third dedicated run in `GraphTheory.Cheeger`): median existence `exists_median` by pure Finset arithmetic — *no sorting*, a route simplification over the survey's `Finset.sort`+`get` sketch (the at-most-half set `T := {i : 2·|{x i < x j}| ≤ n}` is nonempty at a maximizing vertex; a `T`-minimal value works; the failure case is self-refuting: a majority strict lower level set's maximizer lies in `T` below the `T`-minimum); the level-set inclusions `{t ≤ (x−m)⁺²} ⊆ {x > m}`, `{t ≤ (m−x)⁺²} ⊆ {x < m}` at `t > 0` supplying `coarea_core`'s `hy` in its delivered closed-set form; the per-part bound `hardDirection_perPart` (`φ²·d·∑y² ≤ E'(y)`, the 1b co-area core squared and composed with the 1a Cauchy–Schwarz core, `cheegerConstant_nonneg` for the squaring, zero-norm case by nonnegativity); the norm split `median_parts_norm` (`∑x² + n·m² ≥ ∑x²`); the **sweep lemma** `cheeger_sweep` (`φ²/2 ≤ R(x)` for every nonzero `x ⊥ 1`, both median parts through the 1a *fused* contraction — the corrected constant budget consumed exactly); and the retirement through the spike's assembly (`secondEval_variational` + `le_csInf` at the `Pi.single` witness). QA 1423 → 1449 (`Cheeger_QA` 78 → 104): the median forced into `[−1,1]` on the tie-heavy `![1,1,−1,−1]`, the per-part bound at the 1b K₂ equality data (`1 ≤ 2`), the norm split's `+4m²` remainder pinned at two medians, the sweep on K₂ (`1/2 ≤ 2`, the pinned λ₂) and C₄ at `d = 2` (`φ²/2 ≤ 1/8 < 1 = R`), and the retirement instance `1/2 ≤ λ₂ = 2` through the proved theorem. `#print axioms`: the retired theorem, all ten new public theorems, all QA headlines, and the two pre-existing axiom-consuming QA theorems (which silently shed the dependency) read only `propext, Classical.choice, Quot.sound`. Full `lake build` ✔ (2250 targets); lint/citations/links pass. |
| Admit Perron–Frobenius for Irreducible Nonnegative Matrices ([`admit-perron-frobenius.md`](admit-perron-frobenius.md)) | 2026-08-22 | **The scoped admission, explicit axioms 9 → 10 — the first admission since the 2026-08-19 scope decision, opening the directed axis' second spectral toolkit.** The new `Scaffold.Mathlib.LinearAlgebra.PerronFrobenius`: the combinatorial irreducibility predicate `Matrix.IsIrreducible` (global, Fintype-free — `ReflTransGen` on positive entries, the directed analogue of the `supportGraph`/`Connected` pattern) and the admitted axiom `perron_frobenius` at exactly the proposal's scoped qualification level — `0 < r` with a strictly positive eigenvector, `rootMultiplicity r A.charpoly = 1`, every nonzero nonnegative eigenvector a positive multiple of the Perron vector (its eigenvalue therefore `r`), and complex-spectrum domination through the complexified charpoly roots (the pin has no matrix spectral radius; the `spectralRadius` hits are the C*-algebra thread already found inapplicable). Two recorded statement guards: the `hex : ∃ i j, 0 < A i j` degeneracy guard (H&J implicitly work at `n ≥ 2`; over arbitrary `Fintype V` the vacuously irreducible 1×1 zero matrix and the empty type would falsify `0 < r` — `hex` is exactly the missing strength, implied by irreducibility whenever `2 ≤ card V`) and **no strict-dominance clause** (that needs primitivity). QA (36 declarations, the first LinearAlgebra-domain file; `#print axioms` split verified: the five axiom-consuming theorems list `perron_frobenius` + the standard three, the eleven hand theorems only the standard three): the positive witness `!![1,2;1,0]` with the Perron root *derived to be exactly 2* from the axiom's eigen-equation on its unknown witness and the simplicity/domination clauses pinned against hand factorizations (`charpoly = (X+1)(X−2)`, complex roots `{2,−1}`, `rootMultiplicity 2 = 1`); and the proposal's mandated centerpiece `strict_dominance_refuted_QA` — on the asymmetric directed 2-cycle `!![0,4;1,0]` the strengthening is refuted at `|−2| = 2 = r` (hand Perron pair `(2,(2,1))`, hand eigenpair `(−2,(−2,1))`, no axiom consumed) while the axiom's own domination clause instantiated at the same peripheral root holds *with equality* — the weaker conclusion is exactly what survives on imprimitive input. Both fixtures asymmetric and rational; hand-only only-eigenvalue theorems on both, cross-checked against the axiom's clause through the hand Perron eigenvector. Records: Horn–Johnson index row (Theorem 8.4.4), the new `index/map/linear_algebra.md` + README row, the coverage map's named-absent row updated, backlog item 8's third update, README (10 axioms, 1378 QA), radar (QA count 1378/39, held 4.0, hold logged), scoreboard (both Direct rows, `lake build` row, lint row, interpretation bullet). Nothing committed. |
| Directed and Asymmetric Graph Operators — **program COMPLETE** ([`directed-graph-operators.md`](directed-graph-operators.md)) | 2026-08-22 | **All steps across two runs, pure hard crust, zero new axioms (count stays 9).** Steps 0+1 (run `20260822T181701Z-run-1`): the three mandated Step-0 decisions — carrier reuse (no new type), the **out-degree-symmetrized convention** `L_dir := I − ½(SAS + SAᵀS)` (deliberately *not* Chung's Perron-vector form, so no admitted axiom becomes a prerequisite of a definition — the two proposals **decoupled**), and the Mathlib re-survey (no directed Laplacian in the pin) — plus the discovery that `deg`/`walkTransitionMatrix`/`walkLaplacian` already carry the out-degree walk operators hypothesis-free, and the genuinely new degree layer (`inDeg`, `rfl` identifications, symmetric-cone agreements, directed handshaking) with the asymmetric-input QA certification of the pre-existing walk operators. Step 2 + the Step-3 agreement brick (run `20260822T194020Z-run-1`): `directedNormalizedLaplacian` with the entry form, **hypothesis-free symmetry** (the two defining halves are transposes of each other — the directed axis' one symmetric operator), the acceptance-bar agreement `= normalizedLaplacian` under `A.IsSymm`, and the square-root-free conjugate `√D L_dir √D = degreeMatrix A − ½(A + Aᵀ)` (the symmetrized adjacency at out-degree normalization). **The sharpest output is the calibration refutation:** `L_dir` is symmetric but provably *not* PSD on directed input — QA's `dirB = !![0,4;1,0]` fixture (nonnegative, positive out-degrees, `L_dir` symmetric by the theorem) has `quadForm L_dir 1 = −1/2 < 0` (the symmetrized weight `5/2` overweights the out-degree normalization `√4·√1 = 2`, driving `L_dir 0 1` to `−5/4` past `−1`) — so the positivity layer of the undirected toolkit does not transfer and the directed spectral theory needs Perron–Frobenius, the proposal's own Calibration boundary now witnessed numerically. QA 41 → 88 declarations (1342 total; all entries of `L_dir dirA` computed from the entry form at exactly `[[1,−1,−1/2],[−1,1,0],[−1/2,0,1]]`, the conjugate's two sides independently hand-checked at `−2`, and the two new negative witnesses: undirected reuse `L_dir ≠ normalizedLaplacian` on directed input, and the PSD refutation). |
| Prove the Subgaussian Tail Bound — repair-and-retire ([`prove-subgaussian-tail-bound.md`](prove-subgaussian-tail-bound.md)) | 2026-08-22 | **The repository's first measure-theoretic axiom retirement — delivered as an emergency correctness repair (Woodbury precedent), explicit axioms 10 → 9.** The mandated Step 0 spike found the old statement **materially false** in two independent junk ways, both confirmed against the pinned source and refuted in QA with the old hypotheses proved satisfied (`3 • δ₀`, total mass `3`: every MGF integral is `3 > 2` so the defining set is empty, `Real.sInf_empty` makes `subgaussianNorm = 0 ≤ K` vacuous, and the conclusion at `X = 0, K = 1, t = 0` reads `3 ≤ 2`): (1) `Real.sInf_empty` vacuity on measures of total mass above `2`; (2) `integral_undef` — the Bochner integral of a non-integrable function is *defined* to be `0`, so heavy-tailed variables satisfy the defining MGF bound at every scale (the set is **full**, not empty — the `subgaussianNorm` docstring's recorded junk behavior was wrong and is corrected), making the norm `0` and the old conclusion claim subgaussian tails for heavy-tailed variables even under probability measures. The repaired theorem keeps the name, the conclusion with its `2K²` constant, and the `t`/`ht` arguments, with honest hypotheses `(hK : 0 < K)`, MGF integrability at `K`, and the MGF bound at `K` — exactly the content Markov's inequality consumes, strictly stronger than the old shape whenever that was non-vacuous; the junk `K = 0` branch is dropped. Proof: the `Real.exp_half` square-root bridge gives ae-strong-measurability at the half scale *as a function of the hypothesis-side MGF* (no measurability hypothesis on `X` needed), `Integrable.mono'`/`integral_mono_of_nonneg` transfer the moment, shelf Markov `mul_meas_ge_le_integral_of_nonneg` runs at `exp (t²/(2K²))`, `Integrable.measure_norm_ge_lt_top` forces the Markov event finite for the ENNReal conclusion. The proposal's open `K = sInf` boundary question **dissolves** — the repaired hypotheses never mention the `sInf`. `#print axioms` on the theorem: three standard axioms only. QA (`Scalar_QA` 6 → 17, 1254 total, axiom-clean on all twelve touched/new theorems; the zero-QA family no longer consumes `hoeffding_lemma`): the zero fixture through the new hypotheses, the proposal-mandated **nonzero instance** (constant-1 variable, `K = 2`, `t = 1` on `dirac 0`; moment `exp (1/4) ≤ 2` via the pinned `Real.log_two_gt_d9`; event side `1` and bound side `1 ≤ 2·exp (−1/8)` both pinned raw), and the four-piece refutation family (fixture, empty-set junk mechanism exhibited, old hypotheses provably satisfiable, old conclusion refuted). **Named residual recorded:** the same junk-integral mechanism likely infects `hoeffding_inequality`/`bernstein_inequality`'s mean hypotheses on infinite measures and the matrix trio's `MatrixMDS` integrals — flagged for their own Step 0s, not addressed here. Full build ✔ (2229 targets); `lint_axioms` (9), `check_citations`, `check_markdown_links` pass. |
| Relative Entropy and Shannon Entropy for Finite Distributions ([`finite-relative-entropy.md`](finite-relative-entropy.md)) | 2026-08-22 | **Both steps in one run, pure hard crust, zero new axioms (count stays 10).** The new `Scaffold.Mathlib.InformationTheory.Entropy` (namespace `Scaffold.InformationTheory`): the scalar `klTerm` with the proposal-mandated visible `a = 0 ↦ 0` junk convention, `klDiv` and `shannonEntropy` as its sums (entropy's sign visible in the definition), **Gibbs' inequality in both directions** (`klDiv_nonneg`; `klDiv_eq_zero_iff` at full function equality — strict positivity of `q` makes the off-support case self-refuting), the **uniform bridge** `klDiv_apply_uniform` (`klDiv p (n⁻¹) = log n − shannonEntropy p` — the one place the two quantities meet), the **entropy maximum** `shannonEntropy p ≤ log |V|` with its equality case exactly at uniform (`shannonEntropy_eq_log_card_iff`; `Nonempty V` derived from `∑ p = 1`, not assumed), and `shannonEntropy_nonneg`. The mandated Jensen survey found the whole shelf present (stop-and-record clause not triggered), but the delivered route is the *term-wise* information inequality (`Real.one_sub_inv_le_log_of_pos` + `Real.log_lt_sub_one_of_pos` at the reciprocal) — the same Cover–Thomas 2.6.3 proof with no Finset-`smul` plumbing and no support filtering; the junk case is discharged inside the per-term bound, where its strictness is stronger (gap `= b > 0`), so the equality case needs no `∑_{support} q` argument at all. QA `Entropy_QA.lean` (+31, 1243 total, the first InformationTheory-domain file): the biased coin's divergence and entropy each hand-computed to closed log-forms (`¼·log(27/16)`, `¼·log(256/27)`) and the divergence pinned strictly positive; the equality case exercised in both directions against raw computations; **the uniform bridge numerically cross-checked** at the fair coin (`¼·log(27/16) = log 2 − ¼·log(256/27)`, both sides independently pinned); the maximum attained at uniform on `Fin 4` two ways and **strictly missed** by the non-uniform `(1/2, 1/6, 1/6, 1/6)` — strictness derivable only through the equality-case iff, the proposal-prescribed load-bearing use; and the delta distribution's entropy exactly `0` exhibiting the junk convention. Closes the entropy half of backlog item 6 (the reversibility half landed earlier the same day); the inequality layer (Dirichlet/log-Sobolev) stays gated on consumer demand. |
| Spectral Mixing-Time Bound, Step 3 component 2 — the χ² assembly; **program COMPLETE** ([`mixing-time-bound.md`](mixing-time-bound.md)) | 2026-08-22 | **The closing mixing statement, pure hard crust, zero new axioms (count stays 10).** `chiSquareDistance_le_of_connected`: on every connected symmetric-nonnegative positive-degree network under the rate hypothesis `r`, `χ²(t, x) ≤ r ^ (2t) · ((π x)⁻¹ − 1)` — the proposal's target statement, assembled from three new interface lemmas plus one genuinely new mathematical component: the **connectivity kernel characterization of `L_sym`** (the eigen equation at `μ = 0`, the kernel transferred through the proved congruence `√D L_sym √D = L`, the shelf's `exists_const_of_laplacian_mulVec_eq_zero` pinning the pre-conjugated vector constant, and mass conservation `∑ deg (h₀ − 1) = 0` collapsing the mode coefficients to zero — load-bearing on all three). Supporting additions: the constant fix `walkTransitionMatrix_mulVec_one` (`P *ᵥ 1 = 1`) and the `√D`-action entry lemmas in `Normalized.lean` (+3 public theorems there), the centered evolution `walkDensity_sub_one` (`h_t − 1 = Pᵗ *ᵥ (h₀ − 1)`), and the mass-conservation pairing lemma in `Mixing.lean` (+4 public theorems there; all `#print axioms`-clean). QA (`Mixing_QA` 67 → 117, 1212 total): **K₃ with the bound attained *exactly*** at `t = 1, 2` (`1/2 ≤ (1/4)·2`, `1/8 ≤ (1/16)·2` — equalities, both sides pinned raw); the centered evolution and the connectivity-derived mode fact cross-checked against the component-1 hand computations; **P₃'s rate derived basis-independently** from two SOS certificates (the Dirichlet identity `v ⬝ L_sym v = (v₀ − v₁/√2)² + (v₂ − v₁/√2)²` and its `2‖v‖² − ·` companion pinning every eigenvalue into `[0, 2]`), the bound honest and strict at λ* = 1 (`1 ≤ 3`); and the two negative witnesses — **connectivity load-bearing** on the triangle⊕self-loop `Fin 4` fixture (disconnected since loops are not support-graph edges; spectrum `{0, 0, 3/2, 3/2}` derived basis-independently so the rate hypothesis *provably holds* at `r = 1/2` — the loop's `μ = 0` mode is excluded from the rate hypothesis by design, exactly the hole connectivity plugs — while `χ²(3) = 3/8 > 3/64` refutes the conclusion), and **the rate load-bearing** at `r = 1/4` (hypothesis unsatisfiable by the trace, conclusion `1/2 > 1/8` refuted). Radar axis 5 **re-scored 3.0 → 3.5** per the proposal's own gate (the mixing statement is the trigger). Two pin-specific API discoveries recorded in the proposal (this snapshot's `Finset.mul_sum` argument order/orientation; the matrix-literal far-corner entry evaluation route). |
| Spectral Mixing-Time Bound, Step 3 component 1 — the geometric decay engine ([`mixing-time-bound.md`](mixing-time-bound.md)) | 2026-08-22 | **The decay engine, pure hard crust, zero new axioms (count stays 10).** The sub-decomposition the proposal licenses for its hardest step, first half: the conjugated-power transfer `√D *ᵥ (Pᵗ *ᵥ g) = (1 − L_sym)ᵗ *ᵥ (√D *ᵥ g)` (from the new commutation form `√D · P = (1 − L_sym) · √D`, one induction — the non-symmetric power is never diagonalized), eigencoordinate evolution `c_i(t) = (1 − μ i)ᵗ c_i(0)` (through the new generic center lemma `eigvecOf_dotProduct_one_sub_mulVec`), the Parseval-exact decay identity, and the ℓ²(π) contraction `∑ π ((Pᵗ g))² ≤ r^{2t} ∑ π g²` under **value-based** mode exclusion (`eigvalOf i = 0` — true unconditionally on degenerate graphs, unlike index-0 exclusion) and a hypothesis-shaped rate (no decorative sign hypothesis; no case split on `r < 1`). QA 33 → 67 (1162 total): K₃ (λ* = 1/2, **exact** decay `2 → 1/2 → 1/8` at `t = 0, 1, 2`, every eigen-fact basis-independently derived — `μ ≠ 0 → μ = 3/2` from the summed eigen equation + unit norm, kernel = constants, kernel index exists by trace); P₃ oscillation cross-check (the exact identity pins the eigencomponent sum to `4`, tied by the π-norm bridge to the pinned `χ²(2) = 1`); the mode-hypothesis negative witness (conclusion `1 ≤ 1/4` refuted at `g = 1`, hypothesis provably unsatisfiable at the kernel index). Radar axis 5 **held at 3.0** per the proposal's own gate (the mixing statement itself is component 2; hold + trigger logged). |
| Spectral Mixing-Time Bound, Step 2 — the ℓ²-mixing proxy ([`mixing-time-bound.md`](mixing-time-bound.md)) | 2026-08-22 | **The ℓ²-mixing proxy, pure hard crust, zero new axioms (count stays 10).** The Step-2 decide-and-record scoping gate made *before any statement*: ℓ² alone satisfies the proposal's goal (TV stays a separate further step per its own Step-4 framing), and within ℓ² the **weighted χ² form is primary** — `χ²(t, x) = ∑ (ν_t − π)²/π` — because Step 3's decay bound is Parseval-exact only in the π-weighted inner product where the Step-1 transferred eigenbasis is orthogonal; the plain Euclidean sketch-shape is delivered as the corollary bridge `sum_sub_sq_walkDistribution_le`. New `GraphTheory.Mixing` (14 public theorems, `#print axioms` clean): `stationaryVec` (π = deg/vol; positive, sums to one, stationary via the proved degree form), `walkDistribution` (`(Pᵀ)ᵗ *ᵥ δₓ`; zero/succ evolution; mass conservation on row-stochasticity), `walkDensity` + **`walkDensity_succ`** (`h_{t+1} = P *ᵥ h_t` — the density-coordinate evolution, the *first consumer of the same day's Phase A detailed-balance interface* and the exact form the transferred eigenbasis diagonalizes), and `chiSquareDistance` (nonneg; `= 0 ↔ ν_t = π`; `χ²(0) = (π x)⁻¹ − 1` — Step 3's normalization; density-form equivalence; the plain-ℓ² bridge). QA `Mixing_QA.lean` (+33, 1128 total): everything pinned through the theorems *and* raw (`π = (1/4,1/2,1/4)`; laws `δ₀ → (0,1,0) → (1/2,0,1/2)`; `P *ᵥ (4,0,0) = (0,2,0)`; `χ²(0) = 3` by both routes; `χ²(2) = 1` — the bipartite non-decay the Step-3 bound must reproduce on P₃; `3/8 < 1/2` for the bridge), plus two negative witnesses (asymmetric positive-degree matrix refutes hypothesis-free stationarity `1/3 ≠ 2/3` with symmetry provably violated at the same entry — `hA` load-bearing; zero adjacency refutes the hypothesis-free vanishing characterization, `χ² = 0` junk beside `ν₀ ≠ π` — positivity load-bearing). Radar axis 5 **held at 3.0** per the proposal's own gate (the mixing *statement* is Step 3). Step 3 (the decay bound) open. |
| Reversibility Phase A — detailed balance ([`reversibility-and-heat-semigroup.md`](reversibility-and-heat-semigroup.md)) | 2026-08-22 | **Both Phase A steps in one run, pure hard crust, zero new axioms, no new definitions** (the proposal's own mandate). `GraphTheory.Stationary` gained the detailed-balance layer: `walk_detailed_balance` (`deg i * P i j = deg j * P j i`, both sides exactly `A i j` — the degree weight cancels `walkTransitionMatrix`'s `D⁻¹` row factor through the new entry lemma `Normalized.walkTransitionMatrix_apply`, and `A.IsSymm` identifies the entries), `walk_detailed_balance_measure` (the stationary-measure π-form at `π = deg/vol`, by side-condition-free division — **no volume-positivity hypothesis carried**, strictly more general than drafted), `diagonal_deg_mul_walkTransitionMatrix_isSymm` (reversibility *is* symmetrizability: `D * P` symmetric — the self-adjointness interface the mixing-time program's ℓ²(π) proxy consumes), and `transitionMatrix_detailed_balance_uniform` (the regular uniform-measure corollary, *composed* from the already-proved `transitionMatrix_symmetric` — the pre-edit survey's finding — rather than re-proved). QA 12 → 26: the prescribed positive witness (P₃, every index pair through the theorems *and* raw literal arithmetic — `1 = 1` degree form, `1/4 = 1/4` π-form with `vol = 4` pinned independently, symmetrized off-diagonal entries both `1`) and negative witness (the asymmetric `!![0,2;1,0]` with positive degrees: hypothesis-free balance refuted `2 ≠ 1`, symmetry hypothesis provably violated at the same pair — `hA` load-bearing). Radar axis 5 held at 3.0 per protocol (composed identity within the counted walk-operator family; hold + triggers logged). Also repaired: the radar axis-5 table row and "Weakest axes" paragraph left stale at 2.5 by the same day's mixing-time re-score. Phase B (heat semigroup) remains gated on its recorded operator decision. |
| Spectral Band Projectors, Step 4 ([`spectral-band-projectors.md`](spectral-band-projectors.md)) | 2026-08-21 | **The Hilbert-projection specialization, zero new axioms (count stays 11) — the program is COMPLETE.** Three new theorems in `GraphTheory.Band`: `bandProjector_residual_dotProduct_eq_zero` (the residual-orthogonality engine `(x − B*ᵥx) ⬝ᵥ (B*ᵥz) = 0`, load-bearing on exactly the two Step-1 facts — symmetry moves the band across the dot product, idempotence collapses the band of the residual to zero), `bandProjector_toEuclidean_apply_eq_orthogonalProjection` (**the identification**: Mathlib's `orthogonalProjection` at `LinearMap.range (toEuclideanLin B)` maps the packaged signal to the packaged band-filtered signal, through `eq_orthogonalProjection_of_mem_of_inner_eq_zero` — the SGT center's first consumption of `Analysis/InnerProductSpace/Projection.lean`, transport per the resolvent Step-0 precedent; the proposal's mandatory signature survey found the whole shelf present, no obstruction), and `norm_sub_bandProjector_apply_le` (**the closest-point property** `‖e x − e (B*ᵥx)‖ ≤ ‖e x − e y‖` for every fixed point `y` — equivalently every range member — from `orthogonalProjection_minimal` + the conditionally-complete `ciInf_le`). Statements deliberately at `EuclideanSpace ℝ V` (the bare `V → ℝ` default norm is the sup norm); the fixed-point hypothesis form over the membership existential (equivalent by idempotence). QA (+25, 109 in file, 998 total): the identification pinned to the packaged band image; minimality **attained** (distance exactly `4`), **strict over the zero signal** (`4 ≤ 5` — the 3-4-5 triangle), **generic over the whole range line** (`4 ≤ √((3−t)²+16)`) with `band_diag13_hilb_min_line_raw` reproducing the same inequality from bare square-positivity (an independent hand-check the theorem's claim must match); the high band instantiated (not fixture-locked); the residual engine witnessed by theorem and raw routes; and the **fixed-space guard** — the hypothesis-free form refuted at the unfiltered signal (distance `0 < 4`, not fixed by the band), so the fixed-point hypothesis is load-bearing. |
| Spectral Band Projectors, Step 3 ([`spectral-band-projectors.md`](spectral-band-projectors.md)) | 2026-08-20 | **Completeness under a partition, zero new axioms (count stays 11).** Four new theorems in `GraphTheory.Band`: `sum_range_bandProjector_eq_sub` (the **unconditional telescoping law** — `∑_{k<n} B(t_k, t_{k+1}) = P_{t_n} − P_{t_0}` for *any* threshold sequence, ordered or not, by range-succ induction with an `abel` residue — load-bearing on the band definition's exact difference shape), `sum_range_bandProjector_eq_one` (**completeness**: a family whose start is strictly below every eigenvalue and whose end covers them all resolves the identity — exactly the two endpoint covering hypotheses, both load-bearing), `bandProjector_mul_bandProjector_eq_zero_of_monotone` (**distinct members of a monotone family are orthogonal** — the recorded statement-shape decision: monotonicity is deliberately *not* a hypothesis of the sum identity since telescoping does not consume it; it is exactly what this theorem consumes, supplying Step 2's disjointness `t (k+1) ≤ t m`, so the family is a *partition* rather than a sequence), and `sum_range_bandProjector_mulVec_eq_self` (the consumer's form: `∑ B_k *ᵥ x = x`, the frequency-band decomposition, through the inlined `mulVec` analog of `Matrix.sum_mul`). QA (+41, 84 in file, 973 total): the covering two-band family summed to the identity through the theorem *and* from pinned band values; a three-band partition with an **empty middle band** and its top threshold exactly touching the top eigenvalue (the closed right endpoint); the **two endpoint guards** — a family starting above the lowest mode or truncated below the highest provably fails to sum to the identity, each with the violated covering hypothesis separately refuted (the design note's "silently ignores modes" failure mode); the **non-monotone telescoping witness** (junk band `−1` cancelling the covering band `1`, both sides independently `0`); and the vector decomposition by both routes. Step 4 (the Hilbert projection specialization) remains. |
| Spectral Band Projectors, Step 1; Spectral Mixing-Time Bound, Steps 1 and 3-component-1 (consolidated row — these slices' delivery facts previously lived only in the Active-table progress paragraph, consolidated here 2026-08-23 when that paragraph was de-staled) | 2026-08-20 / 2026-08-22 | **Band Step 1 (2026-08-20):** the basic two-sided `(a, b]` band projector layer with its cross-law and mode-selection action interface — in-band modes fixed, out-of-band modes annihilated — zero new axioms (count stays 11). **Mixing Step 1 (2026-08-22):** the walk-matrix eigenpair transfer — eigenpairs crossing the proved similarity identity in both directions, every `walkEvals` entry certified a genuine eigenvalue with an explicit nonzero witness, no characteristic-polynomial interface needed — closing the named walk-spectrum gap, radar axis 5 re-scored 2.5 → 3.0; zero new axioms. **Mixing Step 3 component 1 (2026-08-22):** the geometric decay engine — walk powers conjugated across the proved similarity (`√D (Pᵗ g) = (1−L_sym)ᵗ (√D g)` per step), eigencoordinates evolving geometrically through the generic eigenaction at `1−M`, Parseval resolving the π-weighted norm exactly, and the ℓ²(π) contraction `∑ π (Pᵗ g)² ≤ r^{2t} ∑ π g²` termwise with no case split on `r < 1`; statement-shape decision recorded: mode exclusion value-based, not index-based. Zero new axioms; full detail in the scoreboard interpretation bullets and `docs/AGENT_ACTIVITY.md` of the day. |
| Spectral Band Projectors, Step 2 ([`spectral-band-projectors.md`](spectral-band-projectors.md)) | 2026-08-20 | **Orthogonality of disjoint bands, zero new axioms (count stays 11).** Four new theorems in `GraphTheory.Band`: `bandProjector_mul_bandProjector_eq_zero` / `_eq_zero'` (bands over disjoint intervals `(a, b]`, `(c, d]` — the hypothesis `b ≤ c` is exactly disjointness — compose to zero in **both orders**, by the recorded route: the cross-law's ordered forms expand the band product to the four-term expression `P_{min b d} − P_{min b c} − P_{min a d} + P_{min a c}` that collapses under the ordering), `bandProjector_inner_eq_zero` (the consumer's form: `(B_{a,b} *ᵥ x) ⬝ᵥ (B_{c,d} *ᵥ y) = 0` — filtered components from disjoint bands carry no shared signal), and `eq_zero_of_bandProjector_mulVec_eq_self` (disjoint bands share no mode: a vector fixed by both is zero). QA (+16, 932 total): composition to zero through the theorem *and* by raw literal multiplication in both orders; image orthogonality by both routes with the **same-band counter-witness** (`1 ≠ 0`, separating disjointness from fixture zeros); the composed action annihilating a filtered signal; and the **overlap guard** — bands sharing eigenvalue `3` compose to the nonzero `diag(0,1)`, so the disjointness hypothesis is load-bearing. Steps 3–4 remain. |
| Spectral Band Projectors, Step 1 ([`spectral-band-projectors.md`](spectral-band-projectors.md)) | 2026-08-20 | **The two-sided band projector, zero new axioms (count stays 12).** The new `GraphTheory.Band`: `bandProjector M hM a b := spectralProjector M hM b − spectralProjector M hM a` — the `(a, b]` band, parameterized by spectral interval not index per the design note, total by definition with the `a > b` junk documented — with symmetry, **idempotence through the new nestedness cross-law** (the pre-edit survey's route finding: idempotence of a difference needs `P_{c₁} * P_{c₂} = P_{min c₁ c₂}`, not just idempotence of the factors — delivered in `Spectral` with ordered forms, and the old `spectralProjector_idempotent` re-derived from it at unchanged statement, its 50-line proof now one rewrite), the **mode-selection action interface** (in-band eigenvectors fixed, below-band and above-band annihilated — the bandpass-filtering behavior of the proposal's external consumer), the below-spectrum special case (`spectralProjector` kept, not re-derived), and the covering band `= 1`. Plus the complete projector-eigenvector action description in `Spectral` (`P_c *ᵥ vᵢ = if λᵢ ≤ c then vᵢ else 0`). QA (+27, 917 total): the diagonal fixture `!![1,0;0,3]` whose one-dimensional eigenspaces make eigenvector *directions* computable — spectrum pinned from trace+determinant, projector at any `c ∈ [1,3)` pinned to the hand-computed outer product `e₀e₀ᵀ`, band values computed independently (`diag(1,0)`, `diag(0,1)`, `1`, and the between-eigenvalues band `0` — the gapped-definition guard), idempotence by raw literal multiplication, and the excluded mode **annihilated and provably not fixed**. Steps 2–4 remain. |
| Tikhonov Regularization in the Laplacian Eigenbasis ([`tikhonov-shrinkage-filter.md`](tikhonov-shrinkage-filter.md)) | 2026-08-20 | **All three build steps in one run, zero new axioms (count stays 12).** The new `GraphTheory.Tikhonov`: the minimizer **defined by the eigenbasis formula** `∑_k (π/(λ_k+π)) (v_k ⬝ᵥ y) • v_k` (recorded route: explicit formula over `Classical.choice`), the closed-form eigencoefficient identity (hypothesis-free, via a generic reusable filter workhorse `dotProduct_eigvecOf_filter`), the **normal equation** `(L+π•1) *ᵥ x* = π•y` **with its converse characterization** (any solution *is* the minimizer — the QA-pinning interface), the strict-convexity decomposition `obj(x) − obj(x*) = ∑_k (1+λ_k/π)(d_k−d*_k)²` delivering minimality and uniqueness, mean preservation `∑ x* = ∑ y` (symmetry + `π ≠ 0` only), and the not-a-projection theorem (`T(T v) ≠ T v` at any positive-eigenvalue eigenvector — idempotence failure, the precise distinction from `spectralProjector`; the sketch's nonnegativity hypothesis found unnecessary and deleted). **Two recorded statement-shape corrections:** the factor is *exactly 1* at `λ = 0` (kernel modes pass through untouched — that is mean preservation; the proposal's "never exactly 1" is false there), and "not a projection" is a theorem, not a docstring note. QA (+30, 890 total): minimizer pinned against a hand-solved 2×2 system through the converse characterization (`![2/3,1/3]`), objective pinned exactly (`1/3` vs `1`), exact shrinkage at the pinned spectrum `{0,2}` (factors `1` and `1/3`), `T² ≠ T` numerically, and the `hπ` guard refuted-on-omission at `π = 0`. |
| Resolvent Calculus for PSD Matrices ([`resolvent-calculus-psd.md`](resolvent-calculus-psd.md)) | 2026-08-20 (program complete) | **All four steps delivered, zero new axioms throughout (count stays 13).** Steps 0–1 (2026-08-19): the operator-norm bridge `l2OpNorm_eq_max_abs_evals` (the C*-algebra thread elaboration-verified structurally inapplicable — `CStarAlgebra` requires `NormedAlgebra ℂ` — and the from-scratch eigenbasis route proved instead: both directions, `‖!![2,1;1,2]‖ = 3` pinned in QA), shifted-PSD invertibility (`M + t•1` for any `t > 0`, no symmetry hypothesis), and the resolvent identity. Step 2 (2026-08-19): the norm bound `‖(M+t•1)⁻¹‖ ≤ t⁻¹` and the Lipschitz bound `‖(A+1)⁻¹ − (B+1)⁻¹‖ ≤ ‖A−B‖` by the recorded energy-route deviation (QA attainment with energy/spectral route agreement; shift/hypothesis guards refuted-on-omission). Step 3 (2026-08-20): **injectivity of the resolvent map** — `A ≠ B → (A+1)⁻¹ ≠ (B+1)⁻¹` on quadForm-nonneg matrices, by the proposal's own contrapositive route through the Step-1 identity (equal resolvents collapse it to `0`; multiplying through by the invertible shifts cancels both outer factors), with the core form `eq_of_inv_add_one_eq_inv_add_one` and the certificate iff `inv_add_one_eq_inv_add_one_iff_of_quadForm_nonneg`. QA (+19, 94 in file): two distinct PSD pairs instantiated with both resolvents left-inverse-pinned (`(mat2+1)⁻¹ = (1/8)!![3,−1;−1,3]` a new fixture) and entry-verified distinctness (`1/3 ≠ 0`, `2/3 ≠ 3/8`), plus the **invertibility guard** — the hypothesis-free implication refuted at `A = −1` vs `−1 + nilpotent` (both `+1` shifts singular, both resolvents the junk `0`). The sorted-eigenvalue transfer for transformed matrices stays a named residual for the mixing-time program's similarity transfer. |
| Decidable Spectral Certificates and the Expander Mixing Lemma ([`decidable-spectral-certificates.md`](decidable-spectral-certificates.md)) | 2026-08-19 | **Program complete — all five steps (0–4) delivered 2026-08-19, zero new axioms throughout (count stays 13).** Step 0: the Laplacian-first convention decision (restated EML signature) and the decisive ℚ-`decide` spike (kernel `decide` cannot reduce ℚ arithmetic at all — a reducibility wall; the integer cross-multiplied twin is the adopted checker, kernel-verified at `Fin 4`/`Fin 6` at spike time). Step 1: the discrepancy core (`edgeWeight`, centered indicators, the `d`-regular main-term split). Step 2: **the Expander Mixing Lemma itself** (Rayleigh sandwich on `1⊥` via the `A + L = D` identity; `hloop` found unnecessary and dropped; `C₄` QA with derived-then-attained `μ = 2`). Step 3: **the certificate module** (`GraphTheory.SpectralCertificates` — the ℚ specification checker, the kernel-verifiable ℤ twin + fractional variant, proved cross-multiplication bridges, and the flagship soundness theorem `lambda2_le_of_certificate`, the load-bearing consumer of the proved `lambda2_variational`). Step 4: QA and extraction demonstration — the `C₆` chain kernel-decided end-to-end (`lambda2 (C₆) ≤ 1` from `decide`d integer data), the exact spectrum pins (`lambda2 (C₄) = 2` via a Poincaré inequality through the *lower* half of `lambda2_variational`; `λ_max = 4`; `μ(C₄) = 2` at Ramanujan equality; `K₃` pinned with `μ = 1` and the EML attained on both cuts tested), and the Acceptance-Criterion-4 documentation (scoreboard, radar, traction-plan note). 688 → 752 QA declarations. |
| Foster's Theorem, Phase A ([`spectral-graph-sparsification.md`](spectral-graph-sparsification.md)) | 2026-08-19 | `GraphTheory.Foster` delivered as pure hard crust, zero new axioms (count stayed 13; `#print axioms` confirms only the three standard axioms): `foster_theorem` (`(∑ i, ∑ j, A i j * R i j)/2 = card V − 1` on every connected symmetric-nonnegative network, stated at full strength with no cardinality hypothesis), the per-pair spectral sum `effectiveResistance_eq_sum_eigbasis`, the one-kernel-index count `card_filter_eigvalOf_laplacian_eq_zero`, and the leverage-score objects (`leverageScore`, `sum_leverageScore_eq_two` — the importance-sampling distribution of the blocked Phase B). Route exactly as scoped in the "Correction": pseudoinverse-free eigenbasis, reconciling (not reversing) the `electrical-structure-crust.md` removal, which was of the `L⁺` route only. QA `Foster_QA.lean` (53 declarations): `K₃`/`K₄`/`P₃`/star, every resistance pinned by explicit potentials (`K₄` via the general-pair potential `(e i − e j)/4` proved for *every* pair), ordered sums computed independently of the theorem and cross-checked, the double-counting factor refuted-on-omission on both cliques (`4 ≠ 2`, `6 ≠ 3`), leverage pinned. Phase B remains blocked unchanged. |
| [Electrical Flows, Thomson's Principle, and Rayleigh Monotonicity](electrical-flow-routing.md) | 2026-08-19 | All six steps delivered, zero new axioms (count stayed 13): the flow interface with Kirchhoff conservation (step 1), flow energy with the energy agreement and the double-counting/zero-competitor guards (step 2), Thomson's principle via the divergence-free superposition lemma (step 3), Rayleigh monotonicity in conductance form with the orientation guard (step 4), and the ICP capacity-reinforcement example — `increaseConductance` + the connectivity adapter + the one-hypothesis headline `effectiveResistance_le_increaseConductance`, with the release QA example on the Mathlib path graph (`2 → 3/2` strict, step 5). The resistance API is now a routing object with a certified minimum-energy guarantee and a capacity-monotonicity theorem. |
| Cauchy interlacing retirement (`eigen_interlacing_principal_submatrix`, no dedicated proposal file — the min–max proposal's first named consumer) | 2026-08-18 | `eigen_interlacing_principal_submatrix` proved from the locally built Courant–Fischer engine (14 → 13 axioms) — the first SGT-center retirement, and the engine's first named consumer as `prove-courant-fischer.md` itself anticipated. The interlacing window is pinned numerically in QA. See the delivery note in `prove-courant-fischer.md` for the route. |
| [Derive and Retire the Sherman–Morrison Axiom](retire-sherman-morrison.md) | 2026-08-18 | The `sherman_morrison` axiom retired to a proved theorem at unchanged name, hypotheses, and statement (15 → 14 axioms) — a **pure proof task, not a correctness repair** (the proposal's own pre-check verified the statement correct against the corrected Woodbury shape; the `C`/`C⁻¹` defect is invisible at rank one). Route as proposed: the `k = Fin 1` specialization of the proved `woodbury_identity` — `Fin 1` column/row packing, the 1×1 middle factor with entry `1 + v ⬝ᵥ (A⁻¹ *ᵥ u)` (`Fin.sum_univ_one` computation), unit determinant from `hv` through `Matrix.det_fin_one`, the 1×1 scalar inverse through `Matrix.inv_eq_left_inv`, and the pin's `mul_smul`/`smul_mul` reshaping. Load-bearing on the Woodbury repair. QA (+4 in `MatrixUpdates_QA`): adjugate-independent positive instance at `diag 2 2` + all-ones (`!![3/8,-1/8;-1/8,3/8]`, denominator computed to `1`), and the excluded-denominator negative witness (`v ⬝ᵥ (A⁻¹ *ᵥ u) = -1` attained at a `Fin 1` fixture where the update is the singular zero matrix). |
| [Prove General Courant–Fischer Min-Max](prove-courant-fischer.md) | 2026-08-18 | The `k`-th sorted eigenvalue of any real symmetric matrix characterized at every index, pure hard crust (axiom count stays 15 — general min–max was never admitted): the two witness directions (`exists_submodule_forall_rayleigh_le`, `exists_ne_mem_rayleigh_ge_of_finrank_eq`) and the packaged infimum equation `evals_min_max`, all symmetry-only, proved through the existing eigenbasis algebra plus the pin's dimension-intersection lemma (the proposal's open survey step resolved affirmatively first). New public center API: general-`k` multiplicity pins and component-form Rayleigh bounds. QA (33 declarations): pinned spectra, both directions instantiated (incl. the derived top-eigenvalue-domination universal and exact attainment), and negative witnesses (wrong subspace refuted; dimension hypothesis load-bearing; interior-index wrong subspace refuted on the path Laplacian with cross-engine agreement). The four named consumers (interlacing, Rayleigh monotonicity, Cheeger hard direction, Fiedler Phase B) remain open follow-ons, as the proposal itself requires. |
| [Repair and Retire the Woodbury Identity Axiom](repair-and-retire-woodbury.md) | 2026-08-18 | The verified-false admitted axiom retired to a proved theorem at the same name (16 → 15 axioms), an emergency correctness repair per architecture §9. Statement migrated to the standard middle factor `C⁻¹ + V A⁻¹ U` with exactly the three `IsUnit` determinant hypotheses (the sum's invertibility now derived); proved from Mathlib's `Matrix.invOf_add_mul_mul` plus the `NonsingularInverse` bridges — zero local proof cost, zero new axioms. The old scalar shape is refuted in QA *with its hypotheses proved satisfied* at the counterexample, and the corrected `IsUnit C.det` hypothesis is proved to exclude it. Zero consumers existed, so no migration surface; no deprecated compatibility declaration (it must not restate the false identity). |
| [Prove One Named Inequality](prove-cheeger-easy-direction.md) | 2026-08-18 | The easy direction of Cheeger (`cheeger_upper_bound`, `λ₂(L_sym) ≤ 2φ`) retired from axiom to proved theorem (17 → 16). Route: the `lambda2_variational` argument generalized to any symmetric PSD matrix with `onesVec` in its kernel (`secondEval_variational`), consumed through the new `secondEval_le_rayleigh` at the volume-centered cut indicator. The remaining Cheeger axiom is the hard direction only. Residuals named in the proposal file: the irregular-graph generalization (the proposal's "forces" item) and the supporting moves (meta-work freeze, one Mathlib PR) are not delivered by this retirement. |
| [Grow the Crust Through Electrical Structure](electrical-structure-crust.md) | 2026-08-18 | All six steps landed. `GraphTheory.Electrical` — effective resistance, existence, uniqueness, the energy identity, and the one-sided Dirichlet bound — all proved, zero new axioms. Axiom count held at 18 throughout the program. |
| [Prove λ₂'s Variational Characterization](prove-lambda2-variational.md) | 2026-08-18 | `lambda2_variational` retired as an axiom (18 → 17). Delivered for the combinatorial Laplacian instance; the normalized-Laplacian instance `prove-cheeger-easy-direction.md` needs is a named remaining step, not yet written — see that proposal's updated priority above. |
| [Sustained Autonomous Scaffold Pursuit](sustained-autonomous-pursuit.md) | 2026-08-17 | The adopted design behind `scripts/opencode-pursue`, `AGENTS.md`, and `scripts/README.md` — foundational to how every other proposal in this directory gets executed, not itself an SGT math proposal. |

## Retired

| Proposal | Disposition |
| --- | --- |
| `gem-eng-use-cases.md` (root, never tracked) → `eng-use-cases.md` | Corrected once (proved/admitted tagging added, fabricated quantum persona removed), then judged unnecessary as a standalone document — useful content integrated into `docs/traction-plan.md` (release-readiness proof example, audience narrowing, and the recorded reason broader engineering personas were set aside). File removed. |
