# QA Scoreboard and Repository Health

**Status:** Canonical verification record  
**Last reviewed:** August 22, 2026

This document separates source-derived counts from commands that have actually been run. A QA declaration is counted syntactically; it is considered verified only when its module check passes. QA proves selected consequences relative to Scaffold’s axioms and does not prove those axioms.

## Source metrics

Run `python3 scripts/generate_qa_scoreboard.py` after changing Lean source.

<!-- BEGIN GENERATED SOURCE METRICS -->
_Generated from Lean source on 2026-08-22._

| Metric | Count |
| --- | ---: |
| QA theorem/lemma declarations | 1378 |
| `sorry`/`admit` tokens in QA code | 0 |
| Explicit axioms in `Scaffold/Mathlib` | 10 |
| `sorry`/`admit` tokens in `Scaffold/Mathlib` code | 0 |

### QA declarations by domain

| Domain | Declarations |
| --- | ---: |
| Concentration | 23 |
| Core | 12 |
| Derived | 11 |
| InformationTheory | 31 |
| LinearAlgebra | 36 |
| OperatorTheory | 94 |
| Perturbation | 82 |
| SpectralGraph | 1089 |

### QA files

| File | Declarations | Placeholder tokens |
| --- | ---: | ---: |
| `Scaffold/QA/Concentration/Matrix_QA.lean` | 6 | 0 |
| `Scaffold/QA/Concentration/Scalar_QA.lean` | 17 | 0 |
| `Scaffold/QA/Core/MatrixUpdates_QA.lean` | 12 | 0 |
| `Scaffold/QA/Derived/EventStream_QA.lean` | 7 | 0 |
| `Scaffold/QA/Derived/ProjectorDrift_QA.lean` | 4 | 0 |
| `Scaffold/QA/InformationTheory/Entropy_QA.lean` | 31 | 0 |
| `Scaffold/QA/LinearAlgebra/PerronFrobenius_QA.lean` | 36 | 0 |
| `Scaffold/QA/OperatorTheory/Resolvent_QA.lean` | 94 | 0 |
| `Scaffold/QA/Perturbation/DavisKahan_QA.lean` | 27 | 0 |
| `Scaffold/QA/Perturbation/ProjectionGap_QA.lean` | 37 | 0 |
| `Scaffold/QA/Perturbation/Weyl_QA.lean` | 18 | 0 |
| `Scaffold/QA/SpectralGraph/Band_QA.lean` | 109 | 0 |
| `Scaffold/QA/SpectralGraph/BasicProperties_QA.lean` | 5 | 0 |
| `Scaffold/QA/SpectralGraph/Basic_QA.lean` | 14 | 0 |
| `Scaffold/QA/SpectralGraph/Cheeger_QA.lean` | 33 | 0 |
| `Scaffold/QA/SpectralGraph/Connectivity_QA.lean` | 15 | 0 |
| `Scaffold/QA/SpectralGraph/CourantFischer_QA.lean` | 33 | 0 |
| `Scaffold/QA/SpectralGraph/Cuts_QA.lean` | 11 | 0 |
| `Scaffold/QA/SpectralGraph/Directed_QA.lean` | 88 | 0 |
| `Scaffold/QA/SpectralGraph/Dynamics_QA.lean` | 5 | 0 |
| `Scaffold/QA/SpectralGraph/EffectiveResistance_QA.lean` | 26 | 0 |
| `Scaffold/QA/SpectralGraph/ElectricalFlow_QA.lean` | 88 | 0 |
| `Scaffold/QA/SpectralGraph/Exhaustive_QA.lean` | 87 | 0 |
| `Scaffold/QA/SpectralGraph/Expander_QA.lean` | 88 | 0 |
| `Scaffold/QA/SpectralGraph/Fiedler_QA.lean` | 57 | 0 |
| `Scaffold/QA/SpectralGraph/Foster_QA.lean` | 53 | 0 |
| `Scaffold/QA/SpectralGraph/Interlacing_QA.lean` | 9 | 0 |
| `Scaffold/QA/SpectralGraph/KernelBridge_QA.lean` | 14 | 0 |
| `Scaffold/QA/SpectralGraph/Mixing_QA.lean` | 117 | 0 |
| `Scaffold/QA/SpectralGraph/Normalized_QA.lean` | 34 | 0 |
| `Scaffold/QA/SpectralGraph/PotentialSolvability_QA.lean` | 12 | 0 |
| `Scaffold/QA/SpectralGraph/Projector_QA.lean` | 5 | 0 |
| `Scaffold/QA/SpectralGraph/RandomWalk_QA.lean` | 7 | 0 |
| `Scaffold/QA/SpectralGraph/SimpleGraphAdapter_QA.lean` | 38 | 0 |
| `Scaffold/QA/SpectralGraph/SpectralCertificates_QA.lean` | 39 | 0 |
| `Scaffold/QA/SpectralGraph/Stationary_QA.lean` | 26 | 0 |
| `Scaffold/QA/SpectralGraph/Tikhonov_QA.lean` | 30 | 0 |
| `Scaffold/QA/SpectralGraph/VariationalTransfer_QA.lean` | 15 | 0 |
| `Scaffold/QA/SpectralGraph/Variational_QA.lean` | 31 | 0 |
<!-- END GENERATED SOURCE METRICS -->

## Verification record

| Check | Result | Date | Scope and limitation |
| --- | --- | --- | --- |
| `lake build` | Pass | 2026-08-22 | Post-Perron–Frobenius-admission build (2026-08-22, proposal `admit-perron-frobenius.md` — one new explicit cited axiom, 9 → 10): 2232 targets, "Build completed successfully", zero errors, run detached (log + poll) — the new public module `Scaffold/Mathlib/LinearAlgebra/PerronFrobenius.lean` (the `Matrix.IsIrreducible` predicate plus the admitted Horn–Johnson 8.4.4 axiom with no strict-dominance clause) is inside the umbrella import closure and elaborates clean directly (`lake env lean`, zero errors, zero warnings); its QA consumer `QA/LinearAlgebra/PerronFrobenius_QA.lean` (36 declarations) elaborates and builds explicitly per the scope note below, with `#print axioms` on the five axiom-consuming QA theorems listing exactly `perron_frobenius` plus the three standard axioms and on the eleven hand theorems (including the strict-dominance refutation centerpiece) only `propext, Classical.choice, Quot.sound`. Post-directed-Step-2 build (2026-08-22, proposal `directed-graph-operators.md` Step 2 + the Step-3 agreement brick, zero new axioms): 2230 targets, "Build completed successfully", zero errors, run detached (log + poll) — the extended `GraphTheory.Directed` (now carrying `directedNormalizedLaplacian` and its four theorems) is inside the umbrella import closure and elaborates clean directly; its QA consumer (41 → 88 declarations) elaborates and builds explicitly per the scope note below. Post-directed-operators build (2026-08-22, proposal `directed-graph-operators.md` Steps 0+1, zero new axioms): 2230 targets, "Build completed successfully", zero errors, run detached (log + poll) — the new `GraphTheory.Directed` is inside the umbrella import closure and elaborates clean directly; its QA consumer elaborates and builds explicitly per the scope note below. Post-subgaussian-retirement build (2026-08-22, proposal `prove-subgaussian-tail-bound.md` — the axiom count's first measure-theoretic retirement): 2229 targets, "Build completed successfully", zero errors, run detached per the recorded procedure (log + poll; the only Scaffold diagnostic the documented pre-existing `unused variable K` warning in `Subgaussian.lean`, verified identical in HEAD) — `Probability.Concentration.Scalar.Subgaussian` is inside the umbrella import closure and elaborates clean directly (`lake env lean`; `subgaussian_tail_bound` now a proved theorem, `#print axioms` clean; the QA consumer re-elaborated and built explicitly, 6 → 17 declarations). Post-entropy build (2026-08-22, the finite-relative-entropy program): 2229 targets, "Build completed successfully", zero errors, run in the foreground — the new public module `Scaffold/Mathlib/InformationTheory/Entropy.lean` is inside the umbrella import closure and elaborates clean directly (`lake env lean`, zero errors, zero warnings; `#print axioms` on all nine public theorems reads only `propext, Classical.choice, Quot.sound`). **Scope correction recorded this run (content-change probe: appending a comment to a QA file leaves `lake build` with nothing to do):** the default target is the `Scaffold.lean` umbrella and its transitive imports — it certifies every *public* module (including the new `InformationTheory.Entropy`) but does not itself compile the QA tree; QA modules are certified by direct `lake env lean` elaboration plus explicit `lake build Scaffold.QA.*` targets, which is how this run verified `QA/InformationTheory/Entropy_QA.lean` (31 declarations, zero errors, zero warnings; `#print axioms` on thirteen headline QA theorems reads only the three standard axioms). Post-χ²-assembly buildPost-χ²-assembly build (2026-08-22, the mixing-time program complete): 2228 targets, "Build completed successfully", zero errors (executed detached, log + poll — the recorded procedure; the changed modules `Normalized` (+3 entry lemmas), `Mixing` (+4 theorems: the centered evolution, mass conservation, the connectivity mode derivation, and the closing bound `chiSquareDistance_le_of_connected`), and `Mixing_QA` (67 → 117 declarations) all elaborate clean directly with `lake env lean` — `Normalized`'s only diagnostic the documented pre-existing `congr 1` note, `Mixing`/`Mixing_QA` zero errors zero warnings; `#print axioms` on the seven new public and twelve headline QA theorems reads only `propext, Classical.choice, Quot.sound`). Post-decay-engine build (2026-08-22): 2227 targets, "Build completed successfully", zero errors (executed detached; the three changed modules `Spectral`, `Normalized`, `Mixing` and the QA consumer elaborate clean directly with `lake env lean` — `Spectral`'s only diagnostics its documented pre-existing section-variable warnings, `Normalized`'s only the documented pre-existing `congr 1` note, `Mixing`/`Mixing_QA` zero errors zero warnings). Default target is the library root `Scaffold.lean`; its umbrella now also certifies `GraphTheory.Mixing` (added 2026-08-22 — the ℓ²-mixing proxy of the mixing-time program's Step 2) and `Analysis.OperatorTheory.Perturbation.{ProjectionGap,Duhamel}` (Duhamel added 2026-08-21 — the heat-semigroup/FTC module behind the Davis–Kahan retirement). Post-decay-engine build (2026-08-22): 2227 targets, "Build completed successfully", zero errors (executed detached; the three changed modules `Spectral`, `Normalized`, `Mixing` and the QA consumer elaborate clean directly with `lake env lean` — `Spectral`'s only diagnostics its documented pre-existing section-variable warnings, `Normalized`'s only the documented pre-existing `congr 1` note, `Mixing`/`Mixing_QA` zero errors zero warnings). Post-mixing-Step-2 build (2026-08-22): 2227 targets, "Build completed successfully", zero errors, only the documented pre-existing warnings (executed detached from the tool timeout — the recorded procedure; the new `GraphTheory.Mixing` and its `Mixing_QA` consumer elaborate clean directly with `lake env lean`, zero errors, zero warnings on first complete pass). Post-mixing-time-Step-1 build (2026-08-22): 2227 targets, "Build completed successfully", zero errors, only the documented pre-existing `hγ` warning (executed detached from the tool timeout — the recorded procedure; `GraphTheory.Normalized` extended with the eigenpair-transfer section, its QA consumer re-elaborated clean). Post-Davis–Kahan-retirement build (2026-08-21): 2227 targets, "Build completed successfully", zero errors (executed detached from the tool timeout — the recorded procedure; `davis_kahan_sin_theta` now a proved theorem, `davisKahanTwoPoint` fully hard crust, `eventStreamProjectorDrift` conditional on `matrix_azuma_hoeffding` alone). its umbrella certifies `Core.{RandomVariable,Norms,MatrixUpdates}`, `GraphTheory.{Spectral,SimpleGraphAdapter,Electrical,ElectricalFlow,Foster,Expander,SpectralCertificates,Tikhonov,Band,Cheeger,Fiedler,RandomWalk,Normalized,Stationary,Mixing,VariationalTransfer,Dynamics}` (`GraphTheory.Band` added 2026-08-20 — the two-sided band projector; `GraphTheory.Tikhonov` added 2026-08-20), `Analysis.OperatorTheory.Perturbation.{Weyl,DavisKahan}` (the Weyl module's bound **proved** since 2026-08-20 — the axiom retired), `Analysis.OperatorTheory.Resolvent` (added 2026-08-19: the operator-norm bridge, shifted-PSD invertibility, the resolvent identity; extended 2026-08-20 with the Step-3 resolvent-map injectivity), `Probability.Concentration.{Scalar.*,Matrix.*}`, and the derived layer `Derived.{EventStream,ProjectorDrift}`. Post-Band-Step-4 build (2026-08-21): 2186 targets, "Build completed successfully", zero errors (executed detached from the tool timeout — log file + process poll, the recorded procedure; the only Scaffold diagnostic the documented pre-existing `unused variable 'hγ'` warning in `Derived/ProjectorDrift.lean`, unchanged; `GraphTheory.Band` gained the two Mathlib imports `Analysis.InnerProductSpace.{Projection,PiL2}` for the Hilbert-projection step). Post-Band-Step-3 build (2026-08-20): 2186 targets, "Build completed successfully", zero errors (executed detached from the tool timeout — log file + process poll, the recorded procedure; the run's first `lake build` invocation was killed at its 300 s tool timeout during the Mathlib trace replay — the olean set verified intact by the corrected presence check afterward, so no re-fetch was needed — and the detached re-run replayed the residue and passed). Post-Band-Step-2 build (2026-08-20): 2186 targets, "Build completed successfully", zero errors (executed detached from the tool timeout — log file + process poll, the recorded procedure; the only diagnostic the documented pre-existing `unused variable 'hγ'` warning in `Derived/ProjectorDrift.lean`, unchanged by that run; the four new Step-2 theorems and the +16 QA declarations elaborated clean first pass). Post-Band build: 2186 targets, "Build completed successfully", zero errors (executed detached from the tool timeout — log file + process poll, the recorded recovery procedure; only the documented pre-existing section-variable warnings appear, and `GraphTheory.Spectral`'s re-derivation of `spectralProjector_idempotent` added none). Post-Tikhonov build: 2185 targets, "Build completed successfully" (the first invocation of the day was killed at its tool timeout mid-replay — the recorded hazard — with the Mathlib oleans surviving this time; a re-run completed cleanly). |
| Direct QA module targets | Pass | 2026-08-22 | All thirty-nine QA modules compiled individually; the Perron–Frobenius slice (2026-08-22, proposal `admit-perron-frobenius.md`): the new `LinearAlgebra/PerronFrobenius_QA.lean` (36 declarations, zero errors, zero warnings, built explicitly) — two deliberately asymmetric rational fixtures so the certification runs on genuinely directed input: the **positive witness** `P = !![1,2;1,0]` (primitive, positive diagonal) and the **calibration witness** `D = !![0,4;1,0]` (an asymmetric directed 2-cycle — zero diagonal, period 2, imprimitive), both with their asymmetry refuted (`2 ≠ 1`, `4 ≠ 1`) and irreducibility discharged by one-step arcs. On `P`: the axiom instantiated and the Perron root **derived to be exactly 2** from the eigen-equation on the unknown existential witness (`x₁(r² − r − 2) = 0`, `x₁ > 0`, positivity kills the `−1` root), the Perron vector pinned to a positive multiple of the hand vector `(2,1)`, and the axiom's simplicity clause cross-checked against the hand factorization `charpoly P = (X + 1)(X − 2)` with `rootMultiplicity 2 = 1` (the `rootMultiplicity_mul_X_sub_C_pow` route); the complexified charpoly factored with roots exactly `{2, −1}` and the **domination clause instantiated at both roots** (`|2| ≤ 2`, `|−1| ≤ 2` — the strict case of the primitive fixture). On `D`: the same derivation pins `r = 2` and the vector along `(2,1)`; the hand eigenvector `(−2,1)` at eigenvalue `−2` pinned entrywise; and the **mandated centerpiece** `strict_dominance_refuted_QA` — the strengthening "every other eigenvalue has strictly smaller modulus" is refuted on the Perron pair `(2, (2,1))` and the eigenpair `(−2, (−2,1))` with `|−2| = 2 = r` (no axiom consumed: the refutation targets the strengthening, not the axiom), while `D_axiom_domination_QA` instantiates the axiom's own domination clause at the peripheral root `−2` (`|−2| ≤ r` with equality — the axiom's weaker conclusion is exactly what survives on imprimitive input). Both fixtures also carry the **hand-only only-eigenvalue theorems** (`P/D_only_nonneg_eigenvalue_QA`: eigen-equations + nonnegativity alone force `μ = 2`, killing the `−1`/`−2` roots — no axiom), and `P_axiom_only_eigenvalue_on_hand_witness_QA` feeds the hand Perron eigenvector into the axiom's only-eigenvalue clause so the axiom's content and the fixture's content are independently established and cross-checked. `#print axioms`: the five axiom-consuming theorems list `perron_frobenius` + the three standard axioms; the eleven hand theorems only the standard three. Pin-specific techniques recorded in the file header: `open Polynomial` (plain, name resolution) is what activates `X`/`C` on this pin — `open scoped Polynomial` does not (they are defs, not scoped notation); `fin_cases` leaves eta-expanded indices that defeat positional `rw`, bridged by explicit `show` at the defeq literal goal; and `ring` treats `C c` as an atom, so C-numerals are normalized through one-time `C_add`/`C_1`-based helpers (`C_two_real_QA` et al.) before any `ring` call — `C_eq_natCast` does not match OfNat literals. The directed-operators Step-2 slice (2026-08-22): `SpectralGraph/Directed_QA.lean` re-elaborated clean (41 → 88 declarations, zero errors, zero warnings, built explicitly; `#print axioms` on nineteen headline QA theorems reads only `propext, Classical.choice, Quot.sound`) — all nine entries of `L_dir dirA` computed from the entry form (degrees `(4,1,1)` all squares, so the operator is exactly `[[1,−1,−1/2],[−1,1,0],[−1/2,0,1]]`, visibly symmetric), the hypothesis-free symmetry instantiated on the asymmetric fixture (the load-bearing certification — a definition that secretly needed `IsSymm` would not elaborate here) with the `(0,1)`/`(1,0)` pair cross-checked raw, the square-root-free conjugate instantiated with both sides' `(0,1)` entries computed raw and matching at `−2` (`√4·(−1)·√1` against `0 − ½(3+1)`), the symmetric-edge agreement through the Step-3 theorem with both operators' entries pinned raw, and the two new negative witnesses — undirected reuse (`L_dir dirA ≠ normalizedLaplacian dirA`, entry `−1 ≠ −3/2`: the undirected operator on directed input silently loses the symmetrization) and **the calibration refutation**: on the new fixture `dirB = !![0,4;1,0]` (nonnegative, positive out-degrees, `L_dir` symmetric by the theorem) `quadForm L_dir 1 = −1/2 < 0` — the symmetrized adjacency carries weight `5/2` against the out-degree normalization `√4·√1 = 2`, so `L_dir 0 1 = −5/4` dips past `−1`, and the positivity layer of the undirected toolkit (PSD-ness, variational eigenvalue bounds) provably does not transfer to the directed axis; the directed spectral theory needs Perron–Frobenius, exactly the boundary the proposal's Calibration section draws. Pin-specific techniques added to the file header: `Real.sqrt_eq_iff_eq_sq` for square roots of numerals (`norm_num` alone does not evaluate `Real.sqrt 4` on this pin), and ground `Fin`-index `if`s (from `Matrix.diagonal_apply` in entry computations) resolved by full `simp`, which `norm_num` leaves standing. Earlier: the directed-operators slice (2026-08-22): the new `SpectralGraph/Directed_QA.lean` (41 declarations, zero errors, zero warnings, built explicitly; `#print axioms` on twelve headline theorems reads only `propext, Classical.choice, Quot.sound`) — the genuinely-directed fixture `dirA` with both degree functions pinned raw through the nine-entry `rfl` table, handshaking `6 = 6` by both routes, the symmetric-edge agreement witness, and the two negative witnesses (`outDeg ≠ inDeg`, walk-matrix symmetry refuted `3/4 ≠ 1`). Earlier: `Concentration/Scalar_QA.lean` re-elaborated clean 2026-08-22 for the subgaussian retirement (6 → 17 declarations, zero errors; the file's nine remaining warnings are the documented pre-existing set in untouched hoeffding/bernstein/norm declarations, verified against HEAD; `#print axioms` on all twelve touched/new QA theorems reads only `propext, Classical.choice, Quot.sound` — the zero-QA family no longer consumes `hoeffding_lemma` at all): the zero fixture updated to the repaired interface with both moment hypotheses discharged constructively (`subgaussian_tail_bound_zero_hypotheses_QA`: the MGF integrand is the constant `1`, integrable with integral exactly `1` under any probability measure); the proposal-mandated **genuinely nonzero instance** — the constant-1 variable at `K = 2`, `t = 1` on `Measure.dirac 0` (`subgaussian_tail_bound_instance_QA`) with the moment bound `exp (1/4) ≤ 2` proved through the pinned decimal lower bound `Real.log_two_gt_d9 : 0.6931471803 < log 2` (`subgaussian_exp_quarter_le_two_QA`), the event side pinned raw to exactly `1` (`subgaussian_tail_bound_instance_event_QA`), and the bound side pinned raw (`subgaussian_tail_bound_instance_bound_QA`: `1 ≤ 2 * exp (−(1/8))` — the instantiation carries genuine content on both sides, the left side `1` not `0`); and the **refutation family** for the retired shape — `refutationMeasure := (3 : ℝ≥0∞) • dirac 0` with its total mass and constant-1 integral each pinned (`= 3`, both raw), `old_subgaussian_tail_bound_norm_junk_QA` exhibiting the empty-set junk mechanism (every candidate MGF integral is `3 > 2`, so `Real.sInf_empty` gives norm `0`), `old_subgaussian_tail_bound_hypotheses_QA` (the old hypotheses provably satisfied — vacuously), and `old_subgaussian_tail_bound_refuted_QA` (the old conclusion at `X = 0, K = 1, t = 0` reads `3 ≤ 2` — refuted with hypotheses satisfied, the Woodbury/old-Cheeger falsification pattern); `InformationTheory/Entropy_QA.lean` elaborated clean 2026-08-22 for the finite-relative-entropy program (31 declarations, the first InformationTheory-domain QA file: the biased-vs-fair pair on `Fin 2` with the divergence computed **by hand** to `(1/4)·log(27/16)` from `Real.log_pow`/`Real.log_mul` alone and pinned **strictly positive** (`klDiv_bias_pos` — the bound not vacuously tight), the equality case exercised in both directions against raw computations (the hand-computed diagonal `klDiv pBias pBias = 0` fed *through* `klDiv_eq_zero_iff` to recover `pBias = pBias`; the forward direction deriving `klDiv pBias qFair ≠ 0` from `pBias ≠ qFair`), the biased coin's entropy pinned by hand to `(1/4)·log(256/27)` and the **uniform bridge cross-checked numerically** at the fair coin (`klDiv_bridge_bias`: `¼·log(27/16) = log 2 − ¼·log(256/27)`, both sides independently computed — load-bearing on `klDiv_apply_uniform`: a wrong sign or constant would fail to match the pinned arithmetic), the uniform distribution on `Fin 4` computed to `log 4` two ways (definition and the equality case of the maximum), the **strictness witness** — the non-uniform `(1/2, 1/6, 1/6, 1/6)` pinned strictly below `log 4`, strictness available *only* through the equality-case iff (the plain bound gives `≤`) — plus its divergence-side companion through the bridge, and the delta distribution `(1, 0, 0, 0)` with entropy exactly `0` — the `p i = 0 ↦ 0` junk convention made visible — and strictly below the maximum; `#print axioms` on the thirteen headline QA theorems reads only `propext, Classical.choice, Quot.sound`), including `SpectralGraph/Mixing_QA.lean` re-elaborated clean 2026-08-22 for the χ² assembly`SpectralGraph/Mixing_QA.lean` re-elaborated clean 2026-08-22 for the χ² assembly — the mixing-time program's closing statement (67 → 117 declarations: the final bound instantiated on both fixtures — **K₃ with the bound attained exactly** at `t = 1, 2` (`1/2 ≤ (1/4)·2`, `1/8 ≤ (1/16)·2`, both sides pinned raw from the laws and π), the **P₃ rate derived basis-independently** from two sum-of-squares certificates (the Dirichlet identity `v ⬝ L_sym v = (v₀ − v₁/√2)² + (v₂ − v₁/√2)²` and its `2‖v‖² − ·` companion pinning every eigenvalue into `[0, 2]` — no exact spectrum needed) with the λ* = 1 bound honest and strict (`χ²(2) = 1 ≤ 3`), the centered-evolution and connectivity-derived-mode cross-checks against the component-1 hand computations, and the two negative witnesses — **connectivity load-bearing** on the triangle⊕self-loop `Fin 4` fixture (spectrum `{0, 0, 3/2, 3/2}` derived basis-independently so the rate hypothesis provably holds at `r = 1/2` — the loop's `μ = 0` mode excluded from the rate hypothesis by design, exactly the hole connectivity plugs — while `χ²(3) = 3/8 > 3/64` refutes the conclusion) and **the rate load-bearing** at `r = 1/4` on K₃ (hypothesis unsatisfiable by the trace, conclusion `1/2 > 1/8` refuted); `#print axioms` on the twelve headline QA theorems reads only `propext, Classical.choice, Quot.sound`), including `SpectralGraph/Mixing_QA.lean` re-elaborated clean 2026-08-22 for the Step-3 decay engine (33 → 67 declarations: the path cross-check section — the exact Parseval identity instantiated at `t = 2` on the centered density `(3,−1,−1)` with the conjugated norm pinned raw to `4` and the eigencomponent sum thereby pinned basis-independently, and the π-norm bridge tying the engine's two-step quantity to the already-pinned `χ²(2) = 1`; the triangle section — the entry-table fixture, the basis-independent eigen-facts `tri_eigvalOf_cases` (every eigenvalue `0` or `3/2`, from the summed eigen equation and the unit-norm quadratic form), `tri_kernel_const` (the kernel is the constant direction), `tri_exists_kernel_index` (trace `3` vs `9/2`), the mode and rate hypotheses at `r = 1/2`, the contraction instantiated at `t = 1, 2` with the raw π-norms `1/2`, `1/8` pinned and the bounds exact (`tri_decay_exact_QA`: `(1/4)·2 = 1/2`, `(1/16)·2 = 1/8`), and two negative witnesses — the mode guard (`g = 1`: conclusion refuted `1 ≤ 1/4`, hypothesis provably unsatisfiable at the kernel index) and its hypothesis-failure certificate; `#print axioms` on the thirteen headline QA theorems reads only `propext, Classical.choice, Quot.sound`), including `SpectralGraph/Mixing_QA.lean` added 2026-08-22 (mixing-time Step 2, the ℓ²-mixing proxy, 33 declarations: the P₃ fixture with `π` pinned to `(1/4, 1/2, 1/4)` from degrees `1+2+1`; stationarity instantiated through `walk_isStationary` *and* cross-checked by raw literal arithmetic `Pᵀ *ᵥ π = π`; the walk laws at `t = 0, 1, 2` pinned (`δ₀`, `(0,1,0)`, `(1/2,0,1/2)`) with mass conservation instantiated and recomputed from the pinned literal; the **density evolution** instantiated through `walkDensity_succ` and cross-checked raw (`P *ᵥ (4,0,0) = (0,2,0)` — the walk acts on densities by `P` *itself*, not its adjoint, exactly the detailed-balance content); `χ²(0) = 3` by *both* routes (the `(π x)⁻¹ − 1` theorem and the termwise raw sum `9/4 + 1/2 + 1/4`); `χ²(2) = 1` raw — the bipartite oscillation, never decaying, consistent with the λ* = 1 behavior that Step 3's bound must reproduce on this fixture; the density-form equivalence instantiated with its sum recomputed raw (`1`); the plain-ℓ² bridge instantiated at `c = 1/2` with the unweighted sum pinned to `3/8 ≤ 1/2` strict; and two negative witnesses — the asymmetric `!![0,2;1,0]` with positive degrees where hypothesis-free stationarity is *refuted* at the entry where symmetry provably fails (`1/3 ≠ 2/3`, `hA` load-bearing), and the zero adjacency where `χ²(0,0) = 0` (the junk division) while the law is the point mass `≠ π`, refuting the hypothesis-free `χ² = 0 ↔ ν = π` so degree positivity is load-bearing; `#print axioms` on the twelve headline QA theorems reads only `propext, Classical.choice, Quot.sound`), including `Stationary_QA` re-elaborated clean 2026-08-22 (reversibility Phase A, `proposals/reversibility-and-heat-semigroup.md`, 12 → 26 declarations: detailed balance on the P₃ path fixture instantiated through the theorems at every index pair and cross-checked by raw literal arithmetic — degree-measure form `1 = 1` at the `(0,1)` pair, stationary-measure form `1/4 = 1/4` with the total volume independently pinned to `4` from the degrees `1+2+1` — the symmetrized matrix `D * P` instantiated symmetric through the theorem with its off-diagonal entries computed raw (`1` and `1` — the two directed flows carry equal degree-weighted mass), the regular uniform-measure balance instantiated at the edge through `transitionMatrix_detailed_balance_uniform`, and the proposal's prescribed **negative witness**: the asymmetric matrix `!![0,2;1,0]` with positive degrees `(2, 1)` where the hypothesis-free balance statement is *refuted* (`deg 0 * P 0 1 = 2 ≠ 1 = deg 1 * P 1 0`) and the symmetry hypothesis is provably violated at the same entry pair (`A 0 1 = 2 ≠ 1 = A 1 0`), so `hA` is load-bearing; `#print axioms` on the eight headline QA theorems reads only `propext, Classical.choice, Quot.sound`), including `Normalized_QA` re-elaborated clean 2026-08-22 (mixing-time Step 1: the P₃ transfer QA, +20 declarations — three hand eigenpairs of `L_sym` transferred by theorem and cross-checked by raw arithmetic on the conjugated vectors, both directions, the `walkEvals` existential instantiated, the spanning hand-solve, and two guards refuting the skip-the-conjugation and unreflected-eigenvalue forms) (`lake env lean` batch, zero errors — run clean 2026-08-21 after the Davis–Kahan retirement: **BATCH-DONE fail=0**), including `Perturbation/DavisKahan_QA.lean` (extended 2026-08-21 for the retirement, 20 declarations in file: the **strict non-vacuity witness** — the rotation fixture `dkA = diag(0, 2)` perturbed by `dkE = !![0, 3/4; 3/4, 0]`, both spectra and the perturbation norm pinned independently from trace/determinant/sortedness (`[-1/4, 9/4]`, `[0, 2]`, `[-3/4, 3/4]`, `‖dkE‖ = 3/4` through the proved bridge), the eigenvector *directions* pinned from the eigen equations (`v₀ = −3·v₁` at `−1/4`; `v₁ = 0` at `0`) so both spectral projectors are pinned to explicit literals (`Q = (1/10)!![9,−3;−3,1]`, `P = diag(1,0)`) with no control over Mathlib's classical eigenbasis choice needed; the theorem instance then reads `‖Q − P‖ ≤ ‖dkE‖/(9/4) = 1/3` while the difference's spectrum is pinned to `[−√(1/10), √(1/10)]`, i.e. the exact distance is `1/√10 < 1/3` — the bound is not vacuous and not attained; the fixture exercises the theorem's no-tie (Duhamel) branch; the zero-perturbation instance retained; `#print axioms` on the new QA theorems reads only the three standard axioms) (`lake env lean` batch, zero errors — re-run clean 2026-08-21 after the Step-4 band delivery: **BATCH-DONE fail=0**, the only diagnostics the documented pre-existing section-variable warnings in untouched modules), including `SpectralGraph/Band_QA.lean` (109 declarations after the 2026-08-21 Step-4 addition, 998 total: the **Hilbert-projection slice** — `band_diag13_hilb_identification` instantiating Mathlib's `orthogonalProjection` at the transported `(0, 2]`-band range and pinning its value to the packaged band image `![3, 0]`; `band_diag13_residual_orthogonal` witnessing the residual-orthogonality engine `(x − B*ᵥx) ⬝ᵥ (B*ᵥz) = 0` through the theorem **and** by raw literal arithmetic (`![0, 4] ⬝ᵥ ![1, 0] = 0` — the two routes meet at `0`); the closest-point minimality instantiated at three competitors — **attained** (`band_diag13_hilb_min_attained`: the distance from the signal to its band image is *exactly* `4`), **strict over the zero signal** (`band_diag13_hilb_min_zero_numeric`: `4 ≤ 5`, the 3-4-5 triangle, so the projection genuinely improves on a non-optimal range member), and **generic over the whole range line** (`band_diag13_hilb_min_line`/`_sqrt`: `4 ≤ √((3 − t)² + 16)` for every `t`, both norms pinned to numbers) with `band_diag13_hilb_min_line_raw` proving the same line inequality by raw square-positivity — an independent hand-check of closest-point that the transported theorem's claim must reproduce; the **high-band instance** (`3 ≤ 5` for `(2, 4]`'s projection `![0, 4]` — the property is not locked to the low-band fixture); and the **fixed-space guard** `band_diag13_hilb_guard` — the hypothesis-free form "the projection beats every vector" is refuted at the unfiltered signal itself (distance `0 < 4`, and the signal is provably not fixed by the band), so the fixed-point hypothesis is load-bearing. Supporting pins: `norm_euclidean_{zero_four,three_four,three_zero}` (`4`/`5`/`3`) through the identity `‖e v‖² = v ⬝ᵥ v` (the Resolvent QA idiom, restated locally), the band `(0, 2] = diag(1,0)` from the pinned threshold projectors, and the raw band actions on `![3,4]`, `![1,1]`, and the generic range point `![t,0]`; `#print axioms` on the nine new headline QA theorems reads only the three standard axioms; the file's Step-3 content (84 declarations at that slice) is unchanged below. All earlier per-file records unchanged), including `SpectralGraph/Band_QA.lean` at its Step-3 state (84 declarations, 973 total: the **partition-completeness slice** — the covering two-band family `t k = 2k` summed to the identity **through the completeness theorem and from independently pinned band values** (`diag(1,0) + diag(0,1) = 1` by raw literal addition); a three-band partition `t k = k` with an **empty middle band** `(1,2] = 0` (a zero-width member contributing nothing) and its top threshold exactly touching the eigenvalue `3` — the closed right endpoint exercised; the **two endpoint guards** — a family starting at `2` (above the lowest mode `1`) and a family truncated at `n = 1` (top threshold `2` below the highest mode `3`) each provably fails to sum to the identity (the dropped mode's axis entry `0 ≠ 1`), with each violated covering hypothesis separately refuted at the pinned spectrum (`2 < 1` and `3 ≤ 2` both false) — the design note's "silently ignores modes" failure mode witnessed in both directions, so the hypothesis-free completeness form is refuted; the **non-monotone telescoping witness** — the family `4, 0, 4` whose middle band is the junk value `B(4,0] = −1` cancelling the covering band `B(0,4] = 1`, both sides of the unconditional law independently computed to `0` from pinned extreme projectors and band values; monotone-family orthogonality instantiated at the partition's two bands; and the **vector decomposition** `∑ B_k *ᵥ x = x` on `![7,−5]` by theorem and raw routes; `#print axioms` on the thirteen new headline QA theorems reads only the three standard axioms; the file's Step-2 content (43 declarations, the disjoint-band orthogonality slice — composition to zero through the theorem *and* by raw literal multiplication on the pinned band values in both orders, image orthogonality on concrete vectors by both routes with the **same-band counter-witness** (`1 ≠ 0` — the vanishing is about disjointness, not the fixture's zeros), the composed action `B_high *ᵥ (B_low *ᵥ x)` annihilating a filtered signal by both routes, a low-band range vector killed by the high band, and the **overlap guard** — bands `(−1, 3]` and `(1, 4]` sharing the eigenvalue `3` compose to the provably nonzero `diag(0,1)`, refuting the hypothesis-free form so the `b ≤ c` disjointness hypothesis is load-bearing; the file's Step-1 content, 27 declarations, covers the diagonal fixture `!![1,0;0,3]` with spectrum `{1,3}` pinned from trace + determinant independent of the projector machinery, eigenvector *directions* pinned from the eigen equations (`λ = 1 ⇒ v = ![±1,0]` — sign-independent outer products), the projector at any threshold in `[1,3)` pinned to the hand-computed eigenvector sum `e₀e₀ᵀ = !![1,0;0,0]`, band values computed independently (`B(−1,2] = diag(1,0)`, `B(2,4] = diag(0,1)`, `B(−1,4] = 1`, and the between-eigenvalues band `B(3/2,5/2] = 0` — the gapped-definition guard), idempotence both through the theorem and by raw literal multiplication, the nestedness cross-law instantiated numerically in both orders, and the mode-selection interface witnessed in both directions — the excluded mode **annihilated and provably not fixed** (`B(2,4] *ᵥ v₁ = 0 ≠ v₁`, with `v₁ ≠ 0` from unit norm), the strictly-interior mode fixed, and the low-band action recomputed by raw arithmetic), including `SpectralGraph/Tikhonov_QA.lean` (30 declarations, added 2026-08-20, proposal `tikhonov-shrinkage-filter.md`: the minimizer **pinned through the normal equation** — the candidate `![2/3,1/3]` verified by hand (entrywise Gaussian elimination on `(L+1) *ᵥ z = 1 • ![1,0]`, independent of the module) and promoted by the converse characterization `eq_tikhonovMinimizer_of_add_smul_one_mulVec` to `tikhonovMinimizer adjK2 _ 1 ![1,0] = ![2/3,1/3]`, so the spectral-theorem construction and a hand-solved 2×2 system meet at the same vector; the objective pinned (`obj(x*) = 1/3` exactly against `obj(y) = obj(0) = 1`, minimality instantiated as `1/3 ≤ 1` and strict improvement certified); the `K₂` spectrum pinned (trace `2`, determinant `0`, PSD ⇒ every eigenvalue is `0` or `2`) making the shrinkage story exact — factor exactly `1` on the kernel mode, exactly `1/3` on every other, `1/3 < 1` by the antitonicity theorem, `1/3 ∈ Ioo 0 1` by the strict-attenuation theorem; **not-a-projection numerically** — a second hand-solved system pins `T(T y) = ![5/9,4/9] ≠ ![6/9,3/9] = T y` (a projection would fix its own output); mean preservation instantiated twice (`∑ T y = ∑ T(T y) = ∑ y = 1`); and the **`hπ` guard refuted-on-omission** — at `π = 0` the minimizer is the zero vector (every junk factor `0`) while the objective degenerates to `‖x−y‖²`, so the hypothesis-free minimality would read `1 ≤ 0`; `#print axioms` on the 14 headline QA theorems reads only the three standard axioms), including `Perturbation/Weyl_QA.lean` (18 declarations; extended 2026-08-20 for the axiom retirement: the nonzero-perturbation fixture `A = E = !![2,1;1,2]` with **both spectra pinned independently** from trace/determinant/sortedness (`[1,3]` and `[2,6]`) and `‖E‖ = 3` through the proved bridge's two directions — the bound instantiated at both indices, **attained exactly** at the top (`|6−3| = 3 = ‖E‖`) and **strict** at the bottom (`|2−1| = 1 < 3`, the proposal's required non-vacuity witness), both additive bounds instantiated with the lower endpoint attained at the bottom index and the upper endpoint attained at the top, and the **window-endpoint guard** — the upper additive bound with `λ₁(E)` in place of `λₙ(E)` refuted (`6 ≤ 4` is false), so the window's endpoints are load-bearing and not interchangeable; `#print axioms` on all nine QA theorems reads only the three standard axioms), and `OperatorTheory/Resolvent_QA.lean` (94 declarations, proposal `resolvent-calculus-psd.md` steps 0–3; **Step 3**, added 2026-08-20: the injectivity instantiated at two distinct PSD pairs — `lap2` vs `0` and the less degenerate `lap2` vs `mat2` — each with both resolvents independently computed by left-inverse witnesses and distinctness verified by entries (`1/3 ≠ 0`, `2/3 ≠ 3/8`), the theorem's output bridged to the numeric facts by rewriting; the packaged iff consumed in the certificate direction; and the **invertibility guard** — the hypothesis-free implication "equal resolvents → equal matrices" refuted at `A = -1` vs `B = -1 + E` with `E` nilpotent, both `+1` shifts singular so both resolvents are the junk inverse `0` while the matrices differ, so the core theorem's determinant hypotheses are load-bearing; steps 0–2 as previously recorded: the operator-norm bridge QA'd against a pinned spectrum — the `!![2,1;1,2]` fixture's spectrum `[1,3]` pinned from trace/determinant/sortedness independent of the bridge, both directions instantiated to pin `‖M‖ = 3` exactly (the literature spectral-norm value), the packaged extremal form at `max 1 3 = 3`, and the negative witness `¬(‖mat2‖ ≤ 1)` showing a bound by one eigenvalue's absolute value is refuted, so the `∀ k` hypothesis is load-bearing; the shifted-PSD invertibility theorem cross-checked against the computed determinants `3` (at `t = 1`) and `8` (at `t = 2`), with the shift-load-bearing witness that the *unshifted* Laplacian's determinant is exactly `0` — PSD alone gives no invertibility, the `+ 1` is not decorative; the resolvent identity checked at `A = L(K₂)`, `B = 0` with `(L+1)⁻¹` computed to `(1/3)!![2,1;1,2]` by an independent left-inverse witness and both sides of the identity independently computed from the raw definitions to the same literal matrix `!![-1/3, 1/3; 1/3, -1/3]` — a wrong factoring (order or sign) would fail the check; **Step 2** — the norm bound **attained with route agreement**: `‖(L+1)⁻¹‖ = 1` exactly, the theorem's energy route and the eigenvalue-bridge route (the pinned spectrum `{1/3, 1}` of `(1/3)!![2,1;1,2]`) meeting at the same value, so the bound is tight, not slack; the general-`t` instance `‖(L+2•1)⁻¹‖ = 1/2` exactly (spectrum `{1/4, 1/2}`, the `t = 2` resolvent `(1/8)!![3,1;1,3]` pinned by an independent left-inverse witness) — also attained; the Lipschitz bound instantiated at `A = L`, `B = 0` with **both sides independently pinned** (the difference `!![-1/3,1/3;1/3,-1/3]`'s spectrum `{-2/3, 0}` pinned from trace/determinant, norm `2/3`; `‖L‖ = 2` from the pinned spectrum `{0, 2}`; the instantiated theorem reads `2/3 ≤ 2` — a reversed or badly-factored statement would produce a falsehood); the **shift-load-bearing norm witness** (the proposal's QA item 3): the invertible PSD `(1/4)I` *without* the shift has `‖A⁻¹‖ = 4 > 1` (inverse `4I` by an independent left-inverse witness, norm from the pinned spectrum `{4,4}`), so the unshifted bound is refuted numerically; and the **hypothesis-load-bearing witness**: the symmetric non-PSD `-(3/4)I` has `+1` shift equal to `(1/4)I` — invertible, inverse norm `4 > 1` — while `quadForm (-(3/4)I) ![1,0] = -3/4 < 0` exhibits the violated hypothesis; dropping the quadratic-form assumption breaks the bound); including `SpectralGraph/SpectralCertificates_QA.lean` (39 declarations, proposal `decidable-spectral-certificates.md` steps 3–4: the certificate layer's kernel-`decide` demonstrations on `C₄` — the integer twin accepted at the attained bound `2`, rejected at `1`, non-orthogonal and zero test vectors rejected, fractional `5/2` accepted and `3/2` rejected, all by plain kernel `decide`; the ℚ specification checker proved `= true` **only through the proved cross-multiplication bridge** (kernel `decide` cannot evaluate ℚ arithmetic — the Step 0 wall); the end-to-end soundness instances `lambda2 (C₄) ≤ 2` and `≤ 5/2` consuming `decide`d hypotheses; the checker's arithmetic pinned (`dotOne = 0`, `denom = 2`, `rawNumer = 8`, Rayleigh quotient `4/2 = 2` — the certified bound is exactly the test vector's quotient); the guard refutation — `lambda2 ≤ rayleigh onesVec` false on the connected `C₄` (`0 < lambda2` via `lambda2_pos_of_connected`), so the orthogonality conjunct is load-bearing; **Step 4**: the same chain at the second required size `C₆` — accept at the attained `1`, reject at `0`, fractional `3/2` accepted and `1/2` rejected, arithmetic pinned (`rawNumer = 8`, `denom = 4`, quotient `8/(2·4) = 1`), the ℚ specification through the bridge, and end-to-end `lambda2 (C₆) ≤ 1` and `≤ 3/2`; and the **exact pin** — a `C₄` Poincaré inequality (`2‖x‖² ≤ xᵀLx` on `1⊥`, the `n = 4` Wirtinger identity `E = 2Σx² + 2(x₀+x₂)²`) through the *lower* half of `lambda2_variational` gives `lambda2 (C₄) = 2` exactly, so the certificate chain is exactly tight: the kernel checker accepts precisely at the true `lambda2` and rejects every integer bound below it); including `SpectralGraph/Expander_QA.lean` (88 declarations, proposal `decidable-spectral-certificates.md` steps 1–2 and Step 4: cut weights, centered indicators, and centered cross terms all computed from the raw definitions on a fresh `C₄` fixture — adjacent cut `1`, opposite cut `0`, half-and-half cut `2`, total weight `8`; the matrix form `indicatorVec S ⬝ᵥ (A *ᵥ indicatorVec T)` computed raw and cross-checked against the theorem; centered indicators pinned entrywise (`±1/2` half-set, `3/4/−1/4` singleton) with orthogonality to `onesVec` computed from the pinned entries; the `d`-regular decomposition instantiated on the adjacent cut (`1 = 2·1·1/4 + 1/2`) and the opposite cut (`0 = 2·1·1/4 − 1/2`), each cross term computed independently; and three negative witnesses — the main-term-only statement refuted on the opposite cut (`0 ≠ 1/2`), the main term's degree pinned by regularity (wrong `d = 3` refuted, `1 ≠ 3/4`), and cut-weight symmetry genuinely failing on the asymmetric weight `!![0,2;1,0]` (`2 ≠ 1`); the Step-2 EML with a derived tight `μ = 2` attained exactly on the opposite cut; and **Step 4** — the exact spectrum pins `lambda2 (C₄) = 2` (Poincaré through the lower half of `lambda2_variational`) and `λ_max(C₄) = 4` (the alternating vector through `quadForm_le_evals_last`), so `μ(C₄) = 2` *exactly* and the Ramanujan bound `2√(d−1)` is attained with equality; the `K₃` fixture — the energy identity `xᵀLx = 3‖x‖² − (Σx)²` pinning `λ₂ = λ_max = 3`, `μ(K₃) = 1 < 2√(d−1)` strictly Ramanujan, with the EML **attained exactly on both the singleton and the edge cut**; and the `C₆` EML instance with derived `μ ≤ 2` (test vector + per-edge estimate + PSD), attained exactly on the alternating cut `({0,2,4}, {0,2,4})` with the Ramanujan inequality `μ ≤ 2√(d−1)` holding); including `SpectralGraph/Foster_QA.lean` (53 declarations, proposal `spectral-graph-sparsification.md` Phase A: Foster's theorem instantiated on the cliques `K₃`/`K₄`, the path `P₃`, and the 3-leaf star — every resistance pinned by an explicit potential witness computed from the raw definitions (`K₄` via the general-pair potential `(e i − e j)/4` proved to solve the unit demand for *every* pair, so all twelve ordered terms are pinned by one lemma), each ordered Foster sum computed independently of the theorem (`4`, `6`, `4`, `6` respectively) and cross-checked against the theorem's `card V − 1` (`2`, `3`, `2`, `3`); the **double-counting factor** witnessed on both cliques exactly as the proposal's calibration demands — the ordered sum is `4 ≠ 2` on `K₃` and `6 ≠ 3` on `K₄`, so the theorem's `/ 2` is load-bearing; non-edge pairs (`P₃`'s `(0,2)`, the star's leaf pairs) provably do not contribute (zero conductance); leverage scores computed — the `K₃` edge carries `1/3` of the budget and the ordered total is exactly `2` (`fosterTri_leverage_sum_QA`), the `K₄` edge `1/6`); including `SpectralGraph/ElectricalFlow_QA.lean` (88 declarations, proposal `electrical-flow-routing.md` steps 1–5, the program complete: the current matrix, divergences, unit-flow instantiations, and flow energies all computed from the raw definitions on `K₂` and the 3-path — internal-vertex divergence `0`; energies `1`/`2` cross-checked against independently computed Dirichlet energies and pinned resistance values — plus four step-1–2 negative witnesses: the asymmetric network `!![0,2;1,0]` where the current provably fails antisymmetry (`2 ≠ 1`); the edgeless-network phantom excluded by `IsFlowOn`'s support conjunct, its energy pinned to `0`; the double-counting guard — the raw ordered-pair sum computes to `2 ≠ 1` = the Dirichlet energy, so `flowEnergy`'s `1/2` factor is mandatory; and the zero-energy competitor on the real-edge-plus-isolated-vertex network, an antisymmetric unit-divergence flow with energy `0 < 1` = the resistance it would compete against, excluded by the support conjunct alone — exactly the witness that keeps Thomson's principle (step 3) true; the step-3 variational witnesses on the triangle `K₃`: attainment at the split current (`R = 2/3` = the current's energy, both computed from the raw definitions with the current genuinely splitting `2/3`/`1/3` across the two routes), a strict competitor (the detour unit flow around the two-edge path: valid `IsUnitFlow` with all conjuncts computed, energy `2`, Thomson instantiated as `2/3 < 2`), and the superposition decomposition `2 = 2/3 + 4/3` with all three energies computed independently of the lemma; and the step-4 Rayleigh witnesses: the proposal's QA item 2 capacity increase — conductance `1 → 2` on the unit edge certifies the resistance decrease `1 → 1/2` (`edge2_resistance_value_QA`, pinned by an independent potential witness), Rayleigh monotonicity instantiated (`rayleigh_edge_QA`) with the decrease certified strict (`1/2 < 1`) and the **orientation guard** refuting the reverse inequality numerically (`1 ≤ 1/2` false — weights are conductances, a resistance-direction statement would be false here); the competitor transfer (`edge_current_isUnitFlow_on_edge2_QA` — the `edgeAdj`-electrical current is a unit flow on the doubled network, flow-space growth + Kirchhoff) with its cross-network energy computed to `1/2 ≤ 1` from the raw definitions; and a partial increase on the triangle — doubling one edge's conductance strictly decreases the resistance `2/3 → 2/5` (`triDoubled_resistance_value_QA` pinned by the independent potential `![2/5, 0, 1/5]`, `rayleigh_tri_QA`, `rayleigh_tri_strict_QA`); and the step-5 ICP proof example on the Mathlib path graph — `pathGraph3.toWAdj` bridged entrywise to the `connPathAdj` fixture (`path3_toWAdj_eq_connPathAdj`), the reinforcement `increaseConductance … 0 1 1` computed to exactly the concrete doubled-path matrix (`path3_reinforced_eq_pathDoubled_QA`), the reinforced resistance pinned at `3/2` from the independent potential witness `![1/2, 0, −1]` (`pathDoubled_resistance_value_QA`, with connectivity proved by explicit walks, independent of the new adapter), the one-hypothesis headline instantiated (`path3_reinforcement_QA`), and the decrease certified strict `3/2 < 2` against the adapter-bridged pinned original value (`path3_reinforcement_strict_QA`). Earlier records: the `Core/MatrixUpdates_QA.lean` Woodbury/Sherman–Morrison witnesses and the 2026-08-18 `Cheeger_QA` easy-direction witnesses (`cutTestVector_edge_QA` pinning the test vector to `![1,-1]`; its Rayleigh value computing to `2` on `K₂` — exactly the pinned `λ₂(L_sym)`; `cheeger_upper_bound_edge_eq_QA` attaining `λ₂ = 2φ`; and on `C₄` `cutTestVector_cycle_rayleigh_QA` computing `1` and `cheeger_upper_bound_cycle_le_QA` instantiating `λ₂ ≤ 1` through the exhaustively computed conductance `1/2`). |
| Direct public module targets | Pass | 2026-08-22 | All public modules compile individually; the Perron–Frobenius admission slice (2026-08-22, proposal `admit-perron-frobenius.md`): the new `Scaffold/Mathlib/LinearAlgebra/PerronFrobenius.lean` elaborates clean directly (`lake env lean`, zero errors, zero warnings) and builds explicitly — the global-namespace predicate `Matrix.IsIrreducible` (`ReflTransGen` on positive entries, Fintype-free, mirroring `Matrix.IsSymm`'s reading surface) and the single admitted axiom `perron_frobenius` at the proposal's scoped qualification level: `0 < r` with a strictly positive eigenvector `A *ᵥ x = r • x` (together with the domination clause below: `r` is the spectral radius and an eigenvalue), `Polynomial.rootMultiplicity r A.charpoly = 1` (simplicity over ℝ, multiplicity-invariant under ℝ→ℂ extension), the strong H&J uniqueness (every nonzero nonnegative eigenvector, whatever its eigenvalue, is a positive multiple of `x`) with the only-eigenvalue clause `μ = r` as an explicit conjunct, and `∀ z ∈ (charpoly (A.map (algebraMap ℝ ℂ))).roots, |z| ≤ r` (the pin has no matrix spectral radius — the `spectralRadius` hits are operator-theory/C* files — so the ρ(A) content is encoded directly through complex charpoly roots). Two recorded statement-shape guards: the `hex : ∃ i j, 0 < A i j` degeneracy guard (H&J implicitly work at n ≥ 2 where irreducibility forces a positive entry; over arbitrary `Fintype V` the vacuously irreducible 1×1 zero matrix and the empty type would falsify `0 < r` — `hex` is exactly the missing strength, derivable from `hirr` whenever `2 ≤ card V`), and **no strict-dominance clause** (that requires primitivity; the calibration refutation below fences it). The directed-operators Step-2 slice (2026-08-22, proposal `directed-graph-operators.md` Step 2 + the Step-3 agreement brick, zero new axioms): `GraphTheory.Directed` extended with `directedNormalizedLaplacian` (`I − ½(SAS + SAᵀS)`, `S = degreeInvSqrt` — the out-degree normalization recorded as Step-0 decision (b)) and four theorems — the entry form (`δ_ij − ½·(√d_i)⁻¹(A_ij + A_ji)(√d_j)⁻¹`), **symmetry hypothesis-free for every matrix** (the two halves of the defining sum are transposes of each other, so the sum is symmetric even when `A` is not — the directed axis' one symmetric operator), the Step-3 acceptance bar `directedNormalizedLaplacian_eq_normalizedLaplacian` (on `A.IsSymm` both halves coincide, `½ • (M + M) = M`), and the square-root-free conjugate `√D L_dir √D = degreeMatrix A − ½(A + Aᵀ)` (the symmetrized-adjacency pair, exact analogue of `degreeSqrt_mul_normalizedLaplacian_mul_degreeSqrt`) — elaborates clean directly (`lake env lean`, zero errors, zero warnings); `#print axioms` on all five new public declarations reads only `propext, Classical.choice, Quot.sound`. The directed-operators slice (2026-08-22, proposal `directed-graph-operators.md` Steps 0+1, zero new axioms): the new `GraphTheory.Directed` elaborates clean directly (`lake env lean`, zero errors, zero warnings — the four `DecidableEq`-free theorems silenced with `omit` clauses per the linter's own suggestion), `#print axioms` on all seven public declarations (two defs' theorems included) reads only `propext, Classical.choice, Quot.sound`, and the QA consumer `SpectralGraph/Directed_QA` (41 declarations) elaborates clean (zero errors, zero warnings) and builds explicitly — the QA's load-bearing core is the instantiation of the *pre-existing* undirected-shelf theorems `walkTransitionMatrix_row_sum` and `walkLaplacian_mulVec_one_eq_zero` on the genuinely asymmetric fixture `dirA` (certifying those operators really are symmetry-free), with the negative witnesses `outDeg dirA 0 ≠ inDeg dirA 0` (`4 ≠ 2`) and `¬ (walkTransitionMatrix dirA).IsSymm` (`3/4 ≠ 1`). Pin-specific QA technique recorded: `simp only [outDeg, dirA, …]` unfolds of the *new* degree defs leave `Matrix.vecHead (Matrix.vecTail …)` entry residue on this snapshot (while the same list through `deg` behaves); the robust route is a nine-entry `rfl` table for the fixture plus `simp only [def, Fin.sum_univ_three]` (split only) followed by table rewrites — kernel evaluation is deterministic where simp's entry normalization is not. The subgaussian-retirement slice (2026-08-22, proposal `prove-subgaussian-tail-bound.md` — Step 0 spike + repair-and-retire in one run): `Probability.Concentration.Scalar.Subgaussian` elaborates clean directly (`lake env lean`; the module's only diagnostic is the documented pre-existing `unused variable K` warning in untouched `subgaussianNorm_nonneg` code, verified identical in HEAD) — **`subgaussian_tail_bound` retired from axiom to proved theorem** (explicit axioms **10 → 9**), a *correctness repair + retirement* per the Woodbury precedent: the mandated Step 0 spike (`wip/subgaussian_spike.lean`, git-ignored) confirmed against this pin that the old statement `(hK : 0 ≤ K) (h_sub : subgaussianNorm X μ ≤ K)` was materially false in two independent junk ways (`Real.sInf_empty` makes the norm-hypothesis vacuous when the defining set is empty — any measure of total mass above `2`; `MeasureTheory.integral_undef` makes the MGF integral itself junk-zero for non-integrable MGFs, so heavy-tailed variables get norm `0` even under probability measures — the set is then *full*, not empty, contradicting the definition docstring's recorded junk behavior, which was corrected), so the theorem is stated at the same name and conclusion with the honest hypotheses `(hK : 0 < K) (h_int : Integrable (fun ω => exp (X ω ^ 2 / K ^ 2)) μ) (h_mom : ∫ ω, exp (X ω ^ 2 / K ^ 2) ∂μ ≤ 2)` — exactly the content Markov's inequality consumes; the `K = 0` branch of the old `0 ≤ K` is dropped (its bound evaluated to the junk-dependent `2 * exp 0 = 2`). Proof route (all shelf lemmas at this pin's exact signatures): the pointwise monotone transfer `exp (X²/(2K²)) ≤ exp (X²/K²)`, ae-strong-measurability at the half scale via the `Real.exp_half` square-root bridge (composed with `Continuous.comp_aestronglyMeasurable` — no measurability hypothesis on `X` is needed or carried), `Integrable.mono'` + `integral_mono_of_nonneg` for the transferred moment, shelf Markov `mul_meas_ge_le_integral_of_nonneg` (no `0 ≤ ε` side condition at this signature) at `ε := exp (t²/(2K²))`, `Integrable.measure_norm_ge_lt_top` forcing the Markov event finite, and the ENNReal conversion through `ofReal_toReal`/`ofReal_le_ofReal` — yielding the unchanged conclusion with its `2K²` constant (the √2 bookkeeping lives in the half-scale transfer). The proposal's open `K = sInf` boundary question **dissolves in the repaired statement** (the hypotheses never mention the `sInf`; recorded in the delivery record). `#print axioms` reads only `propext, Classical.choice, Quot.sound`. The `hoeffding_lemma` axiom is untouched (statement unchanged; its stale QA-note referencing the nonexistent `hoeffding_lemma_zero_QA` was corrected); the mixing-Step-2 slice (2026-08-22, proposal `mixing-time-bound.md` Step 2 — the ℓ²-mixing proxy, zero new axioms — `#print axioms` on all fourteen new public theorems reads only `propext, Classical.choice, Quot.sound`): the new `GraphTheory.Mixing` elaborates clean directly (`lake env lean`, zero errors, zero warnings) with the scoping-recorded weighted form as primary — `stationaryVec` (π = deg/vol, positive and sums to one), `walk_isStationary` (the π-form stationarity composed from the proved degree form), `walkDistribution` (`(Pᵀ)ᵗ *ᵥ δₓ`) with zero/succ evolution and mass conservation (load-bearing on row-stochasticity), `walkDensity` with the **density evolution** `walkDensity_succ` (the first consumer of the Phase A detailed-balance interface — load-bearing on it and on the `D⁻¹A` orientation), and `chiSquareDistance` with nonnegativity, the `= 0 ↔ ν_t = π` characterization, the `t = 0` value `(π x)⁻¹ − 1`, the density-form equivalence, and the plain-ℓ² corollary bridge; the reversibility Phase-A slice (2026-08-22, proposal `reversibility-and-heat-semigroup.md` Phase A, both steps, zero new axioms — `#print axioms` on all four new public theorems and the new `Normalized.walkTransitionMatrix_apply` reads only `propext, Classical.choice, Quot.sound`): `GraphTheory.Stationary` extended with the detailed-balance layer — `walk_detailed_balance` (degree-measure form `deg i * P i j = deg j * P j i`, both sides exactly `A i j`: the degree weight cancels `walkTransitionMatrix`'s `D⁻¹` row factor and `A.IsSymm` identifies the entries — the property that makes spectral methods apply to the walk), `walk_detailed_balance_measure` (stationary-measure form `π i * P i j = π j * P j i` at `π = deg/vol`, by side-condition-free division of the degree identity — no volume hypothesis carried), `diagonal_deg_mul_walkTransitionMatrix_isSymm` (the matrix packaging: `D * P` is symmetric — reversibility *is* symmetrizability, the self-adjointness interface the ℓ²-mixing proxy of `proposals/mixing-time-bound.md` Steps 2–3 consumes), and `transitionMatrix_detailed_balance_uniform` (the regular uniform-measure corollary, composed from the already-proved `RandomWalk.transitionMatrix_symmetric` rather than re-proved — the Phase A Step-2 survey finding); `Normalized` gained the entry form `walkTransitionMatrix_apply` (`P i j = (deg A i)⁻¹ * A i j`) next to the `walkTransitionMatrix` definition, and its stale "Named gap (deferred)" similarity docstring was corrected to point at the delivered 2026-08-22 eigenpair transfer. Both modules and the QA consumer elaborated directly (`lake env lean`, zero errors; the only diagnostics are the two documented pre-existing warnings in untouched Stationary code, verified identical in HEAD); full `lake build` ✔ (2227 targets); `lint_axioms` (10), `check_citations`, `check_markdown_links` pass. Also repaired during this run: the radar axis-5 table row, which the 2026-08-22 mixing-time re-score had left stale at 2.5 — synced to the recorded 3.0 with updated evidence. The mixing-time Step-1 slice (2026-08-22, proposal `mixing-time-bound.md` Step 1, zero new axioms): `GraphTheory.Normalized` extended with the eigenpair-transfer section — `walkLaplacian_mulVec_degreeInvSqrt` / `normalizedLaplacian_mulVec_degreeSqrt` (eigenpairs transfer in both directions between `L_sym` and the non-symmetric `L_walk` at the same eigenvalue, by conjugating the eigenvector with `1/√D`/`√D` — pure `mulVec` algebra through the proved similarity identity, closing the module's own named residual gap), `walkTransitionMatrix_mulVec_degreeInvSqrt` (the `μ`-eigenpair of `L_sym` gives a `(1 − μ)`-eigenpair of `P = D⁻¹A`), the eigenbasis instantiations `walkLaplacian_mulVec_eigvecOf` / `walkTransitionMatrix_mulVec_eigvecOf` (consuming Mathlib's `IsHermitian.mulVec_eigenvectorBasis`), `walk_eigvec_expansion` (completeness of the transferred family — every vector reconstructed from the conjugated eigenbasis through `eigvecOf_expansion_apply` plus the invertibility shuffle: the diagonalizability interface the decay step consumes), `walkEvals` with `exists_eigenvector_walkTransitionMatrix_eq_walkEvals` (each transferred spectrum entry certified a genuine eigenvalue of `P` by an explicit nonzero conjugated-eigenvector witness), and the support lemmas `eigvecOf_ne_zero` / `degreeInvSqrt_mulVec_ne_zero`; the pre-edit Mathlib survey (the proposal's mandatory open next step) confirmed **no** general similar-matrices-share-eigenvalues interface exists in the pin, so the direct diagonal-case transfer was the route — both the module and its QA consumer `SpectralGraph/Normalized_QA` elaborated directly (`lake env lean`, zero errors, zero warnings). The Davis–Kahan retirement slice (2026-08-21, proposal `discharge-perturbation-axioms.md` Step 1 complete): the new `Analysis.OperatorTheory.Perturbation.Duhamel` (~900 lines — the vector-level heat semigroup `heatApply` with its expansion/adjoint/Parseval-damping/eigenaction/differentiability layer transferred from the Step-0 spike, the sorted-spectrum step `evals_succ_le_of_lt`, the projector coefficient filter, the two cluster-filtered decay bounds, the pairing (duality) norm reduction `l2OpNorm_le_of_abs_dotProduct_le` (by self-application: the bound at `y := M *ᵥ x` reads `‖M x‖² ≤ c‖x‖‖M x‖`, forcing `‖M x‖ ≤ c‖x‖`), and the headline `l2OpNorm_one_sub_spectralProjector_mul_spectralProjector_le` (`‖(1 − Q) * P‖ ≤ ‖E‖/(b − a)` — the scalar Duhamel pairing `⟨e^{-t(A+E)}z_y, e^{tA}z_x⟩` whose derivative is exactly `−⟨e^{-t(A+E)}z_y, E e^{tA}z_x⟩` by the symmetry shuffle, FTC on `[0,T]` with the explicit exponential majorant, and the `e^{−(b−a)T}` boundary decay sent to `T → ∞`); plus `rank_spectralProjector_eq_card_filter`, the no-tie rank pin `rank_spectralProjector_evals_of_lt` (bracketed by the two public count pins), and `l2OpNorm_sub_le_one_of_isSymm_idempotent`; and the rewritten `Perturbation.DavisKahan` — **`davis_kahan_sin_theta` retired from admitted axiom to proved theorem at the unchanged name, hypotheses, and conclusion** (explicit axioms 11 → 10) by a three-way case split on eigenvalue ties at the two thresholds: no tie at either `k`-th threshold → both projector ranks `k+1` → the delivered equal-rank identity converts to `‖(I−Q)P‖` → the Duhamel bound; tie at either threshold → the separation hypothesis plus the proved Weyl additive bound force `δ ≤ ‖E‖` → the `≤ 1` endpoint finishes. `#print axioms` on `davis_kahan_sin_theta` reads only `propext, Classical.choice, Quot.sound`; the derived consumers verified: `davisKahanTwoPoint` fully hard crust, `eventStreamProjectorDrift` conditional on `matrix_azuma_hoeffding` alone) — both modules elaborated directly (`lake env lean`, zero errors, zero warnings). the Step-4 band slice (`GraphTheory.Band`, extended 2026-08-21, proposal `spectral-band-projectors.md` Step 4 — the program's final step: **the Hilbert-projection specialization** — `bandProjector_residual_dotProduct_eq_zero` (the residual-orthogonality engine: `(x − B *ᵥ x) ⬝ᵥ (B *ᵥ z) = 0`, load-bearing on exactly the two Step-1 facts — symmetry moves the band across the dot product, idempotence collapses the band of the residual to zero), `bandProjector_toEuclidean_apply_eq_orthogonalProjection` (the identification: Mathlib's `orthogonalProjection` at `K := LinearMap.range (Matrix.toEuclideanLin B)` maps the packaged signal to the packaged band-filtered signal — proved through `eq_orthogonalProjection_of_mem_of_inner_eq_zero` with membership via the `toEuclideanLin` transport and orthogonality via the engine; the proposal's mandatory pre-step survey found the whole needed shelf: the identification lemma, `HasOrthogonalProjection.ofCompleteSpace` resolving on finite-dimensional `EuclideanSpace`, and the `rfl` inner transport `EuclideanSpace.inner_piLp_equiv_symm`), and `norm_sub_bandProjector_apply_le` (the closest-point property: for every fixed point `y` of the band — equivalently by idempotence every member of its range — `‖e x − e (B *ᵥ x)‖ ≤ ‖e x − e y‖`, from `orthogonalProjection_minimal` + `ciInf_le`; statements deliberately made at `EuclideanSpace ℝ V` — the bare `V → ℝ` default norm is the sup norm and is not silently used); **zero new axioms** — `#print axioms` on all three public theorems reads only `propext, Classical.choice, Quot.sound`) was elaborated directly (`lake env lean`, zero errors, zero warnings after two argument-shape fixes: `LinearMap.mem_range` carries no explicit arguments on this snapshot, and `orthogonalProjection_minimal` takes its submodule implicitly with the point explicit — plus the conditionally-complete `ciInf_le` in place of `iInf_le`, which does not apply over ℝ), as was its QA consumer `SpectralGraph/Band_QA` (zero errors, zero warnings after three proof-shape fixes recorded in the run log). The Step-3 band slice (`GraphTheory.Band`, extended 2026-08-20, proposal `spectral-band-projectors.md` Step 3: **completeness under a partition** — `sum_range_bandProjector_eq_sub` (the unconditional telescoping law `∑_{k<n} B(t_k, t_{k+1}) = P_{t_n} − P_{t_0}` for *any* threshold sequence, ordered or not, by `Finset.sum_range_succ` induction with the residue closed by `abel`; load-bearing on the band definition's exact difference shape), `sum_range_bandProjector_eq_one` (the covering-family resolution of the identity from exactly the two endpoint hypotheses — `t₀` strictly below every eigenvalue, every eigenvalue `≤ tₙ` — both load-bearing, the QA endpoint guards refute the hypothesis-free form in both directions), `bandProjector_mul_bandProjector_eq_zero_of_monotone` (distinct members of a **monotone** family compose to zero — the recorded statement-shape decision: monotonicity is deliberately *not* a hypothesis of the sum identity since the telescoping law does not consume it; it is exactly what this theorem consumes, supplying Step 2's disjointness `t (k+1) ≤ t m`, so the family is a partition rather than a mere sequence), and `sum_range_bandProjector_mulVec_eq_self` (the consumer's form `∑ B_k *ᵥ x = x`, through the inlined `mulVec` analog of `Matrix.sum_mul` — a sum-interchange needing `Matrix.sum_apply` because `Finset.sum_apply` cannot see through the `Matrix.of` wrapper); **zero new axioms** — `#print axioms` on all four public theorems reads only `propext, Classical.choice, Quot.sound`) was elaborated directly (`lake env lean`, zero errors, zero warnings, with the module olean produced by the recorded fast `lean -o` path during iteration), as was its QA consumer `SpectralGraph/Band_QA` (zero errors, zero warnings after two proof-shape fixes: this snapshot's `Finset.sum_range_succ` appends the new term on the *right* — natural order `f 0 + f 1 + …` — with no `Finset.sum_range_two`/`Nat.cast_mono` available, and the `rw`-closes-`3 ≤ 3`-by-itself linter trap behind one warning, silenced with `simp only [h]`). The Step-2 band slice (`GraphTheory.Band`, extended 2026-08-20, proposal `spectral-band-projectors.md` Step 2: **orthogonality of disjoint bands** — `bandProjector_mul_bandProjector_eq_zero` / `_eq_zero'` (both composition orders, by the recorded route: the cross-law's ordered forms expand `(P_b − P_a)(P_d − P_c)` to the four-term expression collapsing under the ordering; hypotheses folded to `a ≤ b`, `c ≤ d`, `b ≤ c`, with `b ≤ c` exactly interval disjointness — load-bearing, the QA overlap guard refutes the hypothesis-free form), `bandProjector_inner_eq_zero` (image orthogonality, `(B_{a,b} *ᵥ x) ⬝ᵥ (B_{c,d} *ᵥ y) = 0`, by moving the first band across the dot product through `vecMul_transpose`/`dotProduct_mulVec`/`vecMul_vecMul`), and `eq_zero_of_bandProjector_mulVec_eq_self` (a vector fixed by two disjoint bands is zero — the subspace-level "share no eigenvector"); **zero new axioms** — `#print axioms` on all four public theorems reads only `propext, Classical.choice, Quot.sound`) was elaborated directly (`lake env lean`, zero errors, zero warnings, first pass), as was its QA consumer `SpectralGraph/Band_QA` (zero errors, zero warnings; oleans produced directly during iteration). The module added in the 2026-08-20 band-projector Step-1 slice (`GraphTheory.Band`, proposal `spectral-band-projectors.md` Step 1: the two-sided `bandProjector M hM a b := spectralProjector M hM b − spectralProjector M hM a` — parameterized by spectral interval `(a, b]`, total by definition with the `a > b` junk documented — with `bandProjector_symmetric`, `bandProjector_idempotent` (`a ≤ b`, through the nestedness cross-law), the mode-selection action interface `bandProjector_mulVec_eigvecOf_self` / `...eq_zero_left` / `...eq_zero_right`, the below-spectrum special case `bandProjector_eq_spectralProjector_of_lt` (the design note's named special case, `spectralProjector` kept not re-derived), and the covering band `bandProjector_eq_one`; **zero new axioms** — `#print axioms` on all seven public theorems reads only `propext, Classical.choice, Quot.sound`) was elaborated directly (`lake env lean`, zero errors, zero warnings), as were the enabling additions to `GraphTheory.Spectral` (the nestedness cross-law `spectralProjector_mul_spectralProjector` in min form plus its two ordered shapes — proved entrywise by the orthonormal expansion with two threshold filters intersecting, the flipped form by transposing symmetry — the complete eigenvector-action description `spectralProjector_mulVec_eigvecOf` (`P_c *ᵥ vᵢ = if λᵢ ≤ c then vᵢ else 0`) with its two specializations, and `spectralProjector_idempotent` **re-derived from the master law at unchanged statement** (its diagonal case; the 50-line entrywise proof replaced by one rewrite), all verified by `#print axioms` — three standard axioms only) and the QA consumer `SpectralGraph.Band_QA` (zero errors, zero warnings; oleans produced directly during iteration). The module added in the 2026-08-20 Tikhonov slice (`GraphTheory.Tikhonov` — Tikhonov regularization in the Laplacian eigenbasis, proposal `tikhonov-shrinkage-filter.md` delivered in full: the shrinkage-factor arithmetic layer (`tikhonovShrinkage` with `pos`/`ne_zero`/`le_one`/`lt_one`/`eq_one_iff`/strict antitonicity), the generic spectral-filter workhorse `dotProduct_eigvecOf_filter` and the expansion-injectivity `ext_of_dotProduct_eigvecOf_eq`, `eigvalOf_laplacian_nonneg`, the eigenbasis-defined `tikhonovMinimizer` with the closed-form eigencoefficient identity `tikhonovMinimizer_dotProduct_eigvecOf` (hypothesis-free), the **normal equation** `tikhonovMinimizer_add_smul_one_mulVec` (`(L + π•1) *ᵥ x* = π • y`) **and its converse characterization** `eq_tikhonovMinimizer_of_add_smul_one_mulVec`, the **strict-convexity decomposition** `tikhonovObjective_sub_minimizer` (`obj(x) − obj(x*) = ∑_k (1 + λ_k/π)(d_k − d*_k)²`) delivering `tikhonovObjective_minimizer_le` and `eq_of_tikhonovObjective_eq_minimizer`, mean preservation `sum_tikhonovMinimizer_eq_sum` (symmetry + `π ≠ 0` only), `tikhonovMinimizer_eigvecOf`, and the not-a-projection idempotence-failure `tikhonovMinimizer_ne_apply_self_of_eigvalOf_pos`; **zero new axioms** — `#print axioms` on all 18 public theorems reads only `propext, Classical.choice, Quot.sound`; two recorded statement-shape deviations from the proposal's Step 3: the attenuation factor is exactly `1` at `λ = 0` (kernel modes pass through — that is mean preservation), and "not a projection" is delivered as the idempotence-failure theorem rather than a docstring note) was elaborated directly (`lake env lean`, zero errors, zero warnings) and its QA consumer `SpectralGraph.Tikhonov_QA` elaborated (zero errors, zero warnings; olean produced directly during iteration). The modules changed in the 2026-08-20 Weyl-retirement slice were elaborated directly (`lake env lean`, zero errors, zero warnings): `GraphTheory.Spectral` — extended with the bottom-Rayleigh-domination mirror `evals_first_mul_dotProduct_le_quadForm` (`λ_min • (x ⬝ᵥ x) ≤ xᵀMx`, the lower half of the Rayleigh sandwich, mirror of `quadForm_le_evals_last`), and `Analysis.OperatorTheory.Perturbation.Weyl` — **`weyl_inequality` retired from admitted axiom to proved theorem at the unchanged name, hypotheses, and conclusion** (12 explicit axioms), delivered with the two public additive bounds `weyl_additive_upper` / `weyl_additive_lower` (`λᵢ(A) + λ₁(E) ≤ λᵢ(A+E) ≤ λᵢ(A) + λₙ(E)`, proved from the Courant–Fischer engine's two witness forms exactly as the interlacing retirement was) composed with the proved bridge `l2OpNorm_eq_max_abs_evals` (`#print axioms` on `weyl_inequality`, both additive bounds, and `spectral_gap_stability` — now fully hard crust — reads only `propext, Classical.choice, Quot.sound`); the QA consumer `Perturbation.Weyl_QA` re-elaborated (zero errors, zero warnings; olean produced directly during iteration), and the derived consumers verified by `#print axioms`: `davisKahanTwoPoint` now depends on `davis_kahan_sin_theta` only, `eventStreamProjectorDrift` on `davis_kahan_sin_theta` + `matrix_azuma_hoeffding` — both shed the `weyl_inequality` dependency. The 2026-08-20 resolvent-calculus Step-3 slice (`Analysis.OperatorTheory.Resolvent` — extended with the resolvent-map injectivity: the core `eq_of_inv_add_one_eq_inv_add_one` (equal resolvents of `+1`-invertible matrices force equal matrices — contrapose the Step-1 resolvent identity, then cancel both invertible outer factors), the proposal's item-5 form `resolvent_map_injective_of_quadForm_nonneg` (`A ≠ B → (A+1)⁻¹ ≠ (B+1)⁻¹` on quadForm-nonneg matrices), and the packaged iff `inv_add_one_eq_inv_add_one_iff_of_quadForm_nonneg` (resolvent equality as a certificate of matrix equality); zero new axioms — `#print axioms` on all three reads only `propext, Classical.choice, Quot.sound`) was elaborated directly (`lake env lean` zero errors, zero warnings) and its QA consumer `OperatorTheory.Resolvent_QA` re-elaborated (zero errors, zero warnings; olean produced directly during iteration). Step-2 slice, 2026-08-19 (`Analysis.OperatorTheory.Resolvent` — extended with the norm and Lipschitz bounds: `l2OpNorm_inv_add_smul_one_le_inv_of_quadForm_nonneg` (`‖(M + t•1)⁻¹‖ ≤ t⁻¹` for any matrix with nonnegative quadratic form and `t > 0`, **no symmetry hypothesis** — the recorded route deviation: the energy inequality `t‖y‖² ≤ quadForm M y + t‖y‖² = y ⬝ᵥ x ≤ ‖y‖‖x‖` by dot-product Cauchy–Schwarz through the bridge's `opNorm_le_bound` transport spine, not the sketched eigenvalue transfer; the underlying energy inequality holds for any `t`), the `t = 1` instance `l2OpNorm_inv_add_one_le_one_of_quadForm_nonneg`, and `l2OpNorm_resolvent_sub_le_of_quadForm_nonneg` (the `1`-Lipschitz bound `‖(A+1)⁻¹ − (B+1)⁻¹‖ ≤ ‖A−B‖` — the Step-1 resolvent identity + the scoped `NormedRing` submultiplicativity + the norm bound twice, load-bearing on Step 1's invertibility theorem and identity; zero new axioms — `#print axioms` on all three reads only `propext, Classical.choice, Quot.sound`) was elaborated directly (`lake env lean` zero errors, zero warnings) and its QA consumer `OperatorTheory.Resolvent_QA` re-elaborated (zero errors, zero warnings; olean produced directly during iteration). Earlier record from the steps 0–1 slice: the module (`Analysis.OperatorTheory.Resolvent` — the operator-norm bridge: `abs_eigvalOf_le_l2OpNorm` (every eigenvalue bounded by the `L2OpNorm` operator norm, via the unit eigenvector through `toEuclideanCLM`) and `l2OpNorm_le_of_abs_eigvalOf_le` (the converse, via Parseval + the eigenaction identity + `ContinuousLinearMap.opNorm_le_bound`), their sorted-spectrum forms, and the packaged extremal identity `l2OpNorm_eq_max_abs_evals`; plus the Step-1 algebra — `isUnit_det_add_smul_one_of_quadForm_nonneg` (`M + t•1` invertible for any matrix with nonnegative quadratic form and `t > 0`, by the kernel-vector route, *no symmetry needed* — stronger than the proposal's sketch) with the `t = 1` instance, and `resolvent_identity_sub`; plus one generic addition to `GraphTheory.Spectral` — `evals_first_le_eigvalOf`, the first-sorted-entry mirror of `eigvalOf_le_evals_last`; zero new axioms — `#print axioms` on every public theorem confirms only `propext, Classical.choice, Quot.sound`) was elaborated directly (`lake env lean` zero errors, zero warnings), built explicitly, and its QA consumer `OperatorTheory.Resolvent_QA` re-elaborated (zero errors, zero warnings). Earlier records: the module added in the 2026-08-19 decidable-certificates Step-3 slice (`GraphTheory.SpectralCertificates` — the computable certificate layer: the ordered-field transport family (`algebraMap_apply` — the embedding is the rational coercion definitionally — plus symmetry/dot-product/Dirichlet-energy transports and the ℤ→ℚ→ℤ→ℝ matrix agreement), the one-sided Rayleigh consumer form `lambda2_le_rayleigh` (the division-form twin of the Step-2 multiplication bound, proved straight from `lambda2_variational`), the ℚ specification checker `isSpectralUpperBoundCertificate` with its `rfl` unfolding lemma, the flagship soundness theorem `lambda2_le_of_certificate` (an accepted certificate proves `lambda2 (A ℝ) ≤ bound` — the proposal's load-bearing consumer of the proved `lambda2_variational`, `laplacian_quadForm`, and the ordered-field transport), the kernel-verifiable integer twins (`isSpectralUpperBoundCertificateInt`, exactly the Step 0 recorded shape, and the fractional-bound `isSpectralUpperBoundCertificateIntFrac`), the sound-and-complete twin bridge `isSpectralUpperBoundCertificateInt_iff` and the cross-multiplication bridge `isSpectralUpperBoundCertificateIntFrac_iff` (`0 < den` external — genuinely needed for the reverse direction), and the kernel-facing soundness corollaries `lambda2_le_of_certificateInt`/`lambda2_le_of_certificateIntFrac`; zero new axioms — `#print axioms` on every public theorem confirms only `propext, Classical.choice, Quot.sound`) was elaborated directly (`lake env lean` zero errors, zero warnings), built explicitly, and the QA consumer `SpectralGraph.SpectralCertificates_QA` re-elaborated (zero errors, zero warnings; olean produced directly during iteration, then the full `lake build` replayed the trace chain and rebuilt cleanly, 2183 targets). Earlier records: the modules changed in the 2026-08-19 decidable-certificates Step-2 slice (`GraphTheory.Spectral` — the generic additions `eigvalOf_le_evals_last` (the sorted spectrum's last entry dominates every eigenbasis eigenvalue), the top Rayleigh domination in multiplication form `quadForm_le_evals_last` (`xᵀ M x ≤ λ_max • (x ⬝ᵥ x)` for every symmetric matrix, unconditionally in `x`), and the formerly-private positivity idiom `dotProduct_self_pos` made public; `GraphTheory.Expander` — the Step-2 bridge layer: `quadForm_add`, `quadForm_add_sub_eq` (polarization), `quadForm_degreeMatrix`, the `d`-regular identity `quadForm_add_quadForm_laplacian` (`xᵀAx + xᵀLx = d • ‖x‖²`, regularity only, no looplessness), the variational lower bound in multiplication form `lambda2_mul_dotProduct_le_quadForm`, the operator bound `abs_quadForm_le_of_ortho_onesVec` (`|xᵀAx| ≤ μ ‖x‖²` on `1⊥`), the sharp bilinear bound `quadForm_bilinear_sq_le_of_ortho_onesVec`, the variance identity `dotProduct_centeredIndicator_self`, and the headline `expander_mixing_lemma` at the Step 0 restated `√` signature minus the found-unnecessary `hloop` hypothesis; zero new axioms — `#print axioms` on all nine public theorems confirms only `propext, Classical.choice, Quot.sound`) were re-elaborated directly (`lake env lean` zero errors, zero warnings on both), rebuilt explicitly, and the QA consumer `SpectralGraph.Expander_QA` re-elaborated (zero errors, zero warnings). Earlier records: the module added in the 2026-08-19 decidable-certificates slice (`GraphTheory.Expander` — proposal `decidable-spectral-certificates.md` steps 0+1: `edgeWeight` with its degenerate-cut and degree-sum interfaces, the hypothesis-free matrix form `edgeWeight_eq_dotProduct`, `edgeWeight_symm` (sum swap), `indicatorVec`/`centeredIndicator` with `sum_centeredIndicator_eq_zero` and `centeredIndicator_dotProduct_onesVec` (both unconditional — the empty-type case is handled, no `Nonempty` hypothesis carried), the indicator decomposition `indicatorVec_eq_smul_onesVec_add_centeredIndicator`, `mulVec_onesVec_eq_const`, and the headline `edgeWeight_eq_regular_add_centered` (the `d`-regular main-term split with the centered cross term, symmetry load-bearing through `Matrix.dotProduct_mulVec`'s transpose); zero new axioms — `#print axioms` on all seven public theorems confirms only `propext, Classical.choice, Quot.sound`) was re-elaborated directly (`lake env lean` zero errors, zero warnings), rebuilt explicitly, and its QA consumer `SpectralGraph.Expander_QA` re-elaborated (zero errors, zero warnings). Earlier records: the module added in the 2026-08-19 Foster slice (`GraphTheory.Foster` — the kernel-index counting lemma `card_filter_eigvalOf_laplacian_eq_zero`, the spectral-sum lemma `effectiveResistance_eq_sum_eigbasis`, the headline `foster_theorem`, `leverageScore`, and `sum_leverageScore_eq_two`; proposal `spectral-graph-sparsification.md` Phase A only, zero new axioms — `#print axioms` confirms only `propext, Classical.choice, Quot.sound`) was re-elaborated directly (`lake env lean` zero errors, zero warnings), rebuilt explicitly, and its QA consumer `SpectralGraph.Foster_QA` re-elaborated (zero errors, zero warnings). Earlier records: the module added in the 2026-08-19 electrical-flow slice (`GraphTheory.ElectricalFlow` — `EdgeFlow`, `electricalCurrent`, `flowDivergence`, `IsFlowOn`, `IsUnitFlow`, `electricalCurrent_antisymm`, `electricalCurrent_eq_zero_of_weight_eq_zero`, `flowDivergence_electricalCurrent`, `isFlowOn_electricalCurrent`, `isUnitFlow_electricalCurrent`, extended the same day with step 2: `flowEnergy` (explicit zero branch, mandatory `1/2` ordered-pair factor), `flowEnergy_electricalCurrent` (energy agreement with the Dirichlet form, symmetry-only), `flowEnergy_nonneg`, and `flowEnergy_electricalCurrent_eq_effectiveResistance`; extended again the same day with step 3: `flowDivergence_sub`, `isFlowOn_sub` (flow-space linearity), `flowEnergy_add_of_flowDivergence_eq_zero` (the divergence-free superposition lemma — discrete integration by parts, stated with no hypotheses on `A`), and the headline `effectiveResistance_le_flowEnergy` (Thomson's principle); extended again the same day with step 4: `isFlowOn_of_le` (flow-space growth — the competitor-transfer interface), `flowEnergy_le_of_le` (raising conductances lowers dissipated energy, termwise, support load-bearing on the `A i j = 0 < B i j` branch), and the headline `effectiveResistance_le_of_le` (Rayleigh monotonicity in conductance form, `A ≤ B ⇒ R_B ≤ R_A`); extended again the same day with step 5, completing the program: `increaseConductance` (raise one undirected pair's conductance by `δ`, both ordered entries together) with its entry/symmetry/nonnegativity/monotonicity interfaces (`increaseConductance_apply_of_reinforced`, `increaseConductance_apply_of_not_reinforced`, `le_increaseConductance`, `increaseConductance_isSymm`, `increaseConductance_nonneg`), the connectivity adapters `supportGraph_le_of_le` (support graphs grow with entrywise domination — no nonnegativity hypothesis needed) and `supportGraph_connected_of_le` (capacity growth preserves connectivity, via Mathlib's `SimpleGraph.Connected.mono`), and the one-hypothesis headline `effectiveResistance_le_increaseConductance` (`R (increaseConductance A i j δ) u v ≤ R A u v` with only the original network's connectivity hypothesized); proposal `electrical-flow-routing.md` steps 0–5, complete) was re-elaborated directly (`lake env lean` zero errors, zero warnings — the instance-free lemmas silenced with `omit` clauses) and rebuilt explicitly, together with its QA consumer `SpectralGraph.ElectricalFlow_QA` (zero errors, zero warnings). Earlier records: the module changed in the 2026-08-18 Woodbury repair (`Core.MatrixUpdates` — `woodbury_identity` restated at the standard middle factor `C⁻¹ + V A⁻¹ U` with the three `IsUnit` determinant hypotheses and proved from Mathlib's `Matrix.invOf_add_mul_mul` plus the `NonsingularInverse` bridges; `sherman_morrison` unchanged at that milestone) was re-elaborated directly (`lake env lean` zero errors, zero warnings) and rebuilt via `lake build Scaffold.Mathlib.Core.MatrixUpdates` alongside the umbrella build. The same module, changed again in the 2026-08-18 Sherman–Morrison retirement (`sherman_morrison` converted from axiom to proved theorem at unchanged name/hypotheses), was re-elaborated directly (`lake env lean` zero errors, zero warnings), together with its QA consumer `Core.MatrixUpdates_QA` (zero errors, zero warnings), and both were rebuilt explicitly. The module changed in the 2026-08-18 interlacing retirement (`GraphTheory.Spectral` — the padding bridge `padVec`/`padVecLinear` with its preservation lemmas, the subspace bookkeeping helpers, and `eigen_interlacing_principal_submatrix` converted from axiom to proved theorem) was re-elaborated directly (`lake env lean` zero errors; the only new-code notes were silenced with `omit` clauses, leaving the documented pre-existing notes in untouched code untouched), together with its QA consumer `SpectralGraph.Interlacing_QA` (zero errors; +4 public declarations), and both were rebuilt explicitly. The modules changed in the 2026-08-18 Cheeger-retirement slice (`GraphTheory.Spectral` — the general-operator `secondEval_variational`, its consumer form `secondEval_le_rayleigh`, the generic `eigvecOf_ortho_onesVec_of_mulVec_eq_zero`, with `lambda2_variational` re-proved as a corollary at unchanged shape; `GraphTheory.Cheeger` — `cheeger_upper_bound` retired from axiom to proved theorem with the `cutTestVector` machinery) were likewise re-elaborated directly and rebuilt. |
| Mathlib cache provenance | Note | 2026-08-21 | The local `lake exe cache` binary crashes under the current macOS dyld (`__DATA_CONST segment missing SG_READ_ONLY flag`); the cache is fetched by running the same Cache tool logic interpreted via `lake env lean --run`, unpacking 5685 Mathlib oleans. Re-applied on 2026-08-18 after the local `.lake/build` state was found pruned (Mathlib oleans down to 1795); the interpreted fetch restored the full set, after which `lake build` recompiled the residue (2008 targets) and passed. Re-applied on 2026-08-19 (pruned state found at run start; fetch restored the full set; the affected module builds then elapsed normally). Re-applied **twice** on 2026-08-20: the pruned state recurred at run start (fetch #1 restored the full set), and two `lake build` invocations interrupted at their tool timeouts left the Mathlib olean dir wiped mid-replay (fetch #2 restored it); the subsequent *uninterrupted* `lake build` replayed the trace residue and passed (2184 targets). Recorded operational rule: never let `lake build` be killed mid-replay — schedule a timeout comfortably above the ~35–50 min residue replay, or elaborate modules directly with `lake env lean`; alternatively run the build detached from the tool timeout (a log file + process poll), which is how the 2026-08-20 Tikhonov run completed its 2185-target build after one invocation was killed at its tool timeout (the Mathlib olean set survived that kill; no re-fetch was needed). Re-applied at the 2026-08-20 band Step-2 run start (pruned state recurred, 0 present; interpreted fetch restored 5387, "Unpacked in 21270 ms"). At the 2026-08-20 band Step-3 run start the same check pattern returned 0 again and a fetch ran ("Unpacked in 22688 ms"), but a later audit found this was a **wrong-path artifact**: the oleans live at `.lake/packages/mathlib/.lake/build/lib/Mathlib/...` with **no `lean/` path level** (consistent with the case-insensitivity note below), so `ls .../build/lib/lean/Mathlib | wc -l` returns 0 even with the full set present — the correct presence check is a `find … -name "*.olean" | wc -l` at the package build root (5387 with the set intact). A `lake build` invocation killed at its 300 s tool timeout during Mathlib trace replay left the set intact (verified by the corrected count), and one redundant re-fetch ran (idempotent, "Unpacked in 2022 ms"); the final full build of the run was executed detached per the recorded procedure. Recorded operational rule (amended 2026-08-20): check olean presence at the correct path before fetching, never let `lake build` be killed mid-replay — schedule a timeout comfortably above the ~35–50 min residue replay, or elaborate modules directly with `lake env lean`; alternatively run the build detached from the tool timeout (a log file + process poll), which is how the 2026-08-20 Tikhonov run completed its 2185-target build after one invocation was killed at its tool timeout (the Mathlib olean set survived that kill; no re-fetch was needed). Invocation note from the Step-2 run: under `lake --dir=.lake/packages/mathlib`, the `--run` file argument resolves relative to the *current* directory, not `--dir` — invoke from the repo root as `lake --dir=.lake/packages/mathlib env lean --run .lake/packages/mathlib/Cache/Main.lean get` (passing `Cache/Main.lean` alone fails with "file not found"). A macOS case-insensitivity trap was also recorded: creating `.lake/build/lib/lean/` in this workspace shadows the toolchain's core `Lean/` tree in `LEAN_PATH` (breaking all elaboration with "object file ... Lean.Elab.Command.olean does not exist"); this repo's olean layout is `.lake/build/lib/Scaffold/...` directly, with no `lean/` level. Environment healthy at the 2026-08-21 Step-4 run start (correct-path check: 5387 present, no fetch needed); the QA-module batch re-run needed one restart at its 10-minute tool timeout mid-batch (no state damage — `lake env lean` does not replay traces), and completed on the second pass; the full build ran detached per the recorded procedure and passed. |
| `scripts/lint_axioms.py` | Pass | 2026-08-22 | All 10 explicit axioms are covered (the new `perron_frobenius` admitted 2026-08-22 — the Perron–Frobenius theorem for irreducible nonnegative matrices, Horn & Johnson 8.4.4, no strict-dominance clause; the first *admission* since the directed-axis scope decision, reversing the retirement trend deliberately: the directed axis' second spectral toolkit, absent from the pinned Mathlib). Earlier: all 9 were covered by `index/sources/` and `index/map/` (`subgaussian_tail_bound` retired 2026-08-22 — a repair-and-retire: the old `subgaussianNorm ≤ K` hypothesis shape was materially false (junk `sInf ∅ = 0` vacuity on measures of mass > 2; junk-zero Bochner integrals making the defining set full for heavy tails), proved instead at the same name/conclusion with the moment stated integrably; `davis_kahan_sin_theta` retired from axiom to proved theorem on 2026-08-21 — the Davis–Kahan sin Θ bound proved by the Duhamel/exponential-integral route of `proposals/discharge-perturbation-axioms.md`, components 1+2: the equal-rank projector identity `Analysis.OperatorTheory.Perturbation.ProjectionGap` plus the Duhamel bound `Analysis.OperatorTheory.Perturbation.Duhamel`; `weyl_inequality` retired 2026-08-20; `spectral_persistence` removed 2026-08-20 by operator direction — editorial removal, not a proof retirement (`spectral_persistence` removed 2026-08-20 by operator direction — editorial removal of a non-published result, not a proof retirement; `weyl_inequality` retired from axiom to proved theorem on 2026-08-20 — the additive Weyl window proved from the Courant–Fischer engine plus the operator-norm bridge, `proposals/discharge-perturbation-axioms.md`; `lambda2_variational` retired 2026-08-18; `cheeger_upper_bound` retired from axiom to proved theorem on 2026-08-18 — the Cheeger easy direction, proved from `secondEval_variational`; `woodbury_identity` retired from a verified-false axiom to a proved theorem on 2026-08-18; `sherman_morrison` retired from axiom to a proved theorem on 2026-08-18 — the `k = Fin 1` specialization of the proved Woodbury identity, a pure proof task at unchanged name/hypotheses; `eigen_interlacing_principal_submatrix` retired from axiom to a proved theorem on 2026-08-18 — Cauchy interlacing proved from the locally proved Courant–Fischer engine, a pure proof task at unchanged statement; the remaining Cheeger axiom is the hard direction only). |
| `scripts/check_citations.py` | Pass | 2026-08-22 | Every axiom carries a `Source:` citation in its doc comment. |
| `scripts/check_markdown_links.py` | Pass | 2026-08-20 | No broken repository-relative targets in active docs; excludes the historical archive, the dependency checkout, and the local `.opencode/` tooling directory (including its vendored `node_modules`). |

## Interpretation

- **Perron–Frobenius admitted — the directed axis' second spectral toolkit (2026-08-22, `proposals/admit-perron-frobenius.md`; explicit axioms 9 → 10, the first admission since the 2026-08-19 scope decision):** the new `Scaffold.Mathlib.LinearAlgebra.PerronFrobenius` carries the combinatorial irreducibility predicate `Matrix.IsIrreducible` (strong connectivity through positive-weight arcs — the directed analogue of the `supportGraph`/`Connected` pattern) and the admitted theorem at exactly the proposal's scoped qualification level: positive simple Perron root with a strictly positive eigenvector, every nonzero nonnegative eigenvector a positive multiple of the Perron vector (its eigenvalue therefore `r`), and complex-spectrum domination through the complexified charpoly roots (the pin has no matrix spectral radius). Deliberately **no strict-dominance clause** — that needs primitivity, and the QA centerpiece `strict_dominance_refuted_QA` keeps the boundary falsifiable: on the asymmetric directed 2-cycle `!![0,4;1,0]` the Perron pair `(2, (2,1))` satisfies the strengthened hypotheses while the eigenpair `(−2, (−2,1))` has `|−2| = 2 = r`, refuting the strengthening; the axiom's own domination clause instantiated at the same peripheral root gives `|−2| ≤ 2` *with equality* — the weaker conclusion is exactly what survives on imprimitive input. The leverage case is the directed-operators program's own output: the PSD-refutation witness showed the undirected positivity layer does not transfer to directed input, and this is the nonnegative-matrix theory that replaces the symmetric `evals` toolkit there; the named consumers (irreducible stationary distributions, PageRank) are follow-ons, not corollaries.
- **Directed normalized Laplacian delivered — the directed-operators proposal's Lean content complete (2026-08-22, `proposals/directed-graph-operators.md` Step 2 + the Step-3 agreement brick folded in per the Step-0 record's "one line from decision (b)"; explicit axioms unchanged at 9):** the directed axis now has its one symmetric operator, `L_dir = I − ½(SAS + SAᵀS)` at the out-degree normalization — symmetric **hypothesis-free** (the two halves of the defining sum are transposes of each other; contrast the walk operators, provably asymmetric on directed input), agreeing exactly with `normalizedLaplacian` on the symmetric cone (the proposal's acceptance bar: both halves coincide when `Aᵀ = A`), and conjugate by `√D` to the square-root-free symmetric pair `D_out − ½(A + Aᵀ)` (the symmetrized adjacency with out-degree normalization — every quadratic-form statement transfers there). **The delivery's sharpest fact is a refutation:** `L_dir` is symmetric but *not PSD* on directed input — QA exhibits `!![0,4;1,0]` where the quadratic form at `(1,1)` is `−1/2 < 0` (in-degree overweight against the out-degree normalization; `L_dir 0 1 = −5/4` past `−1`), so the positivity layer of the undirected toolkit provably does not transfer, and any directed spectral theory must go through Perron–Frobenius — the proposal's Calibration boundary, now witnessed numerically rather than only stated. The convention stays the Step-0-recorded one (deliberately not Chung's Perron-vector form), keeping the whole program axiom-free end-to-end and decoupled from `admit-perron-frobenius.md`.
- **Directed degree layer delivered — the directed axis opened (2026-08-22, `proposals/directed-graph-operators.md` Steps 0+1; explicit axioms unchanged at 9):** the Step-0 survey found that the undirected shelf already carried the out-degree half of the directed story *hypothesis-free* — `deg` is the row sum (the out-degree) and `walkTransitionMatrix = D⁻¹ A` / `walkLaplacian = I − D⁻¹ A` are defined with no symmetry hypothesis, with row-stochasticity and mass conservation proved for any positive-row-sum matrix. Step 1 therefore delivered the genuinely new layer (`GraphTheory.Directed`: `inDeg` the column sum, the `rfl` identifications `outDeg = deg` and `inDeg = deg ∘ transpose`, the symmetric-cone agreements, and directed handshaking `∑ outDeg = ∑ inDeg` by `Finset.sum_comm`) — and the QA is a *certification of the existing shelf*: row-stochasticity and conservation instantiated on the genuinely asymmetric fixture `!![0,3,1;1,0,0;1,0,0]` through the pre-existing theorems (had `walkTransitionMatrix` secretly been defined through the symmetrized adjacency, or `deg` through a symmetric sum, those instantiations would fail), with the two negative witnesses (`outDeg 0 = 4 ≠ 2 = inDeg 0`; the walk matrix's symmetry refuted, `3/4 ≠ 1`) exhibiting the calibration fact the whole axis turns on — there is no symmetric operator to restrict to. The Step-0 convention decision (out-degree-symmetrized normalized Laplacian, deliberately not Chung's Perron-vector form) **decouples this program from `admit-perron-frobenius.md`** rather than coupling them — no axiom becomes a prerequisite of a definition.
- **Subgaussian tail bound retired from axiom to proved theorem — the repository's first retirement that was also a falsity repair on the measure-theoretic side (2026-08-22, `proposals/prove-subgaussian-tail-bound.md`; explicit axioms 10 → 9):** the mandated Step 0 spike did not find the anticipated `sInf`-boundary plumbing problem — it found that the axiom as stated was **materially false**, in two independent ways, both confirmed against this pin's source and then refuted in QA with the old hypotheses proved satisfied (`3 • δ₀`, total mass `3`: every candidate MGF integral is `3 > 2` so the defining set is empty, `Real.sInf_empty` makes `subgaussianNorm = 0 ≤ K` vacuous, and the conclusion at `X = 0, K = 1, t = 0` reads `3 ≤ 2`). Mechanism 1: `Real.sInf_empty`. Mechanism 2: `MeasureTheory.integral_undef` — the Bochner integral of a non-integrable function *is defined to be `0`*, so a heavy-tailed `X` whose MGF diverges at every scale satisfies `∫ exp (X²/K²) ≤ 2` at every `K` (junk `0 ≤ 2`), making the defining set **full** and the norm `0` — the old conclusion would have claimed subgaussian tails for heavy-tailed variables even under probability measures. The second mechanism also falsified the `subgaussianNorm` docstring's recorded junk behavior ("the index set is empty" for unbounded tails — it is full), which was corrected. The repaired theorem keeps the name, the conclusion (including the `2K²` constant), and the `t`/`ht` arguments; the hypotheses become `(hK : 0 < K)`, MGF integrability at `K`, and the MGF bound at `K` — exactly the content Markov's inequality consumes, and strictly stronger than the old shape whenever the old one was non-vacuous. Load-bearing chain: the `Real.exp_half` square-root bridge (the half-scale function is ae-strongly-measurable *as a function of the hypothesis-side MGF* — no measurability hypothesis on `X` needed), `Integrable.mono'`/`integral_mono_of_nonneg` for the monotone-in-`K` moment transfer, shelf Markov at `exp (t²/(2K²))`, and `Integrable.measure_norm_ge_lt_top` for the finiteness the ENNReal conclusion needs — an error in any would surface here. QA pins the repaired interface in both directions (the zero fixture through the new hypotheses, the nonzero constant-1 instance with the moment bound `exp (1/4) ≤ 2` from the pinned `Real.log_two_gt_d9`, both sides of the instance pinned raw) and refutes the old shape with the Woodbury/old-Cheeger falsification pattern. A named residual: the same junk-integral mechanism likely affects the two sum-of-variables axioms' mean-zero hypotheses on infinite measures (`hoeffding_inequality`/`bernstein_inequality`) — recorded in the proposal as an adjacent hazard for their own future Step 0s, not addressed here.
- **Relative entropy and Shannon entropy delivered (2026-08-22, `proposals/finite-relative-entropy.md` — both steps in one run per its own operating instruction; explicit axioms unchanged at 10):** the entropy half of backlog item 6 is now proved hard crust in the new `Scaffold.Mathlib.InformationTheory` — the scalar-term architecture (`klTerm` with the explicit `a = 0 ↦ 0` junk convention, `klDiv` and `shannonEntropy` as its sums), Gibbs' inequality in both directions (`klDiv_nonneg`, `klDiv_eq_zero_iff` at full function equality — strict positivity of `q` makes the off-support case self-refuting), the uniform bridge `klDiv_apply_uniform` (the one place the two quantities meet), the entropy maximum `shannonEntropy_le_log_card` with its equality case `shannonEntropy_eq_log_card_iff`, and `shannonEntropy_nonneg`. The proposal's mandated Jensen survey ran first and found the whole shelf present (`StrictConcaveOn.map_sum_eq_iff'` et al. — the stop-and-record clause not triggered), but the delivered route is the *term-wise* information inequality (`Real.one_sub_inv_le_log_of_pos` + `Real.log_lt_sub_one_of_pos` for strictness) — the same Cover–Thomas 2.6.3 proof with no Finset-`smul` plumbing, no `Ioi`-membership side conditions, and no support filtering: the `a = 0` junk case is discharged inside the per-term bound (`0 ≥ −b`) and its strictness there is *stronger* (gap `= b > 0`), so the equality case needs no `∑_{support} q` argument at all. QA is load-bearing both ways: every headline quantity is pinned through the theorems *and* by raw log algebra (the divergence and entropy of the biased coin each hand-computed; the uniform-bridge numerically cross-checked at the fair coin — a wrong bridge constant would break it), and the strictness witness is obtainable *only* through the equality-case iff, exactly the proposal-prescribed load-bearing use of the strict route.
- **The χ² mixing assembly delivered — the mixing-time program complete (2026-08-22, `proposals/mixing-time-bound.md` Step 3, component 2; explicit axioms unchanged at 10):** the program's closing statement `chiSquareDistance_le_of_connected` — `χ²(t, x) ≤ r ^ (2t) · ((π x)⁻¹ − 1)` on every connected symmetric-nonnegative positive-degree network under the rate hypothesis — is proved hard crust, assembled from the centered density evolution `h_t − 1 = Pᵗ *ᵥ (h₀ − 1)` (one induction from the delivered `walkDensity_succ` plus the new constant fix `P *ᵥ 1 = 1`), the delivered ℓ²(π) contraction, and the one genuinely new mathematical component: the **connectivity kernel characterization of the normalized Laplacian** — a `μ = 0` eigenvector of `L_sym` lies in the kernel, the kernel transfers through the proved congruence `√D L_sym √D = laplacian A` (the inner `√D (1/√D)` cancelling to `1`), connectivity pins the pre-conjugated vector constant (`v k = √(deg k) · c` entrywise), and mass conservation `∑ deg (h₀ − 1) = 0` (termwise `deg · h₀ = vol · ν₀`) collapses every kernel coefficient to zero — so the contraction's mode hypothesis is **derived** from connectivity, not assumed. Load-bearing on the kernel theorem, the congruence, and mass conservation; an error in any surfaces in this proof. QA is load-bearing in both directions: the positive witnesses pin the *final* statement on both fixtures — K₃ with the bound **attained exactly** at `t = 1, 2` (`1/2 ≤ (1/4)·2`, `1/8 ≤ (1/16)·2`, both sides independently recomputed from the pinned laws and π), P₃ with the λ* = 1 bound honest and strict (`χ²(2) = 1 ≤ 3`, the rate `r = 1` derived basis-independently from two sum-of-squares certificates — the Dirichlet identity `v ⬝ L_sym v = (v₀ − v₁/√2)² + (v₂ − v₁/√2)²` and its `2‖v‖² − ·` companion pinning every eigenvalue into `[0, 2]` with no exact-spectrum computation). The negative witnesses tie both hypotheses to refuted hypothesis-free forms: **connectivity** — on the triangle⊕self-loop `Fin 4` fixture (all degrees 2, π uniform, support graph disconnected because a loop is not a support-graph edge) the spectrum `{0, 0, 3/2, 3/2}` is derived basis-independently (row sums 2 give `μ(∑v) = 0`; the loop's vanishing `L_sym` row gives `μ v₃ = 0`; the zero-sum unit-sphere quadratic form forces `μ = 3/2`), so the rate hypothesis *provably holds* at `r = 1/2` — the loop's `μ = 0` mode being excluded from it by design, exactly the hole connectivity plugs — while the walk never leaves the triangle and `χ²(3) = 3/8 > 3/64 = (1/2)^6 · 3` refutes the conclusion; **the rate** — at `r = 1/4` on K₃ the hypothesis is provably unsatisfiable (any `3/2` eigenvalue violates it and the trace forbids all-zero) and the conclusion `1/2 ≤ 1/8` fails with it. Radar axis 5 re-scored 3.0 → 3.5 per the proposal's own gate (the mixing statement is the trigger).
- **The geometric decay engine delivered (2026-08-22, `proposals/mixing-time-bound.md` Step 3, component 1 of the authorized sub-decomposition; explicit axioms unchanged at 10):** the mathematical core of the mixing-time program's hardest step — powers of the non-symmetric walk matrix are conjugated across the proved similarity (`√D *ᵥ (Pᵗ *ᵥ g) = (1 − L_sym)ᵗ *ᵥ (√D *ᵥ g)`, one `√D` per step), eigencoordinates then evolve geometrically through the *generic* eigenaction at `1 − M` (`v i ⬝ᵥ ((1−M) *ᵥ x) = (1 − μ i)(v i ⬝ᵥ x)`, added to the center), Parseval resolves the π-weighted norm exactly, and the ℓ²(π) contraction `∑ π ((Pᵗ g))² ≤ r^{2t} ∑ π g²` follows term-wise with **no case split on `r < 1`** — the mode hypothesis kills kernel terms and the rate hypothesis bounds the rest. The statement-shape decision recorded before stating: mode exclusion is **value-based** (`eigvalOf (L_sym) i = 0`), not index-based — the index form is only correct when the kernel is one-dimensional, while the value form keeps the contraction true unconditionally and fails honestly on disconnected graphs exactly where no decay to global stationarity exists. QA is load-bearing on two fixtures: the triangle K₃ (connected, non-bipartite — walk spectrum `{1, −1/2, −1/2}`, so λ* = 1/2 and the bound is **exact**: the π-norms `2 → 1/2 → 1/8` against bounds `(1/4)·2 = 1/2` and `(1/16)·2 = 1/8`, equalities) with every eigen-fact derived *without naming Mathlib's classically chosen basis vectors* (`μ ≠ 0 → μ = 3/2` from the summed eigen equation plus unit norm; `μ = 0 → v ∝ 1` from pairwise differences; the kernel index's existence from trace = 3 against three copies of 3/2) — and the path P₃ retained for the degenerate λ* = 1 oscillation cross-check: the engine's exact identity at `t = 2` pins the basis-independent eigencomponent sum to `4`, which the π-norm bridge ties to the already-pinned `χ²(2) = 1` (`1 = (1/vol)·4`) — no decay, exact oscillation, engine and χ² agreeing. Negative witness: the mode hypothesis dropped at `g = 1` on K₃, where the conclusion is refuted (`1 ≤ 1/4` false) *and* the hypothesis is provably unsatisfiable there (the kernel eigenvector's coefficient against `√D *ᵥ 1` is `3·c·√2 ≠ 0` with `c ≠ 0` by unit norm) — so the exclusion is load-bearing, not decorative.

- **The ℓ²-mixing proxy delivered (2026-08-22, `proposals/mixing-time-bound.md` Step 2; explicit axioms unchanged at 10):** the mixing-time program's Step-2 decide-and-record gate was made first (a scoping record the proposal authorizes a run to make): the ℓ² statement alone satisfies the proposal's goal — TV conversion stays a separate further step per the proposal's own Step-4 framing — and *within ℓ² the weighted form is primary*: the proxy is the χ² distance `∑ (ν_t − π)²/π`, because Step 3's geometric decay bound is Parseval-exact only in the π-weighted inner product where the transferred eigenbasis of Step 1 is orthogonal; the unweighted Euclidean distance of the proposal's original sketch is delivered as a corollary bridge instead. The new `GraphTheory.Mixing` defines the objects Step 3 quantifies over (`stationaryVec`, `walkDistribution`, `walkDensity`, `chiSquareDistance`) with the evolution interface — and `walkDensity_succ` is the first theorem to *consume* the same day's Phase A detailed-balance interface: in density coordinates the distributional evolution (`Pᵀ` on laws) becomes the action of `P` itself, which is exactly the form the transferred eigenbasis diagonalizes. QA is load-bearing both ways: every headline quantity pinned through the theorems *and* by raw literal arithmetic (`π = (1/4,1/2,1/4)`, laws `δ₀ → (0,1,0) → (1/2,0,1/2)`, `P *ᵥ (4,0,0) = (0,2,0)`, `χ²(0) = 3 = 9/4 + 1/2 + 1/4`, `χ²(2) = 1` — the bipartite non-decay the Step-3 bound must reproduce on this fixture, `3/8 ≤ 1/2` for the plain-ℓ² bridge), plus two negative witnesses tying the load-bearing hypotheses to refuted hypothesis-free forms (symmetry for stationarity at the asymmetric positive-degree matrix, `1/3 ≠ 2/3`; degree positivity for the vanishing characterization at the zero adjacency, where `χ² = 0` junk-coexists with `ν₀ ≠ π`).

- **Reversibility (detailed balance) delivered (2026-08-22, `proposals/reversibility-and-heat-semigroup.md` Phase A; explicit axioms unchanged at 10):** the simple random walk on a weighted undirected graph is now *proved* reversible with respect to its stationary measure — one of radar axis 5's three named-absent items at the start of the day, and the inner interface the higher-ranked mixing-time program's ℓ²(π) proxy geometry consumes. The delivery is deliberately minimal per the proposal's own mandate ("composes existing lemmas rather than growing the interface"): no new definitions, four theorems — the degree-measure identity (both sides exactly `A i j`), its stationary-measure division form (which turned out to need *no* volume-positivity hypothesis), the symmetrizability packaging `D * P` symmetric, and the regular-case uniform corollary. QA is load-bearing in the prescribed both directions: positive (every index pair of the irregular P₃ fixture, theorem route *and* raw literal arithmetic, volume pinned independently) and negative (an asymmetric positive-degree matrix where the hypothesis-free statement is refuted `2 ≠ 1` with the symmetry hypothesis provably violated at the same pair).

- **Walk-matrix eigenpair transfer delivered (2026-08-22, `proposals/mixing-time-bound.md` Step 1; explicit axioms unchanged at 10):** the non-symmetric walk Laplacian now has a spectrum interface without ever being symmetric — eigenpairs transfer through the proved similarity identity in both directions, the transition matrix acts on the transferred eigenvectors at the reflected eigenvalues `1 − λ`, and every `walkEvals` entry is certified a genuine eigenvalue with an explicit nonzero witness. The gap this closes was named in `Normalized.lean` itself ("turning this identity into an equality of eigenvalue lists requires a characteristic-polynomial interface the pin does not provide") — the delivered route needs no characteristic polynomial at all: eigenpair conjugation plus the eigenbasis expansion. QA is load-bearing both ways: the transferred kernel/alternating/middle eigenpairs are verified through the theorems *and* by raw arithmetic on the conjugated vectors (which are `(1,1,1)`, `(1,−1,1)`, `(1,0,−1)`), and two guards refute the tempting shortcuts — skipping the conjugation (`L_sym *ᵥ (1,−1,1) ≠ −1 • (1,−1,1)`) and forgetting the `1 − μ` reflection (`P *ᵥ (1,−1,1) ≠ 2 • (1,−1,1)`).
- **Davis–Kahan retired from axiom to proved theorem (2026-08-21, `proposals/discharge-perturbation-axioms.md` Step 1 complete; explicit axioms 11 → 10):** the largest single axiom retirement this proposal contemplates, executed exactly along the Step-0 survey's route — the equal-rank projector identity (component 1, delivered earlier the same day) plus the Duhamel/exponential-integral bound (component 2, this run: the new `Analysis.OperatorTheory.Perturbation.Duhamel`). The pre-edit pass found one genuinely new subtlety the survey had not needed to resolve — eigenvalue ties at the thresholds make the two `initialProjector` ranks differ, blocking the equal-rank identity — and the delivery resolves it honestly: both tie cases collapse through the proved Weyl additive bound to `δ ≤ ‖E‖`, finishing via the unconditional `‖P − Q‖ ≤ 1`. Downstream, `davisKahanTwoPoint` becomes fully hard crust and `eventStreamProjectorDrift` is conditional on `matrix_azuma_hoeffding` alone — the persistence chain's trust surface halved. QA gains the required strict non-vacuity witness: a genuinely rotated projector (`1/√10` exact distance against the `1/3` bound), every quantity pinned from trace/determinant/sortedness and the eigen equations, independent of the theorem under test.

- **Spectral band projectors, Step 4 — the Hilbert-projection specialization (2026-08-21, `proposals/spectral-band-projectors.md`, the Active table's only High row at its recorded open next step; the program is COMPLETE):** the proposal's closing statement — the band projector's output is the closest point in its range to the input — delivered as pure hard crust in `GraphTheory.Band` — **zero new axioms** (count stays 11; `#print axioms` on all three new public theorems reads only `propext, Classical.choice, Quot.sound`). Mathlib's Hilbert projection theorem is instantiated at the band's transported range, exactly as the proposal's mandatory pre-step survey scoped it: the shelf had everything needed (`eq_orthogonalProjection_of_mem_of_inner_eq_zero`, `orthogonalProjection_minimal`, `HasOrthogonalProjection.ofCompleteSpace`, and the `toEuclideanLin`/`inner_piLp_equiv_symm` transport — the resolvent Step-0 record's precedent). The engine is the residual-orthogonality fact, which consumes exactly the two Step-1 facts (symmetry + idempotence) — load-bearing on the whole Step-1 algebra rather than sitting beside it. The closest-point statement is deliberately phrased at `EuclideanSpace ℝ V`: the bare `V → ℝ` type carries the sup-norm instance, and a closest-point claim at the bare type would be silently wrong-normed. QA (+25 declarations by the generator metric, 109 in file, 998 total): the identification instantiated and pinned to the packaged band image; minimality attained (`= 4` exactly), strict over the zero signal (`4 ≤ 5`, the 3-4-5 triangle), and generic over the whole range line (`4 ≤ √((3−t)²+16)`) with an independent raw-arithmetic cross-check reproducing the same line inequality from square-positivity; the high band instantiated too; and the **fixed-space guard** refuting the hypothesis-free form (the unfiltered signal is strictly closer to itself than the projection is — the fixed-point hypothesis is load-bearing, not decorative). With this step the four-step band-projector program is complete; the proposal's one-step-per-run rule held throughout (Steps 1–3 on 2026-08-20, Step 4 on 2026-08-21).
- **Spectral band projectors, Step 3 — completeness under a partition (2026-08-20, `proposals/spectral-band-projectors.md`, the Active table's only remaining High row, one-step-per-run):** the assembling statement of the proposal's design note — a family of bands covering the spectral range resolves the identity — delivered as pure hard crust in `GraphTheory.Band` — **zero new axioms** (count stays 11; `#print axioms` on all four new public theorems reads only `propext, Classical.choice, Quot.sound`). The algebraic engine is the *unconditional* telescoping law (`sum_range_bandProjector_eq_sub`): the sum of consecutive band projectors collapses to `P_{t_n} − P_{t_0}` for **any** threshold sequence, ordered or not — a sign-flipped or transposed band definition would leave an uncancellable residue, and the QA non-monotone witness (`4, 0, 4`, junk band `−1` cancelling covering band `1`, both sides independently `0`) exercises exactly that unconditional character. Completeness (`sum_range_bandProjector_eq_one`) consumes exactly the two endpoint covering hypotheses; both are load-bearing — the two QA endpoint guards refute the hypothesis-free form in both directions, each with its violated covering hypothesis separately refuted at the pinned spectrum, witnessing the design note's "silently ignores modes" failure mode. The recorded statement-shape decision: monotonicity is deliberately *not* folded into the sum identity (telescoping does not consume it — it would be a decorative hypothesis); it is delivered as `bandProjector_mul_bandProjector_eq_zero_of_monotone`, where it is exactly what is consumed, so together with completeness the identity resolves into *mutually orthogonal* band projectors — the partition character stated rather than assumed. The consumer's vector decomposition `∑ B_k *ᵥ x = x` completes the external consumer's interface. QA (+41 declarations, 84 in file, 973 total).
- **Spectral band projectors, Step 2 — orthogonality of disjoint bands (2026-08-20, `proposals/spectral-band-projectors.md`, the Active table's only remaining High row, one-step-per-run):** the pairwise half of the partition completeness Step 3 generalizes, delivered as pure hard crust in `GraphTheory.Band` — **zero new axioms** (count stays 11; `#print axioms` on all four new public theorems reads only `propext, Classical.choice, Quot.sound`). Exactly the route the proposal's open-next-step section recorded: the Step-1 nestedness cross-law's ordered forms expand `(P_b − P_a)(P_d − P_c)` to the four-term expression `P_{min b d} − P_{min b c} − P_{min a d} + P_{min a c}`, which collapses under the ordering to the literally-`X − X` residue closed by `sub_self` — the forward order through `_of_le`, the flipped theorem through `_of_le'` (a direct expansion, not a transpose wrapper). The hypothesis set is folded to `a ≤ b`, `c ≤ d`, `b ≤ c`, and `b ≤ c` is *exactly* disjointness of the half-open intervals `(a, b]` and `(c, d]` — load-bearing, not decorative (the QA overlap guard refutes the hypothesis-free form). Beyond the two composition orders, the delivered interface has the two consumer forms: `bandProjector_inner_eq_zero` (the images are dot-orthogonal — filtered components from disjoint bands carry no shared signal; the first band moves across the dot product through `vecMul_transpose`/`dotProduct_mulVec`/`vecMul_vecMul`, its symmetry making the transpose itself) and `eq_zero_of_bandProjector_mulVec_eq_self` (the subspace-level reading of the proposal's "they share no eigenvector": a vector fixed by two disjoint bands is zero, because the second band annihilates the first band's image). QA (+16 declarations, 43 in file, 932 total): composition to zero through the theorem *and* by raw literal multiplication on the pinned band values, in both orders; image orthogonality on concrete vectors by both routes, with the **same-band counter-witness** (the low band paired with itself gives dot product `1 ≠ 0`, so the vanishing is genuinely about disjointness, not the fixture's zero entries); the composed action `B_high *ᵥ (B_low *ᵥ x)` annihilating a filtered signal by both routes; a low-band range vector killed by the high band (the numeric companion of the shared-mode theorem); and the **overlap guard** — the bands `(−1, 3]` and `(1, 4]`, both containing the eigenvalue `3`, compose to the provably nonzero `diag(0,1)` (entry `1` at the shared mode's axis), so `b ≤ c` is witnessed load-bearing.
- **Spectral band projectors, Step 1 — the two-sided band projector (2026-08-20, `proposals/spectral-band-projectors.md`, the Active table's only remaining High row):** the `(a, b]` band projector as the difference of two `spectralProjector` calls, delivered as pure hard crust in the new `GraphTheory.Band` — **zero new axioms** (count stays 12; `#print axioms` on all seven new public theorems reads only `propext, Classical.choice, Quot.sound`). The pre-edit survey (the proposal's mandatory Step-1 check) confirmed the projector lemma set already carries symmetry/idempotence/extremes, and identified the genuinely load-bearing missing piece: **idempotence of a difference does not follow from idempotence of the factors** — it needs the nestedness cross-law `P_{c₁} * P_{c₂} = P_{min c₁ c₂}`, proved entrywise by the same orthonormal expansion as the existing idempotence proof with the two threshold filters intersecting (and the flipped order by transposing symmetry). The old `spectralProjector_idempotent` is now **re-derived from the master law at unchanged statement** — its 50-line entrywise proof is one rewrite, strict generalization replacing duplication. Alongside: the complete eigenvector-action description (`P_c *ᵥ vᵢ = if λᵢ ≤ c then vᵢ else 0`) and its two specializations — the bandpass-selection interface, with the band versions (in-band fixed, below-band and above-band annihilated), the below-spectrum special case `bandProjector_eq_spectralProjector_of_lt` (the design note's named special case — `spectralProjector` kept, not re-derived), and the covering-band `= 1`. Parameterized by interval `(a, b]`, not index, per the design note; the definition is total with the `a > b` junk documented. QA (27 declarations): the diagonal fixture `!![1,0;0,3]` — chosen over the dense `!![2,1;1,2]` because one-dimensional eigenspaces make the eigenvector *directions* computable — with the spectrum `{1,3}` pinned from trace+determinant independent of the machinery under test, direction pins from the eigen equations (`λ = 1 ⇒ v = ![±1,0]`), the projector at any `c ∈ [1,3)` pinned to the hand-computed outer product `e₀e₀ᵀ`, band values computed independently (`B(−1,2] = diag(1,0)`, `B(2,4] = diag(0,1)`, `B(−1,4] = 1`, the between-eigenvalues band `B(3/2,5/2] = 0` — the gapped-definition guard), idempotence by raw literal multiplication as well as through the theorem, the cross-law instantiated numerically in both orders, and mode selection witnessed in both directions (excluded mode annihilated **and not fixed** with `v ≠ 0`; interior mode fixed; low-band action recomputed by raw arithmetic).
- **Tikhonov regularization delivered in the Laplacian eigenbasis (2026-08-20, `proposals/tikhonov-shrinkage-filter.md`, the Active table's top High row):** all three build steps in one run (the proposal's own multi-step clause), pure hard crust in the new `GraphTheory.Tikhonov` — zero new axioms (count stays 12; `#print axioms` on all 18 public theorems reads only `propext, Classical.choice, Quot.sound`). The minimizer is **defined by the eigenbasis formula** (the recorded route decision, per the standing rule — explicit formula over `Classical.choice` strict convexity), so the interface theorems are computations: the closed-form eigencoefficient identity (hypothesis-free, through a *generic* filter workhorse `dotProduct_eigvecOf_filter` any GSP consumer can reuse), the normal equation plus its **converse characterization** (the QA-pinning interface), and minimality/uniqueness both falling out of the single strict-convexity decomposition `obj(x) − obj(x*) = ∑_k (1 + λ_k/π)(d_k − d*_k)²`. Two honest statement-shape corrections to the proposal's Step 3: the factor is **exactly 1 at λ = 0** (the kernel mode passes through untouched — that *is* mean preservation, `sum_tikhonovMinimizer_eq_sum`, which needs symmetry and `π ≠ 0` only, no nonnegativity or connectivity), and "not a projection" is a *theorem* — failure of idempotence `T(T v) ≠ T v` at any positive-eigenvalue eigenvector (with the sketch's nonnegativity hypothesis found unnecessary and deleted). QA (+30, 890 total): the minimizer pinned against a **hand-solved linear system** through the converse characterization — the spectral-theorem construction and plain Gaussian elimination independently meet at `![2/3,1/3]`; the objective pinned exactly (`1/3` against `1`); the shrinkage story exact at the pinned spectrum `{0,2}` (factors `1` and `1/3`); `T² ≠ T` numerically (`![5/9,4/9] ≠ ![6/9,3/9]`); and the `hπ` guard refuted-on-omission at `π = 0` (`1 ≤ 0` false).
- **Weyl perturbation bound retired from axiom to proved theorem (2026-08-20, `proposals/discharge-perturbation-axioms.md` Weyl target, operator-directed):** the explicit axiom count drops 13 → 12. The Step-0 spike (recorded in the proposal) confirmed the proposal's own finding and added the decisive detail — the *additive* Weyl bound is **not** a free corollary of the norm bridge (it consumes both Courant–Fischer witness directions plus *both* Rayleigh domination bounds, the bottom half `evals_first_mul_dotProduct_le_quadForm` newly added to `GraphTheory.Spectral` as the mirror of the EML step's `quadForm_le_evals_last`), but alongside them it is cheap: `weyl_additive_upper` exhibits the existence-direction witness subspace for `A` as a member of the competitor set defining `λᵢ(A+E)` through `evals_min_max` (on it, `R_{A+E} = R_A + R_E ≤ λᵢ(A) + λₙ(E)` by `quadForm_le_evals_last`); `weyl_additive_lower` runs the competitor direction for `A` inside the `(i+1)`-dimensional witness subspace for `A+E` (the competitor vector has `λᵢ(A) ≤ R_A = R_{A+E} − R_E ≤ λᵢ(A+E) − λ₁(E)`); `weyl_inequality` then composes the window with the proved `l2OpNorm_eq_max_abs_evals` (`‖E‖ = max |λ₁(E)| |λₙ(E)|`), at the **unchanged name, hypotheses, and conclusion** (the `sherman_morrison`/`woodbury_identity`/`eigen_interlacing` retirement precedent; the bridge's `1 ≤ card V` hypothesis is vacuous-cover at `card V = 0`). Consequences: `spectral_gap_stability` is now fully hard crust; `davisKahanTwoPoint` is conditional on `davis_kahan_sin_theta` alone and `eventStreamProjectorDrift` on two axioms instead of three (both verified by `#print axioms`). QA (+14 declarations by the generator metric, 18 in file): the nonzero fixture `A = E = !![2,1;1,2]` with both spectra independently pinned (`[1,3]`, `[2,6]`) and `‖E‖ = 3` through the bridge — the bound attained exactly at one index, strict at the other (the proposal's required non-vacuity witness), both additive bounds instantiated at both indices with endpoint attainment, and the window-endpoint guard (`λ₁(E)` for `λₙ(E)` refuted: `6 ≤ 4` is false).
- **Resolvent calculus, Step 3 — injectivity; the program is complete (2026-08-20):** the top High item's recorded open next step, delivered in `Analysis.OperatorTheory.Resolvent` — **zero new axioms** (13 unchanged; `#print axioms` on all three new public theorems reads only `propext, Classical.choice, Quot.sound`). Exactly the proposal's own route: contrapose the Step-1 resolvent identity — equal resolvents `(A+1)⁻¹ = (B+1)⁻¹` collapse the identity's left side to `0`, so `(A+1)⁻¹ * (B − A) * (B+1)⁻¹ = 0`, and multiplying through by `A + 1` on the left and `B + 1` on the right cancels both invertible outer factors (`Matrix.mul_nonsing_inv` / `Matrix.nonsing_inv_mul`), leaving `B − A = 0`. The core theorem `eq_of_inv_add_one_eq_inv_add_one` carries only the two determinant hypotheses (both load-bearing — they are what cancels); the PSD-packaged `resolvent_map_injective_of_quadForm_nonneg` supplies them from Step 1's `isUnit_det_add_one_of_quadForm_nonneg`, and the packaged iff `inv_add_one_eq_inv_add_one_iff_of_quadForm_nonneg` states resolvent equality as a *certificate* of matrix equality for contrapositive consumers. Load-bearing on Step 1 throughout — the factoring *is* `resolvent_identity_sub`. QA (+19 declarations by the scoreboard metric, 94 in file): the injectivity instantiated at **two distinct PSD pairs** (`lap2` vs `0`, and the less degenerate `lap2` vs `mat2` — neither zero), each resolvent pinned by an independent left-inverse witness (`(mat2+1)⁻¹ = (1/8)!![3,−1;−1,3]`) and distinctness verified by entries (`1/3 ≠ 0`, `2/3 ≠ 3/8`), with bridge lemmas rewriting the theorem's output to exactly those numeric facts; and the **invertibility guard** — the hypothesis-free implication "equal resolvents → equal matrices" **refuted** at `A = −1` vs `B = −1 + E` (`E` nilpotent, `B + 1 = !![0,1;0,0]`, determinant `0` by a zero row): distinct matrices whose both `+1` shifts are singular, so both resolvents are the junk inverse `0` — equal while the matrices differ; the core theorem's determinant hypotheses are load-bearing, not decorative.
- **Resolvent calculus, Step 2 — the norm and Lipschitz bounds (2026-08-19):** the top High item's recorded next step, delivered in the existing `Analysis.OperatorTheory.Resolvent` — **zero new axioms** (13 unchanged; `#print axioms` on all three new public theorems reads only `propext, Classical.choice, Quot.sound`). **Recorded route deviation:** the proposal sketched the bounds "through the delivered bridge plus the shifted/inverted eigenvalue transfer"; delivered instead by the *energy route* — for `y = (M + t•1)⁻¹ *ᵥ x`, `t‖y‖² ≤ quadForm M y + t‖y‖² = y ⬝ᵥ x ≤ ‖y‖‖x‖` (dot-product Cauchy–Schwarz transported to `EuclideanSpace` inner products through the bridge's own spine), packaged by `opNorm_le_bound` exactly as Step 0's upper direction was — giving the **strictly stronger general-`t`** statement `‖(M + t•1)⁻¹‖ ≤ t⁻¹` with **no symmetry hypothesis** (Step 1's strengthening style), and the `t = 1` instance (the proposal's item 2, `‖(A+1)⁻¹‖ ≤ 1`). The **Lipschitz bound** (item 4, `‖(A+1)⁻¹ − (B+1)⁻¹‖ ≤ ‖A−B‖`) composes the Step-1 resolvent identity with the scoped `NormedRing` submultiplicativity (`norm_mul_le` — the instance Step 0 verified) and the norm bound on both factors — load-bearing on Step 1 throughout. The sorted-eigenvalue transfer is thereby not needed by this step and stays a named residual (the mixing-time program's similarity transfer is the named consumer). A bonus strengthening: the energy inequality holds for *any* `t` (positivity enters only at division). QA (+45 declarations by the scoreboard metric, 75 in file): the norm bound **attained with route agreement** (`‖(L(K₂)+1)⁻¹‖ = 1` exactly — the energy-route theorem and the eigenvalue-bridge route meet at the pinned spectrum `{1/3, 1}`; likewise `‖(L+2•1)⁻¹‖ = 1/2` exactly at `{1/4, 1/2}`); the Lipschitz instance with both sides independently pinned (`2/3 ≤ 2`); the proposal's shift-load-bearing witness (`‖((1/4)I)⁻¹‖ = 4 > 1` — PSD alone gives no bounded inverse) and a hypothesis-load-bearing witness (`-(3/4)I`'s shift is invertible with inverse norm `4 > 1`, the violated PSD exhibited at `![1,0] = -3/4`).
- **Resolvent calculus steps 0–1 — the operator-norm bridge and the resolvent identity (2026-08-19):** the Active table's top High item (`proposals/resolvent-calculus-psd.md`) opened at its mandatory Step 0 gate, delivered as pure hard crust in the new `Analysis.OperatorTheory.Resolvent` — **zero new axioms** (13 unchanged; `#print axioms` on every public theorem reads only `propext, Classical.choice, Quot.sound`). **Step 0's spike returned a decisive negative on the C*-algebra thread:** `IsSelfAdjoint.spectralRadius_eq_nnnorm` is stated for `[CStarAlgebra A]`, which extends `NormedAlgebra ℂ A` — and `NormedAlgebra ℂ (Matrix (Fin 2) (Fin 2) ℝ)` fails to synthesize (elaboration-verified; a structural obstruction — real matrices are not a complex algebra — not an instance-search gap). The proposal's own contingency was adopted and delivered: the from-scratch finite-dimensional bridge — **the operator norm of a real symmetric matrix equals the max-abs eigenvalue** (`l2OpNorm_eq_max_abs_evals`), both directions proved (`abs_eigvalOf_le_l2OpNorm` via the unit eigenvector through `toEuclideanCLM`; `l2OpNorm_le_of_abs_eigvalOf_le` via the Parseval/eigenaction resolution of `‖M *ᵥ y‖²`), with sorted-spectrum forms consuming the new center mirror lemma `evals_first_le_eigvalOf`. **Step 1:** `M + t • 1` is invertible for every matrix with nonnegative quadratic form and `t > 0` — *no symmetry hypothesis needed* (a kernel vector would force `quadForm M v + t‖v‖² = 0`), a strengthening over the proposal's PSD-symmetric sketch — and the resolvent identity `(A+1)⁻¹ − (B+1)⁻¹ = (A+1)⁻¹(B−A)(B+1)⁻¹` holds by pure `nonsing_inv` algebra. The bridge is the one-time cost the proposal identified: it is the shared route for the Step-2 norm/Lipschitz bounds and the `discharge-perturbation-axioms.md` Weyl target's operator-norm side. QA (`OperatorTheory/Resolvent_QA.lean`, 30 declarations, the first OperatorTheory-domain QA file): `‖!![2,1;1,2]‖ = 3` pinned by composing both directions with a trace/determinant/sortedness-pinned spectrum; the `∀ k` hypothesis refuted-on-omission (`¬(‖M‖ ≤ 1)`); the shift proved load-bearing (the unshifted Laplacian's determinant is exactly `0`); the identity checked against both-sides-computed literals.
- **Decidable spectral certificates, step 4 — QA and extraction demonstration (2026-08-19):** the program's final step, delivered as pure QA (zero new axioms, count stays 13; no public-module changes; 688 → 752 QA declarations). **The exact pins:** a `C₄` Poincaré inequality (`2‖x‖² ≤ xᵀLx` for `x ⊥ 1`, proved by the Wirtinger identity `E = 2Σx² + 2(x₀+x₂)²` under `Σx = 0`) consumes the **lower** half of the proved `lambda2_variational` (`le_csInf` against the constraint set — its first QA consumer in the `≥` direction) to pin `lambda2 (C₄) = 2` exactly, and the alternating vector through the generic top domination pins `λ_max(C₄) = 4`, so `μ(C₄) = 2` exactly — the Ramanujan bound `2√(d−1)` attained with equality, a checkable fact rather than a literature value. **The certificate chain is exactly tight:** the kernel checker accepts precisely at the true `lambda2` and rejects the integer bound `1` below it. **The second required size:** the whole `C₆` chain kernel-decided (accept at the attained `1`, reject at `0`, fractional `3/2`/`1/2`, bridge, end-to-end `lambda2 (C₆) ≤ 1` and `≤ 3/2`). **The `Kₙ`/`Cₙ` Ramanujan fixture family** per the Step 0 scope decision: `K₃` carries the exact energy identity `xᵀLx = 3‖x‖² − (Σx)²` (both Poincaré and top bound at once), pinning `λ₂ = λ_max = 3` and `μ(K₃) = 1` — the classical `μ(Kₙ) = 1` at `n = 3`, strictly inside the Ramanujan range — with the EML *attained exactly on both cuts tested*; the `C₆` EML runs with a derived `μ ≤ 2`, attained exactly on the alternating cut. Implementation notes recorded for future QA on 6-vertex fixtures: the `cons_val` simp family stops at index four (a `rfl`-provable `vecCons_val_five` helper bridges index five for double-sum expansions, where both indices are `OfNat`-literals; `fin_cases`-produced `Fin.mk` row indices defeat the chain, so row sums are proved per row by `show` up to defeq), and ℤ structural facts evaluate by `decide` where ℝ ones cannot.
- **Explicit axiom:** an intentional trust boundary declared with Lean's `axiom` command. QA lemmas and derived theorems using these axioms are conditional on them.
- **Admitted proof:** a `sorry` or `admit` accepted by Lean; this is different from an explicit axiom and remains technical debt in public modules. As of this scoreboard there are no `sorry`/`admit` tokens anywhere under `Scaffold/`.
- **QA declaration:** a theorem or lemma under `Scaffold/QA`; all 1243 declarations have compiled against the current public API.
- **Default build:** the umbrella reaches every public module listed above; no public module is excluded from `lake build`.
- **Decidable spectral certificates, step 2 — the Expander Mixing Lemma (2026-08-19):** `proposals/decidable-spectral-certificates.md` (High) at its named next step, delivered as pure hard crust in `GraphTheory.Expander` plus two generic additions to `GraphTheory.Spectral` — **zero new axioms** (13 unchanged). **The theorem:** for a symmetric, nonnegative, `d`-regular network with Laplacian-spectrum hypothesis `μ ≥ max |d − λ₂(L)| |d − λ_max(L)|`, the `(S,T)` cut weight deviates from the population main term `d•|S|•|T|/|V|` by at most `μ • √(|S||T|(|V|−|S|)(|V|−|T|))/|V|` — the classical discrepancy bridge between algebraic spectral gaps and combinatorial pseudorandomness, at the Step 0 restated `√` signature. **The route is cheaper and stronger than the sketched eigenspace expansion:** a Rayleigh sandwich on `1⊥` — the `d`-regular identity `A + L = D` in quadratic-form form trading the proved lower bound `λ₂•‖x‖² ≤ xᵀLx` (the center's `secondEval_le_rayleigh` multiplied out) against the new generic top bound `xᵀLx ≤ λ_max•‖x‖²` (`quadForm_le_evals_last`, supported by `eigvalOf_le_evals_last`) — giving the operator bound `|xᵀAx| ≤ μ‖x‖²` on `1⊥`, then the sharp bilinear form `(x ⬝ᵥ (A *ᵥ y))² ≤ μ²‖x‖²‖y‖²` by the scaling trick (`√Y•x ± √X•y` through the polarization identity; the choice `a² = Y, b² = X` attains the AM–GM equality — no slack), then the variance identity `‖centeredIndicator S‖² = |S|(|V|−|S|)/|V|` evaluating the geometric factor. No eigenspace split of `A`, no kernel case analysis, no affine spectrum transfer. **Statement-shape strengthening recorded:** the Step 0 sketch's `hloop` hypothesis is dropped — the `A + L = D` identity needs regularity only. QA (+19 declarations, 41 in file): the spectral hypothesis **derived, not assumed** (`μ = 2` proved on `C₄` from a test vector, a direct sum-of-squares estimate routed to `λ_max`, and PSD lower bounds), the lemma instantiated on two cuts, and the bound **attained exactly** on the opposite cut (both sides independently compute to `2`) and on the alternating vector in the operator bound (`8 = 2•4`) — the derived `μ` is tight and the theorem not vacuous.
- **Decidable spectral certificates, step 3 — the computable certificate module (2026-08-19):** `proposals/decidable-spectral-certificates.md` (High) at its recorded next step, delivered as pure hard crust in the new `GraphTheory.SpectralCertificates` — **zero new axioms** (13 unchanged). **The layer:** proof-carrying upper bounds on `lambda2` — an outside solver proposes an inexact rational/integer test vector and a bound; Lean kernel-checks elementary arithmetic (orthogonality to `onesVec`, positive norm, the raw Dirichlet sum against `2 • bound • denom`); and a proved soundness theorem lifts the check to a verified `lambda2 ≤ bound` in the real spectral center. **Both checkers delivered per the Step 0 re-scoping:** the ℚ-facing specification (`isSpectralUpperBoundCertificate`, not kernel-decidable — the recorded reducibility wall) and the kernel-verifiable **integer cross-multiplied twin** (integral and fractional-bound variants), bridged by *proved* cross-multiplication lemmas (pure ordered-field algebra, no `decide`): the twin is sound and complete against the specification at an integer bound, and with `0 < den` the fractional bridge is an exact iff — making the ℚ specification reachable from kernel-verifiable arithmetic. **The soundness theorem** `lambda2_le_of_certificate` is the proposal's flagship load-bearing consumer of the proved `lambda2_variational` (through the new consumer form `lambda2_le_rayleigh`): the certificate's `rawNumer` is exactly `quadForm (laplacian A ℝ) (toReal v)` halved (`laplacian_quadForm` + the ordered-field transport, whose hinge `algebraMap_apply` is `rfl`), `dotOne = 0` places the embedded vector in the constraint set, and `sInf ≤ rayleigh ≤ bound` follows — an error in any link breaks soundness rather than passing beside it. The kernel-facing corollaries compose the chain end-to-end: `decide`d integer data → proved bridge → real bound. QA `SpectralCertificates_QA.lean` (22 declarations): kernel-`decide`d accept (`2`, attained), reject (`1`), non-orthogonal/zero rejects, fractional `5/2`/`3/2`; the ℚ specification reached only through the bridge (the wall made explicit); `lambda2 (C₄) ≤ 2` and `≤ 5/2` from `decide`d data; the certified bound pinned to the test vector's exact Rayleigh quotient (`4/2 = 2`); and the guard-free statement shape **refuted** at `onesVec` (`rayleigh = 0 < lambda2` on the connected cycle) — the orthogonality conjunct is load-bearing, as the proposal's Calibration 2 demands. Step 4 (QA expansion: `C₆`, the `Kₙ`/`Cₙ` Ramanujan fixtures, exact spectrum pins) remains.
- **Decidable spectral certificates, steps 0 and 1 (2026-08-19):** `proposals/decidable-spectral-certificates.md` (High) opened at its gate and through its first build step. **Step 0 (recorded in the proposal before any Lean):** (1) the adjacency-vs-Laplacian convention resolved **Laplacian-first through the `d`-regular bridge** — survey evidence in the proposal (no adjacency-eigenvalue interface exists; the certificate half is already Laplacian; the generic symmetric engine applies to adjacency for free, so the bridge needs no new interface), with the restated `expander_mixing_lemma` signature pinned in Laplacian terms; (2) the ℚ-`decide` spike returned a decisive negative — plain kernel `decide` **cannot** verify the proposal's ℚ-arithmetic certificate, a reducibility wall rather than a performance limit (`((2:ℚ) + 2) = 4` is already stuck: ℚ literals are opaque to elaborator reduction, so `Rat.add`/`Rat.div`/`Rat.blt` never unfold; `#eval` works but is not kernel-checked), with the adopted fallback recorded — the **integer cross-multiplied twin** over ℤ (`rawNumer ≤ 2 * bound * denom`), kernel-decided clean on `C₄` (`Fin 4`, attained `λ₂ = 2`) and `C₆` (`Fin 6`, attained `λ₂ = 1`), accept and reject paths, including the fractional-bound clearing form; (3) the Ramanujan QA scope resolved to the existing `Kₙ`/`Cₙ` fixture family (both are small Ramanujan instances) with the drop-if-awkward fallback. **Step 1 (hard crust, zero new axioms):** `GraphTheory.Expander` — `edgeWeight` with the hypothesis-free matrix form, `edgeWeight_symm`, the unconditional centered-indicator orthogonality (empty-type case included), and the headline `edgeWeight_eq_regular_add_centered` (`e(S,T) = d·|S|·|T|/n + centered cross term`), load-bearing on `deg`'s row-sum shape and the `dotProduct_mulVec` transpose. QA (22 declarations): values computed from the raw definitions on `C₄`, the decomposition instantiated on both an adjacent and an opposite cut with independently computed cross terms, and three negative witnesses (main-term-only refuted `0 ≠ 1/2`; wrong degree refuted `1 ≠ 3/4`; asymmetric weight breaks cut symmetry `2 ≠ 1`).
- **Foster's theorem, Phase A (2026-08-19):** `proposals/spectral-graph-sparsification.md` Phase A (High) delivered as pure hard crust in the new `GraphTheory.Foster` — **zero new axioms** (13 unchanged; `#print axioms foster_theorem` reads only `propext, Classical.choice, Quot.sound`). `foster_theorem`: on every connected symmetric-nonnegative network, `(∑ i, ∑ j, A i j * effectiveResistance A i j) / 2 = card V − 1` — the unordered-pair form of Foster's `∑_{u<v} w_e R_e = n − 1`, with the statement proved at full strength (no cardinality hypothesis; `card V = 1` degenerates to `0 = 0`). Route exactly as the proposal scoped (the pseudoinverse-free eigenbasis route reopening the item `electrical-structure-crust.md` had removed for needing `L⁺`): the per-pair spectral sum `effectiveResistance_eq_sum_eigbasis` (`R u v = ∑_k (v_k u − v_k v)²/λ_k` over nonzero eigenvalues — energy identity + `quadForm_eigvalOf` + self-adjointness), the double-sum swap with per-eigenvector Dirichlet evaluation `∑_{i,j} A i j (v_k i − v_k j)² = 2 λ_k` (via `laplacian_quadForm` and `quadForm_eigvecOf_self`), and the count `#{k : λ_k ≠ 0} = n − 1` from `card_filter_eigvalOf_laplacian_eq_zero` (orthonormality vs. the one-dimensional kernel `laplacian_kernel_eq_span_onesVec`; at least one kernel index because `onesVec` is a nonzero kernel vector). Load-bearing on the whole center chain — an error in solvability, the kernel characterization, the spectral resolution, or eigenbasis orthonormality breaks the proof. Supporting objects: `leverageScore` (the pair's share of the Foster budget — the importance-sampling object of the blocked Phase B; junk below `2 ≤ card V` documented) and `sum_leverageScore_eq_two` (the ordered scores sum to exactly `2`; unordered `1`, the probability-distribution statement). QA `SpectralGraph/Foster_QA.lean` (53 declarations): the cliques `K₃`/`K₄`, the path `P₃`, and the 3-leaf star; every resistance pinned by an explicit potential witness (`K₄` through the general-pair potential `(e i − e j)/4`, proved to solve the unit demand for every pair via the entrywise `L = 4I − J` structure); each ordered sum computed independently of the theorem and cross-checked against `card V − 1`; the **double-counting factor** witnessed on both cliques (`4 ≠ 2`, `6 ≠ 3` — a statement shape dropping the `/ 2` would be refuted); non-edge pairs provably absent from the sum; leverage computed (`K₃` edge `1/3`, total `2`; `K₄` edge `1/6`). Phase B (the sparsification guarantee) remains blocked in the proposal on the matrix-Chernoff scope decision and is not developed.
- **Electrical-flow step 5: the ICP capacity-reinforcement example (2026-08-19) — program complete:** `effectiveResistance_le_increaseConductance` — on a connected symmetric nonnegative network, raising any single undirected pair's conductance by `δ ≥ 0` cannot increase the effective resistance between any two vertices. The one-hypothesis packaging of step 4's monotonicity (`increaseConductance A i j δ` supplies the comparison entrywise, `0 ≤ δ` making it a domination); the reinforced network's connectivity is *derived*, not hypothesized, by the new adapter `supportGraph_connected_of_le` (support graphs grow along entrywise domination — `supportGraph_le_of_le`, which needs no nonnegativity hypothesis since `0 < A i j ≤ B i j` — and Mathlib's `SimpleGraph.Connected.mono` lifts connectedness along the containment). Zero new axioms (13 unchanged); with this step all six build steps and the proposal's acceptance criteria are delivered. QA (+9 declarations, 88 in file): the release-facing example on the Mathlib `Fin 3` path graph — the adapter weights bridged entrywise to the weighted-fixture world, the reinforcement computed to the concrete doubled-path matrix, the reinforced resistance `3/2` pinned by an independent potential witness with connectivity from explicit walks (independent of the adapter the theorem consumes), the headline instantiated in the one-hypothesis form, and the decrease certified strict `3/2 < 2`.
- **Electrical-flow step 4: Rayleigh monotonicity in conductance form (2026-08-19):** `effectiveResistance_le_of_le` — on connected graphs with symmetric nonnegative weights, an entrywise conductance increase `A ≤ B` cannot increase effective resistance, `effectiveResistance B u v ≤ effectiveResistance A u v`; the ICP-facing fact that adding network capacity cannot worsen the certified energy cost of electrical routing. Proved exactly by the proposed route (zero new axioms; count stays 13), through two new reusable interfaces: `isFlowOn_of_le` (flow-space growth — a flow supported on `A` is a flow on every entrywise larger `B ≥ A ≥ 0`; the load-bearing step is support, where a zero entry of `B` above a nonnegative `A` squeezes the `A` entry to zero) and `flowEnergy_le_of_le` (raising conductances lowers dissipated energy — termwise `θ² / B ≤ θ² / A` on nonzero branches since every denominator increased; support is again load-bearing on the `A i j = 0 < B i j` branch, where the `B`-term would otherwise exceed the zeroed `A`-branch). The headline composes them with the delivered chain: the `A`-unit-demand potential's current is a unit flow *on `B`* (growth + Kirchhoff bridge), Thomson's principle (step 3) on `B` bounds `R_B` by its `B`-energy, the comparison bounds that by its `A`-energy, and the step-2 identity evaluates it as `R_A` — load-bearing on solvability, the Kirchhoff bridge, support, Thomson, and the energy identity. Connectivity of both graphs is hypothesized (the proposal's optional adapter deriving `B`'s from `A`'s is deliberately not attempted). QA (+20 declarations, 79 in file): the proposal's QA item 2 — conductance `1 → 2` on the unit edge, resistance certified to decrease `1 → 1/2` from an independent potential witness, monotonicity instantiated, the decrease certified strict, and the **orientation guard** (the reverse inequality `1 ≤ 1/2` refuted numerically — weights are conductances; the proposal names a reversed orientation as a statement bug, and this witness would catch it); the competitor transfer instantiated on computed objects (the `edgeAdj`-current is a unit flow on `edge2Adj`; its cross-network energy `1/2 ≤ 1` computed from the raw definitions); and a partial increase on the triangle — one edge's conductance doubled, resistance `2/3 → 2/5` strict, the new value pinned by the independent potential `![2/5, 0, 1/5]`. Step 5 (the ICP example) remains.
- **Electrical-flow step 3: Thomson's principle (2026-08-19):** `effectiveResistance_le_flowEnergy` — on a connected graph with symmetric nonnegative conductances, every valid unit flow `θ` from `u` to `v` dissipates at least the effective resistance, `effectiveResistance A u v ≤ flowEnergy A θ`; the electrical current is the energy minimizer. Proved exactly by the proposed route (zero new axioms; count stays 13): the unit-demand potential's current `ι` is a unit flow (step 1), the difference `d = θ − ι` is a flow with zero divergence (`isFlowOn_sub`, `flowDivergence_sub`, the Kirchhoff bridge, and the demand equation — an error in any breaks the proof), the superposition lemma `flowEnergy_add_of_flowDivergence_eq_zero` adds `d`'s energy to `ι`'s (the cross term is `∑ i j, (f i − f j) * d i j` after Ohm's law; its row sums vanish by zero divergence and its column sums are the negated row sums by antisymmetry), `flowEnergy_nonneg` discards it, and the step-2 identity evaluates `ι`'s energy as the resistance. **Statement shape:** the superposition lemma is stated with **no hypotheses on `A`** — not even the symmetry the proposal's sketch assumed — only the perturbation's flow properties enter. No `sInf` packaging, per the proposal's instruction. QA (+18 declarations, 59 in file): attainment at the edge (`1 = 1`) and at the **split current** on the triangle `K₃` (`R = 2/3` = the current's energy, computed from the raw definitions, the current genuinely splitting `2/3`/`1/3` across two routes); a **strict competitor** — the detour unit flow around the two-edge path, a valid `IsUnitFlow` with all conjuncts computed, energy `2`, Thomson instantiated as `2/3 < 2` (the inequality is not vacuous on a network with competing routes); and the superposition decomposition composed as `2 = 2/3 + 4/3` with all three energies computed independently of the lemma. Steps 4–5 (Rayleigh monotonicity, the ICP example) remain.
- **Electrical-flow step 2: flow energy and the energy agreement (2026-08-19):** `flowEnergy` delivered exactly as the proposal pinned it — the explicit zero branch (`if A i j = 0 then 0 else θ i j ^ 2 / A i j`), summed over ordered pairs, halved for double counting — with three proved theorems, zero new axioms (13 unchanged): `flowEnergy_electricalCurrent` (`flowEnergy A (electricalCurrent A f) = quadForm (laplacian A) f`; termwise Ohm's-law algebra `(c x)²/c = c x²` on the nonzero branch, zero-on-zero on the branch, then `laplacian_quadForm` — statement shape landed **symmetry-only**, nonnegativity is not needed for the agreement, and QA witnesses both sides of that claim), `flowEnergy_nonneg` (squares over positive conductances — the base order fact steps 3–4 compare flows by; nonnegativity load-bearing, QA exhibits energy `-1 < 0` on a symmetric negative-weight network), and the headline `flowEnergy_electricalCurrent_eq_effectiveResistance` (on a connected graph, a unit-demand current dissipates exactly the resistance it routes — agreement composed with the crust's solution-level energy identity). QA (+25 declarations, 41 in file): energies `1`/`2` computed from the raw definitions on `K₂`/`P₃` and cross-checked against independently computed Dirichlet energies and pinned resistances (three routes meeting at each value); the **mandatory double-counting fixture** — raw ordered-pair sum `2 ≠ 1` = Dirichlet energy, so dropping the `1/2` factor would numerically break the agreement; the step-1 phantom's deferred energy pinning (`0` on the edgeless network); and the **zero-energy competitor** — on a real edge plus an isolated vertex, an antisymmetric unit-divergence flow routing through zero-conductance pairs dissipates `0 < 1` = the real resistance and is excluded by `IsFlowOn`'s support conjunct alone: over flows-satisfying-everything-except-support, Thomson's principle (step 3) would assert `1 ≤ 0` on this fixture. Steps 3–5 (Thomson, Rayleigh monotonicity, the ICP example) not started.
- **Electrical-flow interface, proposal steps 0–1 (2026-08-19):** `proposals/electrical-flow-routing.md` (High) opened. Step 0 (recorded in the proposal before any Lean): fresh survey of the pinned Mathlib confirms no network-flow/circulation/max-flow API, no graph-native divergence (only box/measure-integral divergence theorems on continua), no flow/energy API on `SimpleGraph.Dart`, and no `Matrix.IsAntiSymm` — the matrix representation `EdgeFlow V = Matrix V V ℝ` was pinned, with the zero-edge support semantics carried by `IsFlowOn`'s second conjunct and the `1/2` ordered-pair factor reserved for `flowEnergy` (step 2); target module the new focused `GraphTheory.ElectricalFlow` (coverage map's electrical row re-surveyed). Step 1 delivered as pure hard crust (axiom count unchanged at 13): `electricalCurrent` (Ohm's law `A i j * (f i − f j)` — weights are conductances), `flowDivergence` (net outflow), the predicates `IsFlowOn`/`IsUnitFlow`, `electricalCurrent_antisymm` (symmetry load-bearing), `electricalCurrent_eq_zero_of_weight_eq_zero` (support), the Kirchhoff bridge `flowDivergence_electricalCurrent` (`flowDivergence (electricalCurrent A f) = laplacian A *ᵥ f` — load-bearing on the center's exact sign convention via `laplacian_mulVec_apply`), and the headline `isUnitFlow_electricalCurrent`: every unit-demand potential induces a valid unit flow, turning the delivered potential-based resistance API into a routing object. QA (16 declarations): `K₂` current matrix `!![0,1;-1,0]` and divergence `![1,-1]` computed from the definitions; the 3-path unit flow with internal-vertex divergence `0` (Kirchhoff away from source/sink); the asymmetric-network negative witness (`2 ≠ 1`: `A.IsSymm` load-bearing); and the edgeless-network phantom — antisymmetric, unit divergence, excluded by the support conjunct (the step-1 form of the proposal's zero-conductance trap; the energy-level refutation is deferred to step 2 with `flowEnergy`). Steps 2–5 (energy agreement, Thomson, Rayleigh monotonicity, the ICP example) are explicitly not started.
- **Sherman–Morrison retired from axiom to proved theorem (2026-08-18):** `proposals/retire-sherman-morrison.md` delivered — explicit axiom count 15 → 14. **A pure proof task, not a correctness repair** (recorded per the proposal's acceptance criteria): the statement was verified first against the corrected Woodbury shape — at `C = [1]` the rank-one middle factor is exactly `1 + v ⬝ᵥ (A⁻¹ *ᵥ u)`, so the former axiom already stated the correct specialization (the Woodbury `C`/`C⁻¹` defect is invisible at rank one). The theorem keeps the axiom's exact name, hypotheses (`hA : IsUnit A.det`, `hv : v ⬝ᵥ (A⁻¹ *ᵥ u) ≠ -1`), and statement — zero consumers existed, so no migration surface. Proof: the `k = Fin 1` specialization of the proved `woodbury_identity` — `u`/`v` packed into `Fin 1` column/row matrices, the 1×1 middle factor shown to have entry `1 + v ⬝ᵥ (A⁻¹ *ᵥ u)` (a `Fin.sum_univ_one` computation through `Matrix.dotProduct_mulVec`), its determinant a unit exactly when `hv` holds (`Matrix.det_fin_one`), its inverse the 1×1 scalar inverse (`Matrix.inv_eq_left_inv`), and the final shape matched by the pin's `Matrix.mul_smul`/`Matrix.smul_mul` algebra. The specialization is load-bearing on the Woodbury repair — a residual defect there would surface in this proof rather than pass beside it. QA (`Core/MatrixUpdates_QA.lean`, +4 declarations): the rank-one positive instance at the existing `A2 = diag 2 2` fixture — `A + u vᵀ = !![3,1;1,3]` — with the left side computed independently by the adjugate formula (`sherman_morrison_sum_inv_QA`) and the theorem-consumed right side pinned to the same `!![3/8,-1/8;-1/8,3/8]` (`sherman_morrison_rhs_QA`, denominator computed to `1` from the definitions); and the negative witness at the excluded denominator — `A = !![1]`, `u = ![2]`, `v = ![-1/2]` gives `v ⬝ᵥ (A⁻¹ *ᵥ u) = -1` exactly (`sherman_morrison_excluded_denominator_QA`) and the update `!![1] + !![-1] = !![0]` is singular there (`sherman_morrison_singular_witness_QA`) — the hypothesis excludes exactly the vacuous-junk point.
- **Cauchy interlacing retired from axiom to proved theorem (2026-08-18):** `eigen_interlacing_principal_submatrix` proved in `GraphTheory.Spectral` at unchanged name/hypotheses/statement from the proved Courant–Fischer engine — its first named consumer (explicit axiom count 14 → 13). **A pure proof task, not a correctness repair**: the admitted statement was the true textbook two-sided window `λᵢ ≤ μᵢ ≤ λᵢ₊ₙ₋ₘ`. Route: the extend-by-zero padding bridge (`padVec`/`padVecLinear`, preserving dot products, quadratic forms, and Rayleigh quotients), the lower bound via CF-existence on the submatrix + competitor on the ambient, the upper bound via CF-existence on the ambient at index `i+d` + the subspace-intersection dimension count (`finrank (U ⊓ range pad) ≥ i+1`, through the same `Submodule.finrank_sup_add_finrank_inf_eq` the min–max proof used) + exact-dimension extraction (`exists_submodule_finrank_eq_of_le`) + comap transfer. The proof consumes both CF witness directions and is load-bearing on them. QA (`Interlacing_QA.lean` +4 public declarations, 484 total): the `K₂` Laplacian spectrum `[0, 2]` and its singleton-submatrix spectrum `[1]` pinned from trace/determinant/sortedness independent of the theorem; the window instantiated to `0 ≤ 1 ≤ 2`; both collapsed one-sided bounds refuted in proved form — superseding the axiom-era note that no thin QA of the inequality existed (computational eigenvalue QA machinery did not exist then; it does since the Cheeger repair).
- **General Courant–Fischer min–max proved (2026-08-18):** `proposals/prove-courant-fischer.md` delivered as pure hard crust in `GraphTheory.Spectral` (new `CourantFischer` section; no new axioms — general min–max was never admitted; count stays 15). The `k`-th sorted eigenvalue of *any* real symmetric matrix is characterized at every index, with only symmetry assumed (no positivity, kernel, or graph structure): `exists_submodule_forall_rayleigh_le` (an optimal `(k+1)`-dimensional subspace exists — `Finset.exists_subset_card_eq` extraction from the below-threshold eigenbasis, dimension by `finrank_span_eq_card` through orthonormality, Rayleigh bound through the proved spectral resolution), `exists_ne_mem_rayleigh_ge_of_finrank_eq` (every `(k+1)`-dimensional competitor contains a test vector with quotient ≥ `evals k` — dimension counting against the tail eigenspace through `Submodule.finrank_sup_add_finrank_inf_eq`), and the packaged infimum equation `evals_min_max`. New public center API: general-`k` multiplicity pins (`card_filter_eigvalOf_lt_evals_le`, `succ_le_card_filter_eigvalOf_le` — the count-form generalizations of the `k = 1` pins behind `lambda2_variational`), component-form Rayleigh bounds, `eigvecOf_dotProduct`, `linearIndependent_eigvecOf_finset`, `finrank_span_eigvecOf_finset`. The proposal's open Mathlib-survey step was resolved first (all needed lemmas present in v4.14.0). QA `SpectralGraph/CourantFischer_QA.lean` (33 declarations): the `!![2,1;1,2]` fixture's spectrum `[1,3]` pinned from trace/determinant/sortedness; both directions instantiated — including the derived universal "every Rayleigh quotient ≤ top eigenvalue" (through the existence direction plus `eq_top_of_finrank_eq`) and the witness pinned to attain `evals 1 = 3` exactly through both directions; negative witnesses: the existence-direction property refuted on the wrong line (`3 ≤ 1`), the dimension hypothesis proved load-bearing (its conclusion false on a one-dimensional subspace at index `1`), and — at the interior index `k = 1 < n − 1` on the three-vertex path Laplacian — a wrong two-dimensional subspace refuted (`4/3 ≤ evals 1` contradicts `evals 1 ≤ 1`, itself derived through the theorem) with the older `secondEval_le_rayleigh` engine cross-checking the same bound. The proposal's four named consumers (interlacing retirement, Rayleigh monotonicity, the Cheeger hard direction, Fiedler Phase B) remain separate follow-ons, not claimed here.
- **Woodbury identity repaired and proved, axiom retired (2026-08-18):** the admitted `woodbury_identity` was independently verified **false** — its middle factor `(C + V A⁻¹ U)⁻¹` with no invertibility hypothesis on `C` fails at the scalar instance `A = 1, U = V = 1, C = 0`, where every stated determinant hypothesis holds while the two sides evaluate to `1` and `0`. It is now a theorem at the same name with the standard middle factor `(C⁻¹ + V A⁻¹ U)⁻¹` and exactly the three explicit hypotheses `IsUnit A.det`, `IsUnit C.det`, `IsUnit (C⁻¹ + V A⁻¹ U).det` (the former `IsUnit (A + U C V).det` hypothesis is derivable and dropped — Mathlib's `Matrix.invertibleAddMulMul` constructs the sum's `Invertible` instance). Proof: pure upstream reuse — `Matrix.invOf_add_mul_mul` plus the `NonsingularInverse` bridges `Matrix.invertibleOfIsUnitDet` and `Matrix.invOf_eq_nonsing_inv`, all already imported transitively; zero new axioms, zero `sorry`. Emergency repair per architecture §9 (materially false statement, zero consumers, so no migration surface; no deprecated compatibility declaration — it must not restate the false identity). Explicit axiom count 16 → 15. QA (`Core/MatrixUpdates_QA.lean`, the first Core-domain QA file, 8 declarations): the retired shape restated at `Fin 1` over `ℚ` and refuted *without consuming any axiom* (`old_woodbury_identity_refuted_QA`), with its middle and sum hypotheses separately proved satisfied at the counterexample (the refutation is of a genuinely applicable statement, not a vacuous shape), the corrected `IsUnit C.det` hypothesis proved to exclude that counterexample, and positive instances at nonzero scalars (`(2 + 3)⁻¹ = 1/5`) and at a non-scalar rank-one update (`diag 2 2` + all-ones rank one = `!![3,1;1,3]`, both sides computing to `!![3/8,-1/8;-1/8,3/8]` through the computed middle factor `(1 + 1)⁻¹ = 1/2`) whose right sides are pinned by consuming the theorem and finishing with independently computed left sides. `sherman_morrison` stayed admitted at that milestone (retired later the same day — see the Sherman–Morrison milestone bullet above).
- **Cheeger easy direction proved, axiom retired (2026-08-18):** `cheeger_upper_bound` (`λ₂(L_sym) ≤ 2φ` for `d`-regular graphs) moved from admitted axiom to proved theorem at unchanged name and hypotheses — explicit axiom count 17 → 16; the remaining Cheeger axiom is the hard direction only. Route (in `GraphTheory.Cheeger`): the proved general-operator Courant–Fischer `secondEval_variational` (new in `GraphTheory.Spectral` — the `lambda2_variational` argument generalized to any symmetric PSD matrix with `onesVec` in its kernel; the Laplacian instance re-proves `lambda2_variational` as a two-line corollary at unchanged shape), consumed through its new one-sided form `secondEval_le_rayleigh` at the volume-centered cut indicator `cutTestVector` (`x i = vol Sᶜ` on `S`, `- vol S` off it): orthogonal to `onesVec` under regularity, Dirichlet energy exactly `boundary · vol V²` (no regularity needed — the general weighted-graph cut identity), norm exactly `vol S · vol Sᶜ · vol V / d`, Rayleigh quotient `boundary · vol V / (vol S · vol Sᶜ) ≤ 2 · conductance S`, then the per-cut bound passes to the infimum. The load-bearing chain: PSD of `L_sym` scales from `laplacian_psd` through `d • L_sym = laplacian A` (regularity), the kernel fact is the row-sum identity under `deg = d`, and the cut identity consumes `vol_compl`/`boundary_compl` (cut duality). QA: the test vector pinned to `![1,-1]` on `K₂` and its Rayleigh value computed to `2` — exactly the independently pinned `λ₂(L_sym)` (bound attained); the theorem itself instantiated on `K₂` as `λ₂ = 2φ` from independently computed values, and on `C₄` as `λ₂ ≤ 1` through the exhaustively computed conductance `1/2` of the adjacent-pair cut, whose test-vector Rayleigh value computes to exactly `1`.
- **Fiedler vector and sign partition, Phase A (2026-08-18):** `proposals/fiedler-partitioning.md` Phase A (A1/A2) delivered as pure hard crust in the new `GraphTheory.Fiedler` (no new axioms; count stays 16): `fiedlerIndex`/`fiedlerVector` (an eigenbasis index carrying `lambda2`, fixed by classical choice through `evals_mem_eigvalOf` — the proposal's sketch indexed `eigvecOf` by a sorted-spectrum position, which is not that function's type; the deviation is recorded in the module docstring), the eigenvector equation `fiedlerVector_eigen`, unit norm, nonvanishing, `fiedlerVector_quadForm` (the Fiedler energy is exactly `lambda2`), and orthogonality to `onesVec` under `0 < lambda2`; the algebraic-connectivity certificate `lambda2_pos_of_connected` (connected + symmetric nonnegative weights ⇒ `0 < lambda2` — PSD bounds the eigenbasis, a nonpositive `lambda2` pins the first two sorted entries to `0`, the lower multiplicity pin yields two distinct kernel basis vectors, and `laplacian_kernel_eq_span_onesVec` (electrical step 2) makes two orthonormal vectors on one line contradictory); and `fiedlerPartition` (the sign half-space) with `fiedlerPartition_nonempty`/`fiedlerPartition_ne_univ` — both flowing through the zero-sum identity, exactly where `0 < lambda2` is load-bearing. QA `SpectralGraph/Fiedler_QA.lean` (57 declarations): on `K₂` the partition is pinned to a singleton half (boundary `1`); on the `P₄` barbell (two `K₂` near-cliques joined by one bridge — the proposal's named example shape) `lambda2 ≤ 1` comes from the proved Rayleigh engine at the cut indicator, `lambda2 ≠ 1` from the eigen equations, and the derived sign pattern forces `fiedlerPartition` to be exactly the known good cut `{0,1}` or its complement — boundary `1`, volume `3`, conductance `1/3`, partition card `2`; the disconnected fixture shows the hypothesis load-bearing (`lambda2 = 0`, support graph not connected, and `onesVec` is a nonzero eigenvector at `lambda2` whose sign filter is all of `univ`). Phase B (a certified conductance bound) is explicitly a separate axiom-backed milestone, not delivered here.
- **`SimpleGraph` interop adapter (2026-08-18):** the `SimpleGraph → WAdj` direction of the Mathlib bridge is delivered in `GraphTheory.SimpleGraphAdapter` (no axioms; step 1 of the re-sequenced `proposals/electrical-structure-crust.md`): `SimpleGraph.toWAdj` (defined as Mathlib's `adjMatrix ℝ`, so no parallel matrix construction exists to drift), symmetry/nonnegativity (unlocking every `hnonneg`-hypothesized center theorem), degree/volume agreement with Mathlib (`deg_toWAdj`, `vol_toWAdj_eq_sum_degrees`, handshake `vol univ = 2 * #edgeFinset`), the headline Laplacian agreement `laplacian G.toWAdj = G.lapMatrix ℝ`, the boundary as the crossing-edge count (`boundary_toWAdj_eq_sum_card_neighbors`), the adapter roundtrip `supportGraph (toWAdj G) = G`, the connected-`G` kernel corollary through the delivered span theorem, and the neutral re-exports of Mathlib's kernel/reachable iff and component-count finrank. QA `SpectralGraph/SimpleGraphAdapter_QA.lean` (38 declarations) computes weights, degrees, Laplacian entries, both Laplacian sides, the boundary, the handshake, and kernel membership against hand-expected numbers at the 3-vertex path, and exhibits the disconnected `Fin 4` witness where the component indicator is computed *into* the kernel yet the kernel ≠ `span {onesVec}` — the connectivity hypothesis is load-bearing through the adapter.
- **One-sided Dirichlet bound (2026-08-18):** proposal `electrical-structure-crust.md` step 6 (final step), no axioms, in `GraphTheory.Electrical`: every test potential `f` of positive energy lower-bounds the effective resistance, `(f u − f v)² / quadForm (laplacian A) f ≤ effectiveResistance A u v` (`effectiveResistance_ge_sq_div_quadForm`). Route: a pure-algebra discriminant lemma (`sq_le_mul_of_forall_zero_le_sub` — a real quadratic nonnegative everywhere has nonpositive discriminant), the polarization identity `quadForm_laplacian_sub_smul` (cross terms collapse through the proved reciprocity `laplacian_dotProduct_mulVec`), and the semidefinite Cauchy–Schwarz `laplacian_cauchy_schwarz` (`(f ⬝ᵥ L g)² ≤ quadForm L f * quadForm L g`, no connectivity hypothesis; Mathlib surveyed — the pin's only Cauchy–Schwarz is the definite inner-product one, unusable without quotienting the kernel). The bound's proof is load-bearing on `laplacian_psd` (polarization) and on the step-4/step-5 chain (the demand potential's cross term is exactly `f u − f v`; its energy is exactly `R u v`). QA (9 declarations, in `EffectiveResistance_QA.lean`): equality *attained* at the harmonic potentials on both connected fixtures (edge `1²/1 = 1`, path `4/2 = 2`, energies computed independently from raw definitions), strict at a non-harmonic potential (`1 < 2`), the reverse (upper-bound) inequality **refuted numerically** (`2 ≤ 1` false — the one-sided form is forced; the other direction is the deferred attained-supremum Dirichlet principle), and the `0 <` energy guard shown load-bearing by a disconnected component indicator with zero energy and voltage difference `1` (both sides of an unguarded bound would be junk there).
- **Effective resistance by the potential equation (2026-08-18):** proposal `electrical-structure-crust.md` step 5, no axioms, in the new `GraphTheory.Electrical`. `IsEffectiveResistance A u v r` is the potential-equation relation (`∃ f, L *ᵥ f = e u − e v ∧ f u − f v = r` — the Mathlib pin has no effective-resistance declaration and no pseudoinverse, surveyed before stating); existence consumes the step-4 solvability theorem, uniqueness of `r` consumes the component-form kernel characterization (stated at reachability-pair strength, so it holds within a component of a disconnected graph), and the total function `effectiveResistance A u v : ℝ` is defined by classical choice with an explicit junk fallback `0` whose firing is QA-witnessed. The energy identity holds at two levels: `quadForm (laplacian A) f = f u − f v` for *any* solution `f` (no hypotheses), and `effectiveResistance_eq_quadForm` at the function level; nonnegativity transfers from `laplacian_psd`, symmetry is hypothesis-free at the relation level (negate the potential), and `R u u = 0` is unconditional (`isEffectiveResistance_self_iff` characterizes the diagonal exactly). QA `SpectralGraph/EffectiveResistance_QA.lean` (17 declarations): values computed from the definitions on the unit edge (`R 0 1 = 1`) and the 3-path (`R 0 2 = 2` — series edges add), the energy identity cross-checked against an energy computed independently from the raw definitions, the junk fallback pinned on the disconnected fixture (`0` there is a fallback, not a measurement; no resistance value exists across components), and same-component witnesses showing two distinct potentials pin one value (`1`) through the reachability-form agreement even where the connected-graph theorem's hypothesis fails.
- **Potential solvability, the electrical hinge (2026-08-18):** proposal `electrical-structure-crust.md` step 4, no axioms. On a connected graph (symmetric nonnegative weights), every zero-sum demand `b` admits a potential: `exists_laplacian_mulVec_eq_of_sum_eq_zero` proves `∃ f, L *ᵥ f = b` by the **constructive eigenbasis** route (decision recorded in the execution plan and the proposal), with witness `f = ∑_{λᵢ ≠ 0} (vᵢ ⬝ᵥ b / λᵢ) • vᵢ` — a load-bearing composition of the proved eigenbasis algebra (`exists_mulVec_eq_of_zero_comp`, `mulVec_eigvecOf_sum_apply`) and the step-2 kernel theorem `laplacian_kernel_eq_span_onesVec` (which kills the zero-eigenvalue components of `b`). The unit-demand specialization `exists_laplacian_mulVec_eq_single_sub_single` is the defining equation for effective resistance in step 5. Supporting crust: the reciprocity identity `laplacian_dotProduct_mulVec` (`w ⬝ᵥ (L *ᵥ f) = (L *ᵥ w) ⬝ᵥ f`) and its kernel certificate `dotProduct_eq_zero_of_laplacian_mulVec_eq_zero`. QA `SpectralGraph/PotentialSolvability_QA.lean` (12 declarations): computed potentials on the edge (`![1,0]`) and 3-path (`![1,0,−1]`), theorem instantiations, and proved *unsolvability* witnesses — `e 0` on the connected edge (zero-sum hypothesis load-bearing) and the zero-sum cross-component demand `e 0 − e 2` on the disconnected fixture (connectivity load-bearing, via the component-indicator kernel certificate). Existence here is unconditional proved hard crust; nothing in this slice depends on any axiom.
- **Kernel-equality bridge / weighted component structure (2026-08-18):** proposal `electrical-structure-crust.md` step 3, no axioms. The center gained the component-form kernel characterization `laplacian_mulVec_eq_zero_iff_forall_reachable` (`Spectral.lean`: `L *ᵥ f = 0` iff `f` is constant on each connected component of the support graph — no connectivity hypothesis; new direction proved entrywise via the diffusion-form identity `laplacian_mulVec_apply`, `(L *ᵥ f) i = ∑ j, A i j * (f i − f j)`). The bridge `ker_laplacian_eq_ker_supportGraph_lapMatrix` (`ker (laplacian A) = ker ((supportGraph A).lapMatrix ℝ)`) transfers Mathlib's component-indexed kernel facts onto every symmetric nonnegative weighted graph: kernel dimension = component count (`finrank_ker_laplacian_eq_card_supportGraph_components`) and the component-indicator basis (`laplacian_ker_basis`/`laplacian_ker_basis_apply`, transported via `Basis.map`/`LinearEquiv.ofEq`); the `DecidableRel (supportGraph A hA).Adj` prerequisite is discharged once by `supportGraphAdjDecidable`. QA `SpectralGraph/KernelBridge_QA.lean` (14 declarations, reusing the `Connectivity_QA` fixtures): on the connected 3-vertex path the bridge, dimension `1`, and the constant-`1` basis vector hold simultaneously with the connected span theorem (a mis-stated bridge would contradict it); on the disconnected two-edge fixture the component count is computed to `2` independently of the transferred theorem (block classification + `Nat.card_eq_two_iff`), the transferred dimension reads `2`, and both basis vectors are computed to the component indicators `![1,1,0,0]`/`![0,0,1,1]` — the kernel strictly exceeds the constants.
- **Connectivity and the Laplacian kernel (2026-08-18):** the converse of `laplacian_ones_in_kernel` is proved (no axioms): for symmetric nonnegative weights whose support graph is connected, `ker (laplacian A)` is exactly the constants. New center hard crust: the `supportGraph` adapter (`WAdj → SimpleGraph`, `Adj i j ↔ i ≠ j ∧ 0 < A i j` — the first bridge from the matrix-first representation to Mathlib's `SimpleGraph` connectivity API), `eq_of_laplacian_mulVec_eq_zero_of_pos_weight` (zero Dirichlet energy forces constancy across positive-weight edges), `eq_of_supportGraph_walk` (walk propagation by induction on `SimpleGraph.Walk`), `exists_const_of_laplacian_mulVec_eq_zero`, `laplacian_mulVec_const`, the iff `laplacian_mulVec_eq_zero_iff_exists_const`, and the span form `laplacian_kernel_eq_span_onesVec` (`ker (mulVecLin (laplacian A)) = span ℝ {onesVec}` — the statement shape consumed by future effective-resistance uniqueness arguments). QA `SpectralGraph/Connectivity_QA.lean` exercises both a connected witness (3-vertex path: explicit-walk connectivity, both iff directions, constant recovered and pinned) and a disconnected negative witness (two disjoint `Fin 4` edges: the component indicator is in the kernel but not constant, and the support graph is proved not connected), showing the connectivity hypothesis is load-bearing. This was delivered as step 1 under the proposal's original numbering (it is step 2 after the 2026-08-18 re-sequencing; the adapter above is step 1).
- **Cheeger statement-shape repair (2026-08-18):** both admitted Cheeger axioms were restated at the corrected spectral side `secondEval (regularNormalizedLaplacian A d) …` after QA refuted the old shape in proved form: `lambda2` reads the spectrum of the combinatorial Laplacian *of* its argument, so the old side read `λ₂(L(L_sym)) = λ₂(-L_sym)`, whose two-vertex-edge instance asserts `1/2 ≤ 0` (`old_cheeger_lower_bound_refuted_QA`). The corrected side is independently pinned on the same fixture to the classical value `λ₂(L_sym) = 2` (`edge_normLap_secondEval_eq_two_QA`, computed from trace + determinant + sortedness — the tree's first computational eigenvalue check, conditional on no axiom). Supporting center hard crust added in `GraphTheory.Spectral`: `secondEval`, `lambda2_eq_secondEval`, `evals_mem_eigvalOf`, `eigvalOf_le_of_quadForm_nonpos`, `eigvalOf_sum_eq_trace`. Axiom count unchanged (18); axiom names and hypotheses unchanged. This is an emergency repair of a materially false statement shape (architecture §9), not a mathematical strengthening; downstream consumers of the corrected interface remain conditional on the axioms.
- **Stationary structure (2026-08-17):** the downstream consumer of both walk/normalized interface modules — `GraphTheory.Stationary` proves `L_sym *ᵥ √deg = 0` (kernel of the general normalized Laplacian, counterpart of `laplacian_ones_in_kernel`), `Pᵀ *ᵥ deg = deg` (the degree measure is stationary for the walk — `π ∝ deg`, the Markov-mixing consumer interface), and conservation of mass in both the regular (`randomWalkLaplacian *ᵥ 1 = 0`, consuming `RandomWalk.transitionMatrix_row_sum`) and irregular (`walkLaplacian *ᵥ 1 = 0`, consuming `Normalized.walkTransitionMatrix_row_sum`) cases. No axioms; downstream reuse on the SGT radar re-scored 2.5 → 3.0 → 3.5 with these milestones.
- **Cut duality (2026-08-17):** the first expansion/cut slice delivered in the center — `vol_compl`, `boundary_compl`, `conductance_compl` (cuts are partition-valued: boundary and conductance invariant under complementation for symmetric weights, via volume complementarity and `Finset.sum_comm`), and the degenerate-cut guards `boundary_empty`/`boundary_univ`. These are the structural facts sweep-cut and sparsest-cut consumers assume; no axioms admitted.
- **Random-walk and normalized-Laplacian interfaces (2026-08-17):** the first two broad-SGT backlog items delivered — `GraphTheory.RandomWalk` (transition matrix, row-stochasticity for `d`-regular graphs, walk-Laplacian bridges to both existing worlds) and `GraphTheory.Normalized` (the *irregular* symmetric normalized Laplacian through a diagonal `Real.sqrt` square root — no matrix square root needed — with the congruence `√D L_sym √D = laplacian`, regular-cone agreement, the general walk form `D⁻¹A` with irregular row-stochasticity, and the similarity identity `√D · L_walk · (1/√D) = L_sym`; the eigenvalue-list transfer is the named residual gap — the walk form is not symmetric, so `evals` does not apply, and the pinned Mathlib has no charpoly-roots interface for non-symmetric matrices). No axioms admitted in either module.
- **Citation and assumption review (2026-08-17):** `spectral_gap_stability` was converted from an admitted axiom into a theorem proved from `weyl_inequality` (four Weyl facts plus arithmetic), reducing the explicit axiom count from 19 to 18 at no trust cost; `weyl_inequality`'s doc now records that its Lean form is the spectral-norm corollary of the cited general theorem; `davis_kahan_sin_theta`'s separation hypothesis was tightened from a pairwise quantification to the binding single-pair two-cluster form `λ_{k+1}(A+E) − λ_k(A) ≥ δ` (the form used by the cited Yu–Wang–Samworth Theorem 2), with the derived wrapper simplified accordingly (Weyl at the gap index only). The Chung index's provenance note was corrected: git history shows no earlier page-level locators ever recorded (the initial commit cited "Theorem 2.2" without a page); theorem and page numbering remain explicitly unconfirmed rather than invented.
- **Projector algebra (2026-08-17):** eigenbasis orthonormality (`eigvecOf_inner`) and completeness (`eigvecOf_complete`) are proved from the Mathlib spectral-theorem API, yielding proved `spectralProjector_idempotent` / `initialProjector_idempotent` and the extreme-threshold theorems `spectralProjector_eq_zero` / `spectralProjector_eq_one`. These structural facts were previously consumed implicitly through the admitted perturbation interfaces.
- **Derived layer:** `Derived.EventStream.eventStreamTail` is proved from `matrix_azuma_hoeffding`; `Derived.ProjectorDrift.davisKahanTwoPoint` is proved from `davis_kahan_sin_theta` (and, since the 2026-08-20 Weyl retirement, the *proved* `weyl_inequality` — no longer an axiom dependency); `Derived.ProjectorDrift.eventStreamProjectorDrift` combines them with `eventStreamTail`. All three are checked deductions relative to the trust base (two concentration/perturbation axioms after the Weyl retirement), not foundationally proved results.
- **`spectral_persistence` deprecation (2026-08-17):** the per-step persistence axiom was deprecated with a migration note after a consumer inventory showed zero non-QA consumers and the derived two-endpoint chain covering the motivating use. The axiom is retained through the compatibility window (its QA deliberately exercises the deprecated surface with the linter silenced for that use only); removal is a later release decision.
- **`spectral_persistence` removal (2026-08-20, operator-directed):** the compatibility window closed; the axiom and its sole QA consumer (`persistence_zero_perturbation_QA`) were deleted from `Dynamics.lean`/`Dynamics_QA.lean`. Not a proof-based retirement (no theorem replaces it) — an editorial removal on the stated criterion that it is not well-established published math the way the repository's other axioms are, distinct from every prior retirement in this list, which discharged a citation-backed axiom by proof. `TimeVaryingGraph`, `laplacianSequence`, `IsEventDriven`, and `laplacianSequence_symmetric` are untouched — they remain load-bearing for `Derived.EventStream`/`Derived.ProjectorDrift`, which already covered the motivating persistence use without this axiom. Explicit axiom count 12 → 11; QA declarations 917 → 916. `lake env lean` on the three touched modules (`Dynamics.lean`, `Dynamics_QA.lean`, `Derived/ProjectorDrift.lean`, the last for a dangling-reference docstring fix only) — zero errors. `research/archive/` references to the axiom's original name (`spectral_persistence_under_events`) are left as historical record, per `AGENTS.md`'s archive-immutability rule.
- **`lambda2_variational` retirement (2026-08-18):** the Courant–Fischer characterization of the algebraic connectivity was converted from an admitted axiom (symmetry hypotheses only — a materially false shape for negative weights, refuted in QA) into a proved theorem with the load-bearing `hnonneg` hypothesis, reducing the explicit axiom count from 18 to 17. The proof is pure matrix-world hard crust (eigenbasis expansion, Parseval, spectral resolution of `quadForm`, and two sorted-multiset multiplicity pins), consuming no axioms.
- **Azuma statement repair (2026-08-17, same day as the axiom's introduction):** preparing the derivation exposed that `matrix_azuma_hoeffding` lacked the summand-count factor `m` in the exponent denominator, which made the statement false for `m ≥ 2` (Rademacher-sum counterexample). The denominator is now `8 m R²`, the uniform-bound specialization of Tropp's variance statistic `σ² = ‖∑ Yₖ²‖ ≤ m R²`. The axiom had no downstream consumers before the repair.

## Active priorities

1. Confirm Horn–Johnson section-level and Chung chapter-level locators
   against physical or publisher copies (no local copy available; numbers
   must not be invented).
2. Specify the x90 observable against the completed persistence chain
   (application ring; the inner chain is now credible end-to-end).
3. Add QA based on risk and composability rather than targeting a cosmetic one-to-one ratio.

## Historical reports

Earlier scoreboards and build reports are preserved under [`research/archive/status/`](../research/archive/status/). They are historical snapshots and may contradict current generated metrics.
