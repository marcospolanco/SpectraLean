# QA Scoreboard and Repository Health

**Status:** Canonical verification record  
**Last reviewed:** August 19, 2026

This document separates source-derived counts from commands that have actually been run. A QA declaration is counted syntactically; it is considered verified only when its module check passes. QA proves selected consequences relative to Scaffold’s axioms and does not prove those axioms.

## Source metrics

Run `python3 scripts/generate_qa_scoreboard.py` after changing Lean source.

<!-- BEGIN GENERATED SOURCE METRICS -->
_Generated from Lean source on 2026-08-19._

| Metric | Count |
| --- | ---: |
| QA theorem/lemma declarations | 666 |
| `sorry`/`admit` tokens in QA code | 0 |
| Explicit axioms in `Scaffold/Mathlib` | 13 |
| `sorry`/`admit` tokens in `Scaffold/Mathlib` code | 0 |

### QA declarations by domain

| Domain | Declarations |
| --- | ---: |
| Concentration | 12 |
| Core | 12 |
| Derived | 11 |
| Perturbation | 5 |
| SpectralGraph | 626 |

### QA files

| File | Declarations | Placeholder tokens |
| --- | ---: | ---: |
| `Scaffold/QA/Concentration/Matrix_QA.lean` | 6 | 0 |
| `Scaffold/QA/Concentration/Scalar_QA.lean` | 6 | 0 |
| `Scaffold/QA/Core/MatrixUpdates_QA.lean` | 12 | 0 |
| `Scaffold/QA/Derived/EventStream_QA.lean` | 7 | 0 |
| `Scaffold/QA/Derived/ProjectorDrift_QA.lean` | 4 | 0 |
| `Scaffold/QA/Perturbation/DavisKahan_QA.lean` | 1 | 0 |
| `Scaffold/QA/Perturbation/Weyl_QA.lean` | 4 | 0 |
| `Scaffold/QA/SpectralGraph/BasicProperties_QA.lean` | 5 | 0 |
| `Scaffold/QA/SpectralGraph/Basic_QA.lean` | 14 | 0 |
| `Scaffold/QA/SpectralGraph/Cheeger_QA.lean` | 33 | 0 |
| `Scaffold/QA/SpectralGraph/Connectivity_QA.lean` | 15 | 0 |
| `Scaffold/QA/SpectralGraph/CourantFischer_QA.lean` | 33 | 0 |
| `Scaffold/QA/SpectralGraph/Cuts_QA.lean` | 11 | 0 |
| `Scaffold/QA/SpectralGraph/Dynamics_QA.lean` | 6 | 0 |
| `Scaffold/QA/SpectralGraph/EffectiveResistance_QA.lean` | 26 | 0 |
| `Scaffold/QA/SpectralGraph/ElectricalFlow_QA.lean` | 88 | 0 |
| `Scaffold/QA/SpectralGraph/Exhaustive_QA.lean` | 87 | 0 |
| `Scaffold/QA/SpectralGraph/Expander_QA.lean` | 41 | 0 |
| `Scaffold/QA/SpectralGraph/Fiedler_QA.lean` | 57 | 0 |
| `Scaffold/QA/SpectralGraph/Foster_QA.lean` | 53 | 0 |
| `Scaffold/QA/SpectralGraph/Interlacing_QA.lean` | 9 | 0 |
| `Scaffold/QA/SpectralGraph/KernelBridge_QA.lean` | 14 | 0 |
| `Scaffold/QA/SpectralGraph/Normalized_QA.lean` | 14 | 0 |
| `Scaffold/QA/SpectralGraph/PotentialSolvability_QA.lean` | 12 | 0 |
| `Scaffold/QA/SpectralGraph/Projector_QA.lean` | 5 | 0 |
| `Scaffold/QA/SpectralGraph/RandomWalk_QA.lean` | 7 | 0 |
| `Scaffold/QA/SpectralGraph/SimpleGraphAdapter_QA.lean` | 38 | 0 |
| `Scaffold/QA/SpectralGraph/Stationary_QA.lean` | 12 | 0 |
| `Scaffold/QA/SpectralGraph/VariationalTransfer_QA.lean` | 15 | 0 |
| `Scaffold/QA/SpectralGraph/Variational_QA.lean` | 31 | 0 |
<!-- END GENERATED SOURCE METRICS -->

## Verification record

| Check | Result | Date | Scope and limitation |
| --- | --- | --- | --- |
| `lake build` | Pass | 2026-08-19 | Default target is the library root `Scaffold.lean`; its umbrella certifies `Core.{RandomVariable,Norms,MatrixUpdates}`, `GraphTheory.{Spectral,SimpleGraphAdapter,Electrical,ElectricalFlow,Foster,Expander,Cheeger,Fiedler,RandomWalk,Normalized,Stationary,VariationalTransfer,Dynamics}`, `Analysis.OperatorTheory.Perturbation.{Weyl,DavisKahan}`, `Probability.Concentration.{Scalar.*,Matrix.*}`, and the derived layer `Derived.{EventStream,ProjectorDrift}`. |
| Direct QA module targets | Pass | 2026-08-19 | All thirty QA modules compiled individually (`lake env lean` batch, zero errors — the only diagnostics are the documented pre-existing linter notes in untouched modules; full `lake build` ✔) with no `sorry`/`admit`, including `SpectralGraph/Expander_QA.lean` (22 declarations, proposal `decidable-spectral-certificates.md` step 1: cut weights, centered indicators, and centered cross terms all computed from the raw definitions on a fresh `C₄` fixture — adjacent cut `1`, opposite cut `0`, half-and-half cut `2`, total weight `8`; the matrix form `indicatorVec S ⬝ᵥ (A *ᵥ indicatorVec T)` computed raw and cross-checked against the theorem; centered indicators pinned entrywise (`±1/2` half-set, `3/4/−1/4` singleton) with orthogonality to `onesVec` computed from the pinned entries; the `d`-regular decomposition instantiated on the adjacent cut (`1 = 2·1·1/4 + 1/2`) and the opposite cut (`0 = 2·1·1/4 − 1/2`), each cross term computed independently; and three negative witnesses — the main-term-only statement refuted on the opposite cut (`0 ≠ 1/2`), the main term's degree pinned by regularity (wrong `d = 3` refuted, `1 ≠ 3/4`), and cut-weight symmetry genuinely failing on the asymmetric weight `!![0,2;1,0]` (`2 ≠ 1`)); including `SpectralGraph/Foster_QA.lean` (53 declarations, proposal `spectral-graph-sparsification.md` Phase A: Foster's theorem instantiated on the cliques `K₃`/`K₄`, the path `P₃`, and the 3-leaf star — every resistance pinned by an explicit potential witness computed from the raw definitions (`K₄` via the general-pair potential `(e i − e j)/4` proved to solve the unit demand for *every* pair, so all twelve ordered terms are pinned by one lemma), each ordered Foster sum computed independently of the theorem (`4`, `6`, `4`, `6` respectively) and cross-checked against the theorem's `card V − 1` (`2`, `3`, `2`, `3`); the **double-counting factor** witnessed on both cliques exactly as the proposal's calibration demands — the ordered sum is `4 ≠ 2` on `K₃` and `6 ≠ 3` on `K₄`, so the theorem's `/ 2` is load-bearing; non-edge pairs (`P₃`'s `(0,2)`, the star's leaf pairs) provably do not contribute (zero conductance); leverage scores computed — the `K₃` edge carries `1/3` of the budget and the ordered total is exactly `2` (`fosterTri_leverage_sum_QA`), the `K₄` edge `1/6`); including `SpectralGraph/ElectricalFlow_QA.lean` (88 declarations, proposal `electrical-flow-routing.md` steps 1–5, the program complete: the current matrix, divergences, unit-flow instantiations, and flow energies all computed from the raw definitions on `K₂` and the 3-path — internal-vertex divergence `0`; energies `1`/`2` cross-checked against independently computed Dirichlet energies and pinned resistance values — plus four step-1–2 negative witnesses: the asymmetric network `!![0,2;1,0]` where the current provably fails antisymmetry (`2 ≠ 1`); the edgeless-network phantom excluded by `IsFlowOn`'s support conjunct, its energy pinned to `0`; the double-counting guard — the raw ordered-pair sum computes to `2 ≠ 1` = the Dirichlet energy, so `flowEnergy`'s `1/2` factor is mandatory; and the zero-energy competitor on the real-edge-plus-isolated-vertex network, an antisymmetric unit-divergence flow with energy `0 < 1` = the resistance it would compete against, excluded by the support conjunct alone — exactly the witness that keeps Thomson's principle (step 3) true; the step-3 variational witnesses on the triangle `K₃`: attainment at the split current (`R = 2/3` = the current's energy, both computed from the raw definitions with the current genuinely splitting `2/3`/`1/3` across the two routes), a strict competitor (the detour unit flow around the two-edge path: valid `IsUnitFlow` with all conjuncts computed, energy `2`, Thomson instantiated as `2/3 < 2`), and the superposition decomposition `2 = 2/3 + 4/3` with all three energies computed independently of the lemma; and the step-4 Rayleigh witnesses: the proposal's QA item 2 capacity increase — conductance `1 → 2` on the unit edge certifies the resistance decrease `1 → 1/2` (`edge2_resistance_value_QA`, pinned by an independent potential witness), Rayleigh monotonicity instantiated (`rayleigh_edge_QA`) with the decrease certified strict (`1/2 < 1`) and the **orientation guard** refuting the reverse inequality numerically (`1 ≤ 1/2` false — weights are conductances, a resistance-direction statement would be false here); the competitor transfer (`edge_current_isUnitFlow_on_edge2_QA` — the `edgeAdj`-electrical current is a unit flow on the doubled network, flow-space growth + Kirchhoff) with its cross-network energy computed to `1/2 ≤ 1` from the raw definitions; and a partial increase on the triangle — doubling one edge's conductance strictly decreases the resistance `2/3 → 2/5` (`triDoubled_resistance_value_QA` pinned by the independent potential `![2/5, 0, 1/5]`, `rayleigh_tri_QA`, `rayleigh_tri_strict_QA`); and the step-5 ICP proof example on the Mathlib path graph — `pathGraph3.toWAdj` bridged entrywise to the `connPathAdj` fixture (`path3_toWAdj_eq_connPathAdj`), the reinforcement `increaseConductance … 0 1 1` computed to exactly the concrete doubled-path matrix (`path3_reinforced_eq_pathDoubled_QA`), the reinforced resistance pinned at `3/2` from the independent potential witness `![1/2, 0, −1]` (`pathDoubled_resistance_value_QA`, with connectivity proved by explicit walks, independent of the new adapter), the one-hypothesis headline instantiated (`path3_reinforcement_QA`), and the decrease certified strict `3/2 < 2` against the adapter-bridged pinned original value (`path3_reinforcement_strict_QA`). Earlier records: the `Core/MatrixUpdates_QA.lean` Woodbury/Sherman–Morrison witnesses and the 2026-08-18 `Cheeger_QA` easy-direction witnesses (`cutTestVector_edge_QA` pinning the test vector to `![1,-1]`; its Rayleigh value computing to `2` on `K₂` — exactly the pinned `λ₂(L_sym)`; `cheeger_upper_bound_edge_eq_QA` attaining `λ₂ = 2φ`; and on `C₄` `cutTestVector_cycle_rayleigh_QA` computing `1` and `cheeger_upper_bound_cycle_le_QA` instantiating `λ₂ ≤ 1` through the exhaustively computed conductance `1/2`). |
| Direct public module targets | Pass | 2026-08-19 | All public modules compile individually; the modules changed in the 2026-08-19 decidable-certificates Step-2 slice (`GraphTheory.Spectral` — the generic additions `eigvalOf_le_evals_last` (the sorted spectrum's last entry dominates every eigenbasis eigenvalue), the top Rayleigh domination in multiplication form `quadForm_le_evals_last` (`xᵀ M x ≤ λ_max • (x ⬝ᵥ x)` for every symmetric matrix, unconditionally in `x`), and the formerly-private positivity idiom `dotProduct_self_pos` made public; `GraphTheory.Expander` — the Step-2 bridge layer: `quadForm_add`, `quadForm_add_sub_eq` (polarization), `quadForm_degreeMatrix`, the `d`-regular identity `quadForm_add_quadForm_laplacian` (`xᵀAx + xᵀLx = d • ‖x‖²`, regularity only, no looplessness), the variational lower bound in multiplication form `lambda2_mul_dotProduct_le_quadForm`, the operator bound `abs_quadForm_le_of_ortho_onesVec` (`|xᵀAx| ≤ μ ‖x‖²` on `1⊥`), the sharp bilinear bound `quadForm_bilinear_sq_le_of_ortho_onesVec`, the variance identity `dotProduct_centeredIndicator_self`, and the headline `expander_mixing_lemma` at the Step 0 restated `√` signature minus the found-unnecessary `hloop` hypothesis; zero new axioms — `#print axioms` on all nine public theorems confirms only `propext, Classical.choice, Quot.sound`) were re-elaborated directly (`lake env lean` zero errors, zero warnings on both), rebuilt explicitly, and the QA consumer `SpectralGraph.Expander_QA` re-elaborated (zero errors, zero warnings). Earlier records: the module added in the 2026-08-19 decidable-certificates slice (`GraphTheory.Expander` — proposal `decidable-spectral-certificates.md` steps 0+1: `edgeWeight` with its degenerate-cut and degree-sum interfaces, the hypothesis-free matrix form `edgeWeight_eq_dotProduct`, `edgeWeight_symm` (sum swap), `indicatorVec`/`centeredIndicator` with `sum_centeredIndicator_eq_zero` and `centeredIndicator_dotProduct_onesVec` (both unconditional — the empty-type case is handled, no `Nonempty` hypothesis carried), the indicator decomposition `indicatorVec_eq_smul_onesVec_add_centeredIndicator`, `mulVec_onesVec_eq_const`, and the headline `edgeWeight_eq_regular_add_centered` (the `d`-regular main-term split with the centered cross term, symmetry load-bearing through `Matrix.dotProduct_mulVec`'s transpose); zero new axioms — `#print axioms` on all seven public theorems confirms only `propext, Classical.choice, Quot.sound`) was re-elaborated directly (`lake env lean` zero errors, zero warnings), rebuilt explicitly, and its QA consumer `SpectralGraph.Expander_QA` re-elaborated (zero errors, zero warnings). Earlier records: the module added in the 2026-08-19 Foster slice (`GraphTheory.Foster` — the kernel-index counting lemma `card_filter_eigvalOf_laplacian_eq_zero`, the spectral-sum lemma `effectiveResistance_eq_sum_eigbasis`, the headline `foster_theorem`, `leverageScore`, and `sum_leverageScore_eq_two`; proposal `spectral-graph-sparsification.md` Phase A only, zero new axioms — `#print axioms` confirms only `propext, Classical.choice, Quot.sound`) was re-elaborated directly (`lake env lean` zero errors, zero warnings), rebuilt explicitly, and its QA consumer `SpectralGraph.Foster_QA` re-elaborated (zero errors, zero warnings). Earlier records: the module added in the 2026-08-19 electrical-flow slice (`GraphTheory.ElectricalFlow` — `EdgeFlow`, `electricalCurrent`, `flowDivergence`, `IsFlowOn`, `IsUnitFlow`, `electricalCurrent_antisymm`, `electricalCurrent_eq_zero_of_weight_eq_zero`, `flowDivergence_electricalCurrent`, `isFlowOn_electricalCurrent`, `isUnitFlow_electricalCurrent`, extended the same day with step 2: `flowEnergy` (explicit zero branch, mandatory `1/2` ordered-pair factor), `flowEnergy_electricalCurrent` (energy agreement with the Dirichlet form, symmetry-only), `flowEnergy_nonneg`, and `flowEnergy_electricalCurrent_eq_effectiveResistance`; extended again the same day with step 3: `flowDivergence_sub`, `isFlowOn_sub` (flow-space linearity), `flowEnergy_add_of_flowDivergence_eq_zero` (the divergence-free superposition lemma — discrete integration by parts, stated with no hypotheses on `A`), and the headline `effectiveResistance_le_flowEnergy` (Thomson's principle); extended again the same day with step 4: `isFlowOn_of_le` (flow-space growth — the competitor-transfer interface), `flowEnergy_le_of_le` (raising conductances lowers dissipated energy, termwise, support load-bearing on the `A i j = 0 < B i j` branch), and the headline `effectiveResistance_le_of_le` (Rayleigh monotonicity in conductance form, `A ≤ B ⇒ R_B ≤ R_A`); extended again the same day with step 5, completing the program: `increaseConductance` (raise one undirected pair's conductance by `δ`, both ordered entries together) with its entry/symmetry/nonnegativity/monotonicity interfaces (`increaseConductance_apply_of_reinforced`, `increaseConductance_apply_of_not_reinforced`, `le_increaseConductance`, `increaseConductance_isSymm`, `increaseConductance_nonneg`), the connectivity adapters `supportGraph_le_of_le` (support graphs grow with entrywise domination — no nonnegativity hypothesis needed) and `supportGraph_connected_of_le` (capacity growth preserves connectivity, via Mathlib's `SimpleGraph.Connected.mono`), and the one-hypothesis headline `effectiveResistance_le_increaseConductance` (`R (increaseConductance A i j δ) u v ≤ R A u v` with only the original network's connectivity hypothesized); proposal `electrical-flow-routing.md` steps 0–5, complete) was re-elaborated directly (`lake env lean` zero errors, zero warnings — the instance-free lemmas silenced with `omit` clauses) and rebuilt explicitly, together with its QA consumer `SpectralGraph.ElectricalFlow_QA` (zero errors, zero warnings). Earlier records: the module changed in the 2026-08-18 Woodbury repair (`Core.MatrixUpdates` — `woodbury_identity` restated at the standard middle factor `C⁻¹ + V A⁻¹ U` with the three `IsUnit` determinant hypotheses and proved from Mathlib's `Matrix.invOf_add_mul_mul` plus the `NonsingularInverse` bridges; `sherman_morrison` unchanged at that milestone) was re-elaborated directly (`lake env lean` zero errors, zero warnings) and rebuilt via `lake build Scaffold.Mathlib.Core.MatrixUpdates` alongside the umbrella build. The same module, changed again in the 2026-08-18 Sherman–Morrison retirement (`sherman_morrison` converted from axiom to proved theorem at unchanged name/hypotheses), was re-elaborated directly (`lake env lean` zero errors, zero warnings), together with its QA consumer `Core.MatrixUpdates_QA` (zero errors, zero warnings), and both were rebuilt explicitly. The module changed in the 2026-08-18 interlacing retirement (`GraphTheory.Spectral` — the padding bridge `padVec`/`padVecLinear` with its preservation lemmas, the subspace bookkeeping helpers, and `eigen_interlacing_principal_submatrix` converted from axiom to proved theorem) was re-elaborated directly (`lake env lean` zero errors; the only new-code notes were silenced with `omit` clauses, leaving the documented pre-existing notes in untouched code untouched), together with its QA consumer `SpectralGraph.Interlacing_QA` (zero errors; +4 public declarations), and both were rebuilt explicitly. The modules changed in the 2026-08-18 Cheeger-retirement slice (`GraphTheory.Spectral` — the general-operator `secondEval_variational`, its consumer form `secondEval_le_rayleigh`, the generic `eigvecOf_ortho_onesVec_of_mulVec_eq_zero`, with `lambda2_variational` re-proved as a corollary at unchanged shape; `GraphTheory.Cheeger` — `cheeger_upper_bound` retired from axiom to proved theorem with the `cutTestVector` machinery) were likewise re-elaborated directly and rebuilt. |
| Mathlib cache provenance | Note | 2026-08-19 | The local `lake exe cache` binary crashes under the current macOS dyld (`__DATA_CONST segment missing SG_READ_ONLY flag`); the cache was fetched by running the same Cache tool logic interpreted via `lake env lean --run`, unpacking 5685 Mathlib oleans. Re-applied on 2026-08-18 after the local `.lake/build` state was found pruned (Mathlib oleans down to 1795); the interpreted fetch restored the full set, after which `lake build` recompiled the residue (2008 targets) and passed. Re-applied again on 2026-08-19 (pruned state found at run start; fetch restored the full set; the affected module builds then elapsed normally). |
| `scripts/lint_axioms.py` | Pass | 2026-08-19 | All 13 explicit axioms are covered by `index/sources/` and `index/map/` (`lambda2_variational` retired 2026-08-18; `cheeger_upper_bound` retired from axiom to proved theorem on 2026-08-18 — the Cheeger easy direction, proved from `secondEval_variational`; `woodbury_identity` retired from a verified-false axiom to a proved theorem on 2026-08-18; `sherman_morrison` retired from axiom to a proved theorem on 2026-08-18 — the `k = Fin 1` specialization of the proved Woodbury identity, a pure proof task at unchanged name/hypotheses; `eigen_interlacing_principal_submatrix` retired from axiom to a proved theorem on 2026-08-18 — Cauchy interlacing proved from the locally proved Courant–Fischer engine, a pure proof task at unchanged statement; the remaining Cheeger axiom is the hard direction only). |
| `scripts/check_citations.py` | Pass | 2026-08-19 | Every axiom carries a `Source:` citation in its doc comment. |
| `scripts/check_markdown_links.py` | Pass | 2026-08-19 | No broken repository-relative targets in active docs; excludes the historical archive, the dependency checkout, and the local `.opencode/` tooling directory (including its vendored `node_modules`). |

## Interpretation

- **Explicit axiom:** an intentional trust boundary declared with Lean's `axiom` command. QA lemmas and derived theorems using these axioms are conditional on them.
- **Admitted proof:** a `sorry` or `admit` accepted by Lean; this is different from an explicit axiom and remains technical debt in public modules. As of this scoreboard there are no `sorry`/`admit` tokens anywhere under `Scaffold/`.
- **QA declaration:** a theorem or lemma under `Scaffold/QA`; all 666 declarations have compiled against the current public API.
- **Default build:** the umbrella reaches every public module listed above; no public module is excluded from `lake build`.
- **Decidable spectral certificates, step 2 — the Expander Mixing Lemma (2026-08-19):** `proposals/decidable-spectral-certificates.md` (High) at its named next step, delivered as pure hard crust in `GraphTheory.Expander` plus two generic additions to `GraphTheory.Spectral` — **zero new axioms** (13 unchanged). **The theorem:** for a symmetric, nonnegative, `d`-regular network with Laplacian-spectrum hypothesis `μ ≥ max |d − λ₂(L)| |d − λ_max(L)|`, the `(S,T)` cut weight deviates from the population main term `d•|S|•|T|/|V|` by at most `μ • √(|S||T|(|V|−|S|)(|V|−|T|))/|V|` — the classical discrepancy bridge between algebraic spectral gaps and combinatorial pseudorandomness, at the Step 0 restated `√` signature. **The route is cheaper and stronger than the sketched eigenspace expansion:** a Rayleigh sandwich on `1⊥` — the `d`-regular identity `A + L = D` in quadratic-form form trading the proved lower bound `λ₂•‖x‖² ≤ xᵀLx` (the center's `secondEval_le_rayleigh` multiplied out) against the new generic top bound `xᵀLx ≤ λ_max•‖x‖²` (`quadForm_le_evals_last`, supported by `eigvalOf_le_evals_last`) — giving the operator bound `|xᵀAx| ≤ μ‖x‖²` on `1⊥`, then the sharp bilinear form `(x ⬝ᵥ (A *ᵥ y))² ≤ μ²‖x‖²‖y‖²` by the scaling trick (`√Y•x ± √X•y` through the polarization identity; the choice `a² = Y, b² = X` attains the AM–GM equality — no slack), then the variance identity `‖centeredIndicator S‖² = |S|(|V|−|S|)/|V|` evaluating the geometric factor. No eigenspace split of `A`, no kernel case analysis, no affine spectrum transfer. **Statement-shape strengthening recorded:** the Step 0 sketch's `hloop` hypothesis is dropped — the `A + L = D` identity needs regularity only. QA (+19 declarations, 41 in file): the spectral hypothesis **derived, not assumed** (`μ = 2` proved on `C₄` from a test vector, a direct sum-of-squares estimate routed to `λ_max`, and PSD lower bounds), the lemma instantiated on two cuts, and the bound **attained exactly** on the opposite cut (both sides independently compute to `2`) and on the alternating vector in the operator bound (`8 = 2•4`) — the derived `μ` is tight and the theorem not vacuous.
- **Decidable spectral certificates, steps 0 and 1 (2026-08-19):** `proposals/decidable-spectral-certificates.md` (High) opened at its gate and through its first build step. **Step 0 (recorded in the proposal before any Lean):** (1) the adjacency-vs-Laplacian convention resolved **Laplacian-first through the `d`-regular bridge** — survey evidence in the proposal (no adjacency-eigenvalue interface exists; the certificate half is already Laplacian; the generic symmetric engine applies to adjacency for free, so the bridge needs no new interface), with the restated `expander_mixing_lemma` signature pinned in Laplacian terms; (2) the ℚ-`decide` spike returned a decisive negative — plain kernel `decide` **cannot** verify the proposal's ℚ-arithmetic certificate, a reducibility wall rather than a performance limit (`((2:ℚ) + 2) = 4` is already stuck: ℚ literals are opaque to elaborator reduction, so `Rat.add`/`Rat.div`/`Rat.blt` never unfold; `#eval` works but is not kernel-checked), with the adopted fallback recorded — the **integer cross-multiplied twin** over ℤ (`rawNumer ≤ 2 * bound * denom`), kernel-decided clean on `C₄` (`Fin 4`, attained `λ₂ = 2`) and `C₆` (`Fin 6`, attained `λ₂ = 1`), accept and reject paths, including the fractional-bound clearing form; (3) the Ramanujan QA scope resolved to the existing `Kₙ`/`Cₙ` fixture family (both are small Ramanujan instances) with the drop-if-awkward fallback. **Step 1 (hard crust, zero new axioms):** `GraphTheory.Expander` — `edgeWeight` with the hypothesis-free matrix form, `edgeWeight_symm`, the unconditional centered-indicator orthogonality (empty-type case included), and the headline `edgeWeight_eq_regular_add_centered` (`e(S,T) = d·|S|·|T|/n + centered cross term`), load-bearing on `deg`'s row-sum shape and the `dotProduct_mulVec` transpose. QA (22 declarations): values computed from the raw definitions on `C₄`, the decomposition instantiated on both an adjacent and an opposite cut with independently computed cross terms, and three negative witnesses (main-term-only refuted `0 ≠ 1/2`; wrong degree refuted `1 ≠ 3/4`; asymmetric weight breaks cut symmetry `2 ≠ 1`).
- **Foster's theorem, Phase A (2026-08-19):** `proposals/spectral-graph-sparsification.md` Phase A (High) delivered as pure hard crust in the new `GraphTheory.Foster` — **zero new axioms** (13 unchanged; `#print axioms foster_theorem` reads only `propext, Classical.choice, Quot.sound`). `foster_theorem`: on every connected symmetric-nonnegative network, `(∑ i, ∑ j, A i j * effectiveResistance A i j) / 2 = card V − 1` — the unordered-pair form of Foster's `∑_{u<v} w_e R_e = n − 1`, with the statement proved at full strength (no cardinality hypothesis; `card V = 1` degenerates to `0 = 0`). Route exactly as the proposal scoped (the pseudoinverse-free eigenbasis route reopening the item `electrical-structure-crust.md` had removed for needing `L⁺`): the per-pair spectral sum `effectiveResistance_eq_sum_eigbasis` (`R u v = ∑_k (v_k u − v_k v)²/λ_k` over nonzero eigenvalues — energy identity + `quadForm_eigvalOf` + self-adjointness), the double-sum swap with per-eigenvector Dirichlet evaluation `∑_{i,j} A i j (v_k i − v_k j)² = 2 λ_k` (via `laplacian_quadForm` and `quadForm_eigvecOf_self`), and the count `#{k : λ_k ≠ 0} = n − 1` from `card_filter_eigvalOf_laplacian_eq_zero` (orthonormality vs. the one-dimensional kernel `laplacian_kernel_eq_span_onesVec`; at least one kernel index because `onesVec` is a nonzero kernel vector). Load-bearing on the whole center chain — an error in solvability, the kernel characterization, the spectral resolution, or eigenbasis orthonormality breaks the proof. Supporting objects: `leverageScore` (the pair's share of the Foster budget — the importance-sampling object of the blocked Phase B; junk below `2 ≤ card V` documented) and `sum_leverageScore_eq_two` (the ordered scores sum to exactly `2`; unordered `1`, the probability-distribution statement). QA `SpectralGraph/Foster_QA.lean` (53 declarations): the cliques `K₃`/`K₄`, the path `P₃`, and the 3-leaf star; every resistance pinned by an explicit potential witness (`K₄` through the general-pair potential `(e i − e j)/4`, proved to solve the unit demand for every pair via the entrywise `L = 4I − J` structure); each ordered sum computed independently of the theorem and cross-checked against `card V − 1`; the **double-counting factor** witnessed on both cliques (`4 ≠ 2`, `6 ≠ 3` — a statement shape dropping the `/ 2` would be refuted); non-edge pairs provably absent from the sum; leverage computed (`K₃` edge `1/3`, total `2`; `K₄` edge `1/6`). Phase B (the sparsification guarantee) remains blocked in the proposal on the matrix-Chernoff scope decision and is not developed.
- **Electrical-flow step 5: the ICP capacity-reinforcement example (2026-08-19) — program complete:** `effectiveResistance_le_increaseConductance` — on a connected symmetric nonnegative network, raising any single undirected pair's conductance by `δ ≥ 0` cannot increase the effective resistance between any two vertices. The one-hypothesis packaging of step 4's monotonicity (`increaseConductance A i j δ` supplies the comparison entrywise, `0 ≤ δ` making it a domination); the reinforced network's connectivity is *derived*, not hypothesized, by the new adapter `supportGraph_connected_of_le` (support graphs grow along entrywise domination — `supportGraph_le_of_le`, which needs no nonnegativity hypothesis since `0 < A i j ≤ B i j` — and Mathlib's `SimpleGraph.Connected.mono` lifts connectedness along the containment). Zero new axioms (13 unchanged); with this step all six build steps and the proposal's acceptance criteria are delivered. QA (+9 declarations, 88 in file): the release-facing example on the Mathlib `Fin 3` path graph — the adapter weights bridged entrywise to the weighted-fixture world, the reinforcement computed to the concrete doubled-path matrix, the reinforced resistance `3/2` pinned by an independent potential witness with connectivity from explicit walks (independent of the adapter the theorem consumes), the headline instantiated in the one-hypothesis form, and the decrease certified strict `3/2 < 2`.
- **Electrical-flow step 4: Rayleigh monotonicity in conductance form (2026-08-19):** `effectiveResistance_le_of_le` — on connected graphs with symmetric nonnegative weights, an entrywise conductance increase `A ≤ B` cannot increase effective resistance, `effectiveResistance B u v ≤ effectiveResistance A u v`; the ICP-facing fact that adding network capacity cannot worsen the certified energy cost of electrical routing. Proved exactly by the proposed route (zero new axioms; count stays 13), through two new reusable interfaces: `isFlowOn_of_le` (flow-space growth — a flow supported on `A` is a flow on every entrywise larger `B ≥ A ≥ 0`; the load-bearing step is support, where a zero entry of `B` above a nonnegative `A` squeezes the `A` entry to zero) and `flowEnergy_le_of_le` (raising conductances lowers dissipated energy — termwise `θ² / B ≤ θ² / A` on nonzero branches since every denominator increased; support is again load-bearing on the `A i j = 0 < B i j` branch, where the `B`-term would otherwise exceed the zeroed `A`-branch). The headline composes them with the delivered chain: the `A`-unit-demand potential's current is a unit flow *on `B`* (growth + Kirchhoff bridge), Thomson's principle (step 3) on `B` bounds `R_B` by its `B`-energy, the comparison bounds that by its `A`-energy, and the step-2 identity evaluates it as `R_A` — load-bearing on solvability, the Kirchhoff bridge, support, Thomson, and the energy identity. Connectivity of both graphs is hypothesized (the proposal's optional adapter deriving `B`'s from `A`'s is deliberately not attempted). QA (+20 declarations, 79 in file): the proposal's QA item 2 — conductance `1 → 2` on the unit edge, resistance certified to decrease `1 → 1/2` from an independent potential witness, monotonicity instantiated, the decrease certified strict, and the **orientation guard** (the reverse inequality `1 ≤ 1/2` refuted numerically — weights are conductances; the proposal names a reversed orientation as a statement bug, and this witness would catch it); the competitor transfer instantiated on computed objects (the `edgeAdj`-current is a unit flow on `edge2Adj`; its cross-network energy `1/2 ≤ 1` computed from the raw definitions); and a partial increase on the triangle — one edge's conductance doubled, resistance `2/3 → 2/5` strict, the new value pinned by the independent potential `![2/5, 0, 1/5]`. Step 5 (the ICP example) remains.
- **Electrical-flow step 3: Thomson's principle (2026-08-19):** `effectiveResistance_le_flowEnergy` — on a connected graph with symmetric nonnegative conductances, every valid unit flow `θ` from `u` to `v` dissipates at least the effective resistance, `effectiveResistance A u v ≤ flowEnergy A θ`; the electrical current is the energy minimizer. Proved exactly by the proposed route (zero new axioms; count stays 13): the unit-demand potential's current `ι` is a unit flow (step 1), the difference `d = θ − ι` is a flow with zero divergence (`isFlowOn_sub`, `flowDivergence_sub`, the Kirchhoff bridge, and the demand equation — an error in any breaks the proof), the superposition lemma `flowEnergy_add_of_flowDivergence_eq_zero` adds `d`'s energy to `ι`'s (the cross term is `∑ i j, (f i − f j) * d i j` after Ohm's law; its row sums vanish by zero divergence and its column sums are the negated row sums by antisymmetry), `flowEnergy_nonneg` discards it, and the step-2 identity evaluates `ι`'s energy as the resistance. **Statement shape:** the superposition lemma is stated with **no hypotheses on `A`** — not even the symmetry the proposal's sketch assumed — only the perturbation's flow properties enter. No `sInf` packaging, per the proposal's instruction. QA (+18 declarations, 59 in file): attainment at the edge (`1 = 1`) and at the **split current** on the triangle `K₃` (`R = 2/3` = the current's energy, computed from the raw definitions, the current genuinely splitting `2/3`/`1/3` across two routes); a **strict competitor** — the detour unit flow around the two-edge path, a valid `IsUnitFlow` with all conjuncts computed, energy `2`, Thomson instantiated as `2/3 < 2` (the inequality is not vacuous on a network with competing routes); and the superposition decomposition composed as `2 = 2/3 + 4/3` with all three energies computed independently of the lemma. Steps 4–5 (Rayleigh monotonicity, the ICP example) remain.
- **Electrical-flow step 2: flow energy and the energy agreement (2026-08-19):** `flowEnergy` delivered exactly as the proposal pinned it — the explicit zero branch (`if A i j = 0 then 0 else θ i j ^ 2 / A i j`), summed over ordered pairs, halved for double counting — with three proved theorems, zero new axioms (13 unchanged): `flowEnergy_electricalCurrent` (`flowEnergy A (electricalCurrent A f) = quadForm (laplacian A) f`; termwise Ohm's-law algebra `(c x)²/c = c x²` on the nonzero branch, zero-on-zero on the branch, then `laplacian_quadForm` — statement shape landed **symmetry-only**, nonnegativity is not needed for the agreement, and QA witnesses both sides of that claim), `flowEnergy_nonneg` (squares over positive conductances — the base order fact steps 3–4 compare flows by; nonnegativity load-bearing, QA exhibits energy `-1 < 0` on a symmetric negative-weight network), and the headline `flowEnergy_electricalCurrent_eq_effectiveResistance` (on a connected graph, a unit-demand current dissipates exactly the resistance it routes — agreement composed with the crust's solution-level energy identity). QA (+25 declarations, 41 in file): energies `1`/`2` computed from the raw definitions on `K₂`/`P₃` and cross-checked against independently computed Dirichlet energies and pinned resistances (three routes meeting at each value); the **mandatory double-counting fixture** — raw ordered-pair sum `2 ≠ 1` = Dirichlet energy, so dropping the `1/2` factor would numerically break the agreement; the step-1 phantom's deferred energy pinning (`0` on the edgeless network); and the **zero-energy competitor** — on a real edge plus an isolated vertex, an antisymmetric unit-divergence flow routing through zero-conductance pairs dissipates `0 < 1` = the real resistance and is excluded by `IsFlowOn`'s support conjunct alone: over flows-satisfying-everything-except-support, Thomson's principle (step 3) would assert `1 ≤ 0` on this fixture. Steps 3–5 (Thomson, Rayleigh monotonicity, the ICP example) not started.
- **Electrical-flow interface, proposal steps 0–1 (2026-08-19):** `proposals/electrical-flow-routing.md` (High) opened. Step 0 (recorded in the proposal before any Lean): fresh survey of the pinned Mathlib confirms no network-flow/circulation/max-flow API, no graph-native divergence (only box/measure-integral divergence theorems on continua), no flow/energy API on `SimpleGraph.Dart`, and no `Matrix.IsAntiSymm` — the matrix representation `EdgeFlow V = Matrix V V ℝ` was pinned, with the zero-edge support semantics carried by `IsFlowOn`'s second conjunct and the `1/2` ordered-pair factor reserved for `flowEnergy` (step 2); target module the new focused `GraphTheory.ElectricalFlow` (coverage map's electrical row re-surveyed). Step 1 delivered as pure hard crust (axiom count unchanged at 13): `electricalCurrent` (Ohm's law `A i j * (f i − f j)` — weights are conductances), `flowDivergence` (net outflow), the predicates `IsFlowOn`/`IsUnitFlow`, `electricalCurrent_antisymm` (symmetry load-bearing), `electricalCurrent_eq_zero_of_weight_eq_zero` (support), the Kirchhoff bridge `flowDivergence_electricalCurrent` (`flowDivergence (electricalCurrent A f) = laplacian A *ᵥ f` — load-bearing on the center's exact sign convention via `laplacian_mulVec_apply`), and the headline `isUnitFlow_electricalCurrent`: every unit-demand potential induces a valid unit flow, turning the delivered potential-based resistance API into a routing object. QA (16 declarations): `K₂` current matrix `!![0,1;-1,0]` and divergence `![1,-1]` computed from the definitions; the 3-path unit flow with internal-vertex divergence `0` (Kirchhoff away from source/sink); the asymmetric-network negative witness (`2 ≠ 1`: `A.IsSymm` load-bearing); and the edgeless-network phantom — antisymmetric, unit divergence, excluded by the support conjunct (the step-1 form of the proposal's zero-conductance trap; the energy-level refutation is deferred to step 2 with `flowEnergy`). Steps 2–5 (energy agreement, Thomson, Rayleigh monotonicity, the ICP example) are explicitly not started.
- **Sherman–Morrison retired from axiom to proved theorem (2026-08-18):** `proposals/retire-sherman-morrison.md` delivered — explicit axiom count 15 → 14. **A pure proof task, not a correctness repair** (recorded per the proposal's acceptance criteria): the statement was verified first against the corrected Woodbury shape — at `C = [1]` the rank-one middle factor is exactly `1 + v ⬝ᵥ (A⁻¹ *ᵥ u)`, so the former axiom already stated the correct specialization (the Woodbury `C`/`C⁻¹` defect is invisible at rank one). The theorem keeps the axiom's exact name, hypotheses (`hA : IsUnit A.det`, `hv : v ⬝ᵥ (A⁻¹ *ᵥ u) ≠ -1`), and statement — zero consumers existed, so no migration surface. Proof: the `k = Fin 1` specialization of the proved `woodbury_identity` — `u`/`v` packed into `Fin 1` column/row matrices, the 1×1 middle factor shown to have entry `1 + v ⬝ᵥ (A⁻¹ *ᵥ u)` (a `Fin.sum_univ_one` computation through `Matrix.dotProduct_mulVec`), its determinant a unit exactly when `hv` holds (`Matrix.det_fin_one`), its inverse the 1×1 scalar inverse (`Matrix.inv_eq_left_inv`), and the final shape matched by the pin's `Matrix.mul_smul`/`Matrix.smul_mul` algebra. The specialization is load-bearing on the Woodbury repair — a residual defect there would surface in this proof rather than pass beside it. QA (`Core/MatrixUpdates_QA.lean`, +4 declarations): the rank-one positive instance at the existing `A2 = diag 2 2` fixture — `A + u vᵀ = !![3,1;1,3]` — with the left side computed independently by the adjugate formula (`sherman_morrison_sum_inv_QA`) and the theorem-consumed right side pinned to the same `!![3/8,-1/8;-1/8,3/8]` (`sherman_morrison_rhs_QA`, denominator computed to `1` from the definitions); and the negative witness at the excluded denominator — `A = !![1]`, `u = ![2]`, `v = ![-1/2]` gives `v ⬝ᵥ (A⁻¹ *ᵥ u) = -1` exactly (`sherman_morrison_excluded_denominator_QA`) and the update `!![1] + !![-1] = !![0]` is singular there (`sherman_morrison_singular_witness_QA`) — the hypothesis excludes exactly the vacuous-junk point.
- **Cauchy interlacing retired from axiom to proved theorem (2026-08-18):** `eigen_interlacing_principal_submatrix` proved in `GraphTheory.Spectral` at unchanged name/hypotheses/statement from the proved Courant–Fischer engine — its first named consumer (explicit axiom count 14 → 13). **A pure proof task, not a correctness repair**: the admitted statement was the true textbook two-sided window `λᵢ ≤ μᵢ ≤ λᵢ₊ₙ₋ₘ`. Route: the extend-by-zero padding bridge (`padVec`/`padVecLinear`, preserving dot products, quadratic forms, and Rayleigh quotients), the lower bound via CF-existence on the submatrix + competitor on the ambient, the upper bound via CF-existence on the ambient at index `i+d` + the subspace-intersection dimension count (`finrank (U ⊓ range pad) ≥ i+1`, through the same `Submodule.finrank_sup_add_finrank_inf_eq` the min–max proof used) + exact-dimension extraction (`exists_submodule_finrank_eq_of_le`) + comap transfer. The proof consumes both CF witness directions and is load-bearing on them. QA (`Interlacing_QA.lean` +4 public declarations, 484 total): the `K₂` Laplacian spectrum `[0, 2]` and its singleton-submatrix spectrum `[1]` pinned from trace/determinant/sortedness independent of the theorem; the window instantiated to `0 ≤ 1 ≤ 2`; both collapsed one-sided bounds refuted in proved form — superseding the axiom-era note that no thin QA of the inequality existed (computational eigenvalue QA machinery did not exist then; it does since the Cheeger repair).
- **General Courant–Fischer min–max proved (2026-08-18):** `proposals/prove-courant-fischer.md` delivered as pure hard crust in `GraphTheory.Spectral` (new `CourantFischer` section; no new axioms — general min–max was never admitted; count stays 15). The `k`-th sorted eigenvalue of *any* real symmetric matrix is characterized at every index, with only symmetry assumed (no positivity, kernel, or graph structure): `exists_submodule_forall_rayleigh_le` (an optimal `(k+1)`-dimensional subspace exists — `Finset.exists_subset_card_eq` extraction from the below-threshold eigenbasis, dimension by `finrank_span_eq_card` through orthonormality, Rayleigh bound through the proved spectral resolution), `exists_ne_mem_rayleigh_ge_of_finrank_eq` (every `(k+1)`-dimensional competitor contains a test vector with quotient ≥ `evals k` — dimension counting against the tail eigenspace through `Submodule.finrank_sup_add_finrank_inf_eq`), and the packaged infimum equation `evals_min_max`. New public center API: general-`k` multiplicity pins (`card_filter_eigvalOf_lt_evals_le`, `succ_le_card_filter_eigvalOf_le` — the count-form generalizations of the `k = 1` pins behind `lambda2_variational`), component-form Rayleigh bounds, `eigvecOf_dotProduct`, `linearIndependent_eigvecOf_finset`, `finrank_span_eigvecOf_finset`. The proposal's open Mathlib-survey step was resolved first (all needed lemmas present in v4.14.0). QA `SpectralGraph/CourantFischer_QA.lean` (33 declarations): the `!![2,1;1,2]` fixture's spectrum `[1,3]` pinned from trace/determinant/sortedness; both directions instantiated — including the derived universal "every Rayleigh quotient ≤ top eigenvalue" (through the existence direction plus `eq_top_of_finrank_eq`) and the witness pinned to attain `evals 1 = 3` exactly through both directions; negative witnesses: the existence-direction property refuted on the wrong line (`3 ≤ 1`), the dimension hypothesis proved load-bearing (its conclusion false on a one-dimensional subspace at index `1`), and — at the interior index `k = 1 < n − 1` on the three-vertex path Laplacian — a wrong two-dimensional subspace refuted (`4/3 ≤ evals 1` contradicts `evals 1 ≤ 1`, itself derived through the theorem) with the older `secondEval_le_rayleigh` engine cross-checking the same bound. The proposal's four named consumers (interlacing retirement, Rayleigh monotonicity, the Cheeger hard direction, Fiedler Phase B) remain separate follow-ons, not claimed here.
- **Woodbury identity repaired and proved, axiom retired (2026-08-18):** the admitted `woodbury_identity` was independently verified **false** — its middle factor `(C + V A⁻¹ U)⁻¹` with no invertibility hypothesis on `C` fails at the scalar instance `A = 1, U = V = 1, C = 0`, where every stated determinant hypothesis holds while the two sides evaluate to `1` and `0`. It is now a theorem at the same name with the standard middle factor `(C⁻¹ + V A⁻¹ U)⁻¹` and exactly the three explicit hypotheses `IsUnit A.det`, `IsUnit C.det`, `IsUnit (C⁻¹ + V A⁻¹ U).det` (the former `IsUnit (A + U C V).det` hypothesis is derivable and dropped — Mathlib's `Matrix.invertibleAddMulMul` constructs the sum's `Invertible` instance). Proof: pure upstream reuse — `Matrix.invOf_add_mul_mul` plus the `NonsingularInverse` bridges `Matrix.invertibleOfIsUnitDet` and `Matrix.invOf_eq_nonsing_inv`, all already imported transitively; zero new axioms, zero `sorry`. Emergency repair per architecture §9 (materially false statement, zero consumers, so no migration surface; no deprecated compatibility declaration — it must not restate the false identity). Explicit axiom count 16 → 15. QA (`Core/MatrixUpdates_QA.lean`, the first Core-domain QA file, 8 declarations): the retired shape restated at `Fin 1` over `ℚ` and refuted *without consuming any axiom* (`old_woodbury_identity_refuted_QA`), with its middle and sum hypotheses separately proved satisfied at the counterexample (the refutation is of a genuinely applicable statement, not a vacuous shape), the corrected `IsUnit C.det` hypothesis proved to exclude that counterexample, and positive instances at nonzero scalars (`(2 + 3)⁻¹ = 1/5`) and at a non-scalar rank-one update (`diag 2 2` + all-ones rank one = `!![3,1;1,3]`, both sides computing to `!![3/8,-1/8;-1/8,3/8]` through the computed middle factor `(1 + 1)⁻¹ = 1/2`) whose right sides are pinned by consuming the theorem and finishing with independently computed left sides. `sherman_morrison` stayed admitted at that milestone (retired later the same day — see the Sherman–Morrison milestone bullet above).
- **Cheeger easy direction proved, axiom retired (2026-08-18):** `cheeger_upper_bound` (`λ₂(L_sym) ≤ 2φ` for `d`-regular graphs) moved from admitted axiom to proved theorem at unchanged name and hypotheses — explicit axiom count 17 → 16; the remaining Cheeger axiom is the hard direction only. Route (in `GraphTheory.Cheeger`): the proved general-operator Courant–Fischer `secondEval_variational` (new in `GraphTheory.Spectral` — the `lambda2_variational` argument generalized to any symmetric PSD matrix with `onesVec` in its kernel; the Laplacian instance re-proves `lambda2_variational` as a two-line corollary at unchanged shape), consumed through its new one-sided form `secondEval_le_rayleigh` at the volume-centered cut indicator `cutTestVector` (`x i = vol Sᶜ` on `S`, `- vol S` off it): orthogonal to `onesVec` under regularity, Dirichlet energy exactly `boundary · vol V²` (no regularity needed — the general weighted-graph cut identity), norm exactly `vol S · vol Sᶜ · vol V / d`, Rayleigh quotient `boundary · vol V / (vol S · vol Sᶜ) ≤ 2 · conductance S`, then the per-cut bound passes to the infimum. The load-bearing chain: PSD of `L_sym` scales from `laplacian_psd` through `d • L_sym = laplacian A` (regularity), the kernel fact is the row-sum identity under `deg = d`, and the cut identity consumes `vol_compl`/`boundary_compl` (cut duality). QA: the test vector pinned to `![1,-1]` on `K₂` and its Rayleigh value computed to `2` — exactly the independently pinned `λ₂(L_sym)` (bound attained); the theorem itself instantiated on `K₂` as `λ₂ = 2φ` from independently computed values, and on `C₄` as `λ₂ ≤ 1` through the exhaustively computed conductance `1/2` of the adjacent-pair cut, whose test-vector Rayleigh value computes to exactly `1`.
- **Fiedler vector and sign partition, Phase A (2026-08-18):** `proposals/fiedler-partitioning.md` Phase A (A1/A2) delivered as pure hard crust in the new `GraphTheory.Fiedler` (no new axioms; count stays 16): `fiedlerIndex`/`fiedlerVector` (an eigenbasis index carrying `lambda2`, fixed by classical choice through `evals_mem_eigvalOf` — the proposal's sketch indexed `eigvecOf` by a sorted-spectrum position, which is not that function's type; the deviation is recorded in the module docstring), the eigenvector equation `fiedlerVector_eigen`, unit norm, nonvanishing, `fiedlerVector_quadForm` (the Fiedler energy is exactly `lambda2`), and orthogonality to `onesVec` under `0 < lambda2`; the algebraic-connectivity certificate `lambda2_pos_of_connected` (connected + symmetric nonnegative weights ⇒ `0 < lambda2` — PSD bounds the eigenbasis, a nonpositive `lambda2` pins the first two sorted entries to `0`, the lower multiplicity pin yields two distinct kernel basis vectors, and `laplacian_kernel_eq_span_onesVec` (electrical step 2) makes two orthonormal vectors on one line contradictory); and `fiedlerPartition` (the sign half-space) with `fiedlerPartition_nonempty`/`fiedlerPartition_ne_univ` — both flowing through the zero-sum identity, exactly where `0 < lambda2` is load-bearing. QA `SpectralGraph/Fiedler_QA.lean` (57 declarations): on `K₂` the partition is pinned to a singleton half (boundary `1`); on the `P₄` barbell (two `K₂` near-cliques joined by one bridge — the proposal's named example shape) `lambda2 ≤ 1` comes from the proved Rayleigh engine at the cut indicator, `lambda2 ≠ 1` from the eigen equations, and the derived sign pattern forces `fiedlerPartition` to be exactly the known good cut `{0,1}` or its complement — boundary `1`, volume `3`, conductance `1/3`, partition card `2`; the disconnected fixture shows the hypothesis load-bearing (`lambda2 = 0`, support graph not connected, and `onesVec` is a nonzero eigenvector at `lambda2` whose sign filter is all of `univ`). Phase B (a certified conductance bound) is explicitly a separate axiom-backed milestone, not delivered here.
- **`SimpleGraph` interop adapter (2026-08-18):** the `SimpleGraph → WAdj` direction of the Mathlib bridge is delivered in `GraphTheory.SimpleGraphAdapter` (no axioms; step 1 of the re-sequenced `proposals/electrical-structure-crust.md`): `SimpleGraph.toWAdj` (defined as Mathlib's `adjMatrix ℝ`, so no parallel matrix construction exists to drift), symmetry/nonnegativity (unlocking every `hnonneg`-hypothesized center theorem), degree/volume agreement with Mathlib (`deg_toWAdj`, `vol_toWAdj_eq_sum_degrees`, handshake `vol univ = 2 * #edgeFinset`), the headline Laplacian agreement `laplacian G.toWAdj = G.lapMatrix ℝ`, the boundary as the crossing-edge count (`boundary_toWAdj_eq_sum_card_neighbors`), the adapter roundtrip `supportGraph (toWAdj G) = G`, the connected-`G` kernel corollary through the delivered span theorem, and the neutral re-exports of Mathlib's kernel/reachable iff and component-count finrank. QA `SpectralGraph/SimpleGraphAdapter_QA.lean` (38 declarations) computes weights, degrees, Laplacian entries, both Laplacian sides, the boundary, the handshake, and kernel membership against hand-expected numbers at the 3-vertex path, and exhibits the disconnected `Fin 4` witness where the component indicator is computed *into* the kernel yet the kernel ≠ `span {onesVec}` — the connectivity hypothesis is load-bearing through the adapter.
- **One-sided Dirichlet bound (2026-08-18):** proposal `electrical-structure-crust.md` step 6 (final step), no axioms, in `GraphTheory.Electrical`: every test potential `f` of positive energy lower-bounds the effective resistance, `(f u − f v)² / quadForm (laplacian A) f ≤ effectiveResistance A u v` (`effectiveResistance_ge_sq_div_quadForm`). Route: a pure-algebra discriminant lemma (`sq_le_mul_of_forall_zero_le_sub` — a real quadratic nonnegative everywhere has nonpositive discriminant), the polarization identity `quadForm_laplacian_sub_smul` (cross terms collapse through the proved reciprocity `laplacian_dotProduct_mulVec`), and the semidefinite Cauchy–Schwarz `laplacian_cauchy_schwarz` (`(f ⬝ᵥ L g)² ≤ quadForm L f * quadForm L g`, no connectivity hypothesis; Mathlib surveyed — the pin's only Cauchy–Schwarz is the definite inner-product one, unusable without quotienting the kernel). The bound's proof is load-bearing on `laplacian_psd` (polarization) and on the step-4/step-5 chain (the demand potential's cross term is exactly `f u − f v`; its energy is exactly `R u v`). QA (9 declarations, in `EffectiveResistance_QA.lean`): equality *attained* at the harmonic potentials on both connected fixtures (edge `1²/1 = 1`, path `4/2 = 2`, energies computed independently from raw definitions), strict at a non-harmonic potential (`1 < 2`), the reverse (upper-bound) inequality **refuted numerically** (`2 ≤ 1` false — the one-sided form is forced; the other direction is the deferred attained-supremum Dirichlet principle), and the `0 <` energy guard shown load-bearing by a disconnected component indicator with zero energy and voltage difference `1` (both sides of an unguarded bound would be junk there).
- **Effective resistance by the potential equation (2026-08-18):** proposal `electrical-structure-crust.md` step 5, no axioms, in the new `GraphTheory.Electrical`. `IsEffectiveResistance A u v r` is the potential-equation relation (`∃ f, L *ᵥ f = e u − e v ∧ f u − f v = r` — the Mathlib pin has no effective-resistance declaration and no pseudoinverse, surveyed before stating); existence consumes the step-4 solvability theorem, uniqueness of `r` consumes the component-form kernel characterization (stated at reachability-pair strength, so it holds within a component of a disconnected graph), and the total function `effectiveResistance A u v : ℝ` is defined by classical choice with an explicit junk fallback `0` whose firing is QA-witnessed. The energy identity holds at two levels: `quadForm (laplacian A) f = f u − f v` for *any* solution `f` (no hypotheses), and `effectiveResistance_eq_quadForm` at the function level; nonnegativity transfers from `laplacian_psd`, symmetry is hypothesis-free at the relation level (negate the potential), and `R u u = 0` is unconditional (`isEffectiveResistance_self_iff` characterizes the diagonal exactly). QA `SpectralGraph/EffectiveResistance_QA.lean` (17 declarations): values computed from the definitions on the unit edge (`R 0 1 = 1`) and the 3-path (`R 0 2 = 2` — series edges add), the energy identity cross-checked against an energy computed independently from the raw definitions, the junk fallback pinned on the disconnected fixture (`0` there is a fallback, not a measurement; no resistance value exists across components), and same-component witnesses showing two distinct potentials pin one value (`1`) through the reachability-form agreement even where the connected-graph theorem's hypothesis fails.
- **Potential solvability, the electrical hinge (2026-08-18):** proposal `electrical-structure-crust.md` step 4, no axioms. On a connected graph (symmetric nonnegative weights), every zero-sum demand `b` admits a potential: `exists_laplacian_mulVec_eq_of_sum_eq_zero` proves `∃ f, L *ᵥ f = b` by the **constructive eigenbasis** route (decision recorded in the execution plan and the proposal), with witness `f = ∑_{λᵢ ≠ 0} (vᵢ ⬝ᵥ b / λᵢ) • vᵢ` — a load-bearing composition of the proved eigenbasis algebra (`exists_mulVec_eq_of_zero_comp`, `mulVec_eigvecOf_sum_apply`) and the step-2 kernel theorem `laplacian_kernel_eq_span_onesVec` (which kills the zero-eigenvalue components of `b`). The unit-demand specialization `exists_laplacian_mulVec_eq_single_sub_single` is the defining equation for effective resistance in step 5. Supporting crust: the reciprocity identity `laplacian_dotProduct_mulVec` (`w ⬝ᵥ (L *ᵥ f) = (L *ᵥ w) ⬝ᵥ f`) and its kernel certificate `dotProduct_eq_zero_of_laplacian_mulVec_eq_zero`. QA `SpectralGraph/PotentialSolvability_QA.lean` (12 declarations): computed potentials on the edge (`![1,0]`) and 3-path (`![1,0,−1]`), theorem instantiations, and proved *unsolvability* witnesses — `e 0` on the connected edge (zero-sum hypothesis load-bearing) and the zero-sum cross-component demand `e 0 − e 2` on the disconnected fixture (connectivity load-bearing, via the component-indicator kernel certificate). Existence here is unconditional proved hard crust; nothing in this slice depends on any axiom.
- **Kernel-equality bridge / weighted component structure (2026-08-18):** proposal `electrical-structure-crust.md` step 3, no axioms. The center gained the component-form kernel characterization `laplacian_mulVec_eq_zero_iff_forall_reachable` (`Spectral.lean`: `L *ᵥ f = 0` iff `f` is constant on each connected component of the support graph — no connectivity hypothesis; new direction proved entrywise via the diffusion-form identity `laplacian_mulVec_apply`, `(L *ᵥ f) i = ∑ j, A i j * (f i − f j)`). The bridge `ker_laplacian_eq_ker_supportGraph_lapMatrix` (`ker (laplacian A) = ker ((supportGraph A).lapMatrix ℝ)`) transfers Mathlib's component-indexed kernel facts onto every symmetric nonnegative weighted graph: kernel dimension = component count (`finrank_ker_laplacian_eq_card_supportGraph_components`) and the component-indicator basis (`laplacian_ker_basis`/`laplacian_ker_basis_apply`, transported via `Basis.map`/`LinearEquiv.ofEq`); the `DecidableRel (supportGraph A hA).Adj` prerequisite is discharged once by `supportGraphAdjDecidable`. QA `SpectralGraph/KernelBridge_QA.lean` (14 declarations, reusing the `Connectivity_QA` fixtures): on the connected 3-vertex path the bridge, dimension `1`, and the constant-`1` basis vector hold simultaneously with the connected span theorem (a mis-stated bridge would contradict it); on the disconnected two-edge fixture the component count is computed to `2` independently of the transferred theorem (block classification + `Nat.card_eq_two_iff`), the transferred dimension reads `2`, and both basis vectors are computed to the component indicators `![1,1,0,0]`/`![0,0,1,1]` — the kernel strictly exceeds the constants.
- **Connectivity and the Laplacian kernel (2026-08-18):** the converse of `laplacian_ones_in_kernel` is proved (no axioms): for symmetric nonnegative weights whose support graph is connected, `ker (laplacian A)` is exactly the constants. New center hard crust: the `supportGraph` adapter (`WAdj → SimpleGraph`, `Adj i j ↔ i ≠ j ∧ 0 < A i j` — the first bridge from the matrix-first representation to Mathlib's `SimpleGraph` connectivity API), `eq_of_laplacian_mulVec_eq_zero_of_pos_weight` (zero Dirichlet energy forces constancy across positive-weight edges), `eq_of_supportGraph_walk` (walk propagation by induction on `SimpleGraph.Walk`), `exists_const_of_laplacian_mulVec_eq_zero`, `laplacian_mulVec_const`, the iff `laplacian_mulVec_eq_zero_iff_exists_const`, and the span form `laplacian_kernel_eq_span_onesVec` (`ker (mulVecLin (laplacian A)) = span ℝ {onesVec}` — the statement shape consumed by future effective-resistance uniqueness arguments). QA `SpectralGraph/Connectivity_QA.lean` exercises both a connected witness (3-vertex path: explicit-walk connectivity, both iff directions, constant recovered and pinned) and a disconnected negative witness (two disjoint `Fin 4` edges: the component indicator is in the kernel but not constant, and the support graph is proved not connected), showing the connectivity hypothesis is load-bearing. This was delivered as step 1 under the proposal's original numbering (it is step 2 after the 2026-08-18 re-sequencing; the adapter above is step 1).
- **Cheeger statement-shape repair (2026-08-18):** both admitted Cheeger axioms were restated at the corrected spectral side `secondEval (regularNormalizedLaplacian A d) …` after QA refuted the old shape in proved form: `lambda2` reads the spectrum of the combinatorial Laplacian *of* its argument, so the old side read `λ₂(L(L_sym)) = λ₂(-L_sym)`, whose two-vertex-edge instance asserts `1/2 ≤ 0` (`old_cheeger_lower_bound_refuted_QA`). The corrected side is independently pinned on the same fixture to the classical value `λ₂(L_sym) = 2` (`edge_normLap_secondEval_eq_two_QA`, computed from trace + determinant + sortedness — the tree's first computational eigenvalue check, conditional on no axiom). Supporting center hard crust added in `GraphTheory.Spectral`: `secondEval`, `lambda2_eq_secondEval`, `evals_mem_eigvalOf`, `eigvalOf_le_of_quadForm_nonpos`, `eigvalOf_sum_eq_trace`. Axiom count unchanged (18); axiom names and hypotheses unchanged. This is an emergency repair of a materially false statement shape (architecture §9), not a mathematical strengthening; downstream consumers of the corrected interface remain conditional on the axioms.
- **Stationary structure (2026-08-17):** the downstream consumer of both walk/normalized interface modules — `GraphTheory.Stationary` proves `L_sym *ᵥ √deg = 0` (kernel of the general normalized Laplacian, counterpart of `laplacian_ones_in_kernel`), `Pᵀ *ᵥ deg = deg` (the degree measure is stationary for the walk — `π ∝ deg`, the Markov-mixing consumer interface), and conservation of mass in both the regular (`randomWalkLaplacian *ᵥ 1 = 0`, consuming `RandomWalk.transitionMatrix_row_sum`) and irregular (`walkLaplacian *ᵥ 1 = 0`, consuming `Normalized.walkTransitionMatrix_row_sum`) cases. No axioms; downstream reuse on the SGT radar re-scored 2.5 → 3.0 → 3.5 with these milestones.
- **Cut duality (2026-08-17):** the first expansion/cut slice delivered in the center — `vol_compl`, `boundary_compl`, `conductance_compl` (cuts are partition-valued: boundary and conductance invariant under complementation for symmetric weights, via volume complementarity and `Finset.sum_comm`), and the degenerate-cut guards `boundary_empty`/`boundary_univ`. These are the structural facts sweep-cut and sparsest-cut consumers assume; no axioms admitted.
- **Random-walk and normalized-Laplacian interfaces (2026-08-17):** the first two broad-SGT backlog items delivered — `GraphTheory.RandomWalk` (transition matrix, row-stochasticity for `d`-regular graphs, walk-Laplacian bridges to both existing worlds) and `GraphTheory.Normalized` (the *irregular* symmetric normalized Laplacian through a diagonal `Real.sqrt` square root — no matrix square root needed — with the congruence `√D L_sym √D = laplacian`, regular-cone agreement, the general walk form `D⁻¹A` with irregular row-stochasticity, and the similarity identity `√D · L_walk · (1/√D) = L_sym`; the eigenvalue-list transfer is the named residual gap — the walk form is not symmetric, so `evals` does not apply, and the pinned Mathlib has no charpoly-roots interface for non-symmetric matrices). No axioms admitted in either module.
- **Citation and assumption review (2026-08-17):** `spectral_gap_stability` was converted from an admitted axiom into a theorem proved from `weyl_inequality` (four Weyl facts plus arithmetic), reducing the explicit axiom count from 19 to 18 at no trust cost; `weyl_inequality`'s doc now records that its Lean form is the spectral-norm corollary of the cited general theorem; `davis_kahan_sin_theta`'s separation hypothesis was tightened from a pairwise quantification to the binding single-pair two-cluster form `λ_{k+1}(A+E) − λ_k(A) ≥ δ` (the form used by the cited Yu–Wang–Samworth Theorem 2), with the derived wrapper simplified accordingly (Weyl at the gap index only). The Chung index's provenance note was corrected: git history shows no earlier page-level locators ever recorded (the initial commit cited "Theorem 2.2" without a page); theorem and page numbering remain explicitly unconfirmed rather than invented.
- **Projector algebra (2026-08-17):** eigenbasis orthonormality (`eigvecOf_inner`) and completeness (`eigvecOf_complete`) are proved from the Mathlib spectral-theorem API, yielding proved `spectralProjector_idempotent` / `initialProjector_idempotent` and the extreme-threshold theorems `spectralProjector_eq_zero` / `spectralProjector_eq_one`. These structural facts were previously consumed implicitly through the admitted perturbation interfaces.
- **Derived layer:** `Derived.EventStream.eventStreamTail` is proved from `matrix_azuma_hoeffding`; `Derived.ProjectorDrift.davisKahanTwoPoint` is proved from `davis_kahan_sin_theta` and `weyl_inequality`; `Derived.ProjectorDrift.eventStreamProjectorDrift` combines them with `eventStreamTail`. All three are checked deductions relative to the trust base (three concentration/perturbation axioms), not foundationally proved results.
- **`spectral_persistence` deprecation (2026-08-17):** the per-step persistence axiom was deprecated with a migration note after a consumer inventory showed zero non-QA consumers and the derived two-endpoint chain covering the motivating use. The axiom is retained through the compatibility window (its QA deliberately exercises the deprecated surface with the linter silenced for that use only); removal is a later release decision.
- **`lambda2_variational` retirement (2026-08-18):** the Courant–Fischer characterization of the algebraic connectivity was converted from an admitted axiom (symmetry hypotheses only — a materially false shape for negative weights, refuted in QA) into a proved theorem with the load-bearing `hnonneg` hypothesis, reducing the explicit axiom count from 18 to 17. The proof is pure matrix-world hard crust (eigenbasis expansion, Parseval, spectral resolution of `quadForm`, and two sorted-multiset multiplicity pins), consuming no axioms.
- **Azuma statement repair (2026-08-17, same day as the axiom's introduction):** preparing the derivation exposed that `matrix_azuma_hoeffding` lacked the summand-count factor `m` in the exponent denominator, which made the statement false for `m ≥ 2` (Rademacher-sum counterexample). The denominator is now `8 m R²`, the uniform-bound specialization of Tropp's variance statistic `σ² = ‖∑ Yₖ²‖ ≤ m R²`. The axiom had no downstream consumers before the repair.

## Active priorities

1. Fold or deprecate the unconsumed per-step `spectral_persistence` axiom
   in favor of the two-endpoint derived chain.
2. Confirm Horn–Johnson section-level and Chung chapter-level locators
   against physical or publisher copies (no local copy available; numbers
   must not be invented).
3. Specify the x90 observable against the completed persistence chain
   (application ring; the inner chain is now credible end-to-end).
4. Add QA based on risk and composability rather than targeting a cosmetic one-to-one ratio.

## Historical reports

Earlier scoreboards and build reports are preserved under [`research/archive/status/`](../research/archive/status/). They are historical snapshots and may contradict current generated metrics.
